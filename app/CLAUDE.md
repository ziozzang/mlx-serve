# Swift macOS app (MLX Core) — AI context

Compressed rules. Full war stories: `../docs/gotchas/app.md`. Full service contracts: `../docs/reference.md`. Repo-wide policy (TDD, release, conventions): root `../CLAUDE.md`.

## Build

- ALWAYS ship via `bash app/build.sh` (signs, entitlements, links Zig artifacts, keeps MLXCore + mlx-serve in lockstep); dev: `SKIP_NOTARIZE=1`. **No signing identity on the machine?** `APPLE_DEVELOPER_ID='-' APPLE_TEAM_ID='-' SKIP_NOTARIZE=1 bash app/build.sh` takes the ad-hoc branch (line 282: hardened runtime and productbuild are skipped for `-`), which is how a dev bundle is already signed — `codesign -dv` on it reports `Signature=adhoc`, `TeamIdentifier=not set`. A release still needs the real identity, so cut it where that lives or via `.github/workflows/release.yml`. Swift-only fast iteration escape hatch: `cd app && swift build -c release`. Never force a global Swift language mode through SwiftPM: MLXCore's manifest keeps the app in Swift 5 mode while modern dependencies compile in the mode declared by their own manifests (swift-sdk must stay >= 0.12.1; 0.10.x cannot compile in the MAS Xcode project).
- **`FAST_DEV=1 bash app/build.sh` is the iteration build, and an UNSET var is the release path byte for byte** — one variable gates every shortcut, so a shipping build can never take one by accident. What it changes: Swift compiles `-c debug` (the single biggest lever — `-c release` turns on WMO, so touching one file recompiles the module, and SwiftUI's type-checker is what makes that minutes; the two configurations keep SEPARATE build dirs, so alternating costs a full rebuild of whichever you left); CalVer skips the `gh` round-trip and does NOT stamp the tracked Info.plist (a fast build cuts no release, and a dirty tree is what breaks release.yml's Homebrew rebase); the bundle is updated IN PLACE (no `rm -rf`) and the icon is regenerated only when the bundle has none; the frameworks are re-staged, re-fixed-up and re-signed as ONE decision, skipped while `lib/mlx/lib/libmlxc.dylib` is no newer than the bundled copy — `install_name_tool` rewrites a file and invalidates its signature, so skipping the re-sign while still running the fixups would leave broken signatures in the bundle; and it stops at a signed .app, before notarization, `productbuild` and the DMG. What it deliberately does NOT change: **mlx-serve stays `-Doptimize=ReleaseFast` even under FAST_DEV** — Zig's cache already makes an unchanged rebuild a few seconds, so the Swift phase is the whole cost of an iteration and the Debug arm buys nothing for Swift-side work. **`ZIG_DEBUG=1` is that arm, a SECOND lever rather than something FAST_DEV implies**, because it costs more than it looks like: a Debug engine decodes 2–4× slower (root `../CLAUDE.md` "Building"), so every latency read off that app is fiction — and it is invisible from the running app, which is why the fast-build banner names both configurations — and Zig keys its cache on the optimize mode, so alternating with a release build is a full rebuild each way. Reach for it when the thing you are debugging is Zig-side (safety checks, real panic traces), not while iterating on Swift. It is FAST_DEV-only and refused BY NAME otherwise (`ZIG_DEBUG=1 bash app/build.sh` exits before any build work rather than quietly shipping a Debug engine): FAST_DEV is what stops before notarization and the DMG, so that gate is the entire reason a Debug binary cannot reach an artifact anyone else runs. The trade to know: a resource deleted from `Sources/` lingers in the bundle, so when a fast build behaves strangely, delete `app/MLX Core.app` (or run once without `FAST_DEV`) before believing it. Guard: `tests/test_release_workflow_gates.sh` (the release path still compiles `-c release`, still starts from an empty bundle, the optimize mode defaults to ReleaseFast with the one `-Doptimize=Debug` sitting directly under the ZIG_DEBUG gate behind a refusal that exits before any build work, and the FAST_DEV `exit 0` precedes notarytool/productbuild/create-dmg — the first draft of this change had reindented the script and left `# ... (rest of the block is identical)` where notarization and the App Store .pkg used to be).
- The .app bundles TWO binaries (MLXCore UI + mlx-serve server) — always update together; a stale sibling is the classic "still doesn't work".
- **The bundle's version is STAMPED from the version being released, never copied from the committed plist**: release.yml PlistBuddy's `$CONTENTS/Info.plist` after the `cp` and before `codesign` (stamping the repo file instead breaks the Homebrew step's `git rebase` on a dirty tree). It shipped unstamped once — v26.8.1's DMG contained an app reporting 26.7.12, so `UpdateChecker` offered an update that installing could never satisfy, forever. CalVer's `YY.M` is TZ-pinned in release.yml and app/build.sh together (runners are UTC; an evening release otherwise lands in next month). Guard: `tests/test_release_workflow_gates.sh`; story: `../docs/gotchas/app.md`.
- **An asset dragged out of Finder carries `com.apple.FinderInfo`, and codesign refuses the WHOLE bundle for it** (live 2026-08-09, a screen recording copied off the Desktop): `resource fork, Finder information, or similar detritus not allowed` — reported three minutes into a build, against the BINARY rather than the file that brought the metadata in. build.sh now `xattr -cr`s the staged bundle before signing (fixes the class); `tests/test_resource_xattrs.sh` keeps the checked-in copies clean so it's caught in a second with the file named. It checks ONLY the attributes that actually block signing — `com.apple.macl`/`com.apple.provenance` are added by macOS whenever a sandboxed app touches a file, every existing resource has always carried them, and failing on those would make the guard cry wolf until someone deleted it.
- Agent Sandbox needs `com.apple.security.virtualization` in BOTH signing branches of build.sh (ad-hoc included) or VZ refuses to boot. `swift build` binaries / xctest can't boot VMs; live guard = `SANDBOX_SMOKE=1 <MLXCore binary>`.

## Layout

| Path | Role |
|---|---|
| `MLXServeApp.swift` | Entry, menu bar + Chat/Browser windows |
| `AppState.swift` | Global state, chat session persistence, `deleteSession` (must stop orphaned turns) |
| `Models/{ChatModels,AgentModels,ServerOptions}.swift` | `ChatMessage`, `SerializedToolCall`, launch flags (`toCLIArgs`) |
| `Services/APIClient.swift` | HTTP + SSE streaming client; accumulates tool-call deltas, emits on `finish_reason` (incl. "length") |
| `Services/AgentPrompt.swift` | System prompt, 10 tools, `SkillManager`; env-specific facts ride `executionEnvironmentSection`, never the base prompt |
| `Services/AgentBudget.swift` | Derives context/output budgets for agent CLI configs from server `meta.context_length` — NEVER hardcode |
| `Services/AgentEngine.swift` | Shared agent logic: history, tool exec (`normalizeWriteFileArgs`), repetition tracking, `AgentRetryBudget` (consecutive, resets on real tool round) |
| `Services/ToolExecutor.swift` | Tool handlers (shell, file, search, browse, webSearch, saveMemory) |
| `Services/ChatTurnEngine.swift` | MULTI-TURN: one turn PER SESSION concurrently (`TurnLedger`, token-gated cleanup; chat window + Quick Launcher share it; Tasks/Telegram own instances); `composerState(for:)` (idle/generatingHere — no busyElsewhere), `stop(sessionId:)`, `stopIfOrphaned` per-turn sweep |
| `Services/ChunkedDownload.swift` | Multi-connection ranged transfer: `DownloadChunking` plan, `ChunkedResumeState` sidecar, `PositionalFile`, `DownloadSession.shared` |
| `Services/DocumentIndex.swift` | Folder-RAG: embeds ONLY via server `/v1/embeddings` (auto-downloads bge-small, registers by abs path); server down → lexical-only |
| `Services/{AgentSandbox,VzGuest,OCIClient,SandboxSmoke}.swift` | VZ Linux guest: virtiofs root, hvc1 shell (`ShellSentinel`), hvc2 monitor, `SandboxPortForwarder` |
| `Services/{SandboxSSH,SandboxAgentRegistry}.swift` + `Views/{EmbeddedTerminalView,SandboxSessionTabs,SandboxTerminalView}.swift` | Sandbox CLI sessions (issue #89): ssh transport + keys, pi/hermes registry + `/.vz-bootstrap-<id>`, SwiftTerm seam, multi-session tabs + Activity pane |
| `Services/ClonedVoiceSynthesizer.swift` | Voice-clone TTS pipeline behind `SpeechSynthesizing`; per-utterance fallback to system voice, never dead air |
| `Services/QuickLauncher*.swift` | ⌃Space launcher: Carbon hotkey + non-activating NSPanel; pure `QuickLauncherLogic` |
| `Services/{ImagePreprocessor,BrowserManager,ServerManager,TestServer,AgentMemory}.swift` | Image prep, WKWebView, server lifecycle, test server :8090, agent context |
| `Services/{LaTeXSegmenter,LaTeXRenderer}.swift` + `Views/ChatView.swift` (`MarkdownText`) | Assistant-response TeX: streaming-safe delimiters, SwaTex native layout, inline attachments + display math, source-preserving copy/fallback |
| `Services/TaskScheduler.swift` | Scheduled tasks ("claw"): own ChatTurnEngine, durable runs, `reconcileStaleRuns` |
| `Models/Agent.swift` + `Services/{AgentStore,AgentResolution,AgentWriter,AgentComposer,AgentModelSwitch,AgentWorkspaceSwitch,ActiveAgentVoice,AppStateAgents}.swift` + `Views/AgentsWindow.swift` | Agents (personas): one saved prompt + voice + model + tools + workspace + wake phrase. `AgentResolution.resolve(agent:defaults:)` is the ONE chokepoint every turn site reads; `AgentStore` = `~/.mlx-serve/agents/index.json` (+ read-only `Agent.starters`); `AgentWriter` is the iPhone port (pure, tests ported with it) |
| `Views/{ChatView,StatusMenuView,BrowserView}.swift` | Chat UI + `runAgentLoop()` (150 iters max), menu bar, browser |
| `Services/{SyntaxHighlighter,MarkdownSegmenter}.swift` + `Views/CodeBlockView.swift` | Code blocks: fence→language, single-pass lexer (UTF-16 spans), per-line runs (`CodeLayout`), gutter + copy + `CodeTheme` |
| `Models/{ChatErrorNotice,ContextWindowStats}.swift` + `Views/{ChatErrorCard,ContextPill}.swift` | Failed-turn card (overflow gets the server's counts + "Increase Context Size") and the composer's context gauge + popover |
| `Services/{ChatModelSelection,WebSourceExtractor}.swift` + `Views/{ChatModelPill,WebSourcesChip}.swift` | Toolbar model picker (tag semantics shared with the tray) and web-search provenance (favicon + expandable rows) |
| `Services/{MediaToolArgs,MediaGenProgress}.swift` + `Views/ChatMediaAttachmentView.swift` | In-chat media generation: the four `generate_*` tools' pure arg parse/clamp + `MediaChatDefaults` + `MediaTurnBudget`; the shared SSE mapping + progress model; the transcript's audio/video players (`AVPlayerViewRepresentable` lives here) + `MediaProgressCard` |

Agent loop: tools → parse → exec → feed back; history builder is budget-aware (pins first user+assistant, truncates, auto-compacts tool results). Tool errors echo sent args + retry hint for self-correction.

## Rules

- **A GUI bundle has NO shell environment, so an env var the user set is INVISIBLE — reading it is not the same as honoring it** (HF cache, 2026-08-14): `huggingFaceRoot` already read `HF_HUB_CACHE` then `$HF_HOME/hub`, but from `ProcessInfo.processInfo.environment`, which a Finder-launched app never carries — so those branches were dead in the shipped app and every install fell through to `~/.cache/huggingface/hub`, listing nothing for anyone who moved the cache off the boot drive (the same reason `hfToken` prefers the token FILE over `HF_TOKEN`, and `CLIInstaller` asks the login shell for PATH). `LoginShellEnv` is the ONE probe now: marker-delimited `printf` through `$SHELL -l -i -c`, 5 s watchdog, cached, primed OFF-main at launch (`refreshHuggingFaceRootFromLoginShell`, rescan only when the root MOVED) so nothing blocks on an rc file; process env WINS over the shell's, and an unset var must not come back as `""` and shadow one the process has. `XDG_CACHE_HOME` was missing outright and is now in both the root and token precedence, matching `huggingface_hub`'s own order (`HF_HUB_CACHE` > `$HF_HOME/hub` > `$XDG_CACHE_HOME/huggingface/hub` > `~/.cache/huggingface/hub`). A CONFIGURED root that is not on disk is nil, never a fall-through to the default cache — the point of the variable is that the models are elsewhere. Gated on `BuildFeatures.customModelFolders`: under MAS an out-of-container cache is unreadable whatever the shell says (audit entry in `HostEscapeAuditTests`). Guards: `HuggingFaceRootTests` + the class scan `testHuggingFaceEnvIsNeverReadStraightFromTheProcessEnvironment`, verified red by injecting one direct read.
- **The app is ONE window: Models / Tasks / Settings / the four media generators are MODES of the chat window** (`Models/ChatWorkspace.swift`), not `Window` scenes. Each has exactly one door on `AppState` (`showModels` / `showTasks` / `showSettings` / `showCreate`) that sets the mode AND bumps `pendingChatOpenTick` — setting the mode alone switches a window nobody is looking at, opening the window alone lands on a transcript. Retired scene ids (`modelBrowser`, `tasks`, `settings`, `imageGen`/`videoGen`/`audioGen`/`model3dGen`) are gone from every surface: `openWindow(id:)` on an unknown id is a silent no-op, so a leftover id is a control that opens nothing (guards: `ChatWorkspaceTests`). The sidebar is a list of DESTINATIONS above the conversations and never rearranges — the row that takes you in is the row that brings you back, which is what the tray/welcome/Tools-menu entries (which arrive already in a pane) depend on.
- **Moving a view into another window means moving its ENVIRONMENT with it** (live crash 2026-08-08): the browser's window injected four objects, the chat window three, and opening the models pane trapped in `ModelBrowserPane.downloads.getter` — SwiftUI reports a missing `@EnvironmentObject` at first RENDER, with no compile error. `testTheChatWindowInjectsEveryObjectItsHostedPanesRead` regex-scans every hosted pane's declarations against an explicit injection map, so a new one fails a test naming the type. Corollary: a pane with its own `NavigationSplitView` (Tasks, Settings, Agents) can't just be dropped in — it needs an `embedded` shell (plain HStack, or promoted to real columns as Tasks was), or it fights the outer sidebar.
- **An environment reader has to BE the column, never produce it** (second crash the same day, and the injection audit was green throughout): the Tasks columns were `TasksView().taskList` / `.taskDetail` — a view VALUE whose computed property is evaluated on the spot. SwiftUI populates `@EnvironmentObject` storage when it INSTALLS a view in the hierarchy, and that instance never was one, so the first click on Tasks trapped in `TasksView.$appState.getter`; the `.environmentObject(…)` chained at the call site cannot save it, because it decorates the view the property already returned, long after the property read the empty box. Columns are `TaskListPane` / `TaskDetailPane`, real types declaring their own environment. The guard is a source scan for `SomeView(…).bareProperty` — a MODIFIER is always a call, so "property access not followed by `(`" separates the two shapes with no allowlist to maintain (comments are stripped first, or it matches the prose warning about it). The window-injection audit is blind to this class by construction: it asks what the window injects, and the bug is WHERE the reading happened.
- **`listRowBackground` is the ROW's backdrop, and `listRowInsets` moves only the CONTENT**: putting the conversation highlight in the former made a selected chat run edge to edge under a column of destinations inset 8pt, while both read the same `SidebarRowStyle` colour — one meaning for gray, two shapes. The fill rides the row content as a plain `.background`, inside the same insets, with `.listRowBackground(Color.clear)` so the list cannot draw a second surface beneath it. Guard: `testTheConversationHighlightIsInsetLikeADestinationRow` (walks back from the fill to the nearest enclosing modifier and demands it be `.background(`).
- **Hiding a window toolbar hides its RESIDENTS** (live 2026-08-09): `.toolbar(.hidden, for: .windowToolbar)` removes the BAR, and the traffic lights and the sidebar-collapse button live in it — a window that cannot be closed, zoomed or collapsed. To retire an empty band, hide its MATERIAL (`.toolbarBackground(.hidden)`), which keeps the bar, its chrome and the safe area it reserves. The older trap pointing the other way (`toolbarBackground(.hidden)` leaves `scrollEdgeEffectStyle` attached to an invisible bar, transcript text clipping mid-line, 2026-07-30) was about the floating CLUSTER; with no cluster nothing floats over the transcript. Guard bans `.toolbar(.hidden)` and requires the material form in both splits.
- **A `ToolbarItem` on macOS 26 draws a capsule around EVERYTHING it holds**: a title beside a button becomes one "Tasks +" lozenge with the title's baseline padding fighting the button's hit area. Ask for `.sharedBackgroundVisibility(.hidden)` (the answer the chat's old cluster used) and let the button carry its own hover-lit target. Static content only — the `»`-eviction rule is about members that appear or change WIDTH at runtime, which a fixed word and one button are not. Shared as `PaneTitleBar` so Tasks and Agents cannot drift.
- **`.listStyle(.sidebar)` adds a horizontal margin that `listRowInsets` does not control** (live 2026-08-09): the same "8" therefore drew two different widths — destinations at the panel edge, conversations pushed ~18pt further in — and zeroing the row insets changed nothing on screen because that was never the number doing the work. No API removes it. Rows that draw their own background, hover, selection and separators get nothing else from a List, so the conversation column is a `ScrollView` + `LazyVStack` taking the SAME `.padding(.horizontal, ChatMetrics.sidebarGutter)` the destination stack takes. One gutter + one row inset constant, read by both halves and the section headers.
- **A row's hit target is whatever its BUTTON covers, not what its fill covers**: sizing the row from an outer frame centres the label in it, leaving a dead strip along the top and bottom of the highlight. The button carries the padding, the height floor and the `contentShape`; a trailing control becomes an overlay (a real Button, hit-tested first) with its space reserved at ALL times, since reserving it on hover reflows the title under the pointer.
- **Exactly one row of the sidebar is lit** (`SidebarSelection`): destinations read `chatWorkspace` and conversations read `activeChatId`, so nothing reconciled them and opening Tasks left the last chat lit beside it — each half correct alone. The active chat is where you ARE only while the window is showing conversations.
- **A placeholder title is a CONCEPT, not a literal** (`ChatSessionTitle`): the auto-titler gated on `title == "New Chat"`, so an agent thread starting as "New agent" would have kept that name forever. Placeholders + the derived title live together; `display(title:hasAgent:)` normalizes at DRAW time so threads stored before agents had a section read correctly without rewriting anything on disk.
- **A view expression can stop the compiler finishing** (live 2026-08-09): the agent editor's identity header as one nested expression took the release build from ~48s to over TEN MINUTES, with no warning — SwiftUI's type-checker just grinds. Split into typed sub-views it was 47.9s. Suspect the last view you wrote when a build time jumps an order of magnitude.
- **A redesign is where affordances disappear quietly** — nothing fails when a control simply stops being drawn. Removing the Agents row's menu once left `startChat(withAgent:)` with NO caller anywhere in the UI while its test stayed green, because the test named the control's location instead of the capability. Pin that it is reachable AT ALL; `AgentsPanelTests` lists every affordance the agent editor had, each named so a failure says WHICH one went.
- **One meaning for gray in the sidebar** (`SidebarRowStyle`): destinations and conversations read the SAME fill for hover/selection. A `List(selection:)` also draws its own tint UNDER `listRowBackground` — that double highlight is why the conversation rows are plain Buttons with no selection binding. Row + delete button are real siblings, never a tap gesture around a button (the swallowed-click class).
- **The chat column has a reading measure** (`ChatMetrics.contentMaxWidth`, 740): transcript, composer and the empty-state greeting all cap at it and centre. One number for all three — a composer even slightly off the answers above it is a seam you can't unsee.
- **The composer never becomes a generator** (create mode REMOVED 2026-08-09): a media chip navigates to the Create pane through `appState.showCreate`, exactly like the Tools menu. Two ways to make an asset, not three: chat with tools on (the model drives at preview settings) or the Create pane (you decide everything). Old sessions may still carry a `createMode` key on disk — the decoder no longer asks for it.
- **Per-chat tool switches SUBTRACT at the resolution chokepoint, never at a turn site** (Tools pill → chevron menu): `AppDefaultsSnapshot.disabledTools` is removed from whatever the agent (or the app defaults) already allowed, so a chat tab can never grant a capability an agent forbids — forbidden rows render disabled rather than offering a switch the resolver would ignore. Turning everything off makes the turn plain chat (`loopRuns`: advertising nothing while still looping is a dead offer). `searchDocuments` is exempt from BOTH the menu and the subtraction — its gate is the attached folder, which is stronger — and `AgentToolKind.chatToggleable` × `AgentToolGroup` are pinned against each other (a tool in no group is unreachable from the UI; one in two renders twice). Stored on the session as RAW STRINGS so a retired tool leaves an unknown name instead of failing the whole session's decode. Guards: `SessionToolDisableTests`.
- **A control the AGENT decides shows the agent's value and says who decided it** (composer Think/Tools/MCP): `AgentResolution` takes `toolsEnabled` from the agent's capabilities and `mcpEnabled` from `capabilities.mcp` and ignores the chat's own toggles — and `AgentCapabilities.web` defaults TRUE, so selecting ANY agent (even one with `tools: false`) forced the tool loop on while the wrench rendered OFF and a click on it wrote a field the resolver discards. `ChatModeToggles.resolve(…, agentLock:)` overlays the agent's values and names the owner PER CONTROL; the disc gets a dashed inset ring (never a dimming — ON/OFF is what its colour has always said) and its menu becomes "Set by \<name\>" + "Edit Agent…", because a locked control that does nothing on click is the dead-control class. Thinking locks ONLY when `agent.enableThinking != nil` — resolution falls back to the surface there, so locking it takes away a control nobody is deciding. The lock is built from `resolvedAgentSettings`, NEVER from `capabilities` directly (a second copy of that rule is how they drifted), and the pre-send intent nudge plus both setters refuse while locked. Guards: `ChatModeTogglesTests`, `ComposerModeControlTests`.
- **The pre-send nudge is ONE pure decision, and tools can be made strictly opt-in** (`ComposerIntent.nudge`): every gate — the opt-in setting, mode already on, agent lock, per-chat "Send Anyway", MCP-before-Agent priority — lives in that one function, and `ChatView.detectIntentPrompt` only gathers its inputs. Settings ▸ Agent Sandbox ▸ "Only use tools when I ask" (`ServerOptions.toolsOnlyWhenAsked`, default OFF = shipped behavior, app-level so it never trips the restart banner) short-circuits it: tools reach the model only where the wrench/MCP disc or the tab's agent turned them on. A second copy of the rule in the view is how one branch would keep nudging — source-scan-pinned in `ComposerModeControlTests`, branch-pinned in `ComposerInputTests`.
- **The chat generates media itself, at PREVIEW settings, one per message** (`generate_image` / `generate_speech` / `generate_music` / `generate_video`): speech and music are separate tools because music IS audio and one overloaded tool makes a 2B guess (`generate_audio` was split — `Agent.renamedTools` maps the old name, since the capability decoder DROPS names it doesn't know and a plain rename would silently strip the capability from every agent that had it). The model picks nothing but the prompt: which model comes from the user's saved settings for that modality, the step/duration knobs from `MediaChatDefaults` (a chat generation blocks chat decode on the one GPU, so it is a preview and the tray window stays the place for full-quality work), and every number it CAN send is clamped in `MediaToolArgs` — a 4B asking for 30 seconds of video is not hypothetical (measured: clamped to 4.0s). Read the spellings models actually emit: asked for "widescreen" a model sends `{"size":"16:9"}`, and an aspect resolves against the user's OWN size (an exact-ratio match escalated a preview to the model's largest 16:9, 8× the pixels). **One generation per USER TURN**, keyed by a turn TOKEN the driver passes rather than a `reset()` it must remember — as a reset call, the headless harness (a second driver on the same engine) inherited a spent budget and silently refused every generation forever. The refusal leads with **"NOT GENERATED"** and the words to relay: with the rule first and the fact second, a refused 4B told the user both items had been made. Music takes the WHOLE knob set the engine reads (`bpm` 30–300 — outside it the server 400s, so it clamps; `keyscale`, `time_signature`, `vocal_language`), each validated against `MusicOptions` — the catalogue IS the accepted grammar — and OMITTED when unrecognised, because only `bpm` is checked server-side and an unknown `keyscale` goes into the conditioning as junk (an absent field is the engine's own "model decides"). Results ride `ChatMessage.media` as a PATH (`ChatMediaRef`) — a 30 s track is tens of MB and `chat-history.json` would carry it forever. An IMAGE rides that PATH too: the transcript draws it from the file (`ChatImageAttachment`), never from a second re-encoded copy in the history — measured, 424 KB of a 1.00 MB history whose text came to 29 KB. The output still ships BOTH markers, which is why the ref line sits BETWEEN the caption and the data URI (after it, the caption swallows the whole base64); the decoded bytes are dropped on the floor. A missing file is its own row for EVERY kind now, and a history written before this change carries both — so the bytes row stands down beside an image ref, or the same picture draws twice. Guards: `MediaToolArgsTests`, `MediaGenProgressTests`, `AgentMediaInlineTests`, `GeneratedImageRefTests`.
- **An attachment the user brings in is a FILE under `~/.mlx-serve/attachments/`, and the history carries its path** (`AttachmentStore`, issue #288): base64 in `chat-history.json` was 97% of an ordinary conversation's 1.00 MB, re-read and re-written on every save, and the file is loaded whole at launch. The four capture paths (drop, paste, the paperclip, a dragged browser image) keep the ORIGINAL bytes whenever the SERVER CAN READ THEM, so a PNG screenshot stays lossless and a photo keeps its pixels. **The whitelist is the server's own decoder** (`sniffedExt`: PNG/JPEG/GIF via stb_image, WebP via libwebp — exactly `decodeRgbOwned`), and a sniff that cannot name a format answers nil rather than guessing: NSImage opens HEIC, TIFF, BMP and camera raws that the server cannot, and those bytes decode to nothing server-side, so the image drops out of the prompt with no error anywhere and the model answers about a picture it was never shown. The old code re-encoded EVERY attachment to JPEG and covered this by accident. What the sniff declines is re-encoded from the decoded `NSImage` — JPEG **q0.85, the quality this app re-encoded every attachment at before they moved to disk** (what lands here is a photo, HEIC is already lossy, and the tower smart-resizes to `ENGINE_MAX_PIXELS` long before quality could matter; q0.95 measured TWICE the source HEIC's size) — or PNG when the bitmap `hasAlpha`, since JPEG would turn transparency black. The bitmap comes from the `CGImage`, never `tiffRepresentation`, which re-encodes and can invent an alpha channel the picture never had. A paste with no file behind it is PNG regardless — the pasteboard usually carries uncompressed TIFF, and JPEG rings around the text these are mostly screenshots of. **The stored extension describes the STORED bytes**, never the name they arrived under: a converted `IMG_1234.HEIC` lands as `.jpg`, because the sniff is the only thing that ever reads a stored file back. Files are written on SEND, not on attach (the preview row can drop an attachment again, and a file per abandoned pick is how a folder fills with things no conversation names), named `<uuid>_<name>.<ext>` off the `ChatImage`'s own id so the folder names the record and two `screenshot.png` cannot clobber. A failed write is not fatal: the bytes stay in memory for the turn, and only the next launch finds nothing and says so in the transcript. Deleting chats removes what they own MINUS what a surviving session still names — `ChatFork` copies messages, so a fork and its source point at one file — and never touches a path outside the root. **Deleting one MESSAGE removes its files too** (`removablePaths(orphanedBy:)`, captured before the removal), because the session-level sweep reads the messages a session still HOLDS: a file whose message is gone is not orphaned until the chat is deleted, it is orphaned for good. Those two are the only removal sites (test-pinned) — `truncateMessages` must never join them, since regenerate and edit-and-resend truncate and then rebuild the turn from the very same `ChatImage` values, paths included, so a removal there deletes a file that is about to be used again. `ChatImage` decodes an old history without throwing (its `data` comes back empty, never a decode error: `loadChatHistory` is a `?? []`, so one unreadable image would empty EVERY conversation), and an empty image is filtered out of the wire payload rather than sent as an `image_url` with nothing in it. Guards: `AttachmentStoreTests`.
- **A live ETA from observed laps is floored by the MODEL and carries the after-sampling tail** (`H3TimeEstimate.liveEta(floorPerStep:tail:)` from `livePricing` — the pre-run estimate decomposed): under the fast recipe the lap history is dominated by cheap cached steps while the closing steps always refresh at full cost, and the VAE decode never appears in step events — mean × remaining said "77 sec left" with minutes to go. H3 only; LTX keeps the plain mean (uniform laps, no pre-run model). Guards: `H3PlanningTests` floor/tail + decomposition cases.
- **A media generation shows a meter or it reads as a hang**: `ChatTurnEngine.mediaProgress` (ONE value — the inference thread serializes media gen) drives `MediaProgressCard`, determinate when the engine reports a total and indeterminate when it doesn't (TTS length is model-determined, so it sends a growing frame count against total 0 and a bar at 0/0 looks stuck). Elapsed time is a `TimelineView(.periodic)`, never a `repeatForever` animation or a `Timer` — that is the `VoicePulse` tray-wedge class. The four services' identical SSE parsing collapsed into `MediaSSE.classify`; each gained a `generateForAgent(_:server:onProgress:)` that runs the same load → stream → write → unload pipeline WITHOUT touching its pane's `phase`/`task`/`recent`, so a chat generation never hijacks the tray window.
- **"length" is two different endings, and only one of them may CONTINUE the turn** (`TruncationNotice.Cause`, live 2026-08-05): the server reports both a `max_tokens` cap and its own degenerate-tail loop cut as `finish_reason: "length"` — the only OpenAI value for either — so the app told a user with two thirds of their context free that they had hit an output limit neither side set (pi sends no `max_tokens` at all). The cause comes from the server's sibling `finish_details` (`APIClient.truncationCause(fromChoice:)`, static + dictionary-shaped so it is testable without a live stream; ABSENT reads as `.maxTokens`, which is exactly the old behaviour, so an older server or another OpenAI backend is unchanged), and it rides `SSEEvent.truncated(cause)` — renamed from `.maxTokensReached`, which had stopped being true. A repetition loop **ends the turn** (`TruncationNotice.endsTurn`, checked ahead of every recovery branch): the loop's text is already in the transcript, because a streamed delta cannot be retracted and the server's own trim only reaches non-streaming clients, so the next round sends it back as history and the model reads its own loop and resumes — the error-echo class with our transcript as the error, and from the server it looks like five cuts in a row, each firing sooner. A max_tokens cut keeps every existing recovery (chunk-and-retry, truncated-tool-call nudge) — the reply was fine and ran out of room. The loop notice names NO cap: naming one sends people to raise a setting that was never the problem (same rule as `ChatErrorNotice` refusing a diagnosis it cannot support). Guards: `TruncationNoticeTests`. Server side + the measurements: root `## Rules`.
- **The truncation notice is DATA on the message, never content** (`ChatMessage.truncationNotice`, 2026-08-11): the banner used to be APPENDED into `message.content` (`updateLastMessage(content:)`), so both history builders sent "⚠️ Stopped — the model started repeating itself…" back as assistant prose every turn — the error-echo class with our own warning as the error (live capture: the banner rode the muse chat forever). The notice rides `TruncationNotice.Notice` (cause + cap), renders as a footnote under the bubble (`MessageBubble`), and content stays clean by construction; sessions saved by older builds get the in-content banner scrubbed at history build (`TruncationNotice.stripped(from:)`, both builders — markers DERIVE from the legacy `text(...)` so they cannot drift). Decode is tolerant (unknown cause → nil, absent forever on old messages). Guards: `TruncationNoticeTests` data-not-content cases.
- **A failure is its own transcript row, not text appended to the model's answer**: `[Error: …]` glued onto the assistant's own content read as something the model said and gave the user nothing to do. `ChatErrorNotice` classifies it — `.contextOverflow` (the one failure fixable from here) carries the server's real counts and offers "Increase Context Size"; everything else stays `.generic` and shows the server's wording verbatim, because guessing a diagnosis sends people to change settings that were never the problem. The notice message is `failedRetry`, so it renders but is excluded from the history sent back (an error card fed in as assistant output is how a model starts apologising for a server failure). The counts only exist because the server puts them in the 400 — see the root `## HTTP APIs` note. Guards: `ChatErrorNoticeTests`.
- **The context readout is UNCLAMPED; only the bar it sits next to is clamped** (`ContextWindowStats` vs `ContextMonitor.usedTokens`): the moment anyone looks at that number is the moment they went over, so it must be able to say 100.3%. After a REJECTED request the rejected request's own counts win over the last successful turn's, which are stale and read comfortably under the limit exactly when the user is trying to find out why it failed. The full-width bar above the composer is retired — it spent a permanent strip on a number that matters occasionally and still couldn't say what was in the context; the pill puts the breakdown one click away. Guards: `ContextWindowStatsTests`.
- **Code blocks are their own views; prose RUNS stay one text view** (`MarkdownSegmenter` splits at FENCES, not at markdown blocks): splitting per block is what broke drag-selection at every heading. The trade is deliberate — each block becomes its own selection island, and buys stable line numbers, per-token colour and a copy button that yields the code alone. The lexer emits **UTF-16** spans (a Character offset is short by one for every emoji earlier in the block and mis-colours everything after it), recognizes comments before strings and strings before everything else, and runs unterminated comments/strings to end-of-source because a streaming reply is in that state constantly. An unknown fence gets NO colour — guessing a language and painting prose as code reads worse than plain. Guards: `SyntaxHighlighterTests` (incl. a sorted/non-overlapping/in-bounds invariant over every language), `MarkdownSegmenterTests`, `CodeLayoutTests`.
- **LaTeX renders only in ASSISTANT Markdown, never in the user's bubble or fenced/inline code** (`LaTeXSegmenter` → SwaTex): inline equations remain attachments inside the prose `NSTextView`, including headings and table cells; display equations own a horizontally scrollable `MathView`. An unfinished delimiter, invalid formula, currency/code-span false positive, oversized formula, or renderer failure stays exact source text — streamed model output is never eaten. Attachment copy rehydrates the original delimiters instead of U+FFFC, and light/dark raster cache keys differ. **A SwiftPM resource bundle cannot be found from a hand-assembled .app, and copying it is only half the fix** (issue #233): SwiftPM generates `Bundle.module` as `Bundle(path: Bundle.main.bundleURL + "<Pkg>_<Target>.bundle")` — for an app that is the .app ROOT, which `codesign` refuses to seal ("unsealed contents present in the bundle root", measured), so the only shippable location, `Contents/Resources`, is one the accessor never looks in and its miss is a `fatalError`. Every shipping path is `swift build` (DMG and MAS alike), so LaTeX trapped on the first equation everywhere while the packaging guard — which only asserted the bundle was copied SOMEWHERE — stayed green. `scripts/patch-swatex-font-lookup.sh` rewrites SwaTexRender's one call site to a candidate search before every build (build.sh, release.yml, ci.yml — patch step must precede `swift build`, and it fails loudly rather than silently not applying); `LaTeXFonts.isAvailable` answers the same question app-side so a missing bundle renders LaTeX as its own source instead of trapping. Guards: `LaTeXSegmenterTests`, `LaTeXRenderingTests`, `LaTeXPackagingTests`, `LaTeXFontsTests`.
- **The model picker is the composer row's pill** (`ChatModelPill(compact:)`, name width-capped at `compactNameWidth` — the composer row is a width budget too, and a 60-char repo id must not push it around): it names which model answers the message being written, and it carries the download affordances a toolbar pill had no room for. Its progress hairline filters media-bundle transfers out (`chatDownload` × `DownloadManager.mediaBundleRepos`) — a 30 GB video pack must not render as the chat model arriving. Tag semantics live in `ChatModelSelection`, shared with the tray, because a per-surface copy is how one picker silently stops honouring a LAN selection (same class as routing through `server.chatModelId`). Guards: `ChatModelSelectionTests`, `ChatModelPillDownloadTests`.
- **⌘L is the same picker for the keyboard** (`ModelPalette` + `ModelPaletteSheet`): type to narrow, ↑/↓ to move, ↩ to switch, esc out. It OFFERS what the pill offers — same `isChatPickable` filter, same `lanModels(capability: "chat")` question, same group order with the network last — because two pickers a keystroke apart listing different models is the worse half of having two. Every decision is pure: the query matches on title + repo id + quant/size (all words must land, so a second word narrows), and deliberately NOT on the filesystem path or the section HEADING — every model under `~/.mlx-serve/models` shares most of a path and every row of a group shares its heading, so matching either makes "users", "models" or "mlx-serve" answer "all of them". The selection OPENS on the model already answering (⌘L ↩ is a no-op, not a switch to whatever sorts first), arrows CLAMP rather than wrap (holding ↓ past the end must not put you back on the first row with ↩ armed), and the index is read through `clamped` because typing shrinks the list under it. It is a menu key equivalent so it works from every window, routed through the ONE door `AppState.showModelPalette()` (raises the picker AND brings the chat window forward), presented ONCE on the whole chat window rather than on one split — Tasks and Agents are the other `NavigationSplitView` — with the environment injected AT the sheet, and the blocking model gate stands down while it is up (the one-sheet rule). Applying a pick is now ONE method, `AppState.applyChatModelPick`: the tray and the pill each carried their own copy of "clear the LAN id, then set the path", and the palette would have been the third. Guards: `ModelPaletteTests` (incl. the ⌘L/one-door/one-presentation/one-apply scans).
- **Web-search provenance is recovered from the HIDDEN tool results, and is never guessed**: the transcript filters those messages out, so without `WebSourceExtractor` the user reads a confident answer with no way to check it. A block counts as a source only when its second line is an http(s) URL — the handler falls back to dumping raw page text when a search returns nothing, and presenting that as sources would be fabricating citations. The walk stops at the previous user message so an earlier turn's search can't be cited by a later answer. Favicons are best-effort with a monogram fallback, attempted ONCE per domain (a retry-per-render hammers the host for as long as the chat is open). Guards: `WebSourceExtractorTests`.
- **The pill's hairline is THIS chat's model arriving, so it is keyed to that model** (`ChatModelPill.chatDownload`, 2026-08-14): it took any non-media transfer, so downloading a second model in the background drew a progress bar under the model that was already answering — the same claim-vs-check gap the media-bundle filter closed half of. Every other download surface keys its row `downloads[pick.repoId]`; this one now matches the repo id against `selectedModelPath` (`isTransfer(of:for:)` — a download has no `LocalModel` until its files land, so the join is the layout the downloader WRITES, `<root>/<org>/<name>`, with a `.gguf` selection resolving one level up). The exception is the reason the hairline exists: with nothing chat-pickable on disk the composer cannot answer at all, so whatever chat model is arriving IS what it waits for. Guards: `ChatModelPillDownloadTests`.
- **A download has ONE progress fraction, and it is the whole transfer** (`DownloadState.progress`, 2026-08-14): the state carried a per-file fraction too, and all eight bars in the app rendered THAT — so a repo of four shards ran 0→100% four times and users read the resets as a download that kept restarting (worst on the models that take longest, since a big pack is many shards). The per-file field is DELETED rather than left beside the right one: it was correct data that every surface reached for by reflex, so the compiler is the guard. Which file is moving is said by `currentFile`/`fileIndex`, never by a bar. Resume banks its existing bytes into `progress` at the top of the retry loop so the bar starts where the disk already is, and `onProgress` reports bytes-of-this-file INCLUDING resumed ones, so `baseDownloaded + fileBytesTotal` is the transfer's true position. A media BUNDLE is still per-component (N repos, sizes unknown until each starts) — one reset per model, which is why that row says "Downloading model 1/2". Guard: `testTheProgressBarOnlyEverMovesForward` (two tiny files then a 40 MB one — the shape where per-file fills the bar twice before the real work starts).
- **A control that cannot be clicked is not a control, and a row about FILES has to show you the file** (`ModelRowActions`, 2026-08-21): a My Models row had "Use" and then either a trash or `externaldrive.badge.icloud` — an `Image`, not a `Button`, of a glyph that reads as "external drive" or "cloud" and meant neither. For every model outside `~/.mlx-serve/models` that glyph WAS the row's entire answer to "where is this and how do I remove it", and clicking it did nothing. Three fixes, logic in a pure helper because a SwiftUI row cannot be unit-tested: Reveal in Finder on EVERY row (six other panes already had `NSWorkspace.activateFileViewerSelecting`; the one pane that is about files on disk did not — and `path` is the DIR for safetensors and the FILE for a GGUF quant, which is the behaviour you want); the badge becomes a `lock` Button that ARMS the trash (`showsLock`/`showsTrash` are mutually exclusive — same slot, so a row showing both has one of them lying — and `@State unlocked` is per-row, re-locking on refresh); and a `contextMenu` carrying the same actions plus Copy Path / Copy Model ID (`grep -c contextMenu` on that file was 0). `deleteModel` takes `unlocked:` as a PARAMETER, defaulting false, so every other caller keeps refusing; unlocking widens `roots` to `readRoots` for the same reason a defect does. The read-only rule survives only where it protects something real: the HF cache is still deletable (the user's disk) but its confirmation names the shared-blob damage, and every foreign confirmation names the PATH because "Delete org/m?" does not say which of four tools' copies is going. Guards: `ModelRowActionsTests` incl. a source scan pinning all four controls into the view and asserting the old glyph name is gone — which is why the comment about it does not spell it.
- **A directory that cannot load is LISTED as broken, never dropped and never offered** (`ModelDefect`, `DownloadManager.weightDefect`, 2026-08-21): discovery answered "is this a model?" with a file-EXISTS check and dropped the failures, so `~/MLXModels/OsaurusAI/LFM2.5-8B-A1B-MXFP8` (config, no weights) was invisible in the app while the server — which runs no such check — registered it and would have died on load, and `Hy3-preview-JANGTQ` (a 48 KB stub `.safetensors`) PASSED and was offered as a real, selectable model. You cannot delete what you cannot see, and hiding junk is why 328 GB of auto-detected folders read as a library. Three defects, checked in that order because an interrupted download EXPLAINS thin weights: `.partial`/`.incomplete` present; `model.safetensors.index.json` naming a shard that is not on disk (EXACT — the checkpoint declares its own parts, nothing is inferred); else total `.safetensors` bytes under `minimumWeightBytes` (1 MiB — a "nothing loadable is this small" floor, NOT a guess about model size). A defect makes a row unpickable (`isChatPickable` guards first), always deletable (`isDeletable` returns true BEFORE the source check — junk in a foreign tree is still junk, and the owning app is not showing it either), and never read-only (`externalReadOnlyReason` returns nil first: the badge and the trash are the same slot, so a deletable row that also claims read-only renders a dead button). The delete's ROOT LIST widens with it — `deleteModel` passes `readRoots` for a defect, because that list is what `removeModelFiles` refuses to delete and stops pruning at, and a foreign root missing from it is a root the call would remove. Broken folders group LAST under `ModelBrowserUse.defectGroupTitle`, never inside a tool's section (they are not that tool's models). Guards: `ModelDefectTests`, `DefectDeletionTests`. KNOWN GAP: the Zig discovery still registers these — `model_discovery.requiredMediaMarker` covers media packs and text checkpoints have no equivalent.
- **A model folder MLX Core can already read is still invisible until something LOOKS in it, and the looking happens in two places** (`ToolModelRoots`, 2026-08-21): LM Studio's folder was auto-detected from the first version and no other tool's was, so MTPLX (`~/.mtplx/models`, flat `Org--Name` dirs) and Osaurus (`~/MLXModels`, `org/repo`) — both layouts `discoverModelsInDir` has always read, 126 GB of them on the reporter's Mac — could only be reached by spending the single "Custom folder" slot. The rule for adding one is CANONICAL TOOL-OWNED LOCATION, not a folder somebody likes: `~/models` is the case it excludes, and the custom slot exists for that. Detection takes `home` (and `DownloadManager.lmStudioRootPath(home:)` now does too) because a resolver that reaches the real home cannot be tested without depending on what the test machine has installed — the first version of these tests passed or failed by whether LM Studio was on the box. The SECOND place is the trap: `ModelRoots.scanRoots` only builds the server's `--model-dir` list, while the picker walks folders itself in `DownloadManager`, so a root added to one and not the other is served-but-unselectable — the mirror of the listed-but-not-served bug that created `ModelRoots`. ONE `LocalModelSource` case PER TOOL (`.mtplx`, `.osaurus` — read-only, never in `ownedRoots`), never a shared "other tools" bucket: the read-only badge's whole job is to send you to the app that owns the file, so a heading that cannot name that app cannot do it, and `.lmStudio`/`.huggingFace` were already per-source for exactly that reason. The path->source pairing lives on `ToolModelRoots.orderedWithSource` (with `ordered` derived from it) so the picker cannot list a folder under a foreign tool's heading; `--model-dir` reads `ordered` because a flag has no notion of source. Guards: `ToolModelRootsTests`, `ToolFolderListingTests` (incl. a no-two-sources-share-a-heading check and a source scan pinning that the tray picker filters for EVERY case — it hardcodes its sections and is the one that silently drops a new source). KNOWN GAP: the Hugging Face hub cache is resolved by the app and NOT scanned by the server — `models--<org>--<repo>/snapshots/<sha>/` is a third layout the Zig discovery does not read, so wiring the root in without that would add a scanned folder that always finds nothing.
- **A registry id is not a display NAME, and a hot switch is invisible from the outside** (`ChatModelSelection.pillState`, 2026-08-14): the server keys a register-by-path entry by DIRECTORY BASENAME, which for a Hugging Face cache model (`models--<org>--<repo>/snapshots/<commit>/`, a root no `--model-dir` ever covers) is the COMMIT HASH — so the pill, which prefers `chatModelInfo?.name` because the resident model is the one answering, drew a hex string while the tray showed the repo id off our own scan. Our label wins only where the resident id names the model we PICKED (`name`, or the path's last component — the two shapes a registry id takes); a model another surface loaded keeps the server's id, since renaming it would name the wrong model. And a hot switch never moves `server.status` off `.running` while `chatModelInfo` reports the OLD model for the whole load, so a big checkpoint loaded for a minute under the previous name beside a green dot: `AppState.loadingModelPath` (published beside `pendingModelLoadTask`, cleared in its `defer`) makes the pill name the model arriving and spin. A RESTART is deliberately not tracked — it moves the status, which `ChatServerStartControl` already reports. Guards: `ChatModelSelectionTests` pill-state cases.
- **The chat toolbar holds NOTHING; everything that configures the MESSAGE lives in the composer row** (2026-08-09 — the floating cluster is retired): the model pill, the transient server-start control and the Think / Tools / MCP discs all sit in the composer, because they configure the turn being written or report the thing you discover by typing; Settings is a sidebar destination and still ⌘,. The band's MATERIAL is hidden by the two split views (never the BAR — the residents rule above). Icon-only means state reads from COLOUR alone (`modeIcon`), and it means a bare glyph must not mean two things depending on WHERE you click — but it may mean two things by which BUTTON: Tools, MCP and Think toggle on click (`Menu { … } primaryAction:`) and open their configuration list (per-tool switches + workspace / the Marketplace / the reasoning-effort picker, low default, sent as `reasoning_effort` only while thinking is on) on secondary-click, which is also where press-and-hold lands. These are the most-flipped controls in the composer, so on/off must not cost a click plus a scan down a menu; the menus carry NO on/off row (one boolean, one control) and the tooltip names the right-click, because nothing else announces it. Guard: `ComposerModeControlTests`. `ChatMetrics.togglePill*` is retired — those controls share `composerIconSize`/`composerControlSize` with the rest of the row.
- **Icon-only controls owe a hover card, and the card must not be a POPOVER** (`ComposerTip`, on all four composer glyphs — attach, Think, Tools, MCP): once the captions came off, a `.help` tooltip was the only thing naming them, at ~1.5 s with no room for the two facts that save a trip into a menu (what right-click does, which workspace the tools run against). A real `.popover` is what it looks like and is wrong: NSPopover is transient, so the click that would toggle the wrench just dismisses the card — on the most-flipped controls in the row. It is an overlay with `allowsHitTesting(false)` instead. The control PUBLISHES (`.composerTip` → an anchor preference) and the composer CONTAINER draws (`.composerTipOverlay()`, applied after its `clipShape`): an overlay on the disc itself is cut off at the container's rounded edge and lands on the text field. The text is pure data so the sentences are testable — a toggle's card names its current state AND the opposite action ("ON … click to turn it on" is how you get clicked twice), and the body is capped at a GLANCE (120 chars, test-pinned): the first version read as three sentences of prose, which nobody hovers to read. `.help` and a card together is two competing explanations, so the four carry exactly one (source-audited). And **opening a menu over the composer does not deliver a hover-exit**, so the card sat under the open menu until you hovered the control again: `ComposerTipHoverState.dismiss()` fires on `NSMenu.didBeginTrackingNotification` (SwiftUI's `Menu` and `.contextMenu` are both NSMenus, so one observer covers left-click, right-click and press-and-hold) and BUMPS the reveal token — hover, then click inside the 0.45 s delay, and an in-flight reveal would otherwise pop the card up on top of the menu you just opened. Guards: `ComposerTipTests`, `ComposerModeControlTests`.
- **An empty app opens a composer, not a wall**: with no valid session the detail pane creates one in `onAppear` (never during the view update) instead of showing "Start a conversation" + a button; `ChatDetailView` already renders the greeting above a centered composer while a conversation has no messages, so a fresh launch and a fresh chat look the same, and sending the first message moves it to the bottom.
- **The empty chat's chips, the Tools menu and the sidebar's Create rows are ONE catalog** (`ChatEmptyState`, live confusion 2026-08-04 — media gen / Tasks / the CLI launcher lived only in the tray, where nobody looks; the sidebar rows are `sidebarCreateItems`, a FILTER on `mediaItems` — 3D deliberately stays chip/menu/tray-only): discovery chips render under the GREETING (greeting + chips + composer center as one group via two SIBLING Spacers in the body — a Spacer nested inside `emptyState` shared space unevenly with the trailing one and pinned the group to the bottom) and vanish when the conversation starts; `CommandMenu("Tools")` is the always-available twin and iterates the SAME `mediaItems`; the Code Launcher chip renders the tray Code button's own `CLILauncherMenuItems` (host CLIs + sandbox agents — one list per surface pair, none can drift). Window ids are pinned against the scene graph; the code chip is DMG-gated (`BuildFeatures.cliLauncher`) + server-gated (a launch bakes the live base URL into the session it opens). No ⌃Space chip (removed on request — the Tools menu keeps the launcher, with NO `.keyboardShortcut`: ⌃Space is a global Carbon hotkey a menu key equivalent would race; the menu click also ENABLES the hotkey, which ships off). Guards: `ChatEmptyStateTests`.
- **The welcome screen is a SHEET on the chat window, not a window of its own** (2026-08-09): it was a hand-built `NSWindow` around an `NSHostingView` at `.level = .floating`, which bought three problems at once — it inherited NO environment (every object the starter card reads had to be passed in by hand, or SwiftUI traps at first render), it could sit over an empty desktop, and every exit had to REMEMBER to open Chat or the user was stranded (`WelcomeExit`, live dead end 2026-08-08). As a sheet the dead end is unbuildable: whatever dismisses it, a composer is what was already behind it. So `LaunchDecision.opensChatWindow` is constant TRUE — the `.showWelcome` branch used to skip opening Chat, and a sheet with no host window is a screen nobody can see — and `presentsWelcome` is the only thing the branch decides. **A sheet does NOT inherit the environment of the view it hangs on** (live crash 2026-08-09, the very first launch after this move): SwiftUI presents sheet content in its own hosting context, so hanging it off the chat scene after that scene's whole `.environmentObject` chain still trapped in `WelcomeView.server.getter`. The content is injected AT THE SHEET, which is what every other sheet in the app already did. `testTheChatWindowInjectsEveryObjectItsHostedPanesRead` is blind to this by construction — it asks what the WINDOW injects, and the window did inject `ServerManager`; the bug is WHERE the reading happens, the same blind spot as the `TasksView().taskList` trap. `SheetEnvironmentAuditTests` closes it: for every `.sheet` in the scene and in ChatView it reads the presented view's OWN `@EnvironmentObject` declarations and demands each one at the presentation site. **Two sheets on one window is one sheet plus a thing nobody can see**: the blocking "you need a model first" gate now stands down while the welcome is up (`ChatWorkspace.gateShouldPresent(welcomePresented:)`) — deferred, not dismissed, exactly as it already stands down over the models pane, and the welcome answers that question better anyway since it is the screen with the starter models on it. KNOWN: the sheet declares 850×550 against a window whose minimum is 1070×500 — width fits everywhere now, height still overhangs by 50pt at the minimum. Guards: `WelcomeSheetTests`, `ChatWorkspaceTests`.
- **An empty AGENT thread greets you as the agent, and offers no chips** (`ChatGreeting`): the heading is the agent's NAME with its BRIEF underneath — it was the word "Agent" at 30pt with the name as a caption, the same inversion the sidebar rows had, and the brief is the thing that tells you what to ask. The discovery chips advertise what the APP can do, which is a job for a blank chat and done once; on an agent thread every one of them navigates OUT of the conversation, directly above the field where you were about to type to someone specific — and an agent whose capabilities exclude image generation cannot do what "Create Media" sits there offering (the locked-composer-disc rule: never offer what the resolver will refuse). Nothing is LOST, which is what makes the removal safe rather than the quiet-affordance-loss class — the Tools menu is the always-available twin over the same catalog, and `ChatGreetingTests` pins that it still reaches all four. A stopped server outranks the brief (it is the one thing that must be acted on first); a blank agent name or brief falls through rather than drawing an empty line. Guards: `ChatGreetingTests`.
- **Only the USER gets a bubble**: an assistant reply is the column's main content — long, formatted, full of code blocks and tables — and boxing it wastes the width and fights every block that wants the full measure. Tool-call summaries keep their card so they still read as machinery. The footer's actions are ALWAYS visible and pinned to the trailing edge past the stats: hover-reveal moved them under the pointer as the row re-rendered mid-stream and made them awkward to hit, and a trailing anchor puts them in the same place on every message rather than at a spot that shifts with the stats text's length. `deleteMessage` takes the turn's hidden tool results with it — an orphaned tool result is invisible, so nothing would ever clean it up, and the model can only be confused by it.
- **A turn site never reads a global setting directly — it reads `ResolvedAgentSettings`** (agents/personas). Five surfaces start turns (chat tab, voice tray, scheduled task, Telegram, Quick Launcher) and they all build their `TurnConfig` through the ONE builder `TurnConfig.from(resolved)` out of `AgentResolution.resolve(agent:defaults:)`; the surface's own toggles are the DEFAULTS the agent overrides. Same class as the LAN rule (`server.chatModelId`, never `modelInfo?.name` at a new surface): a per-surface read is exactly how one surface silently ignores the agent. Corollaries the wiring forced: **`agent == nil` must reproduce today's behavior field for field** (`AgentResolutionTests` asserts it; `ChatSession.agentId`/`ScheduledTask.agentId`/`telegram.agentId` all decode absent → nil), and **the two chat paths do NOT share a sampling default** (the tool loop's `agentLoopTemperature` 0.7 vs plain chat's `defaultTemperature`), so the resolution carries the agent's RAW `temperatureOverride`/`maxTokensOverride` alongside the decided values — a "decided" number in `TurnConfig` would silently move one of them for every user who never makes an agent. An agent owns everything EXCEPT the Agent Sandbox, which stays one global flag.
- **Every agent owns its own chat thread** (`AppState.sessionForAgent`): a voice turn routes by AGENT, not by whichever tab is open, so speaking to Chef continues Chef's conversation (created on first use) instead of talking into someone else's — and a spoken handover moves the conversation rather than rebranding the one you were having (no `setAgent` on the session it leaves). An ACTIVE thread of the same agent beats a more recently touched one (the user opened that one deliberately), and task-run / Telegram-bridge sessions are NEVER adopted — they carry an `agentId` now too, and they're hidden, transient or read-only. The chosen thread must also become `activeChatId`: the voice controller speaks the ACTIVE session's trailing assistant message, so a turn running anywhere else is never read aloud. Tray "New" creates a fresh thread for the SAME agent and leaves the old one in the sidebar; launching voice from a chat tab adopts that tab's agent, or a tray default would pull the conversation out of the tab you launched from. Guard: `AgentSessionThreadTests`.
- **Answer length is written INTO the prompt, not exposed as a knob**: a persona covers role, expertise, tone and priorities and says nothing about LENGTH, and outside voice mode nothing else does either (plain chat has no system message, the tool-loop prompt is about tools) — so agents answered at essay length. Fixed in the AI-WRITE path only: `AgentWriter.instructions` asks the model for its own brevity instruction, and `AgentWriter.concise(_:)` appends `brevityLine` when it didn't (`mentionsBrevity` covers the phrasings models actually use, so it's never doubled; the prose is trimmed first so a budget-filling prompt can't push the line off the end). The line is then ordinary prompt TEXT — visible in the editor, rewordable, deletable — instead of a setting to find, and a prompt the user typed is never rewritten. It's applied by `AgentComposer.draftAgent`, NOT by `parse`/`fallbackDraft`, which stay byte-identical to the iPhone's so the port keeps transplanting (three ported tests assert exact prompt equality — they're the tripwire). Starters carry the same expectation in their own wording. Guard: `AgentBrevityTests`.
- **Voice-mode guidance is appended LAST, so it must not claim an IDENTITY when a persona is present** (`VoicePrompt.speakingStyle(phrase:hasPersona:)`): it opened with "You are <wake-phrase name>, a friendly hands-free voice assistant", which — being last, deliberately, so its style rules win conflicts — overrode the agent above it and the agent introduced itself as Jarvis (live 2026-07-26). With a persona the prompt carries the DELIVERY rules only (one shared `deliveryRules` block, so the two modes can't drift) plus "stay exactly the assistant described above"; with no agent the old text is byte-identical. The phrase itself comes from `ResolvedAgentSettings.wakePhrase` via `TurnConfig`, not `serverOptions`, so an agent with its own phrase is named by it. Guard: `VoicePromptTests` persona cases. Second instance (live 2026-07-29): the agent-prompt body's own "You are an autonomous agent…" opener overrode a persona on Laguna ("I'm poolside Malibu" as Elon Musk) — fixed by making the persona REPLACE the normal system prompt entirely; see the persona rule below.
- **An agent's persona IS the entire system prompt** (`composeSystemPrompt(persona:stable:volatileTail:grounding:)`): with a persona present the normal composition — base instructions + env + MCP/docs listings + volatile tail + grounding — is dropped entirely, because the agent-prompt body opens with its own "You are an autonomous agent" identity claim and overrode the persona (live 2026-07-29, Laguna as Elon Musk). Tools still ride the request's tools JSON, so dispatch is unaffected; the agent's prompt must carry anything else it needs (working dir, date — nothing is injected). Empty persona ⇒ byte-identical prompt to the pre-agents build. Plain chat has no system message by design, so a persona there is ONE synthesized system message shared with voice-style guidance (persona first), not a second one. Guard: `AgentTurnConfigTests` persona cases.
- **Gating the advertised tool list is not enough — dispatch refuses too** (models call tools that were never advertised): `AgentPrompt.toolDefinitionsJSON(allowing:)` filters the literal LINE-BASED (a JSONSerialization round-trip would reorder keys and lose `path` before `content`), and `AgentEngine.disallowedToolRefusal` runs at the TOP of `executeToolCall`, ahead of the meta-tools (`createTask`, `generate_*`) which aren't ToolHandlers. Same belt-and-braces shape as `FileToolSandboxGate`. `allowedTools: nil` gates nothing (TestServer, older surfaces). **The MCP toggle gates dispatch too** (`executeToolCall(mcpEnabled:)`, default FALSE): "servers aren't started when MCP is off" is a lie one turn later — sessions from an earlier MCP-on turn stay connected until app quit, `owns()` is a LIFETIME property, and models re-call `<server>__<tool>` names from history; without the flag the call executed (or hung on a dead upstream) with the toggle off. Refusal = `mcpDisabledRefusal` (fact first, no-retry, steer). Guards: `AgentCapabilityGateTests` MCP-gate cases. Two class guards: `AgentToolKind`'s raw values and the parsed `AgentPrompt.toolDefinitions` names must be equal BOTH ways (a tool in the JSON with no case is ungateable — a silent hole in every agent's capability list), and every kind must belong to a coarse group (`loopTools` / `webTools` / the docs gate) or it's unreachable from the UI. `searchDocuments` is deliberately outside the capability toggles — its gate is the attached folder, which is stronger — so it always rides `ResolvedAgentSettings.tools`.
- **An agent's model is never implicitly downloaded, and a spoken switch that can't happen is declined OUT LOUD** (`AgentModelSwitch`): pinned-but-missing ⇒ `.needsDownload` (greyed row + Download button), an unknown `id@peer` ⇒ `.unavailable`, a known LAN id passes through with no local load (`server.lanChatModelId`), and only `.load` calls `useModelAndAwaitReady`. `spokenDecline` is what the voice path says — silently answering as whoever was active is the failure the user can't see.
- **An agent switch remounts the sandbox like the Settings pick, and NEVER writes the global default workspace** (`AgentWorkspaceSwitch.decide` → `noteWorkspaceChanged(_:restartPinnedSessions: true)`): agents carry their own folders, so a switch can move the ground under a live pinned CLI session — that's EXPLICIT intent (restart the sessions in place), not the implicit chat-tab pick the pin declines. Writing `setDefaultAgentWorkspace` there would retarget every session still on the old default, with nothing in the UI to show it; a source audit pins that. Per-agent folders need per-agent bookmarks (`SecurityScopedBookmark.agentWorkspaceName(id)`) — the global default's slot can't stand in.
- **The clone model is resolved against the DISK, and the agent voice is published PER TURN** — two silent-fallback bugs from the same live report ("my uploaded wav isn't going through Qwen3-TTS"). (1) `VoiceCloneTTS` loaded `AudioGenSettings.resolvedModel`, which defaults to the 0.6B repo, so a machine holding only the 1.7B variants resolved no model, `synthesize` returned nil, and every sentence fell back to the system voice while the picker still ticked the clip: `VoiceCloneMenuModel.resolvedCloneModel` now prefers the configured preset when present and otherwise ANY downloaded cloning-capable one (never Kokoro — `ref_audio` there is a named 400), and `ttsModelDownloaded` reads the same answer so the UI can't offer what won't speak. (2) `ActiveAgentVoice` was set only on agent SELECTION, so editing the active agent's voice — or answering as a tab's agent that isn't the tray's — spoke in the app's voice; `runTurn` now publishes `config.voice` (the agent's RAW `voiceOverride`, nil when it has none, so the live per-utterance Settings read is untouched). Guards: `VoiceCloneModelResolutionTests`, `AgentTurnConfigTests` voice cases.
- **Uploaded voice clips are a LIBRARY, not one file** (`VoiceClipLibrary`, sharing `~/.mlx-serve/voice-clips` with the global Settings clip): every upload used to overwrite the single `voice-clone.wav`, which silently cost you the previous clip and would have let a Settings re-record change an agent's voice under it. Clips keep the picked file's own name (`basename`, deduped `morgan` → `morgan-2`, never the global clip's name), are normalized to the same 24 kHz mono wav the TTS reference path wants, and the global clip is filtered OUT of the listing so it can't appear twice. The Agents editor and Settings ▸ Voice read and write the one library, so a clip added in either is selectable in the other; the folder is Finder-managed (like skills), and the listing is snapshotted per appearance — never per render, since those menu bodies re-evaluate ~20 Hz while speaking. **Preview plays the FILE for a clone voice** (`VoicePreviewer.playClip`): auditioning a reference recording asks "is this the right take?", which the raw audio answers instantly and with no model downloaded — Kokoro still synthesizes a sample sentence and the system voice speaks one. Guards: `VoiceClipLibraryTests`, `VoicePreviewerTests` playClip cases.
- **A per-agent voice hangs on the synthesizer's per-utterance re-read, not on new plumbing**: `ClonedVoiceSynthesizer`'s production `voice:` closure already re-read `ServerOptions.load()` once per utterance, so `ActiveAgentVoice` (a lock-guarded holder, because the closure isn't main-actor bound) is consulted first and the voice applies from the very next sentence. `.system` must WIN rather than fall through — an agent asking for the plain Apple voice against a global Kokoro setting would otherwise still sound neural — while a half-saved empty value defers to the global setting instead of producing silence.
- **The Agents editor is Identity + Prompt; everything else is behind "More options"**: an agent is a name, a symbol, a wake phrase, a VOICE and a prompt — voice moved INTO Identity because how it sounds is the same kind of fact as what it's called. Capabilities / Model / Workspace / Sampling are real but rarely touched, and five sections of them between "what should this be?" and Delete made the editor read as a settings panel. Collapsing them creates one hazard — a setting that IS set but invisible, because nobody expands a row that looks empty — so the row carries `AgentAdvancedSummary.text(for:)`, naming every collapsed area that differs from the defaults (`web` defaults ON, so its ABSENCE is what's named; empty-string paths are not pins; the tri-state thinking/approval overrides count). The disclosure is a Button gating four sections rather than a `DisclosureGroup` around them — nesting sections inside a disclosure loses their headers — and it resets on the agent-id change like `showAdvancedTools` (the editor is REUSED across selections). Guards: `AgentAdvancedSummaryTests`.
- **The Agents editor owns its own surfaces** (`AgentEditorChrome`, 2026-08-09): it was a `Form(.formStyle(.grouped))`, which owns the card radius, the row insets, the label typography and the section spacing — so the design frame's geometry was unreachable from inside one — and macOS RIGHT-ALIGNS a TextField's text in a Form row, which had the agent's own name pinned to the trailing edge of a field labelled at the other end. It is a `ScrollView` of hand-built cards: one `AgentEditorMetrics` for the numbers (pinned by relationship, not by literal — a well is never rounder than the card holding it, sections separate more than a label from its field), and `AgentSection` / `AgentCard` / `AgentLabeledField` / `AgentEditorRow` / `AgentPillButton` for the chrome, so six cards cannot end up with five radii. **A Form was also silently promoting `Toggle` to a SWITCH**: outside one, a macOS Toggle is a checkbox, so leaving the Form quietly demoted every capability control — the class is that a container gives you styling you never asked for and never notice until it stops. Everything trailing-aligned through the ONE row type, or a card of leading-aligned controls beside a card of trailing ones reads as two designs. Guards: `AgentEditorLayoutTests` (geometry relationships + a scan that the editor draws no shape of its own), `AgentsPanelTests` (every affordance survived).
- **An agent thread is named for its AGENT** (`ChatSessionTitle.display(title:agentName:)`): the sidebar's Agents section is a list of who you talk to, so the row has to say who — it said so only in a caption under a title derived from the first thing you happened to type. The name is read LIVE from the agent (renaming one renames its threads; nothing on disk is rewritten), a blank name falls through to the placeholder rather than drawing an empty row, and the thread's OWN title moves to `subject(title:agentName:)` as the caption — which is the only thing telling a second thread with the same agent apart from the first. Placeholder normalization and the auto-titler's gate are unchanged. Guards: `ChatSessionTitleTests`.
- **The voice-mode orb picks no voice**: the speaking voice belongs to WHO is answering, so it is set where that is decided — Settings ▸ Voice for the app's own, the Agents editor for an agent's — and `ClonedVoiceSynthesizer` re-reads it per utterance, so a change applies from the next sentence with no control in the orb. The old sheet's `VoiceSelectorMenu` was deleted rather than hidden: a third picker could only disagree with the two that own the value.
- **In-window voice is an INLINE orb above the composer, not a sheet** (`VoiceOrbView`, 128×128; entry = `voiceToggle` in the composer row between the context gauge and Send): the sheet covered the transcript and its toggle row duplicated the composer's own controls. The orb hosts the voice approval card inline; the window's approval sheet is typed-turns only, and the old "suppress while voice is open" sheet binding went with the sheet. ChatView never observes the controller: `VoiceOrbView`/`VoiceComposerToggle` are observing child views, so tint/presence changes don't re-render the whole chat column. Guard: `VoiceOrbInlineTests`.
- **Voice is ONE instance BOUND to ONE chat** (`VoiceModeController.boundSessionId`, decision fn `voiceOwnedHere`): the orb + toggle on-tint render only in the bound session's tab (bare `isActive` lit EVERY tab — live report 2026-07-31). Bound at `begin()` from the launch context (chat tab / tray agent's thread), re-bound by every submitted turn AND immediately on a spoken handover (`selectAgent` — the orb is in mickey's tab before mickey introduces itself), cleared at `end()`. No-agent turns route to the BOUND session, never the active tab (peeking at another chat mid-voice must not move the conversation), the answer-speaking sink keys on it (viewing tab B while voice runs in A must not read B's streaming typed answer aloud — multi-turn made that reachable), and bargeIn/tray-New stop only its turn. Clicking the off-tinted toggle in another chat MOVES voice there (end + start, adopting that chat's agent/toggles). Tray surfaces stay global. Guards: `VoiceModeControllerTests` binding cases, `VoiceOrbInlineTests` scope audit.
- **The phrase hands-free LISTENS for follows the active agent, not just Settings** (`WakeWord.activePhrase`): voice launched from a chat adopts that chat's agent — persona, voice, tools — and the phrase has to come along, or an agent that introduces itself by name still only answers to "hey loki" while the "Say “…”" hint tells the user to say the wrong thing. The controller binds it to `serverOptions` × `defaultAgentId`, because that id is what EVERY route to "who is answering" already sets (tray pick, spoken handover via `selectAgent`, and `startVoiceMode` adopting the tab's agent) — so the phrase tracks a handover mid-session too. A blank or whitespace-only agent field defers to the global setting rather than producing a gate that never matches, and an empty global still resolves to `defaultPhrase` (`""` would make `strip` match everything). The agent's VOICE needs no new plumbing: `ChatTurnEngine.runTurn` publishes `config.voice` per turn and `defaultAgentId`'s didSet runs `applyAgentSelection`. Guards: `VoiceAgentWakePhraseTests`.
- **Content passing under chrome is frosted by the PLATFORM, never a hand-drawn band — and `scrollEdgeEffectStyle` needs a BAR to attach to** (macOS 26+): `.scrollEdgeEffectStyle(.soft, …)` on the transcript and the session list (the sidebar takes `[.top, .bottom]` — rows scroll under the pinned destination block, a `safeAreaInset`). The bar EXISTS with its material hidden (the residents rule), which is enough of an edge; with the floating cluster gone nothing floats over the transcript any more. What must not come back is the HAND-DRAWN strip — a custom band pulled into that area via `ignoresSafeArea` looked native and swallowed every click in it; the scroll view drawing its own effect can't intercept anything. Guards: `ComposerModeControlTests` (both surfaces adopt the effect, both splits hide the material, `.toolbar(.hidden)` banned).
- **A `.bordered` control cannot be given a height — it keeps its INTRINSIC size and centers inside whatever frame you hand it**, so a text button and an icon-only menu side by side never match (sidebar's New Chat + agent menu; measured through the accessibility API at 24pt vs 17pt while both sat in 28pt frames). Two controls are the same size only if you draw them: `.buttonStyle(.plain)` + `sidebarActionButton()` (one exact `frame(height:)`, one fill, one radius), which is the shape the composer's discs already use. Three attempts failed before that, all of them trying to steer a bordered bezel indirectly — a greedy `maxHeight` on the label (it lives inside the already-collapsed bezel), a shared `minHeight` on the labels (a floor only lifts the shorter one), and an outer `frame(height:)` on the controls (moves them, doesn't resize them). **Measure, don't eyeball**: `System Events` → `size of every UI element` gives exact point sizes, and a zoomed screenshot crop read as "fixed" twice when it wasn't.
- **Starting a chat AS an agent lives where agents live**: the Agents pane's list rows and editor carry the start-chat affordance (`startChat(withAgent:)`; reachability pinned by `AgentsPanelTests` — the quiet-affordance-loss rule above), and a session's agent is fixed at creation. The old `newAgentChatMenu` beside New Chat is retired with the destinations sidebar; how an agent thread is TITLED is the `ChatSessionTitle` rule above.
- **A session's agent is decided when the session is created and never after** (`AppState.startChat(withAgent:)`; `setAgent(_:forSession:)` is DELETED, source-audited): a mid-thread switch left half a conversation running under someone else's prompt, tools, model and voice — with only the transcript to show where the seam was, and the three composer discs flipping as it happened. `startChat` both creates the session and runs `applyAgentSelection` (model / workspace / voice live outside the turn — an id-only session runs the persona against whatever is loaded). Editing the agent still applies LIVE to a conversation in progress, because every turn re-reads `AgentResolution`; that is the supported way to change one. Guards: `ComposerModeControlTests`.
- **A deep link into the Agents window must carry WHO** (`AppState.openAgentSettings` → `pendingAgentSelection` → `AgentsWindowFocus.selection`): it's a single reused `Window` that otherwise lands on whoever sorts first, which is the wrong agent every time the user got there from a locked disc that just named a different one — and it has to retarget a window that is ALREADY open. A request for the agent already showing still reloads the draft (`onChange(of: selectedId)` can't fire for an unchanged id, so the click would do nothing); no request + an existing selection leaves it alone, or a re-publish yanks the user to the top of the list mid-edit. Guards: `AgentsWindowFocusTests`.
- **Hands-free listens for every agent's phrase at once, longest FIRST** (`WakeWord.match`): "hey loki" is a prefix of "hey loki coder", so a shortest-first sweep makes the specific agent unreachable by voice. `strip` is untouched (single-phrase), the Loki homophone table stays Loki-specific, and colliding phrases are refused at SAVE time (`WakeWord.collides`, keyed on the phrase's last word — greetings are universal, so two phrases ending in the same word are one gate) because there is nothing to see until you try talking.
- **A custom resolution is validated against a MIRROR of the server's own clamp, and the two failure modes are different sentences** (`ResolutionGrid`, 2026-08-17): the image path's `clampFluxDim`/`clampKreaDim` silently REWRITE an off-grid size, while `handleVideo` returns a hard 400 ("width and height must be multiples of 32") — so the same typo costs a slightly different picture on one surface and the whole generation on the other. In-range-but-off-grid is a CORRECTION with a hint naming the size used and the step (so the next guess lands); out-of-range is a REFUSAL that disables Generate, because silently clamping 4000 to 1536 returns a picture a third of the requested size with nothing explaining why. **The snap rounds UP, matching the server's `((v + 31) / 32) * 32`** — rounding to nearest is the friendly-looking bug that prints a hint naming a size the server never generates (481 → 480 against the server's 512); verified live, a 500×500 request comes back 512×512. The grid is a PROPERTY on the image preset and a FUNCTION on the video one (`resolutionGrid(twoStage:)`), because LTX's alignment is decided by the PIPELINE — a two-stage tier denoises at half the canvas and earns its own /64 refusal — and every shipped LTX row is /64 for exactly that reason while H3's own fastest canvases (544, 672, 960) are /32 and not, so one constant could only ever be right for one of them. `ResolutionOption.custom` is a menu sentinel carrying **-1**, so anything wanting NUMBERS reads `concreteResolution`: `MediaToolArgs.resolution` hands `saved` back verbatim when the model names no size (its own `width > 0` filters guard only the pixel/aspect paths), which put -1 on the wire from the chat's `generate_*` until it didn't. Guards: `CustomResolutionTests` (incl. the snap-direction case, the per-pipeline split, and a passthrough test asserting the sentinel hazard still exists so the fix stays load-bearing).
- **An image preset DECLARES what the backend can do; the pane never guesses** (`ImageModelPreset.supportsImg2Img` / `supportsLoRA` / `supportsReferenceEdit` / `condWeightCount` / `stepsAreFixed`, each mirroring a concrete server fact). Mage-Flow shipped with FIVE dead controls — CFG, negative prompt, conditioning rebalance, LoRA and variation strength — that either did nothing or earned a 400. CFG and negative prompt turned out to be dead for EVERY image backend (`handleImage` parses neither, and `requestJson` never sent them), so both were RIPPED OUT end to end — control, `@State`, `ImageGenSettings`, `ImageQualitySettings`, `ImageGenRequest` — not just hidden. The wire body is unchanged (they never reached it), the tolerant decoder still eats the retired keys from an older build (`testImageSettingsIgnoresRetiredKeysFromOlderBuilds`), and 3D's `guidance` is untouched because `handleMesh` genuinely reads `guidance_scale`. Grep before assuming a field is view-local: the compiler found `ChatTurnEngine` building image requests too. A distilled model (`stepsAreFixed`) hides the quality tiers: they only bought time (measured 4/8/12 steps = 6/12/24 s for a different, not better, image). Where editing exists but img2img doesn't, a source image MEANS edit — `effectiveEditMode` must not depend on a toggle whose picker is hidden. Guards: `testMageFlowDeclaresTheCapabilitiesItActuallyHas`, `testMatchSourceIsOfferedOnlyWhenEditingAndSurvivesModeChanges`.
- **A capability the preset declares gates the FIELDS, not just the controls** (H3 ref2va, 2026-08-05): `VideoModelPreset.supportsReferences` is set true only on `minimaxH3Ref2VA` and only through the shared `minimaxH3Preset` factory's parameter, so the two FL2VA presets cannot drift into claiming a DiT capability they lack — the two partitions ship IDENTICAL files, geometry and file names, and an FL2VA pack handed references generates happily while ignoring every one of them. The pane hides the reference sections AND `requestBody` omits `ref_images`/`ref_videos`/`ref_audios`/`ref_image_size`, because a preset switch leaves the picked files in `@State` (the `firstFrameImageURL` convention — reference state deliberately survives so users can swap tiers) and hiding a control is not the same as not sending its field: that is the class that made every H3 request carry `pipeline` and 400. `refPaths` are ALSO gated at `VideoGenRequest` construction, belt-and-braces with `audioPath`, so a file never reaches the reader whose failure is a hard error. A reference video is an OBJECT (`VideoRefPayloads.Video` = frames + its own optional soundtrack), never a parallel `ref_video_audios` array where a silent clip shifts everyone else's pairing; frames are extracted at 24 fps with `AVAssetImageGenerator` capped at a 1024 edge and JPEG-encoded, and only as many as the server's 17k+5 ladder will keep (`refVideoFrameCount` — a 124-frame clip is ~10 MB of base64 before anything is generated). Guards: `testRef2vaFieldsAreGatedOnTheModelsOwnCapability`, `testRef2vaPresetIsTheOnlyOneAdvertisingReferences`, `testRefVideoFrameCountSnapsToTheModelsOwnLadder`.
- **H3 Turbo + chained windows follow the same capability discipline** (2026-08-06): `supportsTurbo`/`supportsChainedWindows` are DERIVED in the shared `minimaxH3Preset` factory as the complement of `supportsReferences` (`supportsLastFrame` rides the same complement on H3, but since #260 the three LTX presets declare it TRUE outright — the server pins the last latent frame on both pipelines) (both ride fl2va machinery — the LoRA is untested on the REF2VA DiT, and a reference has no keyframe row to chain through), so the fl2va presets cannot drift apart and the ref2va preset cannot claim either. `requestBody` emits `turbo`/`chain_windows` only when declared AND engaged; the pane's Turbo toggle snaps Steps into the LoRA's own 4...16 range (default 4, `effectiveStepsRange` — hydrate restores turbo BEFORE clamping steps or a saved 4 bounces up to the preset's 16 floor), disables the now-meaningless Max-quality toggle, and every plan/estimate call reads `effectiveFast` (turbo forces the server recipe off), never `!bestQuality` alone. Picking a quality tier turns turbo OFF — a tier's step counts describe the full-render schedule. `H3Plan.peakBytes(turbo:)` bills the resident LoRA beside the DiT (on the real packs the staged load floor already covers it; the mechanism is pinned on a mutated preset). A missing `turbo_lora.safetensors` is the SERVER's named 400, and the app fetches it on demand now that the mirrors carry it (`startTurboLora`, alerting on failure — the user ticked a box, so silence just moves the discovery to Generate). Guards: `testTurboAndChainingAreFl2vaOnlyAndGateTheirFields`, `testTurboBillsTheLoraBesideTheDiT`.
- **A pane that refuses what the SERVER accepts makes its own controls useless, so a quality floor is a SENTENCE, not a range** (H3, issue #254, 2026-08-23): `stepsRange` started at 16 and the frame ladder at 124 because those are the lowest values we have a verdict on — but `gen.handleVideoH3` has no step floor (only turbo's own 4) and `temporalShape` aligns `max(5, length)`, so the pane was strictly tighter than the API it drives. That locked out every distilled few-step adapter the pane's OWN Style LoRAs slot exists to load, worst on REF2VA, the one pack with no Turbo toggle to unlock the low end — the reporter had to hand-write HTTP or rebuild the app. Both floors moved onto `testedStepsFloor`/`testedFrameFloor` (+ per-preset note text; 0 = the range is fully tested, so LTX says nothing) and render as an orange line under the control. Two things do NOT move with the slider: the quality TIERS still start at 124 frames / 16 steps, and the UNATTENDED chat path (`MediaToolArgs.videoFrames`) floors at `testedFrameFloor` — nobody reads a warning there. `stepsAdvisory` takes `distilled` (Turbo toggle OR any attached LoRA), because advising against 4 steps to someone who just attached a 4-step LoRA is advice against the setup. Guards: `H3ReachableRangeTests`.
- **Style LoRAs are a STACK, and the rows are the state** (PR #118, merged 2026-08-06): `ImageGenSettings`/`VideoGenSettings` carry `loras: [LoraAdapter]` (path + scale) instead of one `loraPath`/`loraScale` pair, sent as `lora_paths`/`lora_scales` arrays that the server SUMS — so order never matters and a second adapter is an addition, not a replacement. `LoraAdapter.id` is deliberately NOT persisted (`CodingKeys` omits it): it exists so `ForEach` keeps row identity while the list is edited, and persisting a UUID would make two identical stacks compare unequal. The decoder MIGRATES the pre-stack blob through a separate `LegacyLoraCodingKeys` container — the old keys are no longer stored properties, so they cannot ride the synthesized `CodingKeys`, and without the migration every existing user silently loses their adapter. Rows with an empty path (mid-edit, right after "+") are dropped before the request is built, never sent as `""` for the server to reject. Hydration drops entries whose file has moved, same as the single-path version did. **H3 takes them too** (`supportsLoRA` is true on every video preset now): the server resolves adapters against H3's own module names and sums them with the Turbo distillation, so the pane's gate stays the preset flag rather than a backend check. Guards: `MediaGenServiceTests` LoRA cases, `testMiniMaxH3DeclaresTheCapabilitiesItActuallyHas`.
- **A capability a pack cannot deliver is DECLARED at the point of use, in the control's own label** (ACE-Step Cover, #269): a pack downloaded before cover mode has no `fsq.safetensors`, and the only signal was the server's named 400 — earned after the user had already attached a source track. `CoverWeightsFetch` (pure: task x source-audio support x remote x pack dir x in-flight) names the file IN the mode picker's Cover segment, so the reason is readable before the mode is selected, and offers the ONE 420 MB file through the `startPackFile` contract (never the 5 GB pack — `model.safetensors` is byte-identical across the two revisions). The check is purely LOCAL: `ServerManager.resolveModelDir` already spans every SERVED root (`readRoots`), so an LM Studio / custom-folder pack answers too and no protocol change was needed. **A pack we cannot write is `.missingUnwritable` and says WHERE the file goes** rather than offering a button that would fail. `Engine.fsqAvailable` re-stats on every cover request, so a resident model picks the file up with no reload — nothing to warn about. Only `task == .cover` reads the tokenizer; `complete` (Vocal to BGM) builds its context straight from the source latents and must never be gated on it. Guards: `CoverWeightsFetchTests` (incl. a source scan pinning the pane's wiring, verified red, and a scan pinning `src/gen.zig`'s cover-only gate).

- **A dependency that ships INSIDE a pack still has to reach the installs that predate it** (H3 Turbo adapter, 2026-08-06): `turbo_lora.safetensors` is allowlisted in the H3 bundle so every new download brings it, and deliberately NOT a ready marker — a pack downloaded before it existed must keep reading as complete, or the pane offers a 69 GB re-download for one 744 MB file. Those installs fetch it ON DEMAND instead: `TurboLoraFetch.decide` (pure: requested x preset-supports x remote x on-disk) fires `DownloadManager.startTurboLora` the moment the toggle flips — not at Generate, because 744 MB discovered thirty seconds into a job reads as a hang — and Generate re-checks as belt-and-braces, since turbo can be persisted ON from a previous session. `startTurboLora` ATTACHES to an in-flight transfer rather than starting a second one, which is what makes those two triggers safe together; it routes through `download(repoId:selection:)` like everything else (a hand-rolled fetch is source-audited against) and asks for the one file, so the size-matched skip leaves the other 69 GB alone. **It downloads INTO the pack's resolved dir** (`download(destDirOverride:)`) — writing to the destination root while the pack lives in another owned root created a fragment dir that read as a (broken) model to every resolver and killed the server (live 2026-08-08; full story: root `## Rules` incomplete-media-pack entry). The toggle's off-flip cancels via `cancelTurboLora`, which is surgical (the one file's partials, never the dir — that dir is a live pack) and a no-op with nothing running. Guards: `TurboLoraFetchTargetTests`. A LAN model is `.unavailableRemotely` and says so: the pack is on someone else's Mac and completing OURS would fix nothing. The FILENAME is a three-way contract (server resolve, bundle allowlist, HF fetch) with no compiler between them, so a test scans `src/gen.zig` for it — verified red by renaming the constant. Guards: `H3PlanningTests` turbo-fetch cases.
- **Prompt GUIDANCE is per-backend, and a model whose prompt is a FORMAT needs the format named where the prompt is typed** (2026-08-06): the video pane gave LTX advice on every model — a "4-8 sentence" hint, a link to LTX's prompting site and one static LTX-shaped Examples list, shown unchanged with MiniMax-H3 selected. H3 is not prompted in prose at all: MiniMax expands the user's request into a labelled document (H3-Context-IR, hosted, no weights published) before the DiT sees it, and mlx-serve sends the prompt VERBATIM, so the document is the user's to write. `VideoModelPreset.promptFormat` is the ONE chokepoint (derived from `backend` x `supportsReferences`, never sniffed from an id) behind all four surfaces — examples, placeholder, hint and the tips link — and the bodies live in `H3PromptExamples` in the format VERBATIM, the `ImagePromptExamples` rule (a reword is a quality change). Measured before shipping, not assumed: two same-seed pairs (one per pack) varying only the format had both labelled arms hold the described shot while both prose arms drifted off subject — root `## Rules`. The placeholder NAMES the section labels because nothing else can: `integrated_multimodal_description:` is unguessable, and for ref2va `retention_analysis:` is the only place a reference's role is stated. Two things the wiring forced: a stale cross-model LINK is the dead-control class (LTX's site is advice for a different engine), and the two engines spell SPEECH differently (LTX quotes vs H3's `<d>[English] ...</d>` + `(S1)`), so the old dialogue guard was re-pointed at the API and now pins BOTH — it had been scanning `VideoGenView.swift` for strings that moved, which passes vacuously off unrelated view text. Guards: `testPromptFormatFollowsTheModelsOwnCapabilities`, `testEveryH3ExampleCarriesItsFormatsSectionsInOrder`, `testNoLtxExampleLeaksIntoTheH3ListsOrViceVersa`, `testHintAndTipsLinkAreSelectedPerBackend`, `VideoGenDialogueExampleTests`. Format spec: MiniMax's two writing guides in `MiniMaxAI/MiniMax-H3` `docs/` (deliberately NOT vendored — the pane links to theirs; a working copy lives in `~/claude-tmp/h3-ref2va/repo-docs/`).
- **Every path that picks a video model must land on the right PRESET, because the preset shapes the request** (H3/LTX cross-pollution audit 2026-08-04): a LAN pick adopts the local preset matching its base id (`LanPick.base(of:)` in the selection binding + `VideoGenSettings.resolvedModel` — the blanket LTX fallback sent a remote H3 off-canvas sizes and frame counts below its 124 trained floor); chat previews run the model's OWN fast tier (`MediaChatDefaults.videoSteps(for:)` — the shared constant 8 was LTX's tier, under H3's validated 16-step floor); a stale audio clip is cleared on switch to a no-audio-input backend AND gated out of `audioPath` (its transcode failure is a hard error). Corollary: a hand-listed tolerant decoder drops the field you forget — `VideoGenSettings.bestQuality` was saved but never decoded, so H3's Max-quality toggle reset every launch. Guards: `LanSharingTests`, `MediaGenSettingsTests`, `MediaToolArgsTests` fast-tier case, `VideoGenA2VWiringTests`.
- **An audio preset DECLARES `supportsCloning`, and the catalog it lives in follows from it**: `AudioModelPreset.all` is the CLONING-CAPABLE media-gen catalog (AudioGenView's reference-clip control and AudioGenService both send `ref_audio`, and VideoGenView's "Speak text" composer picks from the same list), `allIncludingVoiceOnly` is the full list for the model browser and the catalogue guards. A non-cloning backend in `.all` reaches `ref_audio` and earns a named 400, so Kokoro is voice-mode only (Settings ▸ Voice, tray picker, web console) and every path that speaks with it names `.kokoro82M` DIRECTLY, meaning nothing in voice mode depends on `.all`. The same flag picks the download bundle: `MediaBundle.kokoro` (recursive, markers `voices.safetensors` + `g2p`) vs `.tts` (`speech_tokenizer`). Reusing `tts(...)` made a fully-downloaded Kokoro read as permanently incomplete, so the pane offered Download forever. Corollary now that the app fetches these itself: a voice backend's "is it here?" check is its bundle's READY MARKERS (`VoiceCloneMenuModel.bundleOnDisk`), never a bare `config.json` probe, which goes true early in a 345 MB pull and offers a voice that can only produce silence. Guards: `testKokoroIsAbsentFromTheMediaGenCatalog`, `testAudioBundleDispatchFollowsTheDeclaredCapability`, `testKokoroBundleIsNotTheTtsBundle`, `testKokoroReadinessNeedsTheG2pDictionaries`.
- **Custom (user-added) media models ride their FAMILY preset** (`CustomMediaModels`): every gen pane's picker adds "On This Mac" rows for local `/v1/models` entries whose `meta.architecture` maps to a served media family and whose id matches no catalog repo, synthesized via `asCustom` (family knobs + capability declarations, id = name = repo = the discovered `<org>/<name>` dir). Settings resolve through `resolvedModel(models:)` (chat `generate_*` tools included); LAN picks of a peer's custom model adopt the family by BASE id (the H3-sent-LTX-shapes class); Kokoro stays voice-mode-only and a custom H3 is the FL2VA family (`tasks` isn't in `/v1/models`). Rows share the LAN rows' limitation: nothing renders while the server is down. Guards: `CustomMediaModelsTests`.
- **A community media pack is downloadable from search only after its TREE proves the family bundle's markers** (`HFModel.mediaFamilyModelType` from `expand[]=config`'s `model_type`, a served diffusers `_class_name` — `servedDiffusersClasses`, Mage-Flow only: diffusers repos carry no model_type, and a class maps ONLY where our engine loads that repo's own layout (raw krea/BFL-flux upstreams don't; their CONVERSIONS ship a root config.json and come through the model_type door) — or a verbatim media tag): the pipeline tag ("text-to-video") is NOT whitelisted — that would hand a button to every diffusers repo — and a raw upstream checkpoint declares the same `model_type` as a converted pack, so `mediaStructureVerified` (the family bundle's own `readyMarkers` vs the tree the size column already fetches; nil/false = no button) is the gate. The download IS the family bundle with the repo swapped in (`CustomMediaModels.bundle(arch:repoId:)` → `startBundle`; cancel must be `cancelBundle` — the task is keyed by BUNDLE id), and onFinish calls `server.rescanModels()` (`POST /v1/models/rescan`) because server discovery only walks the roots at boot. Guards: `CustomMediaRepoTests`, `tests/test_model_rescan.sh`.
- **Dropping a ready MARKER is not enough — the repo has to RESOLVE first** (configless-repo class, third bite): `mlx-community/flux2-klein-9b-4bit` ships four weight subdirs and NO root json at all, so removing `config.json` from its bundle markers changed nothing — `DownloadManager.existingModelDir` still counted a dir as holding a model on `config.json` / `model_index.json` / a `.gguf`, and a complete 8.9 GB download read as absent: "Download (~10 GB)" forever, and a click flickers and reverts (files present → size-matched skip → instant finish → re-check still false). `holdsWeightLayout` is now the ONE answer for every non-GGUF layout and accepts a `transformer/` holding real `.safetensors` (not the bare subdir — a created folder or an in-flight `.partial` must still read incomplete, and no chat model has that dir). `ServerManager.resolveModelDir` CALLS it instead of re-listing the markers; that duplicate list is what made this a two-site bug, the second site turning the same repo into `.modelMissing` at generate time. Guards: `testKlein9BOnDiskWithNoRootJsonAtAllReadsAsReady`, `testEmptyWeightSubdirsAreNotAModel`, `testResolveModelDirAcceptsTheConfiglessWeightSubdirLayout`. Server-side twin: `peekMfluxFlux2` (root `## Rules`).
- **An MLX repo can be a SHELF too — one complete model per quant SUBFOLDER** (`LiquidAI/LFM2.5-2.6B-MLX`: 4bit/5bit/6bit/8bit/bf16/mxfp4/mxfp8/nvfp4, nothing loadable at the root; live 2026-08-05): the chat-default depth gate matched zero files, so Download created an EMPTY `models/<org>/<repo>/` and reported Complete, and the RAM column read "Unknown" (HF computes `safetensors` metadata from a ROOT index a subfoldered repo doesn't have). Same treatment as a GGUF shelf: `MlxVariantScan.variants` + `MlxVariantMenuModel` + `MlxVariantMenu` mirror the GGUF trio, and detection is FREE — such a repo always fails `needsFallbackFetch`'s metadata test, so `parseFallbackSize` returns `.mlxVariants` off the tree call the size column already makes (checked BEFORE its safetensors sum, which would report all eight quants added together). **The layout is the load-bearing decision**: a variant downloads to its OWN two-level dir `<org>/<repo>-<folder>`, NEVER nested at `<org>/<repo>/<folder>` — `model_discovery.discoverModelsInDir` walks exactly two levels, so a nested variant is a fully downloaded model invisible to `list`, `/v1/models` and the picker; as a sibling it's an ordinary model to every layer above the download and the server needed NO change. Two gates keep normal repos out (root `config.json` ⇒ the repo IS a model, its `mtp/`/`original/` are sidecars; root `model_index.json` ⇒ diffusers components) and a variant must be standalone-loadable — config + weights + TOKENIZER, which is what tells a quant from a diffusers component and from klein-9b's config-less weight subdirs. `FileSelection.subfolder`/`localPath(forRemote:)` strip the prefix on the way to disk, `download(…, destRepoId:)` splits WHERE from WHICH-repo while progress stays keyed on the clicked repo, `startMlxVariant` scopes cancel to one variant; a whole-repo download that selects NOTHING now refuses before creating the dir. Guards: `MlxVariantTests` (incl. live legs behind `MLX_SERVE_LIVE_VARIANT=1`).
- **A media generator is not the chat model, and `modelInfo` can't tell you which is which** (live 2026-08-05): gen panes load image/video/audio models into the SAME registry and the server sorts its default first, so `ServerManager.modelInfo` (= `allModels.first`) is regularly a model that would 400 a chat turn — the pill named it and dotted it GREEN while every `chatModelId` reader addressed the turn to it. The picker's LIST was already chat-only (`isChatPickable`), which is exactly why it was invisible. `chatModelId`/`chatModelInfo` now resolve through `residentChatModel`: `modelInfo` only when it `servesChat` (`slotKind == .chat` — a multimodal chat model advertising vision/audio INPUTS qualifies, an empty-capabilities GGUF/pre-Phase-G entry still counts), else a resident non-LAN chat entry, else **nil** — never an unloaded stub, since the dot keys on nil to stay amber rather than claim ready. A LAN pick still wins undiscovered (the 15 s rediscovery wait). Same class one hop out: the CLI launcher (chat empty state + tray + Tools menu), setup instructions and TestServer baked `modelInfo?.name`/`contextLength` into agent configs — source-scan guard, no `servedModelId:` may be passed `modelInfo?.name`. Guards: `ChatModelResolutionTests`, `ChatSurfaceModelSourceTests`.
- **An instruction editor's repertoire IS its prompt vocabulary** — so the Examples menu is a FEATURE SURFACE, not a nicety (`ImageEditExamples.swift`, same `Menu("Examples")` idiom as VideoGenView, grouped into submenus; content switches on `promptExamples(editing:)`). Mage-Flow-Edit does depth/canny/HED/segmentation/normal/pose maps, the reverse map→photo direction, de-blur/de-haze/de-rain/de-flare/colourise/low-light, camera moves and try-on — all through the ONE edit endpoint, none of it discoverable without the sentence that triggers it. Bodies are the model's OWN published phrasings, VERBATIM: shortening the depth prompt to "make a depth map" returns a grey photo instead of a depth map, so a reword is a quality regression (pinned by `testControlMapPromptsAreTheModelsOwnWordingVerbatim`). A model only lists what we VERIFIED on it — FLUX gets the generic groups, not the control-map ones.
- **An editor's default output size is the SOURCE's size** (`ResolutionOption.matchSource`, width/height 0 → the request omits `size`): the reference pipeline's `max_size = source size`, and the only choice that can't distort the picture the user handed over. An explicit size is the override, interpreted server-side as a pixel BUDGET at the reference's aspect. The option only appears while actually editing, and `validResolution` re-points a stale selection when the mode or model changes (per-model menus differ — Mage-Flow offers 2048 and 4:1 shapes FLUX doesn't). Guard: `testImageRequestJsonOmitsSizeForMatchSource` + `tests/test_mageflow_edit.sh`.
- **A new arch goes on BOTH model_type lists in the same commit**: Zig `model_discovery.supported_model_types` and Swift `HFModels.supportedModelTypes` (plus `supportedArchitectureTagPrefixes` when the HF tag has no family prefix). `SupportedModelTypeSyncTests` reads the Zig list and fails on any entry the app lacks (qwen3_5_text had been missing).
- **An arch gate that spells the model_type by hand is a gate that can miss it, and an EMPTY config block is not a capability** (LFM2-VL, 2026-08-13): `supportedModelTypes` carried `"lfm2-vl"` while every checkpoint ships `lfm2_vl` — an exact-match set, so the server served the model happily while the Downloaded tab called it "Unsupported architecture" (Zig matches the `lfm2` PREFIX, which is why only one side was wrong). Same round, the other direction: `parseConfigMetadata` badged vision off `cfg["vision_config"] != nil`, and mlx-community's TEXT-ONLY LFM2.5 packs declare `Lfm2ForCausalLM` with a vestigial `"vision_config": {}` — the `_text`-suffix guard cannot see them, since the arch is plain `lfm2`. Both guards stay: `_text` for the suffixed siblings, non-emptiness for the ones that keep the key but not the tag. Guards: `testLfm2VlIsSupported`, `testParseConfigMetadataEmptyVisionConfigIsNotAVisionTower`.
- **A checkpoint that only works next to ONE other checkpoint is a dependency, not a catalog entry**: the Gemma 4 assistant drafter had its own Model Browser destination — five `*-it-assistant-bf16` repos whose relationship to the models you actually own was invisible, and the answer to "which of these is mine?" was the whole feature. It now rides along with its target (`DownloadManager.companionDrafterRepo` → `start()` pulls it after the model lands), the same shape as a ds4 GGUF quant pulling its MTP head. The MoE 26B-A4B is EXCLUDED: the drafter regresses decode there (the server already defaults it off on MoE targets), so fetching it would spend bandwidth on something we then refuse to use. One size parser for everyone (`gemmaVariantFor`) — a new Gemma size taught to the pairing path but not the download path is a silent half-feature. A companion failure never alerts (`download(alertOnFailure:)`): a dialog naming a repo the user never asked for reads as a bug in the download they did ask for. Settings' `DrafterRow` is now the ONLY way to fetch one for a Gemma that landed earlier — HF search filters drafter repos out, so its old "Browse" button would have sent people somewhere that couldn't help. Guards: `DrafterAutoDownloadTests` (pure pairing rule + the real download loop against the stub origin).
- **A default that can't tell "not decided" from "switched off" turns itself back on behind the user**: once the drafter ships WITH its model, leaving the Settings switch off by default means downloading a speedup and never using it — so the app pairs it itself (`DrafterPairing.decide`, called from `selectedModelPath`'s didSet and, add-only, from `refreshModels`). `drafterPath` alone cannot carry that: empty is both states at once, so an explicit off would have re-paired at the next model switch. `ServerOptions.drafterOptOut` is that bit (persisted, NEVER a launch flag; the toggle writes both halves). The model switch both pairs and UNPAIRS (wrong-size drafter = `DrafterTargetMismatch` at start) while the refresh path only ADDS — it runs at 1 Hz during a download and would otherwise strip a drafter the user deliberately enabled on a MoE target. The MoE is never auto-paired even with its drafter on disk (an old Drafters-pane fetch), and the hot-switch path reads the DECIDED `serverOptions.drafterPath` instead of re-reading the disk, so it can't load a drafter the restart path wouldn't. Guards: `DrafterPairingTests`.
- **Once N connections write one file, the `.partial`'s SIZE stops being the resume cursor** (`ChunkedFileDownloader`): ranged chunks write at their own offsets via `PositionalFile`/`pwrite` (never a `FileHandle` — a shared write cursor is the one thing they must not share), so the file is SPARSE and its size is the highest offset touched. Per-chunk progress lives in a `<name>.partial.parts` sidecar written AFTER the bytes land — under-reporting is harmless (refetched, rewritten at the same offset), over-reporting leaves a hole in a file we then call complete. Sidecar + `.partial` are ONE unit (`commitPartial`, `removeGgufQuant` retire both). Three distinct fallbacks: a `200` to a ranged chunk (origin ignored `Range`) and a `Content-Range` total contradicting the planned size both void the plan → wipe + re-stream; a chunk closing CLEANLY short is reported by no socket error, so success is gated on banked bytes == file size. No sidecar + an existing `.partial` = a contiguous prefix from the old single-stream path → `planAdopting` keeps it. `DownloadSession.shared` is ONE session for everything with `httpMaximumConnectionsPerHost` raised (URLSession's default 6 would silently queue chunks 7-8 and make the feature look inert); that shared session is the measurable win (10 small files 2.30s → 1.34s), while chunking is insurance for a single-stream-limited line and a WASH when the line saturates — never quote it without naming the link. Token: `hfToken` reads `HF_TOKEN` then `$HF_HOME/token` then `~/.cache/huggingface/token`, because a Finder-launched bundle has no shell env. **Six entry points** (`start`, both `startGguf`, `startBundle`, `download`, `downloadGguf`) over ~14 call sites all funnel into `download`/`downloadGguf` → the two `transferFile` calls; a seventh that hand-rolled a `dataTask` would WORK and silently opt out of all three wins, so it's source-audited — as is "no service touches huggingface.co on `URLSession.shared`" at FILE scope (a line-scope version passes vacuously, since the URL is built a line above the fetch — that false negative hid `HFSearchService` eating rate limits untokenised). Guards: `ChunkedDownloadTests` (transport; `URLProtocol` stub speaking real Range, asserting assembled bytes; live legs behind `MLX_SERVE_LIVE_DOWNLOAD=1`) + `DownloadManagerTransferTests` (the loop around it: retry, resume, skip-if-present, cancel-leaves-nothing, both audits).
- **First run picks the model, and ONE function decides which** (`RecommendedModelPick.starterPick(physicalMemoryBytes:)`, RAM-tiered, bands upper-inclusive so a 16 GB Mac gets E4B): three surfaces make that recommendation — the welcome window, the chat gate, the Model Browser's "Best for your Mac" card — and the two first-run ones render the SAME view (`RecommendedStarterCard`), because two copies shown minutes apart is how they start naming different models. The card's download hook doesn't stop at "downloaded": it selects the model and starts the server (`useModelAndAwaitReady`), since the flow exists to end in a working chat, not in a folder of weights. It handles a `ggufFilename` pick (quant path, not the whole repo) even though no tier is one today — the assumption is what would break the first time one is. Guards: `RecommendedModelsTests` tier/boundary/fit cases, `FirstRunFlowTests`.
- **A blocking sheet still needs exactly ONE door, or its Cancel is unimplementable**: the chat gate first presented on `.constant(true)`, which made it properly un-dismissable and made Cancel do NOTHING — AppKit refuses to close a window that has an attached sheet, so `dismissWindow(id:)` was a silent no-op and the user was stuck (measured through the accessibility API: the sheet was still there afterwards). The binding's SETTER is what swallows Esc/click-away; Cancel flips a `@State` that ends the sheet and only then closes the window, in that order. Gate condition counts LAN peer chat models too (`ChatGateState.resolve`, same as `trayHasNoUsableModels`) — blocking someone out of a conversation they can already have is worse than the dead composer it replaces. Pure seams: `ChatGateState`, `LaunchDecision` (`Models/FirstRunFlow.swift`).
- **The recovery goes where the problem is DISCOVERED**: a stopped server is found out in the chat window — you type and nothing answers — and the only thing that said so was the model pill's dot going grey, with the fix over in the tray. `ChatServerStartControl.resolve(status:hasStartableModel:)` puts a red **Start** beside the picker (the leading toolbar item, not the trailing cluster). It earns a slot in that width budget only because it is TRANSIENT: gone the moment the server is up. Red belongs to the one actionable state — `.starting` is the same control still reporting (spinner, disabled, grey) rather than a second thing to press, and it stays visible during the tens of seconds a model takes to load, since vanishing on click reads as the click not landing. Nothing startable ⇒ hidden, not a disabled red button (dead-control class; the pill already says "Select a model"). The action is `appState.ensureServerForLan()` — the app's ONE start path, which loads the selection or boots headless when the model answering is on another Mac. Drawn `.plain` with explicit padding, never `.bordered` (intrinsic-size class). Guard: `ChatServerStartControlTests`.
- **A `UserDefaults.bool` default is FALSE for a key nobody ever set**, which is not the same as "the user turned it off": `autoStartServer` read that way meant a fresh install downloaded a model and then sat there with the server stopped. It reads `object(forKey:) as? Bool ?? true` now. Safe because the launch gate is `autoStartServer && !selectedModelPath.isEmpty` — a no-op until a model exists — so nothing starts eagerly; the first download's completion hook is what starts the server. Deliberately NO migration: existing users who never touched the toggle get it on.
- **A model's SPEED score is ours; its intelligence score is someone else's** (`RecommendedModels.swift` header, three bars in the browser): artificialanalysis's speed is measured on cloud GPUs and that ordering does not survive the move to Apple Silicon, where decode is bandwidth-bound and ACTIVE parameters dominate — same M4 Max, `gemma4-26b-a4b` 118 tok/s vs `gemma4-31b` 25 tok/s, a 4.7× gap a cloud comparison shows as level. So speed is hand-tuned against our own bench history (`benchmarks.md`, and the retired `docs/perf-csvs/` CSVs in git history), and `activeParamsB` (a fact per checkpoint) exists to CHECK it: a model that wakes more parameters per token can never be scored faster than one that wakes fewer (ties unconstrained — that's where 4-bit vs 8-bit legitimately differ). Scores describe the ORIGINAL weights, not our quants; the ones the site has no entry for are flagged `intelligenceIsEstimated` and say "estimated" in the row rather than passing as measured. Guard: `testABiggerModelIsNeverScoredFasterThanASmallerOne`.
- **ServerOptions defaults mirror Zig defaults** (`src/main.zig` CLI vars / `src/server.zig` pub globals). Changing a server default = update the Swift field default AND the `toCLIArgs` emit-guard in the same change; memory-critical caps always-emit (prefix-cache-entries, RAM-clamped). Guard: `testDefaultLaunchOmitsAllMatchDefaultFlags`. The one deliberate divergence is `enableMetrics`, ON by default here: the tray's throughput rows poll `/metrics.json`, and there is no zero-cost way to get them otherwise.
- **A child process is killed by whatever SPAWNED it, on a notification every quit path fires — `deinit` is not that** (#133, live 2026-08-07): ⌘Q / the app menu / Dock ▸ Quit left `mlx-serve` running with the whole model resident, because the tray's power button was the ONLY route calling `server.stop()` before `NSApplication.terminate`; everything else went straight to termination. `ServerManager.deinit` looked like the safety net and never runs at app exit — a `@StateObject` is not deallocated on termination, the process just goes away. So the teardown is a `willTerminateNotification` observer registered in `ServerManager.init`: one object owns the process, so every quit route is covered by construction, and no new button can forget it. `queue: nil` is load-bearing — delivery must be SYNCHRONOUS on the posting thread, since `.main` enqueues the teardown for a runloop turn that during termination may never come (the same leak with extra steps). Guards: `ServerQuitTeardownTests` (posts the notification and asserts `.stopped` with NO runloop turn in between, which is what pins the synchronous delivery).
- **A flag the app cannot emit does not exist, and there is no passthrough to fall back on** (`maxResidentMemGB`, 2026-08-07): `--max-resident-mem` is the gate that decides whether a cold load is ADMITTED — and it was absent from `toCLIArgs`, so every GUI launch ran the auto cap and a model that cap refuses was unloadable at any setting. `--skip-mem-preflight` is not the escape hatch it looks like: the registry gate runs BEFORE the preflight it skips. **It is a NUMBER off a `snappingSlider`, not typed text**: `main.zig` exits on a `--max-resident-mem` it cannot parse, and the first cut answered that with a Swift-side validator mirroring `parseSizeArg`'s grammar — a whole class (and its test) that a value which cannot be malformed deletes outright. 0 = Auto, `residentMemPresets` stops the ladder at physical RAM (a cap above it can never be reached). New `ServerOptions` field also needs its `SettingsReset` section entry (`testEveryServerOptionsFieldBelongsToExactlyOneSection` is the tripwire). Guards: `testMaxResidentMemIsEmittedWhenSetAndOmittedOtherwise`, `testResidentMemPresetsAlwaysOfferAutoAndNeverExceedRAM`.
- **A spawn-time placement decision must be re-evaluated when the setting changes** (MCP-outside-the-sandbox class): stdio MCP servers are pinned to where they were SPAWNED (host process vs guest bridge), so flipping the sandbox toggle must respawn running ones — `AgentSandbox.configure` posts `placementChanged` (rule: `mcpRestartNeeded`), `MCPManager.restartStdioForSandboxChange` stops + respawns strictly what was running (no-op with zero sessions, so the launch-time announce can't eagerly start servers; HTTP sessions have no placement). Guards: `testConfigurePostsPlacementChangeOnToggleFlip`, `testMcpRestartNeededRule`, `testSandboxRestartNoOpsWhenNothingIsRunning`.
- **Changing a persisted setting's DEFAULT does nothing for existing users** (sandbox base-image class): the tolerant decoder restores the stored value, so a renamed default strands upgraders on the old one forever (and factory reset / re-pull can't fix it — they operate on the cache, not the blob). Ship an explicit decode migration, or PIN the value if the feature requires one exact artifact (the sandbox image is pinned: `SandboxConfig.baseImage` static, stored values ignored). Guard: `testStoredBaseImageIsIgnoredImagePinned`; story: docs/gotchas/app.md.
- **Assistant-history reasoning round-trips** (laguna nothink class): both history builders send `reasoning_content` on assistant turns (`ChatTurnEngine.plainHistoryDict`, `AgentEngine.buildAgentHistory` — key omitted when absent), and `tokenCostForMessage` bills it. Guards: `ReasoningHistoryRoundTripTests`.
- **writeFile append-flag conflation**: a boolean flag adjacent to a large free-text value is a JSON-mangle magnet — type it `boolean` in the schema, never string; `normalizeWriteFileArgs` peels flag+body welds; `appendFlagIsTrue` parses dirty flags (`"true,"`); the missing-content error must NEVER claim truncation (only the loop, which knows `maxTokensHit`, may say "cut off"). Guards: WriteFileHandlerTests, AgentWriteFileRecoveryTests.
- **Error steers**: an error sent back to a model must never contain an empty/placeholder example (`toolExample` extracts from any `Example…{` marker — class guard `testEveryToolDescriptionExampleIsExtractable`) and must name the missing key RELATIVE to keys actually sent, with a concrete example built from the model's own values. `parseLineNumber` reads `"45,"` as 45. Guards: EditFileRecoveryTests.
- **Reproduce from the real capture**: fix model-output bugs from the failing model's actual bytes (`chat-history.json`, `last-agent-request.json`, `tool-calls.log`), never a steered/different-format analogue.
- **LSUIElement activation**: go `.regular` BEFORE presenting any window/panel, synchronously — everything through the `AppActivation` chokepoint; never bare `openWindow(id:)` or `panel.runModal()` (source-audit tests enforce). Symptom: half-focused window/picker that activates only after a click.
- **A scroll-geometry callback runs INSIDE the layout flush — never write `@State` scroll position from it synchronously** (issue #136, crash log 2026-08-09): the transcript's follow-correction called `scrollPosition.scrollTo` from `onScrollGeometryChange`'s action; under a streaming re-layout storm (code-block re-highlight, markdown re-measure, or reopening a window that re-measures every row) each correction forces another layout that re-fires the callback — the beachball — until one write lands mid-`NSDisplayCycleFlush` and AppKit throws (`StoredLocationBase.beginUpdate` → `setNeedsUpdateConstraints` → `_crashOnException`, uncatchable). Geometry-originated corrections go through `DispatchQueue.main.async` (`ChatDetailView.applyScroll` — one turn later is outside the flush and coalesces the storm); user-initiated jumps (send, the button) stay synchronous, they run from event handling.
- **A file drag is `.fileURL` and NOTHING else, so the type filtering belongs in `validateDrop`** (Create panes' `MediaDropTarget`, 2026-08-13): filtering after the providers resolve is too late (the drop is already accepted and animated in — a dropped `.txt` lit the border and did nothing), but naming the kind's UTTypes in `onDrop(of:)` is not the fix — a Finder drag carries `public.file-url` alone, so a target registered for `.image`/`.movie`/`.audio` is never offered the drag and every pane stopped accepting files. The target registers `.fileURL`; `MediaDropValidation` reads the drag pasteboard in `DropDelegate.validateDrop` and refuses by the pane's own extension allow-list while the drag is in the air (`dropEntered` fires on validated drops only, so one verdict also decides the border). A drag we can read NOTHING off is no information, not a refusal — it falls back to filtering after the resolve, because bouncing everything is the worse failure. Guards: `MediaDropTests`.
- **A parent `.onTapGesture` + `.contentShape` swallows plain child Buttons on macOS** (sandbox chip ✕ class): a composite interactive row is built from REAL side-by-side Buttons (each with its own contentShape), never a tap gesture wrapped around embedded buttons — the child clicks silently die with no error anywhere.
- **A `DisclosureGroup` label is not a click target — macOS hit-tests only the chevron**: an accordion whose header should toggle needs `contentShape(Rectangle())` + `onTapGesture` on the label, safe ONLY because the label holds no buttons (see the parent-gesture rule above). Shipped twice: the Agents editor's Advanced row, the transcript's Thinking block (`MessageBubble.thinkingBlock`). Guard: `VoiceOrbInlineTests` thinking case.
- **One legacy `.alert(item:)` per window** (sandbox ✕-confirm class): an alert modifier on an ANCESTOR shadows a descendant's — not just same-node — so the second alert silently never presents. Every dialog a window shows rides the single window-level alert item (extend its `Action` enum); siblings on separate branches are fine. Guard: `testSandboxWindowHasExactlyOneAlertPresentationPath`.
- **NSToolbar can't re-measure dynamic content** — any strip whose members appear/change density at runtime is a plain view row (`.safeAreaInset(edge: .top)`), density from measured width (`ChatMetrics`), never ToolbarItems (» eviction, mangled overflow). Corollary (live 2026-07-19): the chat header cluster's width budget is FULL — even a static-size icon added there re-triggers » eviction at small widths and killed the band's material background. New chat-surface indicators go in the COMPOSER row, not the toolbar band. The sandbox shield that used to sit there is GONE (2026-07-29): the composer row is a budget too, and a status glyph that only ever opened Settings didn't earn a permanent slot next to the controls that configure the turn.
- **↑ recalls your own messages, and every rule in it exists to keep the arrow keys being arrow keys** (`ComposerHistory`): the walk arms only from the EDGE of the text (↑ at offset 0, ↓ at the end — so the caret still moves inside a multi-line draft, which is the only way to edit line two of what came back), only on an EMPTY draft (↑ at the start of a paragraph you are writing must not replace work — there is no undo for a swallowed keystroke), and it ENDS the moment the recalled text is edited. Consecutive repeats collapse (a retry should cost one press, not two), and a stale index reads as no walk rather than an out-of-bounds trap — a walk survives a reply landing mid-conversation. `GrowingTextEditor` hands over BOTH caret edges with the key, because the rule cannot tell "move the caret" from "recall" without them; `updateNSView` moves the caret to the END on any external text change, or a recall lands typing in front of the message it just brought back. Distinct from Edit & Resend by construction: recall only fills the field, so what you send is a new turn and history is untouched. Reset on send and on tab switch. Guards: `ComposerHistoryTests` (incl. both wiring scans).
- **A branch is cut at a boundary the model can be handed, and inherits the settings but not the GRANTS** (`ChatFork`): "Branch Chat From Here" keeps everything through the chosen message, then trims back — a tool call's results come after it, so cutting on the caller (or mid-results) hands the model a call with no answer, and our own error cards and tool-call summaries are machinery, not a turn to resume after. `isForkable` is that same trim, so the menu never offers a fork that would make an empty chat. The new session takes everything that decides how a turn RUNS (agent, mode, thinking, effort, MCP, per-chat tool switches, working directory) or its next turn answers under different settings than the turns above it in its own transcript — but NOT `attachedFolderPath` (its security-scoped bookmark is keyed by the SOURCE's id, so the path would arrive without the grant that opens it) and NOT `taskRunId`/`isExternalBridge` (transient sessions are hidden and never persisted, so the fork would vanish). Message ids are kept, not regenerated: every lookup is `(sessionId, messageId)`. Title carries over, and a placeholder stays a placeholder so the auto-titler names the fork from its own content. Guards: `ChatForkTests`.
- **A menu key equivalent outranks the first responder, so a menu command on an editing chord must ROUTE, not claim** (`ChatDeleteShortcut`, live 2026-08-12): `performKeyEquivalent` is offered the keystroke before the focused view ever sees it, so File ▸ Delete Chat on ⌘⌫ took that chord from every text field in the app — typing it mid-message raised "Delete this chat?", and `.disabled(chatDeletionTarget == nil)` cannot help, since a chat is open in exactly the state where you are typing into one. `requestChatDeletionFromMenu` asks who holds the keyboard and, for a text editor, PERFORMS `deleteToBeginningOfLine` itself — returning early is not enough, the menu already swallowed the event and ⌘⌫ would simply do nothing. `KeyboardFocus.isTextEditor` names `NSTextView` only: a focused `NSTextField` never becomes first responder, its FIELD EDITOR does. A multi-selection OUTRANKS focus (`route(editingText:selectedChats:)`) — several rows picked is unambiguous, and it is the belt-and-braces half against a stuck reading on a key that deletes conversations. Which is why taking the keyboard INTO the composer collapses the selection to that chat (`SidebarMultiSelect.focusingComposer`, on the `inputFocused` mirror so every route in is covered): typing is a statement that you are working in one conversation, and without it the two rules each read a true fact and disagreed — a selection left lit behind the field kept ⌘⌫ raising a delete dialog mid-message. Guards: `ChatDeleteShortcutTests` (incl. the field-editor case — a guard naming `NSTextField` passes its own test and fails live).
- **This window has no focus model, so "is a text field focused?" is only a usable question because two things now make it one** (`KeyboardFocus`, live 2026-08-12 — after one click in the composer, ⌘⌫ never deleted a chat again): (1) a conversation row is a `.buttonStyle(.plain)` Button and takes no first responder — the same fact behind `.onDeleteCommand` never firing — so clicking a chat moved the selection while the COMPOSER kept the keyboard and nothing ever took it back; `selectRow` calls `KeyboardFocus.resignTextEditor` (only when a text editor holds it — an unconditional `makeFirstResponder(nil)` yanks focus off whatever else legitimately has it), and `quickSwitch` deliberately does NOT, since ⌘\<digit\> exists to jump to a chat and type in it. (2) `GrowingTextEditor.updateNSView` drove first responder from the LEVEL of `isFocused`, re-taking it on every SwiftUI update — and `onResignFocus` publishes the cleared flag ASYNCHRONOUSLY, so the re-grab always won and the field could never be left; `isFocused` is set true on appear and again whenever a turn goes idle, and nothing ever cleared it. It is edge-triggered now (`Coordinator.appliedFocus`). Either one alone keeps the composer holding the keyboard forever. Guards: `KeyboardFocusTests` (both wiring scans + the resign's two cases).
- **State seeded for a message that does not exist yet is HELD, never written to `messages.last`** (`AppState.pendingRevisionSeed`, live 2026-08-12): regenerate captured the reply it was replacing and wrote it straight onto the last message after `runTurn` — correct on the plain path, where the streaming placeholder is appended synchronously, and silently wrong on the AGENT path, where `runAgentLoop` appends one per tool round from inside a Task and the reply the pager belongs to is the LAST of them. The seed landed on the user's own message, the `role == .assistant` guard dropped it, and the version pager simply never appeared with Tools on. It is now applied at the turn EXIT (`finishRevisions`, seed + commit as one `MessageRevisions.finishing` step) from exactly three sites — `stop`, `endTurn`, `appendErrorNotice` (before the error row, so the pager lands on the reply and not on our card) — and targets the last ASSISTANT message, since a turn stopped mid-tool ends on a `tool` row. A fourth call inside the loop would attach the pager to the first tool round. Guards: `RegenerationSeedWiringTests` (both scans), `MessageRevisionsTests` finishing cases.
- **A continuation EXTENDS the version being read; the pager counts REGENERATIONS, which a continuation is not** (`AppState.markContinuing` / `finishRevisions`, 2026-08-14): Continue and Regenerate both end at the ONE turn exit, and it could not tell them apart — `finishRevisions` proceeds on `!msg.revisions.isEmpty` alone, so a reply already at 2/2 went to **3/3** the moment you finished it, with 2/3 showing that same reply minus the ending just written. Nothing errors and nothing is lost, which is what makes it quiet. A continuation is the reply carrying on, so it syncs into the active revision (`MessageRevisions.applyingEdit` — the same helper an in-place edit uses, and for the same reason: stepping away and back reloads `content` from the stored revision). The mark is HELD like `pendingRevisionSeed` and carries its ordering hazard — `continueReply` opens with `stop(sessionId:)`, which IS a turn exit, so a mark set before it is consumed immediately. Everything else describing that message has to keep up too: the truncation notice is cleared (it is being un-cut) and `updateLastMessage(addingCompletionTokens:)` ADDS the counts, or a 900-token reply finished by a 42-token continuation reports 42 (the prompt count and the rate stay the latest generation's — a continuation's prompt already holds everything before it, and a rate is not a quantity to sum). `ContinueReply.isEligible` also takes the `ServerEngine`: ds4 renders its template inside the embedded engine with nowhere to append a prefill, so the server refuses by name and a live button there is the dead-control class. Guards: `ContinuedReplyBookkeepingTests`, `ContinueReplyTests` engine cases, `RegenerationSeedWiringTests` continuation cases.
- **A `.global` frame is not re-published when a view merely MOVES, so anything measured against one is pinned to the window that last laid it out** (⌘-badge band, live 2026-08-13): the sidebar numbers only the conversation rows in the CLEAR, and the band's top edge came from the pinned destination block's global maxY — which entering fullscreen TRANSLATES without resizing, so it kept reporting the number from the smaller window while the rows slid out from under it (measured in an isolated harness: `blockMaxY` 370.0 in windowed, tall and fullscreen alike, while row 0 moved 378 → 396). All three measurements — block edge, column edge, every row span — now share ONE named coordinate space (`ChatSidebar.bandSpace`), where the top edge is the block's HEIGHT and the bottom is the column's, neither of which a window move can invalidate. The old "belt and braces" second top publisher (`frame.minY + safeAreaInsets.top`) is deleted rather than kept: it described a different line entirely (measured 104 against a frost line at 370) and was harmless only because the key reduces by `max` — a fallback that can only ever be wrong is worse than none. The filter is `ChatQuickSwitch.numbering(rowSpans:clearBandTop:clearBandBottom:)`, so "a taller window numbers more rows" is a test and not a screenshot. Guards: `ChatQuickSwitchTests` band cases + a source scan (zero `.global` frames, exactly three readers in the named space).
- **Per-tab chat state keyed by session id** — `ChatDetailView` is REUSED across tabs (no `.id(sessionId)`): per-conversation state lives in id-keyed structures (`SessionToolAllowList`), persisted toggles on `ChatSession`, synced in BOTH `.onAppear` and `.onChange(of: sessionId)`; never a shared flag, never reset-on-switch. Guard: PerSessionUIStateTests.
- **Ghost turns**: any session-removal site must `chatEngine.stopIfOrphaned()` (per-turn sweep — deleting one chat stops ONLY that chat's turn); `runAgentLoop` re-checks the session exists each iteration. Symptom: an invisible turn keeps `last-agent-request.json` updating with nothing streaming anywhere, survives server restart. Guard: OrphanedTurnTests.
- **Multi-turn engine invariants** (`TurnLedger`): every turn carries a cleanup TOKEN — a superseded task's async unwind must present it and no-op (never clear its successor's slot); a cancelled task touches NOTHING in the session (stop already cleared the streaming flag, and a successor's placeholder may be live); `MediaTurnBudget` spends PER TOKEN, never "reset on token change" (interleaved concurrent turns alternated the stored token and got unlimited generations); `mediaProgress` carries `mediaProgressSessionId` so only the asking tab renders the card; `ActiveAgentVoice` is published only for voice turns or the ACTIVE chat (a background tab's agent must not hijack the speaking voice); voice end-of-turn detection keys on `$activeTurnSessionIds` ∋ activeChatId, and bargeIn/tray-New stop ONLY the voice session's turn. `isGenerating` stays "any turn active" for the TaskScheduler defer gate + TelegramBridge own-instance wait. Guards: MultiTurnEngineTests.
- **TaskRun.summary is a 280-char UI cap** — full results via `fullLastAssistantText(transcript…)`; Telegram sends split at 4096 (`splitForTelegram`) at every send site. Stale `.running` runs healed at launch (`reconcileStaleRuns`); `.needsApproval` durable; live-run Stop via `cancelledRunIds`.
- **No launch-eager audio graphs** (TCC class): AVSpeechSynthesizer/AVAudioEngine graphs built lazily on first use, idle paths must not force creation — else mic permission prompt at APP LAUNCH (re-prompts every ad-hoc rebuild). Guard: VoiceActivityTests lazy-init. Diagnose: `/usr/bin/log show --predicate 'process == "tccd"'` (zsh `log` builtin shadows it).
- **VAD is relative**: threshold = `max(0.015, ambient_floor × 2)` (min-statistics window) + a 2s transcript-stall backstop; never an absolute energy level. Symptom: run-on partials, stuck "Listening…", no user message ever lands.
- **Guest payloads ship in EVERY bundle variant** (issue #89): `Resources/guest/vz-agent` was MAS-only in build.sh and absent from release.yml, so every Developer ID build booted the legacy console shell and sandboxed MCP could never connect. Both packagers now stage it and FAIL the build when it's missing; only kernel+rootfs stay download-provisioned outside MAS. The MCP transport error names the missing half (`transportFallbackReason`). Guards: SandboxTransportTests fallback-reason cases + the build.sh/release.yml existence checks.
- **Sandbox**: `TerminalOutput.sanitize` normalizes `\r\n`→`\n` BEFORE bare-`\r` collapse (tty ONLCR class — else every line wiped); hvc2 lines are CRLF → split with `whereSeparator: \.isNewline` (Swift treats `"\r\n"` as ONE grapheme); port forwarder binds BOTH 127.0.0.1 AND ::1 (localhost resolves v6-first); served-URL form is env-specific (localhost in sandbox, LAN IP on host) and rides the env section + tool-result steer, never the base prompt.
- **Multi-input AVAssetWriter**: feed the single-shot track (audio) fully + `markAsFinished()` BEFORE the frame loop, or backpressure deadlocks; toy-scale stays green — guard runs at realistic scale (97×256×256).
- **A dropped frame is a FAILED mux, never a `continue`** (#170): `writeMP4` throws on a pool miss (after a standalone `CVPixelBufferCreate` fallback) and on `adaptor.append == false`, cancels the writer and deletes the file. Skipping frames shipped 28 KB black mp4s after hours-long H3 renders with a green log.
- **SwiftUI VideoPlayer is unsafe under state transitions (macOS 26.4)** — wrap AVPlayerView via NSViewRepresentable, and via the ONE wrapper (`AVPlayerViewRepresentable` in `Views/ChatMediaAttachmentView.swift`, shared by the Video window and the transcript): a second copy is a second thing to get the workaround wrong in.
- **Sandbox CLI sessions are ssh-only** (issue #89 follow-up): pi/hermes/shell run IN the guest via `/usr/bin/ssh` → `localhost:<sshPort>` (dedicated `SandboxPortForwarder`, `targetPortOverride → 22`; the general forwarder filters port 22 or the two fight over localhost:22) → dropbear BAKED into the image (`-R -s` + a devpts mount — devtmpfs alone fails PTY allocation; stale cache → `staleImageMessage` + re-pull). Keys are app-owned (`~/.mlx-serve/sandbox/ssh`, never the user's `~/.ssh`; known_hosts RESET per boot — the dropbear host key lives in the rootfs, so a re-pull changes it at the same `[localhost]:<port>` and a stale entry hard-fails ssh with the MITM banner); configs carry `__MLX_HOST__`, sed'd in-guest to the NAT gateway by `/.vz-bootstrap-<agent>`; guest→host is NON-loopback so the real `--api-key` rides them. SwiftTerm is imported by exactly ONE file (`EmbeddedTerminalView` — the libghostty seam). A live session PINS the shared guest: remount-needing workspace switches throw (`remountBlockMessage`), never a silent VM kill; app quit kills the VM. **Terminals are never unmounted while live** — every `EmbeddedTerminalView` stays in a ZStack and hides via opacity (unmount = dismantle = SIGTERM to ssh; flipping Terminal↔Activity killed the session, live 2026-07-19); sessions are MULTI: one tab per concurrent pi/hermes/shell (`SandboxSessionTabs`, one sshd multiplexes them all). Agent config files mirror what each CLI's OWN setup flow saves, verified from its source — never its docs (hermes's documented `model:` key left "provider: none" and re-ran the wizard; the real gate is `OPENAI_BASE_URL` in `~/.hermes/.env`). Guards: SandboxSSHTests, SandboxAgentRegistryTests, SandboxSessionTabsTests, EmbeddedTerminalLayoutTests, VzGuestTests ssh arm.
- **File tools require a working directory in EVERY mode, and the sandbox toggle gates them too** (file-tool bypass class): `resolveAndConfine` hard-fails on nil wd — the old "yolo passes nil = unconfined" lever is GONE (yolo task runs anchor at the default agent workspace via `TaskScheduler.workDir`; yolo stays unrestricted only at the APPROVAL layer). The five file tools stay host-side (virtiofs = same bytes) but check `FileToolSandboxGate` FIRST — a live pinned VM whose share doesn't cover the session's folder declines like shell's remount-block. New file tool = takes the gate + a `fileToolCases` entry in FileToolSandboxGateTests.
- **The default agent workspace is a SETTING** (`ChatSession.defaultWorkingDirectory`, UserDefaults-backed, Settings → Agent Sandbox): change ONLY via `AppState.setDefaultAgentWorkspace` (retargets sessions still on the old default, global security-scoped bookmark, sandbox remount). Workspace picks call `AgentSandbox.noteWorkspaceChanged` — stale share tears down for a fresh remount (+ `placementChanged` so guest MCP bridges respawn). EXPLICIT Settings pick = `restartPinnedSessions: true`: remounts even under live CLI sessions and posts `workspaceRemounted` → the Sandbox window restarts each living tab IN PLACE (same name, `SandboxSessionTabs.restart`; `sessionExited` is runtime-identity-guarded against stale ssh exits). IMPLICIT chat-tab pick stays declined while pinned. Guards: AgentWorkspaceDefaultTests, `testWorkspaceChangeActionRules`, `testWorkspaceChangeActionForceRestartsPinnedSessions`, `testRestartKeepsIdentityAndReturnsToPreparing`.
- **A model switch is a hot-switch, not a restart** (`AppState.modelSwitchAction` — read by the picker's `didSet` AND `TaskScheduler.switchModel`): a RUNNING server gets `/v1/load-model` with the model's ABSOLUTE PATH (a dir basename 404s on two-level `org/name` registry ids; register-by-path resolves either) + `"default": true` — without the flag the "mlx-serve" alias, `/v1/models`' default-first sort, and therefore `chatModelId` all stay on the OLD model. Restart only mid-boot or as the failure fallback; media side-loads never send the flag. The old `hotSwitchEnabled` gate (default-off, NO UI — so every picker change restarted the server, for everyone) is GONE. KNOWN: the server ignores the body's `drafter_path`, so a hot-loaded Gemma 4 runs drafterless until the next restart. Guards: `AppStateUseModelTests`, `APIClientLoadModelTests`, `tests/test_load_model_default.sh`.
- **LAN picks**: chat routes through `server.chatModelId`/`chatModelInfo` (LAN wins over local default) — never read `modelInfo?.name` directly at a new chat surface; gen services route `ServerManager.prepareGenModel` (LAN ids pass through, no local load/unload); `AppState.ensureServerForLan` keeps the server running for either toggle.
- **WKWebView**: `BrowserManager` is `@MainActor`, created eagerly at launch; `browse.readText`/`webSearch` navigate first, then extract.
- **JSONSerialization**: tool-call `arguments` must be a JSON String, not a nested dict (server checks `v == .string`); `[String: Any]` key order non-deterministic; `""` stays `""`.
- **The composer's "/" menu is a SIBLING view, not a popover** (`SlashSkillMenu` + the pure `SlashCommands`): an NSPopover takes first responder, and the menu has to stay open while you keep typing into the field it writes to. It opens only on a LEADING "/" whose token has no whitespace yet (`src/main.zig` is a path; a space means the command is chosen), and `slashMatches` asks `SlashCommands.query` BEFORE touching `skillManager.summaries` — that read stats the skills folder, on a property the body re-evaluates ~20 Hz while a reply streams. Keys route through `GrowingTextEditor.onKeyCommand` (up/down/accept/cancel), which returns false when the menu is closed so the composer keeps every key it owns; Return asks the menu before `ComposerKey.onReturn`, or a half-typed `/mus` would send. Typing `/name` INVOKES that skill whatever its triggers, in every chat mode (`SkillManager.invokedSkill`), and the text goes to the model as typed — transcript and history stay the bytes the user wrote.
- **A built-in skill ships per FILE, not per FOLDER** (`SkillManager.builtinSkills` + the `.seeded` ledger in `~/.mlx-serve/skills`): the old gate was "does the skills dir exist", so every built-in added after `review.md` would have reached brand-new installs only. Seeding writes each built-in whose NAME is not in the ledger, so deleting one still sticks, and a pre-ledger folder is migrated by taking what is already on disk (plus `review.md`, whose fate the old scheme already decided) as seeded. Triggers stay narrow — a false hit injects the whole body on every unrelated turn (issue #92). Guard: `AgentPromptTests.testNewBuiltinSkillReachesAnExistingSkillsDir`.
- **The Music pane's Examples are keyed on the engine FAMILY** (`MusicPrompt.builtinStyles(for:)`): ACE-Step takes a one-line genre description and reads tempo/key/meter off its own controls, which Music 3 does not have (the server 400s those fields) — so a Music 3 example is a full three-block caption (Global Metadata / Vocal Details / Arrangement), the only place its tempo, key and arrangement can be stated. An omitted `duration_seconds` is sized from the LYRICS (`MediaToolArgs.secondsForLyrics`, ~4 s a sung line + 15, tags excluded) — the flat 30 s default cut full songs off mid-verse, and the tool schema invited it ("omit for 30"). The `music3` built-in skill teaches the agent the same format, and the agent loop appends `AgentPrompt.musicEngineNote` (derived from the preset's flags) so the model knows WHICH engine `generate_music` will run — the tool schema is static, and a Music 3 call with no `lyrics` fails before the model loads.
- **An abandoned Task is not a cancelled Task, and the SDK's continuation only listens to `disconnect()`** (`MCPManager.connectOrFailFast`, live leak 2026-08-15): racing `client.connect(transport:)` against a death-watcher/30s timeout and resuming the outer continuation on the loser does NOT stop the loser — its `withCheckedThrowingContinuation` is only ever resumed by `Client.disconnect()` (swift-sdk `Client.swift`), so an abandoned connect attempt ran forever, one leaked spinning task per failed/timed-out MCP server. Symptom: a 23h app session sat at ~160% average CPU with `sample <pid>` showing threads permanently parked in `Client.connect(transport:)`. Fix: both losing paths in `connectOrFailFast` now call `client.disconnect()` (plus `child.terminate()` on the timeout path) — the same break-glass `executeToolCall`'s watchdog already used a few hundred lines above it in the same file. Guard: `MCPConnectLeakTests`.

- **A knob whose "Automatic" is probe-backed shows what it RESOLVED, it does not offer "Probe" as a second entry** (`SpecCostInfo`, `/props` `spec_cost`): a user cannot choose between "Automatic" and "Probe" without benchmarking, so Automatic stays ONE entry (the A/B lives in `MLX_SERVE_SPEC_COST_PROBE=0`) and its label names the measured width — a bare "Automatic (6)" reads the same as a hardcoded cap, so the label says `measured`. `draftBlockSize` deliberately gets NO picker: it only clamps DOWNWARD against the sidecar's trained block, so picking 8 on a block-5 sidecar is a silent no-op while picking 8 for MTP is a real change — a knob that can only make things worse. (The two also differ in UNIT: `--mtp-depth N` = N drafts, `--draft-block-size N` = verify WIDTH = 1 + drafts.)

## Debugging

- `print()` is invisible when launched via `open` — run the binary directly or write to file.
- Every agent request → `~/.mlx-serve/last-agent-request.json` (replay via curl); history `~/.mlx-serve/chat-history.json`; server stderr tail in the menu-bar log viewer (`ThrottledLogBuffer`, 1 MB; full history in `~/.mlx-serve/logs/`).
- Headless agent-loop driving: TestServer on :8090 (`TESTING_MODE=1`, `/test/agent` + poll `/test/history`).
