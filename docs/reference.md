# mlx-serve reference (moved out of CLAUDE.md)

Verbatim deep detail: per-file contracts, subsystem designs, API surfaces. Root CLAUDE.md holds the compressed operational summary; read the relevant section here BEFORE deep work on that subsystem. New subsystem detail goes here, with a one-line summary in root.

### Zig server (`src/`)

| Path | Role |
|------|------|
| `main.zig` | Entry, CLI flags + subcommands (`run`/`pull`/`list`/`serve`/`launch`) |
| `cli.zig` | Ollama-grade CLI: short-name alias table → HF repo, curl-based resumable pull into `~/.mlx-serve/models/<org>/<repo>`, `list`, and the `run` chat REPL (drives the server's own `/api/chat`) |
| `launch.zig` | `mlx-serve launch <agent>` (issue #188): claude/pi/omp/opencode/codex/hermes/aider. Probes the server (auto-starts the MLX Core app via `open -g -a` when down, instructions when the app isn't installed), reads `/v1/models` for the chat-capable list + advertised context (budget = clamp(ctx/4, 1024, 65536), the Swift `AgentBudget` formula), writes configs into dedicated `~/.mlx-serve/<agent>/` dirs, execs through a login zsh (user PATH). `--print` = write configs + print the script; `--` passes args through (`launch codex -- resume`). Contracts (measured): omp reads `PI_CODING_AGENT_DIR` and its models.yml gets a STATIC chat list (discovery would pull media models in at omp's 128k default); codex is Responses-wire-only (`CODEX_HOME`, keyless skips login); hermes rides `HERMES_HOME` (config.yaml + the `.env` wizard kill switch); aider gets a litellm metadata file. Swift twin: `CLILauncher`/`AgentConfigs`. User doc: `docs/integrations.md` |
| `mlx.zig` | mlx-c FFI |
| `model.zig` | Config + safetensors loading |
| `tokenizer.zig` | BPE tokenizer |
| `transformer.zig` | Forward pass; arch dispatch (attention, MLP, MoE, GatedDeltaNet) |
| `generate.zig` | Autoregressive generation, sampling, PLD/drafter/MTP orchestration |
| `chat.zig` | Chat templates (ChatML, Gemma, Llama-3, Jinja2); thinking tags; tool call parsing |
| `vision.zig` | Gemma 4 SigLIP vision encoder |
| `server.zig` | HTTP: `/health`, `/v1/models`, `/v1/chat/completions`, `/v1/completions`, `/v1/messages`, `/v1/responses`, `/v1/embeddings` (encoder-only BERT + bidirectional EmbeddingGemma — `gemma3_text` with `use_bidirectional_attention` routes to `transformer.forwardGemma3EncoderWith` (no cache, symmetric-band sliding masks) with the sentence-transformers `dense.0`/`dense.1` head between mean-pool and normalize and `<bos>…<eos>` input wrapping; parity vs fp32 sentence-transformers ≥0.98 via `tests/dump_embeddinggemma_fixtures.py`; whole input array batched into padded masked GPU forwards. Pooling is per-checkpoint (issue #116, `ModelConfig.pooling_mode` mean|cls|last_token): resolution config.json `pooling_mode` > sentence-transformers `1_Pooling/config.json` sidecar (unsupported modes REJECT the load — never a silent mean-pool) > known-family name fallback `model.poolingFromDirName` (qwen3 *embedding* → last_token, bert bge-/mxbai-embed → cls; logged at load). Last-token models (Qwen3-Embedding: plain `qwen3` causal trunk, `embedding_mode` return in `forwardStandardWith`, cache-less, right-padded so causal masking needs no key-pad mask) pool the APPENDED `config.eos_token_id` position — the checkpoint tokenizer's TemplateProcessing contract (`wrapEncoderIds` eos-only arm). A non-null pooling mode advertises `embeddings` capability on ready models, discovery stubs and `/api/show` WITHOUT flipping `is_encoder_only` (chat stays honest). Parity: `tests/dump_qwen3_embedding_fixtures.py` — 4-bit DWQ worst cos ~0.958 vs bf16 reference, proven quantization-limited by a same-weights mlx-lm probe at ≥0.9996; bge CLS ≥0.98. Per-input ceiling (issue #117): `--embedding-max-length N` (`server.embedding_max_length`, 0/auto = model window; `embedEffectiveLimit` clamps the flag to the declared window) — over-limit inputs 400 BEFORE the forward with index + token count + limit (`embedOverflowMessage`), same contract on `/api/embed` + legacy `/api/embeddings` (one funnel); effective limit surfaced as `meta.embedding_max_length` on ready embedding-capable models; sub-batches bounded by `generate.embedSubBatchEnd` (item cap 64 AND padded-footprint budget `EMBED_TOKEN_BUDGET` 64×512 — one long input can no longer inflate 64 rows' padding)), `/v1/load-model` (register-by-path for dirs outside every `--model-dir`; optional `"default": true` promotes the loaded model to the server default — re-points requests that omit `model`/the "mlx-serve" alias and `/v1/models`' default-first sort; only sent on a model SWITCH, never a media side-load, and only a load that SUCCEEDS is promoted; the body's `drafter_path` is currently ignored — a hot-loaded model gets no drafter), `/v1/unload-model`, `/v1/images/generations`, `/v1/audio/speech`, `/v1/audio/music-generations`, `/v1/video/generations`, `/v1/3d/generations`, `/metrics` + `/metrics.json` (opt-in `--metrics`), WebSocket on `/v1/responses`, Ollama `/api/*` (glue only — see `ollama.zig`). Optional `--api-key` gate (`g_api_key`, `apiKeyAuthorized`/`peerIsLoopback`): non-loopback requests need the key (Bearer/x-api-key/Basic/`?api_key=`); loopback + `/health` + OPTIONS exempt. Index page HTML extracted to `src/html/` (`@embedFile` in `handleStatusPage`) + optional live metrics panel. |
| `metrics.zig` | Lock-free, zero-when-off observability core (`--metrics`): `Counter`/`Gauge`/`Histogram` (relaxed atomics, no per-token work — recorded once per request at `finishSlot`), `renderPrometheus` (`vllm:` standard + `mlx_serve:` Apple-specific names) + `renderJson`. `liveGenerationTokens(m, inflight)` = completed + the scheduler's `inflight_generated_tokens` aggregate (published once per decode tick — race-free live tok/s). Gauge sampler thread in `server.zig` samples GPU%/mem/queue every 2 s. See "Observability" below. |
| `ollama.zig` | Ollama-compatible API translation layer: request shapes (`/api/chat\|generate\|embed` → OpenAI bodies), SSE→NDJSON `Sink` (installed on `Conn.ollama_sink`, the `WsBridge` pattern), tags/show/ps renderers, `resolveName` (`:latest` strip + basename/substring match). See "Ollama-compatible API" below |
| `lan.zig` | LAN model sharing v1: Bonjour advertise/browse (dns_sd, in libSystem — zero deps), `SharedSet` + `routeClass` allowlist policy, peer table, `<id>@<peer>` mirroring into /v1/models, and the transparent streaming proxy tunnel. Pure transport — no scheduler/MLX. See "LAN model sharing" below |
| `gen.zig` | Unified native media generation (image/audio/video/3D) hosted by the main server. Modality-named engine wrappers (`ImageEngine`/`AudioEngine`/`VideoEngine`/`MeshEngine` — swappable internals); `ImageEngine` is a backend union (`FluxImpl` / `krea.Engine`) and `AudioEngine` is one too (`tts.Synthesizer` / `acestep.Engine`), both dispatched on peeked `model_type`; `GenRoute` maps the speech/music endpoints onto the shared `.audio` slot (wrong backend for an endpoint → explicit 400). Per-request handler bodies, `detectModality`/`modalityFromType`/`peekModelType`, `normalizeSize`, stub CPU-state builders. See "Unified media generation" below. |
| `krea.zig` | Krea-2-Turbo image backend (single-stream MMDiT + Qwen3-VL-4B encoder + Qwen-Image 3D VAE + flow-match Euler). `MixedLinear` infers quant geometry → loads 8bit/mixed-4-8/bf16. Self-contained sibling of flux.zig; hosted by `gen.ImageEngine`'s krea arm. Four env-gated cos oracles. |
| `hunyuan3d.zig` | Hunyuan3D-2.1 SHAPE engine (image → untextured GLB): DINOv2-L conditioner, 3.3B MoE flow-match DiT (timestep-as-token-0, U-ViT LIFO skips, softmax-before-top-2 router NO renorm + shared expert), ShapeVAE decoder + geo-decoder SDF queries (per-mesh precomputed cross-attn KV), FlashVDM-class hierarchical volume decode (`decodeVolumeHierarchical`: coarse stride-4 pass → sign-change cells dilated by 1 → trilinear far field → exact refine; 6× volume-stage / 4.2× e2e at res 384, mesh byte-identical; dense `decodeVolume` kept for oracles + non-multiple-of-4 res fallback). Dense dtype **fp16** (not bf16). Loads the CONVERTED layout from `tests/convert_hunyuan3d_weights.py` (8-bit default) — DiT AND VAE fused-QKV per-head interleaves are baked out at convert time, so the engine does STANDARD head reshapes. Five env-gated `HY3D_*` cos oracles (fixtures via `tests/dump_hunyuan3d_fixtures.py`). Hosted by `gen.MeshEngine`. See "Unified media generation" below. |
| `hunyuan3d_paint.zig` | Hunyuan3D-2.1 PAINT engine (P2 texture stage): DDIM scheduler (v-pred + zero-SNR + trailing, diffusers-golden tests), SD2.x AutoencoderKL enc/dec, DINOv2-giant (SwiGLU) + ImageProjModel, and `PaintEngine.paintMeshToGlb` — the full orchestration (xatlas unwrap → 6-view geometry renders → VAE/DINO conditioning → 2-batch-CFG DDIM denoise via the 2.5D UNet → VAE decode → cos⁴ back-project bake → vertex-graph inpaint → textured GLB). Loaded lazily per `"texture": true` request beside the shape engine, freed after. ALL `HY3DP_*` oracles pass at fp16 (VAE 0.99999, DINO 0.999995, full UNet step 1.00000); live 8-bit e2e green on a 16 GB Mac. Contract: `tests/hy3d_paint_weights_contract.md`; fixtures `tests/dump_hunyuan3d_paint_fixtures.py` (fp32-CPU for DINO/UNet — see the fixtures gotcha). |
| `hunyuan3d_paint_unet.zig` | Paint 2.5D UNet (main 12-ch + dual 4-ch SD2.1 streams): MDA/RA/MA+3D-PoseRoPE/DINO attention per `tests/hy3d_paint_unet_dossier.md` (READ the raForward war-story before touching RA — the value layout looks wrong and is trained-in); pure-CPU voxel-index/RoPE/timestep helpers with reference goldens; 2-batch CFG (reference 3-batch collapses). Oracle-exact: dual-UNet ref cache 0.99999+, full 2.5D step cos 1.00000 vs fp32-CPU fixtures. |
| `acestep.zig` | ACE-Step v1.5 XL Turbo MUSIC engine (text2music, second `.audio` backend beside Qwen3-TTS): Qwen3-Embedding-0.6B text encoder (full forward for the prompt, embed-table lookup for lyrics), condition encoder (lyric 8L + timbre 4L CLS over the silence latent, packed [lyric|timbre|text]), 32-layer AdaLN DiT (bidirectional alternating sliding-128/full, cross-attn K/V computed ONCE per generation), 8-step Euler flow-match (shift=3) + DCW haar "double" correction, AutoencoderOobleck VAE (48 kHz stereo, hop 1920 = exactly 25 Hz latents, Snake in f32). Loads the CONVERTED layout from `tests/convert_acestep_weights.py` (fp32 source → bf16/8-bit; the FSQ audio tokenizer + detokenizer land in a SEPARATE, always-DENSE-bf16 `fsq.safetensors` (420 MB; an 8-bit pooler flips near-tie codes and the cover stops being the reference's: e2e 0.64 vs 0.985) — `--fsq-only` adds it to an existing pack — loaded lazily on the first cover request). Tasks: `text2music`, `complete` (vocal2bgm: the source clip's VAE latent as the DiT context, instruction "Complete the input track with A | B:"), `cover` (source latent → acoustic proj → 6-token CLS pooler windows → ResidualFSQ `[8,8,8,5,5,5]` (soft clamp `c·tanh(z/c)` then the preserve-symmetry hard-clamp floor grid; f32) → detokenizer 5-token windows → 25 Hz hints as the context; `cover_strength` switches to a text2music condition set for the tail of the schedule, `cover_noise_strength` starts from a noise/source blend at the nearest schedule step). The source's length is the track length (`vaeEncodeMeanChunked`, 30 s windows + 64-frame halo). Dossier: `music-dossier.md` (BINDING tensor contract). Env-gated `ACESTEP_*` cos oracles (`tests/dump_acestep_fixtures.py` — run on the 128 GB Mac, the fp32 reference is ~20 GB) ALL PASS on the bf16 build: text 0.9998, cond 0.999996, DiT 0.99905, e2e 0.99898, VAE dec 0.99994 / enc 0.99999 (encode is f32 end-to-end — bf16 on the raw AUDIO signal alone costs 0.958). Live 8-bit e2e green (`tests/test_music_gen.sh` ALL PASS; 30 s track in ~4 s). Hosted by `gen.AudioEngine`'s music arm. |
| `marching_cubes.zig` | Pure-Zig marching cubes (Lorensen tables, edge-keyed vertex dedup → watertight indexed mesh, CCW-outward winding along −∇field, trilinear central-difference normals). Zero MLX; hermetic manifold/Euler/normal tests. |
| `glb.zig` | Minimal glTF 2.0 binary writer (JSON + BIN chunks, u32 indices always, POSITION min/max accessors) + `writeGlbTextured` (TEXCOORD_0, embedded-PNG baseColor/metallicRoughness PBR material — glTF packs G=roughness B=metallic; the CALLER remaps the generated R=metallic/G=roughness image). Zero MLX; hermetic parse-back tests + Swift fixture generators (`GLB_FIXTURE_OUT`, `GLB_TEXTURED_FIXTURE_OUT`). |
| `uvwrap.zig` | xatlas UV unwrap wrapper (vendored MIT amalgamation + C shim in `lib/xatlas/`), reproducing the Python `xatlas.parametrize` contract (vmapping/indices/normalized uvs, all-default options). Hermetic cube-unwrap tests. |
| `rasterize.zig` | Verbatim Zig port of the reference CPU rasterizer (clip-space input, pixel centers +0.5, (width−1) screen map, int64 z-token min-compare, perspective-corrected barycentrics; f32/f64 mixing matches the C++). Zero MLX; hermetic coverage/z-order/barycentric tests + reference camera math. |
| `texinpaint.zig` | Texture-atlas inpaint: vertex-graph color propagation (port of the reference `meshVerticeInpaint` "smooth" mode) + iterative-diffusion hole fill (documented NS-inpaint stand-in). Zero MLX; hermetic maximum-principle/determinism tests. |
| `responses.zig` | OpenAI Responses API: parser, envelope, in-memory `ResponseStore`, compaction blob |
| `ws.zig` | RFC 6455 WebSocket framing (server-side, generic over `Conn`) |
| `pld_index.zig` | PLD n-gram index (`PldLookup.findMatch`, `ngramRepeatScore`) |
| `kv_disk_cache.zig` | SSD tier for the hot prefix cache (`--prefix-cache-disk`, **default OFF** — opt-in because it can persist many GB; the Swift app exposes a Settings toggle, off by default, and ALWAYS emits `--prefix-cache-disk off`/`<size>` so it's never silently on): committed KV prefixes persist as position-chunked safetensors under `~/.mlx-serve/kv-cache/<model-fingerprint>/e<id>/` and are RESTORED across RAM evictions AND server restarts instead of recomputed (restart TTFT on an 11K-token prompt: e4b 5.9s→0.7s, gemma-3-12B 40.6s→2.7s, byte-identical output). Commit = chunk-APPEND (only the new tail, ≤`max_flush_bytes`/turn, runs POST-markFinished so the client never waits); lookup falls back to disk when it beats the RAM match by ≥256 tokens. meta.json (written last, tmp+rename, v3) records per-chunk AND per-SSM-file byte sizes — scan salvages the valid prefix after a kill -9 mid-flush. **Phase 3 — hybrid SSM archs (qwen3_5/3_6 GatedDeltaNet, lfm2, nemotron_h):** the RAM tier's per-position `SSMCheckpoint`s persist beside the chunks as immutable `s{pos:0>7}.safetensors` (per-layer `l{i}.conv`/`l{i}.ssm`; absent key = null state; `initialized` bitmap in the safetensors metadata map), bounded per entry by `SSM_DISK_MAX_PER_ENTRY=8` (evict-lowest). A hybrid restore (`restoreIntoHybrid`) rebuilds KV `[0,cp_pos)` AND the SSM state at `cp_pos`, mirroring the RAM rewind-both; the RAM-vs-disk fairness compares EFFECTIVE checkpoint positions, not raw match length. The disk tier is now attached whenever the RAM tier accepted the arch (`ssm_checkpoint_stride > 0`), unblocking Qwen3.6-27B's ~50s cold re-prefill. Schemes off/affine, B==1. Guard: `tests/test_prefix_cache_disk.sh` (hybrid section) + hermetic DiskTier tests. |
| `drafter.zig` | Gemma 4 assistant drafter (cross-attention spec-decode) |
| `mtp.zig` | Qwen 3.5/3.6 native MTP head (sidecar spec-decode; self-contained — delete this + `Generator.nextMtp` to remove the feature). MLP is a union: dense SwiGLU OR the 35B-A3B MoE layout (`language_model.mtp.` prefix, mlx-lm `switch_mlp` split experts + shared expert + SEG — forwards through the trunk's `moeMLP`); quant params re-solve PER WEIGHT per call (`transformer.affineParamsFromGeometry` — sidecars mix 5/6-bit gs-128 beside 4-bit gs-64). Prefill history is FULL by default (`--mtp-history-window` opts into last-window capture; the A/B showed windowing costs 14 acceptance points at 64K on the stock head) |
| `diffusion.zig` | DiffusionGemma block-diffusion generation: canvas denoising loop, entropy-bound sampler, self-conditioning, per-slot `Runner` |
| `status.zig` | TUI status bar |
| `log.zig` | Leveled logging + persistent file sink (`--log-file`, default `~/.mlx-serve/logs/mlx-serve-<port>.log`, 32 MB rotation). libc `write(2)` + pthread mutex — callers carry no `Io`. |
| `build.zig` | Links mlx-c, libjinja, libwebp, stb_image |

CLI flags: `--model --serve --host --port --prompt --max-tokens --temp --top-p --top-k --ctx-size --timeout --reasoning-budget --no-vision --pld --pld-draft-len --pld-key-len --drafter --draft-block-size --no-mtp --mtp --mtp-depth --mtp-history-window --kv-quant --prefix-cache-entries --prefix-cache-mem --prefix-cache-disk --max-concurrent --skip-mem-preflight --metrics --api-key --lan-share --lan-discover --lan-name --no-tool-autocorrect --ssd-streaming --no-ds4-mtp --model-dir --log-level --log-file --version --help`

CLI subcommands (Ollama-grade, `src/cli.zig`): `mlx-serve run <model>` (pull-if-missing + serve + TTY chat REPL), `pull <model>`, `list`, `serve` (headless over `~/.mlx-serve/models`). Short names resolve via the alias table in cli.zig (mirrors `gemmaModelOptions`); `org/repo`, `hf.co/org/repo`, and `:tag` forms accepted everywhere.

Sampling defaults for request fields the client OMITS resolve as: request body > `--temp`/`--top-p`/`--top-k` launch flags (the app passes its Settings values) > the model's `generation_config.json` (Qwen 3.6: top_k 20 / top_p 0.95; Gemma 4: top_k 64 / top_p 0.95) > hardcoded (1.0/1.0/off). Claude Code omits all sampling params, so pre-2026-06 it sampled the full untruncated distribution at temp 1.0.

### Swift macOS app (`app/Sources/MLXServe/`)

| Path | Role |
|------|------|
| `MLXServeApp.swift` | App entry, menu bar + Chat/Browser windows |
| `AppState.swift` | Global state, chat session persistence |
| `Models/{ChatModels,AgentModels}.swift` | `ChatMessage`, `ChatImage`, `SerializedToolCall`, `AgentPlan` |
| `Services/APIClient.swift` | HTTP + SSE streaming client |
| `Services/AgentPrompt.swift` | System prompt, 10 tools, `SkillManager` |
| `Services/AgentBudget.swift` | `AgentBudget.forServerContext` + `AgentConfigs` — derives the context/output budget written into third-party agent CLI configs (pi `models.json`, opencode provider, Claude Code `CLAUDE_CODE_MAX_OUTPUT_TOKENS`) from the server's advertised `meta.context_length`. Never hardcode these. |
| `Services/AgentEngine.swift` | Shared agent logic: history, tool exec, repetition tracking, overflow |
| `Services/ToolExecutor.swift` | Tool handlers (shell, file, search, browse, webSearch, saveMemory) |
| `Services/DocumentIndex.swift` | Mini in-memory RAG for "attach a folder": chunker, hybrid retrieval (cosine + IDF lexical), `searchDocuments` tool. Embeds via `ServerEmbedding` only (server `/v1/embeddings`, GPU): `autoProvider` probes `/v1/models`, auto-downloads `bge-small-en-v1.5-8bit` (35 MB) when no encoder is known, and registers it via `/v1/load-model` by absolute path. Server down / provisioning failure → lexical-only retrieval (no NLEmbedding fallback) |
| `Services/{AgentSandbox,VzGuest,OCIClient,SandboxSmoke}.swift` | Agent Sandbox: shell tool routed into an isolated Linux guest on Apple Virtualization.framework. `AgentSandbox` (manager: settings, lazy boot, transcript, provisioning) → `VzGuest` (VZVirtualMachine boot + persistent dash shell driven via `ShellSentinel` over a dedicated hvc1 console port) + `OCIClient` (anonymous Docker/OCI pull + gunzip). See "Agent Sandbox" gotcha. |
| `Services/{ImagePreprocessor,BrowserManager,ServerManager,TestServer,AgentMemory}.swift` | Image prep, WKWebView, server lifecycle, embedded test server (port 8090), agent context |
| `Services/ClonedVoiceSynthesizer.swift` | Voice-clone speech for voice mode: a global "Voice clone clip" recorded/picked in Settings ▸ Voice (`ServerOptions.voiceClonePath`, normalized via `AudioReference` and persisted to `~/.mlx-serve/voice-clips/` — `VoiceCloneClipStore`) makes the hands-free assistant answer in the cloned voice. `ClonedVoiceSynthesizer` wraps `SystemSpeechSynthesizer` behind the same `SpeechSynthesizing` protocol (wired in `VoiceModeController(server:)`): per utterance it re-reads the clip path; clip set → pipelined FIFO synth+play via Qwen3-TTS `/v1/audio/speech` + `ref_audio` (`VoiceCloneTTS` keeps the model resident across sentences; sentence N plays while N+1 synthesizes); clip empty OR a synthesis fails → THAT utterance falls back to the system voice at its turn — never dead air, never reordered. `stop()` (barge-in) clears both pipelines but keeps the model warm; `SpeechSynthesizing.shutdown()` (default = stop; called by `VoiceModeController.end()`) additionally unloads it. **Pre-warm** (docs/qwentts-cache.md): `SpeechSynthesizing.prewarm()` (default no-op) fires on voice-mode activation — the clone arm loads the TTS model and POSTs `{"warm_only":true,"ref_audio":…}` (`VoiceCloneTTS.warmBody`, pure/tested) so the FIRST sentence starts from a warm speaker-embedding cache; server replies `{"warmed":true,"cache":"hit"|"miss"}`, misuse is a named 400 (missing ref, non-cloning checkpoint, Kokoro). Routing pinned by `ClonedVoiceSynthesizerTests`. |
| `Services/QuickLauncher.swift` + `Views/QuickLauncherView.swift` | ⌃Space quick launcher (tray toggle under Voice): Carbon `RegisterEventHotKey` (no TCC prompt/entitlement) + non-activating borderless floating `NSPanel`. Plain-chat turns through the shared `ChatTurnEngine` into a NORMAL sidebar session (follow-ups keep context; ⌘↩ opens that session in the chat window via `AppState.quickLauncherChatOpenTick`, observed by the always-installed menu-bar label — the SwiftUI-`openWindow`-from-AppKit bridge). Decisions/geometry are pure in `QuickLauncherLogic` (`QuickLauncherTests`); submit must respect the engine's one-turn-at-a-time rule (`stopThenSubmit` own turn / `blocked` someone else's — `runTurn` silently no-ops while `isGenerating`). macOS may consume ⌃Space for input-source switching (multiple input sources enabled) — registration still succeeds, the keystroke just never arrives. |
| `Views/{ChatView,StatusMenuView,BrowserView}.swift` | Chat UI + `runAgentLoop()`, menu bar, browser |

## Supported Architectures

Dispatched on `model_type` in `config.json` via `model.zig` (config/weights) and `transformer.zig` (forward). **GGUF files bypass this MLX dispatch entirely** and route to an embedded engine by file format — see "GGUF auto-routing" below.

| `model_type` | Family | Weight prefix | Vision | MoE | Notes |
|---|---|---|---|---|---|
| `gemma4`, `gemma4_text` | Gemma 4 | `language_model.model` | SigLIP | -- | Full vision, clipped linears, PLE |
| `diffusion_gemma` | DiffusionGemma | `model.decoder` | -- (tower not wired) | 128/top-8 | Gemma 4 26B-A4B trunk; BLOCK-DIFFUSION generation (src/diffusion.zig), not AR decode. See "DiffusionGemma" below. |
| `gemma3` | Gemma 3 | `language_model.model` | -- | -- | |
| `qwen3` | Qwen 3 | `model` | -- | -- | QK norm |
| `qwen3_5`, `qwen3_5_moe(_text)` | Qwen 3.5/3.6 | `language_model.model` | -- | Optional | GatedDeltaNet + MoE/dense, shared expert routing |
| `qwen3_next` | Qwen 3-next | `model` | -- | Optional | DeltaNet |
| `nemotron_h` | Nemotron-H | `backbone` | -- | -- | Hybrid transformer + Mamba2 SSM |
| `lfm2`, `lfm2_vl` | Liquid LFM2.5 / LFM2.5-VL | `model` | -- | `vision_tower` + `multi_modal_projector` | Hybrid gated conv + full attention; the VL tag adds a SigLIP2-NaFlex tower (`src/lfm2_vision.zig`) |
| `laguna` | poolside Laguna S 2.1 | `model` | -- | 256/top-10 | Pure-attention MoE coder (nvfp4 experts); per-layer Q-heads, softplus attn gate, YaRN rope. See "Laguna" below. |
| `llama`, `mistral` | Llama/Mistral | `model` | -- | -- | |
| `*.gguf` (any) | via llama.cpp | -- | -- | -- | Embedded libllama engine; reported as `model_type=gguf`. See Embedded engines. |

**TODO**: `phi`/`phi3` (different layout), `command-r` (different arch).

### GGUF auto-routing

A `.gguf` model path (or a directory containing one) bypasses the MLX safetensors path. The embedded engine is picked from the file's GGUF header metadata (`gguf_meta.preferredEngine`, issue #15): **antirez-style DeepSeek-V4-Flash → ds4**, **every other GGUF → llama.cpp** (the basename heuristic `model_discovery.isDs4GgufBasename` survives only as the Swift client's mirror, `DownloadManager.ggufModelType`). The Swift app surfaces any `.gguf` as a selectable base model and the server auto-routes — no engine selector. Both engines share the same HTTP/chat-template/tool/SSE machinery via per-request dispatch on `LoadedModel.{ds4,llama}_engine`.

GGUF dirs are also **discoverable + cold-loadable** (issue #59 — pulled GGUF repos ship NO config.json): discovery and `probeModelDir` classify a dir holding a non-mmproj `.gguf` as `model_type "gguf"` — GGUF presence WINS over a stray config.json, mirroring `--model` routing — with `bytes_on_disk` = the file `resolveGgufFile` will pick (alphabetically-smallest LLM quant), not the sum of every quant. On demand, `ensureLoaded` routes through the same engine arms as startup: `preloadCpuState` builds a stub CpuState (`buildGgufStubCpuState`; ctx from `LoadParams.ctx_size`, else llama 8192 / ds4 clamp) and sets `LoadRequest.{ds4,llama}_path`. The GGUF path helpers (`isGgufModelPath`/`resolveGgufFile`) live in `model_discovery.zig`, shared by main.zig and the scheduler. Guard: the headless section of `tests/test_llama_gguf.sh` (discover → cold-load → chat → unload → reload) + the GGUF discovery/probe/resolve tests in `model_discovery.zig`.

Models with `vision_config` but no vision weights (e.g., text-only quantized Qwen 3.5) gracefully disable vision at init. Swift app flags unsupported archs via `supportedModelTypes` in `HFModels.swift`.

### DiffusionGemma (block diffusion)

`model_type: diffusion_gemma` (google/diffusiongemma-26B-A4B-it, June 2026) reuses the Gemma 4 26B-A4B MoE trunk verbatim — same `forwardMoeWith` gemma4 branch, sigma-MoE router, v_norm, K=V alias on full layers, proportional RoPE — but GENERATION is a canvas-denoising loop (`src/diffusion.zig`), not autoregressive decode. `config.isDiffusion()` (canvas_length > 0) gates everything; `config.isGemma4Layers()` collapses gemma4 + diffusion onto the shared binding/forward arms.

- **Encoder/decoder split over one trunk.** The *encoder* runs the trunk causally (normal prefill, fills the slot's KV cache) using its own per-layer scalars (`model.encoder.language_model.layers.N.layer_scalar` — the only untied encoder weights; `ForwardCtx.use_encoder_scalars`). The *decoder* (`forwardDiffusionDecoder`) re-forwards a 64–256-token canvas of noise tokens BIDIRECTIONALLY each denoising step: q/k/v computed for the canvas only, cache K/V concat-READ (never written), sliding layers sliced to the last `sliding_window − 1` cached positions, no mask at all. `ctx.skip_lm_head` skips the 262K-vocab projection on encoder passes.
- **The loop** (`diffusion.Runner.nextCanvas`, one canvas per scheduler tick): uniform-RANDOM canvas init (no mask token), ≤48 steps, linear temp schedule (t_max 0.8 → t_min 0.4 on countdown), entropy-bound acceptance (sort entropies asc, accept while `cumsum − cummax ≤ 0.1`, renoise the rest), self-conditioning via soft embeddings (`softmax(processed) @ dequantized_embed_table × √hidden` — table dequantized once per request, ~1.5 GB transient), early stop when argmax is stable across `stability_threshold` steps AND mean entropy < 0.005. COMMIT = the argmax canvas; committed tokens are causally re-encoded at the START of the next tick (so an EOS-terminated request never pays for it). User temp < 0.01 → greedy denoiser; ≥ → categorical (temp 1.0 ≡ HF reference). Measured: 256-token canvas in ~10 steps on prose (25.6 tokens/forward), ~30% faster than the mlx-vlm reference.
- **Dispatch**: third engine-style branch (`runPrefillDiffusion` / `runDiffusionDecodeTick`, `Slot.diffusion`) — tokens flow through normal slot machinery, so all four HTTP surfaces + block-wise streaming work unchanged. PLD/drafter/MTP/batching never apply (MoE already non-batchable; `modelBatchable` also gates `isDiffusion`). Hot prefix cache is intentionally NOT consulted (restored snapshots leave cache VIEWS stale; the decoder reads views via denseView before any update would rebuild them) — wire it later by refreshing views post-restore.
- **Weights**: prefix `model.decoder`; experts packed as `experts.gate_up_proj` [E, 2M, X] (gate rows first) → `splitPackedGateUp` slices + MATERIALIZES halves at load; self-conditioning module under `model.decoder.self_conditioning.*`; `model.encoder.vision_tower.*` dropped at load (tower not wired — `has_vision` forced false).
- **Template/EOS**: standard Gemma 4 `<|turn>`/`<|channel>thought` template; EOS set {1, 106, 50}. Raw `/v1/completions` (no chat template) degenerates — reference behaves identically; instruct-only checkpoint trait, not a bug.

### Laguna (poolside Laguna S 2.1)

`model_type: laguna` (poolside/Laguna-S-2.1-NVFP4-mlx, July 2026 — 117.6B total / 8.5B active MoE coder, 256K ctx, 67 GB nvfp4). From-reference port (no upstream mlx-lm support); reference is the self-contained `modeling_laguna.py` (Apache-2.0). Pure attention — no SSM/hybrid — so it rides `forwardMoeWith` (qwen3.5/hy_v3 arm) with the standard KV/prefix-cache/PLD infra unchanged. Weight prefix `model`; `norm_has_offset=false`, `scale_embeddings=false`, `has_pre_ff_norm=false` (qwen/hy3-style RMSNorm + residuals); untied lm_head.

- **Per-layer Q-head count** (`num_attention_heads_per_layer`, `config.layerNumHeads`): 48 heads on full-attention layers, 72 on sliding — KV heads UNIFORM at 8 (cache machinery untouched). `lagunaAttnWith` carries the per-layer count; batched decode never applies (MoE → `modelBatchable` false).
- **Layer mix**: `layer_types` 1:3 full:sliding (`isGlobalLayer` = full_attention). Sliding window 512, reusing the Gemma sliding-mask machinery (`createSlidingWindowMask`/`createSlidingWindowDecodeMask`; full untrimmed KV during prefill, decode trims to the window). head_dim 128 → no fused hd-256 kernel.
- **QK norm**: per-head RMSNorm over head_dim before RoPE (qwen3 pattern).
- **Attention output gate** (`config.laguna_attn_gate`): `self_attn.g_proj` [n_heads, hidden] → `softplus` in fp32 (`logaddexp(0, x)`, numerically stable) → cast to bf16 → per-head scalar × attn output (reshaped [.., H, D]) BEFORE `o_proj`. Gate input is the same normed hidden states as q_proj. Distinct from qwen3-next's sigmoid/doubled-q_proj gate — separate weight, softplus, per-head broadcast.
- **RoPE** (per layer type, `attn_scale = head_dim^-0.5`): full-attention layers use **YaRN** (theta 5e5, factor 32, orig_max 8192, beta_fast 32, beta_slow 1, partial_rotary_factor 0.5 → rotary_dim 64). `computeYarnFreqs` precomputes the HF `_compute_yarn_parameters` denominator array (mlx_fast_rope's `freqs`; angle = pos/freqs); `attention_factor` (1.3466) is applied as an mscale VECTOR ([head_dim] = af on the rotated dims, 1.0 on the pass tail) multiplying post-rope q AND k — exactly the reference's `cos/sin *= attention_factor` on the rotated slice only, NOT folded into the sdpa scale. Sliding layers use default RoPE (theta 1e4, full rotary). Both precomputed on `Transformer` (`rope_freqs_yarn`, `yarn_mscale`).
- **MoE routing**: qwen3_moe WEIGHT NAMING (`mlp.gate` router bf16, `mlp.switch_mlp.*` nvfp4 experts, `mlp.shared_expert.*` bf16 shared) with hy_v3 ROUTING SEMANTICS — DeepSeek-V3 sigmoid scores in fp32, `+ mlp.gate.e_score_correction_bias` for top-10 SELECTION only, weights = the UNBIASED gathered scores, L1-renorm (`norm_topk_prob`), × `moe_routed_scaling_factor` (2.5). Reuses `hy3RoutingChain`/`computeHy3Routing` (compiled via `moe_sigmoid_router`). The route-scale multiplies the routed sum only; the shared expert is ALWAYS added, NO gate (`shared_ungated`) — matches the reference's post-expert scale (linear, never touches the shared branch). nvfp4 experts carry u8 fp8 scales, no biases (bias-less mode). `moe_router_logit_softcapping` is 0 on the shipped checkpoint and >0 is rejected at config parse (untested).
- **Converter naming**: the router base is PROBED (`lagunaRouterBase`) — poolside's NVFP4-mlx build flattens it to `mlp.gate.*`, mlx-community's oQ builds (`Laguna-S-2.1-oQ4e-fast`, `-oQ5e`) keep HF's nested `mlp.gate.proj.*`; the flat name wins when both could match. Those packs are all-affine mixed-bit (4/5/6/8, gs 64/128) solved per weight from geometry, use the `language_model.` weight prefix, and declare a 1M-ctx YaRN (`factor: 128`) where poolside declares 256K/32 — honoured as declared. Their `tokenizer_config.json` points at the sidecar template with an `{% include %}` stub (see the tool-calling gotcha).
- **Dense MLP** on `mlp_only_layers` (layer 0): resolved by a per-layer `mlp.switch_mlp.gate_proj.weight` PRESENCE probe at load (converter-proof — no `first_k_dense_replace` config dependency).
- **Chat/tools**: `chat_template.jinja` PRE-OPENS `<think>` at the generation prompt (thinking default on; emits bare `</think>` when disabled) — handled by the existing template-opened-think path (`promptTailOpensThink`, `splitThinkBlock(opened_by_template)`). Tool calls are GLM-style (`tokenizer_config.tool_parser_type: glm47`): BARE `<tool_call>NAME<arg_key>K</arg_key><arg_value>V</arg_value>…</tool_call>`, no plural wrapper. `parseHy3ToolCalls` was extended to route a bare `<tool_call>` whose body has an `<arg_key` before its close into the GLM parser (Hermes `{JSON}`/`<function=` bodies still fall through). String arg values stay strings; schema coercion types scalars. Format-corpus family `laguna`.
- **Thinking is PROMPT-OPENED but MODEL-GATED**: `enable_thinking` reaches the template (verified ON-vs-OFF divergence at temp 0), which ends the prompt with `<think>` — but laguna closes that block immediately on chat-y turns and on tool-dispatch turns, so `reasoning_content` is legitimately empty there; a reasoning-shaped prompt gets a real block. "Thinking never triggers" on this arch is checkpoint behaviour, not a serving bug — reproduce with a math/puzzle prompt before suspecting the pipeline. The app's default `reasoning_effort: "low"` is a 512-token budget (`responses.effortBudget`) and visibly cuts thinking mid-sentence; `high` is 8192.
- **Sampling / EOS**: `generation_config.json` dropped into the model dir (poolside's recommendation: temp 1.0 / top_p 1.0). EOS `[2, 24]` (〈|EOS|〉, `</assistant>`), parsed generically.
- **Parity**: `tests/dump_laguna_fixtures.py` (instantiate `modeling_laguna.py` at a TINY random config, CPU fp32) dumps per-module oracles (YaRN cos/sin, gated-attention block, sigmoid router, MoE block w/ shared expert); env-gated Zig cos oracles (`LAGUNA_FIXTURES=…`). Pure-unit: config parse ("parses laguna") + `computeYarnFreqs`.
- **v1 scope**: DFlash draft model (spec decode) is future work; PLD works generically.

## Unified media generation (image / audio / video / 3D)

Image (FLUX.2), audio (Qwen3-TTS), video (LTX-Video), and 3D (Hunyuan3D-2.1 shape) generation are hosted by the **ONE main server** through the multi-model registry — NOT standalone `mlx-serve --serve` subprocesses (the old `flux_server.zig`/`tts_server.zig`/`ltx_server.zig` are retired). One process, one port, one memory budget; chat + media coexist.

- **Modality slots** (`src/gen.zig` + `model_registry.zig`): `LoadedModel` has `image_engine`/`audio_engine`/`video_engine`/`mesh_engine` (named by MODALITY — the implementations are swappable internals — never by the implementation). `detectModality`/`modalityFromType` route off `config.json`'s `model_type` (`flux2*`/`krea*`/`qwen3_tts`/`AudioVideo`/`hunyuan3d*`). `model_discovery.isMediaModelType` accepts these (bypassing the LM quant gate) so they're discoverable + loadable by `/v1/load-model` — the two lists are documented duplication, keep them in sync.
- **3D shape (`.mesh` modality, `POST /v1/3d/generations`)**: `{"model", "image" (b64 PNG/JPEG, REQUIRED), "steps" (30), "guidance_scale" (5.0), "octree_resolution" (256, range [64,512]; 384 = reference default), "seed", "stream", "texture" (bool, default false — runs the P2 full-PBR paint stage after the shape; explicit 400 when the paint weights are absent, never a silent untextured downgrade), "texture_steps" (30)}` → `{"created":0,"format":"glb","data":"<b64 GLB>"}` (SSE progress + complete in stream mode; `capabilities:["3d"]` on `/v1/models`). Unknown fields are ignored (the descoped `"rig"`/`"face_hints"` of the avatar experiment now fall in that bucket). `gen.handleMesh` stb-decodes to RGBA8; `hunyuan3d.Engine.generateGlb` does recenter/composite-on-white (alpha bbox — an app-side Vision cutout conditions best, opaque photos work) → DINO → 50-step-class CFG denoise → SDF grid → `marching_cubes.extract` → `glb.writeGlb`. Weights are LOCAL-ONLY for now: user runs `tests/convert_hunyuan3d_weights.py` (torch ckpt → `~/.mlx-serve/models/local/hunyuan3d-2-1-8bit`; `--bits 16` is the parity-debug build the `HY3D_*` oracles target). The converted layout BAKES OUT the reference's per-head QKV interleaves (DiT re-fused to_q/k/v AND VAE fused c_qkv/c_kv) — the engine does standard head reshapes; never "fix" that in the engine. The texture path (`gen.paintedGlb`) runs `generateMeshRaw` → `hy3d_paint.PaintEngine.paintMeshToGlb` (loaded lazily from the sibling dir `local/hunyuan3d-2-1-paint-8bit` or `HY3D_PAINT_DIR`, freed after the request).
- **Music (`.audio` modality, second backend, `POST /v1/audio/music-generations`)**: `{"model", "prompt" (style/genre/mood, REQUIRED), "lyrics" ("" → "[Instrumental]"), "instrumental" (bool — the EXPLICIT spelling of an empty lyric block, resolving to the same `acestep.INSTRUMENTAL_LYRICS` marker; sending it beside non-empty lyrics is a named 400 via the shared `gen.instrumentalConflicts`), "vocal_language" ("en"), "bpm", "keyscale", "timesignature", "duration_seconds" (default 60, valid [10,600]), "seed", "stream", "ref_audio" (base64 WAV, any rate/channels — #259: the clip's VAE latent MEAN fills the condition encoder's timbre slot INSTEAD of the silence latent; `acestep.referenceWindow` makes it always 30 s = 750 latents (tiles a short clip, takes the start of each third of a long one — the reference samples the thirds at random offsets and a posterior sample; ours is deterministic per seed); bad base64 / non-WAV / clip under 1 s are NAMED 400s, never a silent text2music; engagement line `[acestep] reference audio: 750 latent frames`; parity oracle 7 (`ACESTEP_REFCOND`, fp32 torch on the HF checkpoints, 2026-08-22): packed states cos 0.99995 AND the timbre ROW alone cos 0.99989 — the row is pinned separately because it is 1 of 86 and the silence/reference fixtures differ only there (row cos 0.075 between them); Music 3 names the field a 400. Expectation: upstream's "Refer audio" is ONE pooled timbre token among hundreds of lyric/text tokens — style/feel guidance, never a cover), "task" ("text2music" default | "cover" | "complete"), "src_audio" (base64 WAV 10–600 s, REQUIRED for cover/complete, a 400 on text2music; full length, NOT windowed — its VAE latent is the context stream and the track is exactly as long as the clip, `duration_seconds` ignored with a log line), "cover_strength" (0–1, default 1: fraction of the steps on the source codes, the rest on a text2music condition set), "cover_noise_strength" (0–1, default 0: start from `t·noise + (1−t)·source` at the nearest schedule step), "track_classes" (complete: array from `acestep.TRACK_NAMES`, upper-cased and ` | `-joined into the instruction; unknown names are a named 400). Cover on a pack without `fsq.safetensors` is a named 400 (the app fetches the file into the pack first — temporary migration). Engagement lines: `[acestep] cover: source N frames -> M codes`, `[acestep] complete: N source frames as context, classes=…`; Music 3 names `task`/`src_audio` a 400}` → raw `audio/wav` bytes non-stream / SSE `progress` (encode → 8× diffuse → per-chunk decode) + base64 `complete` when streaming (mirrors `/v1/audio/speech`). `gen.AudioBackend` is the union `{tts: tts.Synthesizer, music: *acestep.Engine}` dispatched on peeked `model_type` (`"acestep"` → music); the TTS endpoint on a music model (and vice versa) is an explicit 400. Ready + stub models advertise `"music"` ADDITIVELY beside `"audio"`. Weights: published combined repo `ddalcu/ACE-Step-1.5-XL-Turbo-MLX-Serve-8bit` (DiT+encoders + Oobleck VAE + Qwen3-Embedding text_encoder/ in ONE bundle, ~6.2 GB; one-click download in the Music tab); rebuild from source with `tests/convert_acestep_weights.py` (torch fp32; needs BOTH HF repos — the XL checkpoint AND `ACE-Step/Ace-Step1.5` for the VAE + text encoder; the bf16 parity build stays local for oracles). Turbo is guidance-free (no CFG) and distillation-fixed at 8 steps — `steps` is deliberately not exposed.
- **Music 3 (`.audio` modality, third backend, same `POST /v1/audio/music-generations`)**: MiniMax Music 3 (`model_type: "minimax_music3"`, `src/music3.zig`) — a HIERARCHICAL AR model, not an ACE-Step variant: the 8B Qwen3 global LLM decodes one semantic code per 25 Hz frame (batch 2 for CFG 1.5, top-k 50, vocab masked to the 16384-code range + `<|audio_end|>`, threshold from the CONDITIONAL branch), a 4L depth decoder samples 7 residual codes per frame (KV-cached incremental decode — no RoPE/GQA there so the cache is trivially correct; `MLX_SERVE_MUSIC3_DEPTH_KV=0` keeps the reference full-re-forward shape), and the 8 per-frame HIDDEN STATES (1 global + 7 depth, conditional rows only) feed a condition encoder (load-time softmax mix × layer_scale → Conv1d 4096→2048 → nearest resample ×441/128 onto the 86.13 Hz latent grid) that conditions a 2.4B flow-matching DiT (timestep as a PREPENDED TOKEN — plain LayerNorms, NO AdaLN; REVERSED SwiGLU `first * silu(second)`; partial RoPE dims 32 theta 10000 with the temb token at position 0; k=1 pre/post convs applied as residuals), denoised in 200-frame windows (hop 100, the previous window's trailing 172 latents blended into the overlap at EVERY Euler step, CFG 1.7 against a ZERO condition, uniform t_i = i/steps ascending with 0 = noise), then a weight-norm-fused Snake/DAC Flow-VAE vocoder (alpha-only Snake, NO exp — not ACE-Step's) folds 128 latent channels into two 64-ch streams → 44.1 kHz stereo WAV (chunk crops 86/258 latents tile the song exactly). Fields: `{"model", "prompt" (caption, REQUIRED), "lyrics" (REQUIRED — lyric-conditioned; `[verse]`-style tags on their own lines; text on a tag line is DROPPED by the normalization contract), "duration_seconds" (default 60, valid [1,360], an UPPER bound — the LLM may emit `<|audio_end|>` earlier), "steps" (default 30, valid [4,100]), "seed", "stream"}`; ACE-Step's bpm/keyscale/timesignature/vocal_language have NO equivalent and are NAMED 400s. The prompt is a BYTE CONTRACT (`_clean_caption`/`_normalize_lyrics` ported scanner-exact, table-pinned against the reference regexes; assembled template `<|im_start|><|caption_start|>…<|audio_start|>`, cap 5000 tokens → named 400; the CFG row blanks ids[1:-2] to `<|audio_cfg|>`). Engine fully resident (~13.5 GB + `MUSIC3_GEN_BUFFER_BYTES` = 6 GB AR working set in the load gate); AR stage measured 29.4 ms/frame ReleaseFast after the 2026-08-13 perf round (43.8 before; probe-armed ladder 30.2/31.4 across reps; 60 s song ≈ 45 s AR + DiT). Round levers, each with an engagement log line: lm_head PRUNED at load to the 16385 legal rows (semantic codes + `<|audio_end|>`; bit-exact per-row slice of the packed q8 head, the prune IS the vocab mask; `MLX_SERVE_MUSIC3_LMHEAD_PRUNE=0` restores full-vocab + additive mask), depth-decoder KV cache (above), per-layer weight handles preresolved once at load (`QLin` — byte-identical, WAV-compared), frame-buffer eval every 32 frames instead of per frame. Env experiment, default OFF: `MLX_SERVE_MUSIC3_DEPTH_BITS=4` re-quantizes the depth LAYER linears at load (a QUALITY lever — depth codes are output, not verified drafts; measured 26.3 ms/frame but replay agreement 0.942→0.913, so it stays OFF). Per-stage attribution via `MUSIC3_COST_PROBE=1` (+`MUSIC3_COST_FRAMES`): lm decode 17.5 ms is the batch-2 q8 8B weight-read floor and no lever moves it. Pack `ddalcu/MiniMax-Music3-MLX-Serve-8bit` built by `scripts/convert_music3_weights.py`; the engine owes the load-time transforms the converter leaves out (vocoder weight_norm fusion g·v/‖v‖, PT→MLX conv axis swaps — `conv_t1` identified BY NAME, the bias-length heuristic is ambiguous on square convs —, Snake alpha [1,C,1]→[C], DiT f32 1-D params narrowed to bf16, quant widths solved per weight via `affineParamsFromGeometry`, never literal). Parity: `tests/dump_music3_fixtures.py` (transcription oracle, fp32 torch on OUR dequantized weights) → env-gated `MUSIC3_TEST_MODEL`/`MUSIC3_FIXTURES` oracles in music3.zig, all cos ≥ 0.999 + rms_ratio within 1% (vocoder cos 1.000000), greedy AR replay 94% code agreement; guard `tests/test_music3_gen.sh`.
- **LTX pipelines** (`pipeline` request field; `src/ltx_video.zig` + `gen.zig handleVideo`): `one_stage` (default — prefers `transformer-distilled.safetensors` with its FIXED 8-step `DISTILLED_SIGMAS` table, falls back to dev + dynamic-shift schedule with a warn), `two_stage`, `two_stage_hq` — the reference-faithful two-stage: dev transformer + CFG/STG/modality guidance (`GuiderParams`; STG = self-attn→values skip in block 28, modality = AV-cross-attn skip, all via `DitPerturb`) at HALF resolution (Euler, or the `res_2s` second-order SDE sampler for HQ — its "bongmath" anchor loop is implemented as the closed-form fixed point, pinned by a unit test), then latent denormalize → spatial x2 upsampler (`spatial_upscaler_x2_v1_1.safetensors`, oracle cos 0.9998 via `tests/dump_ltx_upsampler_fixtures.py`) → renormalize → 3-step `STAGE_2_SIGMAS` refine on the DISTILLED transformer at full res. The 11 GB transformer slot holds ONE variant at a time — `VideoEngine.ensureTransformer` swaps dev↔distilled (never both resident). Two-stage REQUIRES vae_encoder (latent stats even for t2v) + upsampler + both transformer variants and returns 400 when anything is missing (never a silent one-stage downgrade); the pixel grid must be divisible by 64. Fields: `pipeline`, `cfg_scale` (default 3.0 two-stage / 1.0 one-stage), `cfg_audio_scale` (7.0), `stg_scale` (0.0), `steps` (stage-1; 30, HQ 15), `stage2_steps` (0 = all 3). The negative prompt is only Gemma-encoded when a guider needs the unconditional forward.
- **LTX image-to-video** (first-frame conditioning): `VideoEngine` carries an optional `vae_encoder: ?ltx.Component` (`vae_encoder.safetensors`, ~0.6 GB, separate download from the decoder-only q4 bundle; absent → graceful t2v). `handleVideo` parses `first_frame_image` (base64 PNG/JPEG, the `ref_audio` precedent), `decodeImageToBCFHW` (stb_image + bilinear resize + `x/127.5-1`) → `[1,3,1,(H/32)*32,(W/32)*32]`, and `generateVideoFrames` VAE-encodes it (`vaeEncode`) → pins it as the clean latent frame 0 with PER-TOKEN-timestep AdaLN in the DiT. See the per-token-AdaLN gotcha for the t2v-byte-identity rule + the uniform-mask tripwire. **First/last frame (#260)**: `last_frame_image` (same encoding) pins the LAST latent frame (`F_lat-1`, the last pixel frame on the causal VAE) the same way, on BOTH pipelines (two-stage encodes the half-grid pair for stage 1 and the full-grid pair for stage 2); `ltx_video.keyframeMask` is the ONE place the token ranges are computed and `buildKeyframeCond` splices the anchors over the noise + clean latent (`VideoConditionByLatentIndex` at strength 1, mask 0 — upstream's CURRENT pipelines guide a non-zero `frame_idx` through APPENDED keyframe tokens instead; latent replacement is the older FLF recipe and needs no extra tokens/attention mask). Unlike the graceful first-frame path this one is NOT: missing `vae_encoder` / bad base64 / undecodable image / `num_frames < 9` (one latent frame cannot hold an anchor and still generate) are named 400s. Engagement line `[ltx] keyframe conditioning: first=… last=… (N of Nv tokens)` (stage-suffixed on two-stage); the 2.5 `keyframes_abs_pos_embedding` gate follows the same mask, so both anchors get it. Guard: `tests/test_video_gen.sh` FLF arm.
- **LTX audio-to-video (a2vid)**: `audio` (base64 WAV — PCM16/24/f32, any rate, `wav.decode`/`resampleLinear`/`toStereoInterleaved`) on `/v1/video/generations`. The clip → 16 kHz stereo → log-mel + audio-VAE-**encoder** (`ltx_audio.encodeAudioCond`; encoder weights already ride in `audio_vae.safetensors`, oracle cos 1.000000 via `tests/dump_ltx_audio_encoder_fixtures.py`) → `[1,Na,128]` tokens FROZEN through stage 1 (`generateVideoFramesTwoStage(audio_cond:)`: audio starts clean, scalar `audio_sigma=0` swaps ONLY `audio_adaln_single` + `av_ca_audio` to the audio timestep per the reference, x0.a pinned to clean each prediction — an exact fixed point of the guided Euler loop, and the res2s SDE re-pin dance for HQ), audio guidance forced off in stage 1; stage 2 renoises + jointly refines unchanged. The mp4 gets the ORIGINAL clip (native rate/channels, trimmed to the video duration via `a2vidMuxSampleCount`) — never a VAE re-synth. Two-stage ONLY: `one_stage`+audio → 400 (`a2vidPipelineError`); NOT graceful — bad WAV/missing weights → explicit 400s (the user asked for THIS soundtrack). Swift: `VideoGenRequest.audioPath` → `audioFileToWavBase64` (any AVFoundation format → PCM16 WAV, ≤30 s) → `requestBody` upgrades a one-stage preset to `two_stage` AND drops its cfg/stg (server reference defaults apply); Video pane "Speech & sound" section (file picker or Qwen3-TTS chaining via `AudioGenService`), frames auto-snap via `VideoModelPreset.framesCovering`. Guards: a2vid tripwires in ltx_video.zig (weights-gated), `tests/test_video_gen.sh` a2vid case (mux byte-fidelity + engagement log line + 400), `MediaGenServiceTests` request contract.
- **LTX DiffVAE decoder (`vae_diffusion_decoder.safetensors`, 417M — SERVED since 2026-08-13 via `"decoder":"diffusion"`)**: the decoder Lightricks' own published clips are decoded with; `vae_decoder` is the cheap conv one, and the MLX reference (`ltx-2-mlx`) implements neither. Only the 8-bit LTX-2.5 pack ships the file — a request against a pack without it is a NAMED 400, never a silent downgrade. Three files: `src/ltx_diffvae.zig` (geometry + tiling plan, hermetic, no MLX), `src/ltx_diffvae_kernel.zig` (the fused 3D neighborhood-attention Metal kernel), `src/ltx_diffvae_forward.zig` (the MLX pass, the sampler and the tiled decode driver). **Architecture**: four DETERMINISTIC neighborhood-attention stages (dims 2048/1024/512/512, depths 4/6/4/2, 3D kernels (3,7,7),(3,7,7),(3,5,5),(3,5,5), head_dim 64) upsample the latent through four `LinearPixelShuffleUpsample`s ((1,2,2)r2, (2,1,1)r2, (2,2,2)r1, (2,2,2)r2 — each time-doubling shuffle emits a duplicate leading frame that only the chunk owning t=0 drops, so 13 latent frames → 97 pixel frames exactly) into a 256-wide context volume; then 8 `CombinedDiffusionNABlock`s (kernel (3,7,7), AdaLN modulation from a 384-d timestep embedding + a per-block `scale_shift_table[7,256]`, context injected per block via `context_proj`) denoise patch-4 pixel tokens. Widths/depths are read off the checkpoint; the kernels are the reference's production `L` layout, which every checkable field (depths + all four upsample proj shapes) reproduces exactly. `type_emb` ships in the file and the reference's forward never reads it. **Sampler (MEASURED, not declared — no pack ships a `vae` config)**: x0 prediction, ONE step, timesteps scaled by 1000; the reference's class defaults (v-prediction, 2 steps, x1) decode the shipped weights to static. `ltx_diffvae_forward.Sampler` owns all three with `MLX_SERVE_DIFFVAE_{OUTPUT,STEPS,TSCALE}` kill switches. **Attention**: NATTEN's `na3d` SHIFTS its window inward at a boundary rather than clamping and masking (`naWindowStart`), every query attends exactly `Kt·Kh·Kw` keys, and at the diffusion stage that is 2.4M queries × 147 keys — one thread per (query, head) with a 64-float accumulator and an online softmax in registers, K/V read through the cache (a useful query tile's halo is 145 KB, well past Metal's 32 KB). Three window variants are compiled ((3,7,7), (3,5,5), (3,3,3)); the window and head_dim are the only template args, since a per-call one is a fresh JIT per value. RoPE is absolute per axis with the head split (16,24,24) at head_dim 64, positions 0-based per tile (a global phase cancels inside a local softmax, which is what makes tiled decode exact). **Tiling**: stages 1-3 run on the FULL volume; stage 4 and the diffusion stage are cut until one tile's stage-5 token count fits a budget derived from free RAM (`tileTokensForMemory`, 10 KiB/token measured), seams overlapping by `max(stage-4 halo, diffusion halo)` and blended with trapezoid ramps that sum to exactly 1. **Cost**: the decode stage measures 21.2 s and peaks at 20.7 GiB for an untiled 768×512×97, or 46 s and 6.4 GiB at a 1.2M-token budget; end to end against the conv decoder in the same session it was inside variance (165.7 s vs 163.8 s). **Guards**: kernel-vs-f32 parity incl. boundary-only volumes; per-stage parity against `tests/dump_ltx_diffvae_fixtures.py` (a self-contained PyTorch transcription — natten is CUDA-only, so the reference's vendored pure-torch NA is what makes a CPU oracle possible) at cos ≥ 0.99997 with `rms_ratio` beside every cosine; whole-vs-tiled agreement including a seam-centred crop; and the `tests/test_video_gen.sh` section that asserts engagement, difference from the conv decode, and the static metric relative to the conv arm.
- **Image-backend seam** (the image modality hosts MULTIPLE architectures): `gen.ImageEngine.backend` is a tagged union `ImageBackend{ flux: FluxImpl, krea: *krea.Engine }`. `ImageEngine.load` re-peeks `config.json`'s `model_type` (`gen.peekModelType`) and dispatches: `krea*` → Krea (`src/krea.zig`), else → FLUX (`flux.zig`, untouched). The `LoadedModel.image_engine` slot stays single + modality-named. Adding SD3/Qwen-Image later = one more union arm + one impl file; **this is the established convention** — audio/video keep a single backend until they gain a second arch, at which point the same union pattern applies. `ImageEngine.normalizeSize` resolves the request per backend (FLUX fixed 1024²; Krea any multiple of 16 in [256,2048] — `handleImage` parses the `size` "WxH" field and clamps). Krea = single-stream MMDiT + Qwen3-VL-4B encoder + Qwen-Image 3D VAE + flow-match Euler; `MixedLinear` infers (bits, group_size) from tensor geometry so ONE engine loads 8bit / mixed-4-8 / bf16 builds. Parity: four env-gated cos oracles in `krea.zig` (encoder/DiT/VAE/e2e) fed by user-run `tests/dump_krea_fixtures.py` (Krea is gated — license + download required); integration `tests/test_krea_gen.sh`. The Krea dir is assembled (transformer_*.safetensors + `text_encoder/` + `vae/` + `tokenizer/` + a synthesized `config.json` `{"model_type":"krea2_turbo"}`); macOS-app download/UI is a follow-up (engine-first).
- **Image img2img + instruction EDIT + conditioning rebalance + runtime LoRA** (`handleImage` request fields): `image` (base64 PNG/JPEG) + `mode` ("variation" default / "edit") + `strength` (0,1], variation only. The source is NEVER stretched: `decodeImageToBCHW` has COVER semantics (bilinear from the largest centered source window matching the target aspect — mismatched aspects center-crop, never distort). VARIATION (both backends) — cover-decode to the resolved output size → the backend's VAE **encoder** (`flux.VaeEncoder` / `krea.VaeEncoder`, structural mirrors of the oracle-validated decoders, pinned by encode→decode round-trip tests at corr 0.999+ — run with `FLUX_TEST_MODEL`/`KREA_TEST_MODEL`) → flow-match scale_noise `x=(1−σ)z₀+σ·noise` at `img2imgStartStep(steps, strength)` (diffusers convention; ≥1 step always runs). EDIT (FLUX.2 ONLY — a trained capability; Krea → 400): the source's CLEAN latents ride the DiT token sequence as in-context reference tokens with **t=10·(i+1) position ids** (`buildEditIds`/`buildLatentIdsT`; official flux2 sampling.py multi-reference convention, generated tokens keep t=0), generation starts from pure noise, prediction sliced to the generated span — instruction edits ("make the hair blue") keep the subject (live: 0.966 structure corr vs 0.163 without the reference). MULTI-REFERENCE: `ref_images` (JSON array of base64 PNG/JPEG, edit mode only, ≤3 beside `image` — `gen.MAX_EDIT_IMAGES` caps the total at 4) adds extra in-context references, each fit to its own grid at its own t offset — "replace the face in image 1 with the face from image 2" (live-verified face transplant on klein-4B); `ref_images` outside edit mode / non-array / over-cap → 400. The edit reference keeps its OWN aspect ratio (`fitRefDims`: ≤1MP, /32 dims, no upscale — its latent grid is independent of the output grid; flux `patchify`/`unpatchify` are shape-derived, not hardcoded 1024², pinned by the non-square round-trip test), so a portrait photo edited into a square output is recomposed, not squished. SDEdit/variation CANNOT do semantic edits — don't try to fix that with strength tuning; route to edit mode. `cond_gain` [0,10] + `cond_weights` (JSON array OR comma/space string; count MUST equal the backend's text-encoder tap count — FLUX concatenates layers 9/18/27 → 3, Krea stacks 12 → 12; wrong count → 400; `applyCondRebalance` in each backend scales the tapped-layer axis). `lora_path`/`lora_paths` (absolute `.safetensors` path(s), up to `lora.MAX_LORAS`=8; diffusers `lora_A/B` + `lora_down/up` aliases + `alpha`, wrapper prefixes stripped, `to_out.0` normalized — `src/lora.zig`) + `lora_scale`/`lora_scales` — runtime UNFUSED adapters on `QLinear`/`MixedLinear` (`y = base + Σᵢ scaleᵢ·(x@Aᵀᵢ)@Bᵀᵢ`, one term per attached adapter, so quantized bases stay lossless and stacking is a sum, not a merge — mirrors mflux's `FusedLoRALinear`); lifecycle via `ImageEngine.setLoras` (same path+scale set, in order → no-op reuse, request without either field → detach, 0 matching modules across the stack → 400). Missing `lora_scales` entries default to 1.0 (mflux's `resolve_scales` convention); a single `lora_path`+`lora_scale` behaves byte-identical to before multi-LoRA support. GOTCHA: `mlx_load_safetensors` must run on a **CPU stream** — `Load::eval_gpu` is Not Implemented and kills the whole server; `lora.loadFile` creates its own cpu stream (mirrors `model.loadWeights`). Guards: `tests/test_image_gen.sh` (img2img structure-retention on a half-dark source at strength 0.2, multi-ref engagement + output-must-differ-from-single-ref, rebalance output-must-differ, zero-B LoRA byte-transparency, all the 400s) + `tests/test_krea_gen.sh` (img2img + 12-count validation); hermetic tests in `lora.zig`, `gen.zig` (parseFloatList/img2imgStartStep/extractCondWeights/iterJsonStringArray), `flux.zig`/`krea.zig` (buildEditIds, applyCondRebalance, MixedLinear LoRA delta).
- **OpenAI image-edit compatibility (`POST /v1/images/edits`)**: the only multipart surface we serve. `src/multipart.zig` parses the form (RFC 7578, zero-copy slices into the already-buffered body — a 40 MB upload costs nothing extra), and `gen.openaiEditFormToJson` rewrites it into the `/v1/images/generations` `mode:"edit"` JSON body, which `handleGen`/`handleImage` then serve unchanged: one request schema, one engine path, every existing guard and capability check applies for free. Field map: `image` (or repeated `image[]`) → `image` + `ref_images` in order (cap `MAX_EDIT_IMAGES`), `prompt`, `model`, `size` (`"auto"` → omitted so the edit path resolves geometry from the primary reference). Explicit 400s with human text (`gen.editFormErrorMessage`) for everything the vendor API allows but this server can't do: `mask` (maskless in-context editors — no inpainting), `n>1` (single-choice engine), `response_format:"url"` (we don't host files), `output_format` ≠ png, `stream` (our SSE isn't OpenAI's partial-image event shape). `background`/`quality`/`input_fidelity`/`output_compression`/`user`/`partial_images` are accepted and ignored — they don't change the output, so rejecting them would break working clients for nothing. The response is already the vendor shape (`{"created":0,"data":[{"b64_json":…}]}`). Validated live with the official `openai` Python SDK (single-image and multi-image `client.images.edit`) against both FLUX.2-klein and Mage-Flow-Edit; guards = the hermetic translation tests in `gen.zig`, the parser tests in `multipart.zig`, and the `/v1/images/edits` block of `tests/test_image_gen.sh`.
- **MageFlow (`src/mage_flow.zig`, third image backend — Microsoft Mage-Flow Turbo / Edit-Turbo, MIT)**: the union's `mage_flow` arm. Native-resolution double-stream flow DiT (12 blocks, hidden 3072, 24 heads, 3-axis MSRoPE `[16,56,56]`) + a DiCo generative VAE (128-ch latent, /16, deterministic t=0 decode) + a Qwen3-VL 36L text encoder tapped at its final hidden state (no layer stack ⇒ `condWeightCount == 0`, rebalance UI hidden; no LoRA). Distilled Turbo: 4 steps, guidance 1.0, static-shift FlowMatchEuler. **Detection differs from every other backend**: the official repos ship NO root `config.json`, only diffusers `model_index.json` (`_class_name == "MageFlowPipeline"`), so `gen.peekModelType` AND `model_discovery.peekMageFlowIndex` both classify it as `"mage_flow"` — keep the two in sync, and note Swift mirrors it (`DownloadManager.holdsModel` / `ServerManager.resolveModelDir` accept `model_index.json`, else a fully-downloaded repo reads as "not downloaded"). **Precision is load-bearing**: DiT + text encoder + vision tower bf16, VAE f32, RoPE rotated in f32, timestep value AND freq table bf16-rounded (`roundBf16`) — see the models-media gotcha; f32 everywhere produces a grey blob.
  - **Multi-reference in-context EDIT** (`Engine.editImage`, Edit checkpoint only): each reference is VAE-**encoded clean** at the target grid (`VaeEncoder`, the `student.dconv_encoder.*` subtree) and its packed latents are concatenated AFTER the target in the DiT image stream; the same references, resized for the VLM (long-edge 384 → smart-resize to /32 within the checkpoint's `preprocessor_config.json` pixel budget), run the Qwen3-VL `VisionTower` (24 blocks, 2D rotary, 48²-grid bilinear pos-embed, 2×2 merger) whose merged features REPLACE the `<|image_pad|>` embeddings and whose 3 DeepStack taps (blocks 5/11/17) scatter-ADD at LM layers 0/1/2 (`TextEncoder.encodeEdit`, drop-64 rather than txt2img's drop-34). Each step re-concatenates the constant reference latents, forwards, slices the velocity back to the target tokens, and Euler-steps in f32. The multi-image RoPE is just `buildDitRope(frames = 1 + nrefs)` — bit-identical to the reference's per-image temporal offsets because all images share (lh, lw). Edit capability is gated on the repo/dir NAME (both repos ship the same `_class_name` and the same `visual.*` weights, so nothing in the checkpoint distinguishes them); the load banner says which mode it came up in.
  - **Request path**: MageFlow is the first backend that consumes RAW reference bytes (`ImageGenOpts.edit_image_bytes`, flagged by `ImageEngine.editUsesRawBytes`) instead of pre-decoded `edit_images` — it needs two different resizes per reference (target-size for the VAE, 384-ish for the VLM). `handleImage` keeps the source bytes and reshapes the requested size to the primary reference's aspect (`fitAspect`, see the gotcha). Same `mode:"edit"` + `image` + `ref_images` contract as FLUX (≤4 total); `mode:"variation"` is a 400 (no img2img path yet).
  - **Parity**: nine env-gated cos oracles in `mage_flow.zig` (VAE decode/encode, DiT plain + masked, TE txt2img + edit, ViT + DeepStack, e2e bf16 loop, edit loop) plus two exact ones (edit tokenization for 1 AND 2 references, VLM preprocessing round-trip). EVERY one is reproducible from a persisted script — `tests/dump_mageflow_{vae,dit,te,e2e,vae_encoder,vit,te_edit,edit_e2e}_fixture.py` run against the mflux reference (`ivanfioravanti/mflux@mage-flow-mlx`); `dump_mageflow_dit_fixture.py` writes the plain AND masked fixtures in one pass. Components dump fp32, the two LOOP fixtures native bf16. Full set verified together: cosine 1.000000 on every component, 0.9998 at e2e step 4, 0.994 on the edit loop, 15/15 with zero skips. The VAE-decode and both DiT dumpers were written LATER than the code they check — the Phase 1-3 fixtures had been generated ad hoc in a scratchpad and lost, leaving three oracles nobody could re-run, which is a claim rather than a test.
- **MiniMax-H3 / Hailuo 3.0 (`src/minimax_h3.zig` + `minimax_h3_vae.zig` + `minimax_h3_audio.zig`, second VIDEO backend)**: text-to-audio-video — the `VideoEngine` union's `h3` arm (`model_type: "minimax_h3"`, marker `transformer.safetensors`). A 50-block single-stream DiT (hidden 5376, 56 heads × hd 128, SwiGLU ffn 14336, per-block AdaLN over 3 modalities × 6 mods — 13.04B of the 34B params are AdaLN) denoises video (24ch latents, patch 1x2x2) AND stereo audio (32ch, 40 Hz) JOINTLY in one packed full-attention sequence `[text | cond | audio | video]`, conditioned on Qwen3-VL-32B layer-50 hidden states (UNNORMALIZED — the repack truncates there and ships no `model.norm`; raw tokenizer ids, NO chat template, no special tokens). Reference = ComfyUI `comfy/ldm/minimax/*` + `comfy_extras/nodes_minimax_h3.py` (mirrored at `~/claude-tmp/h3-ref/`).
  - **Request surface** (`gen.handleVideoH3`, `POST /v1/video/generations`): `prompt` (append `overall_soundscape:` / `non_diegetic_music:` clauses to steer audio), `width`/`height` (multiples of 32; 400 otherwise), `num_frames` (snapped UP to the 17k+5 ladder and the response SAYS SO — 40→56), `steps` (30), `seed`, `stream`, `preview` / `preview_frames` / `preview_max_side` (opt-in per-step JPEG on SSE `progress` events — Latent2RGB of predicted x0, default off; `preview_frames`>1 is a JPEG filmstrip, not animated WebP; cached-velocity steps stay preview-less), `fast` (default TRUE — the 2.83x recipe of
velocity caching + attention broadcast; `false` = full per-step compute for
final renders; env `MINIMAX_H3_STEP_CACHE`/`MINIMAX_H3_ATTN_BCAST` override
both ways for A/Bs). Only `lora_path` is refused (the one field the backend cannot honor in any form); `cfg_scale`/`stg_scale`/`pipeline` are IGNORED, not 400d — the app sends them for every video backend, and H3 is CFG-distilled + single-pipeline so no selected setting is dropped. Response = the LTX shape (rgb8 frames + `pcm_s16le` 32 kHz stereo `audio_data`), 24 fps fixed.
    - **fl2va** (FL2VA packs): `first_frame_image` / `last_frame_image`, base64 PNG/JPEG. First = plain STRETCH to the canvas (the geometry anchor), last = aspect-preserving center-COVER, per the reference node. An undecodable image is a named 400, never a silent t2va.
    - **ref2va** (REF2VA packs only — gated on `config.json`'s `tasks`, and a named 400 on an FL2VA pack rather than a silently ignored field): `ref_images[]` (≤9 base64 PNG/JPEG), `ref_videos[]` (≤3 OBJECTS `{"frames":[b64,…],"audio":"<b64 wav>"}` — the soundtrack is a FIELD on its own clip, so a silent clip cannot shift the pairing of the ones that have sound), `ref_audios[]` (≤3 base64 WAV, any rate/channel count — resampled to the VAE's 32 kHz stereo server-side), and `ref_image_size` (`"match"` default | `"max"`). **`"max"` is SEVERAL TIMES SLOWER**: it sizes each reference to a 2048 short edge instead of the generation's pixel area, and reference rows ride through EVERY sampling step. A reference video needs ≥5 frames and is snapped DOWN to the 17k+5 ladder after being truncated to the generation's own length. Order and ordinals are fixed by the reference node and are a contract with the checkpoint — images, then videos (each soundtrack's `<Audio j>` label emitted immediately before its `<Video k>`), then standalone audio, 1-based PER TYPE. Every rejection is named (`RefReject.message()` + per-field decode failures carrying the index).
  - **Staged residency** (`minimax_h3.generate`): the TE (~28 GB 8-bit / ~48 GB bf16) is loaded, run and FREED before the DiT (~35 / ~62 GB) loads; VAEs load after the DiT is released. The media preflight bills `gen.h3PeakBytes` = max(TE, DiT) + VAEs, never the sum — see the scheduler preflight. All weights load through mage_flow's `MfLinear` (one geometry solve for bf16 and affine-quantized); the DiT's patch projections, time embedder and output heads are fp32 islands BY CHECKPOINT.
  - **Schedule math** (all pinned by the hermetic layout fixture vs the reference's own output, `tests/dump_minimax_h3_layout.py`): frames live on 17k+5 (VAE folds 17 source frames into 5 latent, spans cycling `FRAME_PER_TOKEN {1,4,4,4,4} × 5/3`); positions are AREA-NORMALIZED per axis (aspect ratio moves every coordinate); RoPE is PARTIAL split-half (96 of hd 128, 16 inv-freqs per axis, top 32 pass through); dual sigma schedule (video shift 12.0 / audio 3.0) with the audio velocity pre-scaled by `d(σ_a)/d(σ_v)` so both streams Euler-integrate on the video schedule; per-forward timesteps are ANALYTIC (≤4 unique — text/video, audio, cond pin near 1) via `TimestepPlan` → AdaLN row `t_row*3 + modalityTag`.
  - **Visual VAE** (`minimax_h3_vae.zig`): the decoder is a 36-block ViT3D (f16t4d24), NOT a conv stack. Spatial tiling is SEMANTIC (`create_token_ids` normalizes over the extent handed to it — a tile is not a slice of an untiled pass; `decode` refuses >256px without the tile path) and temporal chunking is trained-in (5-token chunks, 2 overlap, 3-frame pre-pad drop, 5-frame cross-fade — the VAE was trained on 17-frame clips). Encode is the 3D-causal-CNN half from `vae.py`, and ONE path serves both T==1 (an fl2va keyframe or a ref2va reference image) and T>1 (a reference video, in 17-frame clips) — the reference's tap-truncation branch is an optimization over identical arithmetic, not a second semantic. Audio VAE = BigVGAN-style 32 kHz decoder (`minimax_h3_audio.zig`), optional: absent → silent clip, never a failure.
  - **Conversion / packs**: `tests/convert_minimax_h3_weights.py` from the ComfyUI bf16 repack ONLY (the int8/nvfp4 packs are `convrot` CUDA tensor-core layouts, unreadable on Metal; safetensors' numpy cannot represent bf16 so the converter reads through `mx.load`'s lazy mmap). `--partition fl2va|ref2va` picks which DiT is converted; everything else (text encoder, both VAEs, tokenizer, geometry) is shared, which is why `tests/fetch_convert_minimax_h3_ref2va.py --reuse-from <existing pack>` CLONES those instead of re-downloading 52 GB and re-quantizing — copying a proven shard is also the correct move, since reconversion is not byte-stable across sessions. Published: `ddalcu/MiniMax-H3-FL2VA-MLX-Serve-8bit` (~69 GB), `-4bit` (~40 GB), `ddalcu/MiniMax-H3-REF2VA-MLX-Serve-8bit` (~69 GB). LICENSE/NOTICE/MODIFICATIONS.md ship in the dir; converter treats a missing LICENSE as fatal; the MiniMax Community License's Applicable Territory EXCLUDES the EU/UK/South Korea/USA — re-read Section III before changing how it is published.
  - **Profiling hooks** (Phase-0 instrumentation, harness-only): every `generate` logs phase laps (tokenize/TE/DiT-load/steps/VAE/audio) and per-step ms; `MINIMAX_H3_ABLATE=ladder` cycles cost-attribution arms (`none/sdpa/attn/mlp/adaln`) inside ONE run without inserting syncs; `MLX_SERVE_MF_DQ_GEMM` overrides the wide-M dequant+GEMM route (DEFAULT-ON for the H3 path, −13%/step; MageFlow still opt-in pending its own A/B).
  - **LoRAs, Turbo included** (`lora_paths`/`lora_scales`, and `"turbo": true`): H3 takes the SAME stacked-adapter grammar as the image and LTX handlers (shared `gen.parseLoraFields`, ≤8, summed per linear by `LoraSlot`/`loraAdd` → `lora.deltaSum`), resolved through the SAME loader (`lora.loadFile` with `Arch.minimax_h3`) — so kohya/PEFT/flat-`lora_unet_` spellings all reach H3's own module names, and the arch table is pure self rows because the checkpoint's names ARE ours. The Turbo distillation is not special-cased: it is file 0 of that stack, which is what lets a style LoRA and the distillation ride the same linears. Because H3 stages its weights PER REQUEST the stack is loaded and freed with the model, its handler pre-validates paths (`lora.validatePath` — `loadFile`'s own check would be reached minutes into a generation, far too late for a 400), and per-FILE attach counts are logged: the Turbo file must reach all `Model.LORA_TARGETS` (259) modules or it is a broken artifact, while a style LoRA matching a subset is ordinary and matching NOTHING is the wrong architecture (named 400). Costs ~155 MB more peak than the pre-stack design, which freed the adaln adapters with the weights at precompute.
  - **Turbo LoRA** (`"turbo": true`): the larryvrh/MiniMax-H3-Turbo-Lora 4-step distillation (Apache-2.0), served as a RUNTIME BYPASS — `y = base(x) + Σ scaleᵢ·(x@Aᵀᵢ)@Bᵀᵢ`, never folded (the reference node is explicit: merging rounds the delta away in bf16 and requantizes it away on quantized bases, and both packs are quantized). The verbatim HF file drops in as `<pack dir>/turbo_lora.safetensors` (recommended: `minimax_h3_turbo_4step_ckpt500.safetensors`, ~744 MB bf16) — its key names match our module prefixes 1:1, zero conversion. Targets per block: fused `attn.qkv_proj`, `attn.out_proj`, `mlp.fc1/fc2` (r=64), `adaln_proj.linear` (r=16, input silu(t_emb)), plus final-layer adaln and both token_refiner blocks. Attach runs BEFORE `precomputeAdaln` so the adaln delta folds into the tables and frees with the weights. Steps default 8 (comfort zone 6-8), floor 4 (named 400 below), missing file = named 400 pointing at the HF repo. `resolveSpeed` forces the fast recipe OFF under turbo (env still strongest), and the audio stream switches to EXACT mapped Δσ_audio integration (`audioStepFactor`) — the first-order form over-steps the audio clock 2.5x on a 4-step schedule's last leg, which is the "audio blown out at low steps" failure the reference node's custom sampler exists to fix. The estimator bills the LoRA file on the DiT term whenever present.
  - **Chained windows** (`"chain_windows": N`, 2-6, FL2VA pack only — a reference has no keyframe row to chain through, so the REF2VA pack 400s by name): N back-to-back `num_frames`-frame windows (num_frames is PER WINDOW), window N's last decoded frame VAE-encoded as window N+1's `.first` keyframe through the ordinary fl2va path; the duplicate join frame is dropped (`chainDeliveredFrames` = N·wf−(N−1)), pixels butt-join, audio joins with a one-frame equal-power cross-fade (`crossfadeJoin`, `CHAIN_OVERLAP_SAMPLES` = 32000/24). The user's own `.first` keyframe applies to window 0, their `.last` to the final window; window w draws seeds `seed+2w`/`seed+2w+1` (window 0 reproduces the single-window generation exactly). Packed S — and so the attention S² term and the broadcast-cache footprint — caps at the WINDOW size regardless of clip length. v1 reloads the staged pipeline per window (the validated single-window path runs unchanged); progress remaps onto the whole chain ("Window w/n: …").
  - **Guards**: `tests/test_minimax_h3.sh` (end-to-end + named 400s + SSE + snapping), hermetic layout/schedule/DiT-transcription tests in the three source files, live `zig build test -Dtest-filter="minimax h3 live"` with `MINIMAX_H3_DIR`/`MINIMAX_H3_MODEL`. DiT parity note: the reference block calls comfy_kitchen CUDA kernels that cannot run on a Mac, so `tests/dump_minimax_h3_fixtures.py` is a TRANSCRIPTION oracle (its header says exactly what a green test proves); the layout dump executes the real reference.
- **Load** (`scheduler.doLoadGenOnInferenceThread`): the gen load arm dispatches off the (stub) config's media `model_type`, builds the modality engine on the inference thread, installs it on the entry, marks ready. Works for both the startup gen-primary path (`main.runGenServe`) and the cold-load path (`preloadCpuState` builds a gen stub instead of a transformer parse). `model_type` for stubs is a static `"flux2"`/`"qwen3_tts"`/`"AudioVideo"` (borrowed-static — never a heap dupe).
- **Generation runs on the INFERENCE thread**, not the connection thread — posted via a `gen_queue` (mirrors `load_queue`/`vision_queue`/`embed_queue`) and run by `runGenRequest`. This deliberately does NOT use a process-wide `gpu_mutex`: the codebase's hard invariant is "inference thread is the sole mlx caller — even array frees go there", so a mutex would risk the MLX-from-two-threads crash class. Decoupled via an opaque `GenRequest{ctx, run}` (server.zig owns the job body via `genJobRun`/`gen.handle{Image,Audio,Video}`) so scheduler.zig needs no server/gen import. A long gen (e.g. video) BLOCKS chat decode for its duration — accepted (single GPU, gen is the user's foreground action).
- **Unload** (`/v1/unload-model` → `scheduler.unloadModel` → `unload_queue` → `runUnloadRequest`): marks the entry `.evicting`, drains refcount, frees mlx on the inference thread, returns the stub to `.unloaded`. The app's default flow is **load → generate → unload**; a per-model "Keep loaded" toggle (off by default) skips the unload for instant reuse.
- **Headless boot**: `mlx-serve --serve --model-dir <dir>` with NO `--model` (`main.runHeadlessServe`, `LoadParams.no_initial_load`) starts idle — chat AND media load on demand. The Swift app uses this so a gen-first launch starts the server headless; a chat-first launch reuses the same process (`ServerManager.ensureRunning(forGenModelDir:)`).
- **Downloads / bundles** (`app/Services/MediaBundle.swift` + `DownloadManager`): media models download through the SAME `DownloadManager` → `~/.mlx-serve/models` (the SINGLE source of truth — no HF-cache lookup, in BOTH Swift `ServerManager.resolveModelDir` and Zig `gen.resolveGemmaDir`). A `MediaBundle` = primary model + dependency repos (LTX → `[ltx, gemma-3-12b-it-4bit]`; FLUX/TTS → just the primary). `FileSelection` makes each download pull ONLY what the engine reads: FLUX/TTS are `recursive` (weight subdirs — FLUX's `transformer/`/`vae/`/`text_encoder/`, TTS's `speech_tokenizer/`), LTX uses a safetensors ALLOWLIST (the 3 files the engine reads, ~18 GB — NOT the repo's ~50 GB of LoRAs/upscalers/alt transformers). `selectNeededFiles(from:selection:)` (default `.chatDefault` = top-level + `mtp/`, unchanged for chat). Surfaced INLINE in each gen pane via `BundleDownloadBar` (Download/progress/Retry; Generate gated on `downloads.bundleReady`). Gemma-3-12B is also a chat-catalog entry (`gemmaModelOptions`) — pulled as the LTX dep AND selectable as a chat model. LTX's gemma encoder is found at `~/.mlx-serve/models/mlx-community/gemma-3-12b-it-4bit` (`$LTX_GEMMA_DIR` is an explicit override).
- **Guard**: `tests/test_unified_gen.sh` (headless → load-by-path → gen → unload + coexistence). The standalone `tests/test_{image,video}_gen.sh` / `test_tts.sh` still boot with `--model <gendir>` (now via the registry path). 3D: `tests/test_3d_gen.sh` (headless → load-by-path → `"3d"` capability → generate → GLB magic/JSON-chunk/vertex validity → streaming → coexistence → unload; SKIPs without a converted model). Bundle download logic: `MediaBundleTests` (allowlist, recursive-subdir, readiness, preset→bundle mappings). 3D parity: five `HY3D_*` cos oracles in `hunyuan3d.zig` fed by `tests/dump_hunyuan3d_fixtures.py` against the fp16 build (oracle 5 REUSES oracle 4's injected-noise fixtures — same trajectory, so the two dumps must never diverge); hermetic manifold/GLB/gate-math tests always run. Cross-layer: `GLBMeshLoaderTests` loads the Zig-written fixture `app/Tests/MLXCoreTests/Fixtures/hy3d_sphere.glb` through ModelIO/SceneKit.

## OpenAI Responses API (`/v1/responses`)

Pure data in `responses.zig`; HTTP/orchestration in `server.zig`. Supports `POST`/`GET`/`DELETE /{id}`.

- **Envelope** (`buildResponsesEnvelope` + `ResponseEcho`): echoes `tools`, `tool_choice`, `text`, `reasoning`, `usage` (with `cached_tokens` + `reasoning_tokens`), `truncation`, `parallel_tool_calls`, sampling params, `metadata`, etc. Renderers reshape into the strict ResponseResource schema (flat tool form, not nested chat-completions).
- **Streaming SSE**: `response.created`, `response.in_progress`, `response.output_item.added`, per-type deltas/`.done`, `response.completed`, then the terminal `data: [DONE]` sentinel (HTTP SSE ONLY — gated on `ws_mode == null`; a [DONE] frame over WS is misread as the next turn's marker on chained sessions, pinned by Test C/E in `tests/test_responses_streaming.sh`). **Every event needs `sequence_number`** — `sendResponsesEvent` injects it; the POST handler threads a per-request `seq_num`.
- **`background: true` → 400** (honest rejection — there is no queue/poll machinery; accepting it and running synchronously returned status "completed" on a request the caller expects to poll, which llmprobe flags). The WS transport already rejected it at body-build time.
- **Stateful chains**: `ResponseStore` keyed by id. `previous_response_id` replays history; missing → 404. `inputContainsFunctionCallOutput` triggers final-answer mode (tools disabled).
- **Compliance**: `experiments/openresponses` validates strict schema; currently 17/17. `top_level response_format` accepted as alias for `text.format`.
- **Compaction (`POST /v1/responses/compact`)**: pure data, no LLM call. Synthesizes opaque base64 `encrypted_content` over `{"v":1,"msgs":[...]}`. `appendCompactionInputItem` reconstitutes on round-trip. `model` required (422 on missing). Drops tool calls + images.
- **WebSocket transport (`ws[s]://host/v1/responses`)**: same endpoint, opt-in via `Upgrade: websocket`. Each text frame is a `response.create`-shaped JSON; SSE events become single WS text frames via `WsBridge` on `Conn.ws_mode`. **No `[DONE]` on success** (`response.completed`/`.failed`/`.incomplete` is the terminator). Sequence numbers reset per response. `WsLocalCache` holds `store: false` responses for the connection lifetime; failed continuations evict the chain root.

## Anthropic Messages API (`/v1/messages`)

For Claude Code and Anthropic SDK clients with local models.

- `system` (top-level) → internal system message
- Typed content blocks (`text`, `tool_use`, `tool_result`, `thinking`) → internal `Message` structs
- `input_schema` → OpenAI `parameters` for chat templates
- `tool_result` in user messages → internal `role: "tool"`
- `thinking` → `enable_thinking` + `reasoning_budget`; thinking blocks emit fake `signature`
- Stop reasons: `stop`→`end_turn`, `length`→`max_tokens`, `tool_calls`→`tool_use`
- SSE events: `message_start`, `content_block_{start,delta,stop}` (with `text_delta`/`thinking_delta`/`signature_delta`/`input_json_delta`), `message_{delta,stop}` — explicit start/stop lifecycle per indexed block

**Claude Code launcher**: app sets `ANTHROPIC_BASE_URL`, dummy `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN`, `ANTHROPIC_DEFAULT_*_MODEL=mlx-serve`, `CLAUDE_CODE_SUBAGENT_MODEL=mlx-serve`.

## Ollama-compatible API (`/api/*`)

Drop-in surface for the Ollama client ecosystem (Raycast, Obsidian, Enchanted, Open WebUI, ollama-python/js). Design rule: **NO duplicated inference pipeline** — pure translation in `src/ollama.zig`, thin glue in server.zig.

- **Request path**: `/api/chat`, `/api/generate` (raw:true → `/v1/completions`), `/api/embed(dings)` translate the Ollama body into an OpenAI-shaped body (`translateChatRequest`/`translateGenerateRequest`/`translateEmbedRequest` — always inner-`stream:true` + `include_usage`) and call the EXISTING `/v1` handler. Ollama differences handled in translation: `options.num_predict`→`max_tokens`, `images` (b64 array)→data-URL `image_url` blocks, history `tool_calls.arguments` OBJECT→string, `format` ("json" | schema)→`response_format`, `think`→`enable_thinking`, stream DEFAULTS TRUE, `num_ctx`/`keep_alive`/generate-`context` deliberately ignored.
- **Response path**: an `ollama.Sink` on `Conn.ollama_sink` (the `WsBridge` interception pattern) receives every byte the inner handler writes, re-frames SSE chunks into NDJSON lines (or one aggregate JSON for `stream:false`), converts tool-call `arguments` string→object, maps `reasoning_content`→`thinking`, `finish_reason`→`done_reason`, usage→`eval_count`/`prompt_eval_count`, and translates inner error JSON to Ollama's `{"error":…}`. Keepalive SSE comments are dropped. The sink writes through `Conn.writer()` directly (bypasses the hook); glue MUST clear `stream.ollama_sink` before `sink.finish()`.
- **No-model endpoints** (`handleOllamaEarly`, before scheduler resolution — never trigger a cold load): `/api/version` (build CalVer — parses as a high semver so client feature-gates pass), `/api/tags`, `/api/ps`, `/api/show`, `/api/pull` (native HF pull via cli.zig + `registerByPath`); `/api/create|copy|delete|push|blobs` → explicit 501.
- **Name resolution**: Ollama clients send `name:tag`. For `/api/` paths only, an unknown `model` tries `ollama.resolveName` (strip `:tag` → exact → case-insensitive → unique basename → unique substring) against registry ids before the default-model fallback; `/v1` fallback semantics unchanged.
- **Discovery is two-level**: `discoverModels` also scans `<root>/<org>/<model>` (id `"org/model"`) so `~/.mlx-serve/models` pulls are listable/loadable. GOTCHA (class: two ids, one path, double residency): a path registered under an org/name id must never be re-registered under its basename — `registry.peekByPath` guards the `--model` primary registration and `registerByPath`; any new registration site must check it, or the same weights can load twice and OOM.

## LAN model sharing (`--lan-share` / `--lan-discover`)

v1 is LAN-only (WAN rooms would layer onto the same seams); both halves OFF by default and independent. Design rule: **the proxy is a TRANSPORT** — no scheduler, no MLX, no inference-thread involvement. `src/lan.zig` owns discovery (dns_sd/Bonjour `_mlxserve._tcp`, browser thread) + the peer table + the byte-pump tunnel; server.zig adds three hooks (gate, dispatch interception, /v1/models filter/augment). `serve()` starts/stops it at the ONE chokepoint every serve path flows through, so the advertised port is always the bound port.

- **Share** (`--lan-share <all|id,csv>`, `--lan-name <n>` default hostname): advertise over Bonjour and open exactly the SHARED INFERENCE SURFACE to non-loopback clients — `lan.routeClass` allowlist (the four chat surfaces + embeddings + all five media-gen endpoints + `/api/chat|generate|embed(dings)`; open: OPTIONS, /health, filtered GET /v1/models, /api/version) × a shared-model check (`SharedSet`, symmetric basename-tolerant matching so the app's share names match `org/name` registry ids). Everything else — load/unload, /metrics, the status page, stored responses, WS — is host-local → 403. The gate exists only in KEYLESS mode: with `--api-key` set the existing key semantics rule unchanged. An empty parsed share set = sharing disabled (warn), never advertise-nothing.
- **Discover** (`--lan-discover`): browse peers, fetch their (shared-filtered) /v1/models, mirror entries as `<id>@<peer>` with a top-level `"lan_peer"` badge; a DIRECT request naming one — loopback app, the Agent Sandbox guest over the VM NAT interface, a LAN client — is TUNNELED byte-for-byte with only the model field rewritten to the bare id (`rewriteModelValue` takes the SAME slice `parseModelFromBody` returned, so the decide and rewrite layers can never disagree). Remote-id compares CANONICALIZE JSON's `\/` escape first (`lan.unescapeJsonSlashes` at the handleLanProxy chokepoint; the tunnel splices the canonical bare id so the peer never sees escaped bytes and silently serves its default) — Swift/PHP clients escape every slash, so an org-prefixed id arrives as `ddalcu\/gemma…@peer` and missed the byte-compare (live app 404 "no longer shares this model"; third bite of the load-model `\/` class — any id compared from a RAW body scan must canonicalize). The app's own serializers now also emit `.withoutEscapingSlashes`. The proxy's failure modes are DISTINCT (one "peer offline" 404 covering all three sent the user debugging the wrong Mac): discovery off on THIS server → says so; peer known but model unlisted → instant honest 404; peer unknown → `pokeDiscovery` + poll `lookupRemote` up to 15 s (`LAN_PEER_WAIT_MS`, client disconnect abandons the wait) before the offline 404 — covering a local restart's cold peer table AND a peer Mac mid-reboot (a chat fired during a peer redeploy now waits for rediscovery and succeeds). Never a silent local-default answer; a registered LOCAL id containing '@' wins via the registry peek. `/v1/load-model`+`/v1/unload-model` on a remote id are 200 no-ops (the peer loads on demand and manages its own residency) so client load→generate→unload flows work unchanged. Engagement log: `[lan] proxy <method> <path> -> "<peer>" @ ip:port`.
- **Loops are impossible by construction**: proxying is bounded to ONE hop by the tunnel marker — `lan.tunnel` stamps `X-MLX-LAN: 1` on every request it forwards, and a request carrying that marker is never proxied again (`isTunneledRequest` at the keyless gate and at dispatch; any DIRECT client may initiate the single hop — loopback-ness is not the bound, which is what let the Agent Sandbox guest work, live 2026-07-21). Remote entries never re-export: the list served to a discovery fetch (identified by the same marker, so loopback fetches between two servers on one Mac are covered too) filters out remote stubs, AND `parsePeerModels` skips any entry carrying `lan_peer` (defends against old peers). Self-advertisements are skipped via a random per-process TXT token (`t=<hex>`); a fetch that lands on OURSELVES anyway (stale Bonjour record of a former self: same name+port, different TXT token) is caught by the `X-MLX-LAN-Token` header `/v1/models` responses carry — token match → `error.SelfFetch` → the service is forgotten. Browse REMOVE events and refresh failures route through the same resolve-and-reinstall path, so interface churn and transient fetch failures self-heal (a peer is only dropped when it no longer resolves).
- **Zero dependencies**: mDNSResponder does all mDNS (dns_sd FFI — part of libSystem); outbound sockets are raw libc (`connectTimeout` does the nonblocking-connect dance because a blocking connect to a powered-off host hangs ~75 s); the tunnel's 1 s poll tick doubles as the client-disconnect probe (`conn.peerClosed`) so an abandoned generation tears down on the peer too. The tunnel takes `conn: anytype` (writeAll + peerClosed) — server-independent and hermetically testable against a fake peer socket.
- **Discovery robustness (two live-caught classes)**: (1) dns_sd callbacks fire once PER INTERFACE — resolve/addr waits complete on SUCCESS + `MoreComing` clear, never on the first callback (an lo0 error arriving first must not abort a resolve en0 would have answered), and `AddrOut` collects ALL A records with the fetch trying them LOOPBACK-FIRST — macOS **Local Network privacy** silently blackholes an unapproved process's SYN to LAN addresses (poll timeout, `SO_ERROR=0` — the signature) while loopback is exempt, so a same-machine peer resolved to its en0 record looked permanently unreachable. The app declares `NSLocalNetworkUsageDescription` + `NSBonjourServices [_mlxserve._tcp]` (both plists) so macOS prompts for the real two-Mac case. (2) A transient resolve failure at browse-ADD time must not lose the peer forever (browse never re-announces a live service): the browser keeps a `known` services registry retried every 10 s — the PEER table is cleaned on the first failure (no stale entries) while `known` quietly re-attempts, forgetting a name only after `KNOWN_MAX_FAILS` consecutive failures or when it turns out to be our own advertisement.
- **Swift app**: Settings ▸ LAN Sharing (share toggle + share-all/per-model checkboxes + name + discover toggle + the privacy disclosure — the host SEES tunneled prompts; that's inherent, not a bug). LAN models appear in the tray picker ("On Your Network", tags `lan:<id>`) and in all five media panes (capability-filtered via `server.lanModels(capability:)`; `LanPick` owns the `lan:` persistence format and the shared picker binding; `LanModelPickerRows` renders the section). Chat routes through `server.chatModelId` / `server.chatModelInfo` (LAN selection wins over the local default) — **never read `modelInfo?.name` directly at a new chat surface** or LAN selections silently miss it. Gen services route through `ServerManager.prepareGenModel` (a LAN id passes through; nothing is resolved/loaded/unloaded locally), and panes skip download-gating for LAN picks. Enabling either toggle keeps the server running (headless when no local model is selected); `AppState.ensureServerForLan` is the chokepoint.
- **Guards**: hermetic lan.zig tests (id split/rewrite, routeClass, SharedSet, TXT codec, parsePeerModels, tunnel pump + dead-peer 502-before-bytes), `lanShareDenial` unit test (server.zig), `tests/test_lan_share.sh` (20 live checks incl. the peer-restart rediscovery wait + both fast-fail messages), Swift `LanSharingTests` + the LAN cases in `ServerOptionsTests` (defaults-off emission, restart detection, tolerant decode).

## Observability (`--metrics`) + optional API key (`--api-key`)

Opt-in, performance-safe. Design contract: **zero cost when off** (a single `?*Metrics` null-check per REQUEST at `finishSlot`, never per token), negligible when on (per-request relaxed atomic adds off the decode path + a 2 s gauge sampler thread).

- **Core** (`src/metrics.zig`): lock-free `Counter`/`Gauge`/`Histogram` (all `.monotonic`). `renderPrometheus` emits `vllm:` (standard — drop-in vLLM/Grafana dashboards) + `mlx_serve:` (Apple-specific: GPU%, memory, live tok/s) names; `renderJson` feeds the index panel. `Metrics.recordRequest` is called once per request from `scheduler.finishSlot` (success ⇒ all histograms/counters; cancel ⇒ only `requests_cancelled_total`).
- **Wiring**: `--metrics` in `main.zig` allocates one `Metrics` on the main stack frame, publishes it via `server_mod.g_metrics` (+ `defer g_metrics = null` so an early `serve()` failure can't dangle), and every `LoadParams` builder reads it back (`.metrics = server_mod.g_metrics`) into `Scheduler.metrics`. Covers ALL serve paths (MLX/ds4/llama/headless/gen).
- **TTFT is exact, not derived**: `Slot.first_token_ns` is captured directly at prefill completion (`request_start_ts.untilNow`), so a slot finishing mid-tick can't skew it; `e2e = first_token_ns + decode_ns`.
- **A dashboard rate must be DERIVED, never remembered** (`src/html/metrics.js`). `lastPrefillTps` was a module-level `let` assigned only when the 60 s window contained prefill work, and never reset — so the Prefill tile kept displaying the last prefill speed through an entire decode, and only a page REFRESH cleared it. That refresh-clears-it symptom is the signature of state living in a variable instead of being derived from the current feed. Fix: all rates now come from a pure `computeRates(now, samples, counters, gauges, psum)` hoisted above the IIFE (which is guarded on `typeof document !== 'undefined'` and re-exports through `globalThis.__mlxPanel`), with zero carry-forward — Prefill reads 0 when not prefilling, exactly like Decode. The stable "how fast does this machine prefill" answer moved to the sub-line as `803 tok/s avg · 1.5K ms` (cumulative `prefill_tokens_total / prefill_time_sum`, so it can never go stale). **Rule: if a tile survives a condition changing, ask whether a page refresh would show the same value.** Guard: `tests/metrics_panel_test.mjs` (node; run as Phase 0 of `tests/test_metrics.sh`, SKIPs without node) — includes `a page refresh and a live tick agree`.
- **A rate's numerator and denominator must measure the SAME work** (prefill tok/s, 10.6x over-report). `vllm:prompt_tokens_total` bills EVERY prompt token; `prefill_time_seconds` only ticks for tokens actually forwarded. With the hot prefix cache warm, most billed tokens are RESTORED, not computed — so `Δprompt_tokens / Δprefill_time` overstates prefill throughput by `prompt/(prompt-cached)`. Live 2026-07-09: a 35B MoE at a 91-97% token-level hit rate showed **9.8K tok/s** on the panel while its own `[prefill: … tok/s]` log line (which correctly does `(prompt-cached)/ns` via `generate.prefillTokensPerSec`) read 400-1150. Fix: `mlx_serve:prefill_tokens_total` counts FORWARDED tokens (`prompt - cached`, saturating) and is the panel's numerator; `mlx_serve:prefix_cache_tokens_total` counts restores. Invariant `prefill_tokens_total + prefix_cache_tokens_total == prompt_tokens_total` is asserted in both the unit test and `tests/test_metrics.sh` Phase 5 (same prompt twice → the warm request bills its full prompt but forwards ~1 token). `vllm:prompt_tokens_total` keeps its vLLM meaning for dashboard compat. The cache tile now also shows `N% tokens reused`, which is what explains a "low" prefill number on warm turns. **Rule: before dividing a counter by a time histogram, check that every unit in the numerator actually consumed time in the denominator.** (The LIVE gauge below was already correct — it stores the chunk loop's `pos`, i.e. forwarded tokens.)
- **Prefill is live too** (`mlx_serve:prefill_tokens_live`, JSON `prefill_tokens_live`): `prompt_tokens_total` and the `prefill_time_seconds` histogram only advance at request COMPLETION, and generated tokens only accrue during DECODE — so a multi-minute prefill pinned the GPU while the panel read `0` decode / `—` prefill, indistinguishable from a hung server. The prefill chunk loop (`generate.zig`, `GeneratorOptions.prefill_progress`) stores absolute progress into `Scheduler.inflight_prefill_tokens` once per CHUNK (8192 tokens; never per token), `runPrefill`'s `defer` zeroes it on every exit path, and the sampler copies it into the gauge. The panel shows the live rate (Δ over a 30 s window — wide enough to span one chunk on a slow model) and turns the Prefill sub-line into a phase indicator. A SECOND gauge `mlx_serve:requests_prefilling` flips at prefill ENTRY (not at the first chunk), because on a 27B one 8192-token chunk takes ~40 s and the panel would otherwise still read "idle" for that whole span — and because ds4/llama prefill never reaches the MLX chunk loop, so their token gauge stays 0 while the phase flag is still correct. Sub-line: `prefilling` → `prefilling · N tok` once the first chunk lands. Guards: the `prefill progress is exposed live` render test (metrics.zig) + Phase 4 of `tests/test_metrics.sh` (gauge is 0 at rest, >0 mid-prefill, 0 again after).
- **Live tok/s without per-token work** (the data-race fix): the inference thread stores the sum of active slots' `completion_tokens` into ONE atomic (`Scheduler.inflight_generated_tokens`) once per `runDecodeTick` (via `sumInflightGeneratedTokens`, which EXCLUDES finished/cancelled/errored slots — so the last slot finishing drops it to 0 ⇒ "live == total at rest"). The gauge sampler reads that atomic (`metrics.liveGenerationTokens(m, inflight)`) — never touches per-slot fields off-thread.
- **Index page** (`src/html/`, `@embedFile` in `handleStatusPage`): the page markup lives in `index.html` (a `std.fmt` format string — `{{`/`}}` are literal braces, `{s}`/`{d}` the model fields; ONE `{s}` slot after the "Loaded model" card holds the optional panel). When `g_metrics != null` that slot = a `<div id=mlx-metrics>` mount + `<script>` with `@embedFile("html/metrics.js")`; else `""` (page byte-identical to no-metrics). `metrics.js` carries BOTH the panel markup (a template literal it injects into the mount) AND the polling logic — one file, so its raw `{`/`}` never need escaping. It CAN'T live inline in `index.html` because that's a `std.fmt` format string; the `{s}` slot is a runtime arg, so std.fmt never re-parses the JS braces. Decode/prefill tok/s are ACTIVE speeds (Δtokens ÷ Δphase-time), with in-browser sparklines (non-persisted ring buffers). Grow HTML features by adding files under `src/html/`.
- **API key** (`--api-key`, `g_api_key`): when set, every NON-loopback request (OpenAI/Anthropic/Ollama APIs + index page + `/metrics*`) needs the key — `Authorization: Bearer`, `x-api-key` (Anthropic), HTTP Basic (browser pages, key = password; a `WWW-Authenticate: Basic` 401 prompts), or `?api_key=`/`?key=`. **Loopback (127.0.0.0/8, ::1) is TRUSTED and exempt** (`peerIsLoopback` off `conn.stream.socket.address`), so the same-host app + a local browser never need credentials — the key protects network exposure (`--host 0.0.0.0`). `/health` + OPTIONS always open. `apiKeyAuthorized` is pure/unit-tested; `constTimeEql` avoids a timing oracle. NOTE: because localhost is exempt, `curl localhost` won't see the 401 — that's by design (verify with a non-loopback client or the hermetic tests).
- **No admin surface**: the PR-53 admin dashboard / `--admin-key` / cache-eviction POST were deliberately dropped — the panel is open (behind the optional API key) and read-only.

### Server (Zig)
- **Detection**: when `tools` present, server buffers tokens, checks `<tool_call>`, Hermes XML, Gemma 4 `<|tool_call>`, raw JSON. Gemma 4 double-brace args (`{{...}}`) unwrapped before parse. Thinking tokens buffered separately if enabled.
- **Serialization** (`chat.serializeMessagesJson`): `role: "tool"` passed natively (Gemma 4 templates render `<|turn>tool` directly). Tool call `arguments` serialized as JSON strings, not objects.
- **Streaming**: full args sent in one SSE delta (avoids client accumulation bugs). Thinking buffered until close tag, emitted as `reasoning_content`.
- **Fallback** (`chat.fallbackFormatChat`): ChatML, Llama (`ipython` role), Gemma (`Tool result:` in user turn).
- **KV cache**: `reuseKVCache()` token-by-token prefix compare; auto-invalidated after tool calls and pad-only gens. Sliding-window layers keep full buffer; views slice to last `sw` during decode.

### Client (Swift)
- **Agent loop** (`ChatView.runAgentLoop`): up to 150 iters; tools → parse → exec → feed back → repeat. Synthetic user nudge after tool results for some models.
- **History builder**: filters errors/pad-only/summaries, truncates assistant at 500 chars, budget-aware (walks backward, fits within `context_length - max_tokens - system_prompt`), pins first user + first assistant, auto-compacts tool results when tight.
- **SSE parsing**: accumulates tool-call deltas; emits `.toolCalls` on `finish_reason: "tool_calls"`; fallback if stream drops without `finish_reason`.
- **Storage**: `SerializedToolCall` (id, name, args as JSON string) on `ChatMessage.toolCalls`; Codable-persisted; backwards-compat (optional field).
- **Error recovery**: tool errors include sent args + retry hint → enables self-correction.

## Prompt-based Skills

`.md` files in `~/.mlx-serve/skills/` with YAML frontmatter:
```markdown
---
name: deploy
description: Deploy to production
trigger: deploy, release, ship it
---
Steps...
```
- `trigger`: comma-separated, case-insensitive substring match in user message → body injected into system prompt
- Skill index (name + description) always included
- `SkillManager` re-scans on dir mtime change
- No UI entry point — users manage `~/.mlx-serve/skills/` directly in Finder/editor (the old toolbar folder button was removed to keep the chat toolbar lean)

## Resumable Downloads

`DownloadManager` streams to `<file>.partial` via `URLSessionDataTask`. Resume sends `Range: bytes=<existing>-`; 206 → continue, 200 → truncate+restart. 3 retries with 2s/4s backoff. Cancel preserves `.partial`. UI shows "Resume" when `hasPartialDownload()`. Already-complete files (size matches HF metadata) are skipped.

### Server logs
- **Persisted by default** to `~/.mlx-serve/logs/mlx-serve-<port>.log` on every serving path (`--serve`, `mlx-serve run`); `pull`/`list` never write one. Rotates at 32 MB into a single `.log.1` backup (bounded at 2x). `--log-file <path>` relocates it, `--log-file off` disables. stderr is unchanged, so the app's rolling buffer still works. Implemented as a file sink in `log.zig` (raw libc `write(2)` + a pthread mutex — log calls come from the accept loop, every conn thread and the inference thread, none of which carry an `Io`). The sink is opened after `--log-level` parses (level gates what reaches disk) and before model discovery (so weight-load + auto-context lines land in it). A failure to open warns and falls back to stderr-only; a failed write drops the line rather than killing the server.
- Per-port so the app's server (11234) and a test server never interleave into one file. This is the file to read for a post-mortem — the app's in-memory buffer dies with the app, and a restarted/crashed server takes its history with it (that gap is what made the 2026-07-08 pi `terminated` bug hard to diagnose).
- `--log-level debug` for verbose output
- App captures stderr in a `ThrottledLogBuffer` (1 MB rolling tail, `ServerManager.serverLogMaxBytes`); view via the log button in the menu bar. CONTRACT: the buffer always retains at least `maxBytes` and never exceeds `maxRetained` (= 1.25×) — `append` only re-trims past a high-water mark, because an O(n) `count` + `suffix` on every stderr chunk is invisible at 64 KB and a real main-thread cost at 1 MB. It also tracks its own length (`ThrottledLogBuffer.count`, O(1)) since `String.count` walks grapheme clusters. Full history is on disk (see above) — the tail is just what the window renders.
- Manual: `./zig-out/bin/mlx-serve --model <path> --serve --port 8080 --log-level debug 2>&1`
- Patterns: `jinja error:`, `[cache] reusing N/M tokens`, `[cache] invalidated`, `<- N+M tokens (Xms) [reason]`, `tool_msgs=N`, `[spec-stats] mode=...`, `spec-gate: ngram-score=...`

### Swift logs
- `print()` invisible when launched via `open` — run binary directly or write to file
- Every agent request dumped to `~/.mlx-serve/last-agent-request.json`; replay via curl
- Chat history at `~/.mlx-serve/chat-history.json`

### Reproducing
- Tool calling: curl with `stream: false` first, then `stream: true`
- Jinja offline: `pip3 install jinja2`, render `chat_template.jinja` with dumped request
- KV cache: `pkill -f mlx-serve` between tests — one bad request can poison the cache

### Embedded engines (`lib/<engine>/`)

DSV4-Flash is served by the embedded **ds4** engine (antirez/ds4, since renamed "DwarfStar" upstream), pinned at `lib/ds4/@477c0e8`. `build.zig` compiles `ds4.c` + `ds4_distributed.c` + `ds4_metal.m` (the library path; `ds4_kvstore/web/help/agent.c` are CLI/server-only and not embedded). The Zig MLX path no longer carries a DSV4 forward — `model_type=deepseek_v4` on a `.safetensors` checkpoint returns `error.UnsupportedDsv4MlxFormat`; users load the GGUF artifact through ds4 instead.

- C library API: `lib/ds4/ds4.h` (~200 lines, engine + session model).
- FFI: `src/ds4_ffi.zig` (mechanical mirror of the header).
- Bridge: `src/arch/ds4.zig` (`Ds4Engine` + `Ds4Session` — owns Metal-kernel extraction, tokenizer lookups, and the speculative-decode API).
- Session context: `arch_ds4.clampSessionCtx(--ctx-size)` — honors the user's `--ctx-size` (the app's Settings context slider), floored at ds4's `prefill_chunk` (2048) so an under-sized ctx can't produce junk output; `0`/unset → ds4's default 32768. Carried on the stub `ModelConfig.max_position_embeddings` by `runDs4Serve` and read back in `scheduler.runPrefillDs4` (and the `runDs4Offline` CLI path). NOT subject to the MLX `computeMaxSafeContext` auto-budget — ds4 manages its own KV outside MLX, so the per-token estimate there doesn't model it.
- **An MLX memory/working-set guard must EXEMPT embedded-engine requests (ds4/llama) — class: MLX-only estimate applied to a non-MLX forward path.** The ds4 (and llama.cpp GGUF) stub `ModelConfig` advertises real-looking dims (ds4: 56 heads / 61 layers / hidden 7168) so the MLX per-token estimators bill a full transformer that never runs. The auto-context budget already exempts ds4 (above); the prefill preflight `server.checkAttentionMemory` did NOT — live 2026-07-15 (pi + ds4): an 8.6K-token prompt projected `~25189MB` and got a `400 "requires ~…MB GPU memory but only ~…MB available"`, a prompt the SAME server had just served on the MLX qwen35 engine one model-switch earlier. Fix: `server.mlxMemoryGuardApplies(uses_ds4, uses_llama)` early-returns `true` at the top of `checkAttentionMemory` (the SINGLE chokepoint all four HTTP surfaces route through — one gate covers every current + future call site) so an embedded-engine request skips the MLX estimate entirely; the context-length guard (`prompt_ids.len > getEffectiveContextLength`) still bounds the prompt against ds4's own session ctx. Symptom signature: a `requires ~NMB GPU memory` 400 on a ds4/GGUF model whose prompt is well under its context, at a token count another engine served fine. Rule: any NEW MLX memory guard (working-set, KV, prefill-envelope) must skip when `lm.ds4_engine != null or lm.llama_engine != null`, or route through `checkAttentionMemory`. Guard: the `mlxMemoryGuardApplies` unit test in server.zig (red-on-revert: `TestUnexpectedResult`).
- Build: `build.zig` compiles `lib/ds4/ds4.c` + `lib/ds4/ds4_metal.m` and links Foundation/Metal. Metal kernel sources are embedded via `@embedFile` (`lib/ds4_metal_sources.zig`); at first launch we stage them under `~/.mlx-serve/ds4-metal/<binary-hash>/` and point ds4 at them via its `DS4_METAL_<NAME>_SOURCE` env-var overrides — zero patches to the submodule.
- Convention: each new model-specific engine lives at `lib/<engine>/`, exposes a single-header C API, and gets a thin Zig wrapper at `src/arch/<engine>.zig`. See `CONTRIBUTING.md` for the embedding checklist.

Generic GGUF models (everything except DSV4-Flash) are served by the embedded **llama.cpp** engine via `libllama`.

- Staging: `scripts/fetch-llama.sh` downloads the pinned llama.cpp XCFramework (`LLAMA_TAG`, currently `b9851`), thins the macOS slice to a single self-contained arm64 `lib/llama/lib/libllama.dylib` (llama + ggml + ggml-metal merged, Metal shaders embedded), rewrites its install-name to `@rpath/libllama.dylib`, and copies headers to `lib/llama/include`. **Not vendored** — `lib/llama/` is git-ignored and re-fetched by CI / `app/build.sh`. Bump `LLAMA_TAG` to upgrade.
- C shim: `lib/llama_shim/llama_shim.{h,c}` — a small, stable C API over the ABI-fragile `llama.h` structs (compiled by clang against the real header, so the ABI is always correct). Mirrors the ds4.h discipline.
- FFI: `src/llama_ffi.zig` (mechanical mirror of the shim header).
- Bridge: `src/arch/llama.zig` (`LlamaEngine` + `LlamaSession` — model load, tokenize/detokenize, chat-template string, prefill `sync`, `eval`, `argmax`/`sample`). Tests gate on `LLAMA_TEST_MODEL`.
- Build: `build.zig` `addLlamaLib` adds `lib/llama/include`, links `libllama` (`use_pkg_config = .no` so a Homebrew `llama.cpp` install can't hijack the link), compiles the shim, and adds a dev rpath. Bundling: `release.yml` / `app/build.sh` copy `libllama.dylib` into the app `Frameworks/` (and the CLI tarball `lib/`), rewrite `@rpath/libllama.dylib` → `@executable_path/...`, and sign it through the existing dylib loop — no new executable, so notarization is unchanged.
- Chat templating reuses mlx-serve's Jinja engine: the GGUF's embedded template is adopted into the stub `ChatConfig.chat_template` at load (`Scheduler.doLoadLlamaOnInferenceThread`), then `chat.encodeChatViaLlama` renders via `renderChatTemplate` (which supplies the tool-synthesis fallback) and tokenizes through libllama (`add_special=false`, the template owns BOS).
- Scope (v1, like ds4): serial (`max_concurrent=1`), no PLD/drafter/kv-quant/hot-switch (those are MLX-specific). Tested by `tests/test_llama_gguf.sh` (gated on `LLAMA_GGUF_MODEL`).

## Sandbox agent CLI sessions (pi / hermes inside the guest)

App-side (Swift). The Sandbox window's Terminal tab runs agent CLIs INSIDE the Agent Sandbox VM (issue #89 follow-up); the Activity tab is the old transcript UI.

- **Transport — ssh only.** dropbear is baked into `ddalcu/agent-shell-mlxserve` (image source: `containers/agent-shell-mlxserve/` in this repo — `make push` publishes; the Dockerfile is cross-pinned by `SandboxSSHTests.testGuestImageSourceBakesDropbearIn`); `/.vz-init`'s ssh arm (VzGuest.Config.sshEnabled) starts `dropbear -R -s -p 22` and mounts devpts (devtmpfs alone fails PTY allocation). Host side: `SandboxSSH` owns an app-scoped ed25519 keypair + known_hosts in `~/.mlx-serve/sandbox/ssh/` (never the user's `~/.ssh`), injects `/root/.ssh/authorized_keys` + the distinct-prompt `/root/.profile` into the rootfs pre-boot, and a DEDICATED `SandboxPortForwarder` (`targetPortOverride → 22`) mirrors `localhost:<first free from 2222>` → guest `:22` (the general forwarder filters 22 so the two never fight). The embedded terminal (SwiftTerm behind the `EmbeddedTerminalView` seam — the only file importing SwiftTerm) spawns `/usr/bin/ssh` with `SandboxSSH.sshArgs`; `SandboxSSH.displayCommand` is the same session as a copyable one-liner for the user's own terminal.
- **Registry** (`SandboxAgentRegistry`): `SandboxAgentSpec` rows — v1: pi (pinned `npm i -g @earendil-works/pi-coding-agent@…`; models.json = `AgentConfigs.piModelsJSON` verbatim, budgets from advertised context, model = `server.chatModelId`) and hermes (official installer with pip fallback; config mirrors `hermes setup`'s custom-endpoint SAVE path verbatim — `model.default` + `provider: custom` + `base_url` + `api_mode: chat_completions` in `~/.hermes/config.yaml`, `context_length` in a `custom_providers` entry, and `OPENAI_BASE_URL` in `~/.hermes/.env`, which alone defuses the first-run wizard). Config files are materialized host-side into the rootfs with the literal `__MLX_HOST__`; `/.vz-bootstrap-<id>` resolves the NAT gateway (`ip route`), seds the placeholder, installs the CLI if absent (progress streams into the terminal), then `exec`s it. Guest→host arrives non-loopback, so the real `--api-key` rides the configs when set.
- **Session lifecycle** (`SandboxSessionTabs` + per-tab runtimes): MULTIPLE concurrent sessions, one tab each (N sessions = N ssh connections into the same dropbear/mirror; stable display names "pi"/"pi 2"); per tab preparing (`AgentSandbox.startCliSession`: boot → dropbear preflight → materialize → mirror wait) → live → exited. Live terminals are NEVER unmounted (ZStack + opacity — unmount terminates the ssh). Each live session PINS the shared guest (`pinCliSession`; remount-needing workspace switch throws `remountBlockMessage`); Stop guest / app quit end it. Preflight gates (`SandboxCliPreflight`, each a distinct alert with the fix named): sandbox on, guest networking on (offer to enable), server running, server bound `0.0.0.0`, dropbear present (stale cached image → `staleImageMessage` + `repullBaseImage`, which stops the guest BLOCKING before deleting the cached image dir — same ordering rule as the factory reset, pinned by `testRepullStopsTheGuestBeforeDeletingTheImageCache`).
- **Factory reset** (Settings → Agent Sandbox → Reset Sandbox, red + confirmation): `AgentSandbox.resetAllData` stops the guest (blocking, off-main — deleting a rootfs under a still-stopping VM risks a partial cache), deletes `~/.mlx-serve/sandbox` (kernel, images incl. ALL in-guest state, ssh identity — scope pinned by AgentSandboxTests) and clears the transcript; next use re-provisions from scratch.
- **Smoke**: `SANDBOX_SMOKE=1` phase 5 proves boot → mirror → `ssh 'echo SSH_OK'` (SKIPs on a pre-dropbear image); `SANDBOX_SMOKE_AGENT=pi` adds the in-guest install + `--version`.
- **Out of scope (v1)**: PTY-over-vsock (networking-off sessions), vsock reverse tunnel, VM surviving app quit, multi-session manager, opencode/claude rows (add as registry rows later).

## Website & LLM tier list (`website/`)

The GitHub Pages marketing site (deployed by `.github/workflows/pages.yml` on main pushes touching `website/**`; static guard `tests/test_website_pages.sh`). `docs/` is AI/dev docs and is NOT published. Bench numbers live in `benchmarks.md` and the per-run llmprobe reports stay under `~/claude-tmp/` — the CSV/chart pipeline was retired 2026-08-13; the README's one remaining chart is `docs/perf-vs-engines.png`, and the charts the site embeds are its own pinned copies at the `website/` root (og:image URLs are baked — never move those). `website/llm-tier-list/` is the one interactive page:

- The board is ranked by `tierForModel` — the ONE chokepoint, source-scan-pinned — whose `TIER_MODE` is currently `"intelligence"` (published AA index; an unscored model waits in the table rather than being placed on an estimate) because a Wilson board with an empty vote table is an all-U board; **flip it to `"votes"` to restore community ranking, that is the whole change**. Plus community voting (Firebase Auth+Firestore).
- The **quant playground** is a setup calculator (Mac chip × RAM × model × quant × KV quant × context × spec mode → decode tok/s, prefill tok/s, intelligence, RAM fit). Its speed model is a ROOFLINE FIT, not a table: decode = achieved GB/s ÷ bytes-read-per-token, prefill = achieved FLOP/s ÷ 2·active-params, with four efficiency constants fitted to the 26.7.12 bench (`docs/perf-csvs/all-26.7.12.csv`, M4 Max 128 GB, in git history) — `tests/website_tier_list_logic.mjs` REPLAYS those four bench cells plus the 64k ladder decay through the page's own code, so a constant that stops reproducing our bench fails CI. Intelligence is an Artificial Analysis Intelligence Index snapshot (dated in-page); an unscored model is BADGED `est` and inferred from scale — never a fabricated citation.
- **Usable RAM is a fixed reserve, not a percentage** (`usableRamGb`, ONE rule behind both the board's `minRamFor` tier and the playground verdict): a flat 75% is about right at 8 GB and badly wrong at 128 GB, where Metal's own `max_recommended_working_set_size` is ~115 GB (`src/server.zig`'s #64 note) — the 75% rule computed 96 GB and so hid our own 115.4 GB DSV4 mirror from every 128 GB Mac. Past that set but still RAM-resident is a third state, `"tight"`, not "won't fit". The quant ladder carries a `"mix"` rung (0.406 B/weight, from the published `mixed-2-3-8bit` mirror) because large-MoE mirrors do not ship at a uniform width and a uniform-only ladder misreports the hardware needed.
- `tests/website_tier_list_logic.mjs` also stubs a DOM and runs the real `renderRig()` (the dead-control class: a typo'd `getElementById` renders nothing while every byte-level assertion passes).

## Licensing & third-party code (NOTICE)

- **A ported kernel goes in `NOTICE`, and `NOTICE` is the ONE place** (2026-08-06): we shipped MTPLX/dflash/oMLX/mlxfast/mlx-lm-derived Metal kernels and a vendored Apache-2.0 Jinja engine (`lib/jinja_cpp` = wangzhaode/jinja.cpp, NOT llama.cpp's — the old note was wrong about its origin) for months under an MIT-only LICENSE with no NOTICE and no Apache text. Provenance was never MISSING from the source — every one of those sites already named its upstream in prose — it was unenumerable, so add the NOTICE entry and leave the code alone; do NOT sprinkle marker comments (tried, reverted, pure diff noise). Say WHAT WE CHANGED in the NOTICE entry, not the source (§4b), and note that "verbatim" stops being true the moment you extend it — the NAX tile's had, 4-bit to 4/5/6-bit.
- **Enumerating what we ported: grep the comments for `mlxfast|oMLX|MTPLX|mlx-lm|port` — NOT a keyword-window scan** (same day, three misses): a 20-line window for "port|from|based on" missed the mlx-lm GDN kernel, the MLX steel-attention base of `msv_attn_p256`, and the whole mlxfast round-3/4 set (`--decode-attn-quant` int8+nvfp4 tail, `fusedQkNormRope`, `gatherQmvDownReduce`) whose headers say "mlxfast … class" — a phrase no generic provenance word matches. The reliable source is the memory notes (`project_mlxfast_round3_port.md` lists the round's contents) plus the upstream tree itself. Genuinely ours: `qkv_attn_dec`, `gatherQmv` + gate+up, the MoE routers, `mlxserve_swiglu`, `mlxserve_add_rmsnorm`, `mlxserve_attn_out_gate`, `GDN_KERNEL_SEQ`, and all seven dsv4 kernels.
- **Attribution that only lives in the git repo reaches nobody who downloads a binary**: §4(a)/(d) bind whoever receives the Work, and every packaging path shipped the binary alone. `LICENSE`, `LICENSE-APACHE-2.0` and `NOTICE` now ride the CLI tarball, the `.app` bundle (`Contents/Resources/`, both release.yml and app/build.sh) and the brew formula (`doc.install`), pinned by `tests/test_release_workflow_gates.sh`. Only the notices that PERTAIN are reproduced (§4d): MTPLX's own NOTICE also credits vllm-metal and PipeNetwork for parts we don't use, and those are deliberately omitted.

## Supported-architecture addenda (moved out of CLAUDE.md, 2026-08-08)

### DeepSeek-V4-Flash (native `deepseek_v4`)

DeepSeek-V4-Flash NATIVE (284B-A13B, 1M ctx; safetensors dirs only — `.gguf` files keep routing to the embedded ds4 engine). MQA over ONE 512-d latent; window-128 raw + gated-pooling compressed history (ratio 4 w/ top-512 indexer on even layers, 128 on odd, pure sliding layers 0-1); per-head attn sinks in the softmax denominator; INVERSE rope on the attention output; fp8/fp4 KV QAT sims (bit-exact GPU mirrors); Sinkhorn mHC hyper-connections (f32 stream BY DESIGN — don't "fix" it); first 3 MoE layers hash-routed by token id, rest sqrt-softplus top-6 + clipped SwiGLU shared expert. Serial (isMoe), spec hard-off (`is_dsv4`), prefix cache OFF (`HotPrefixCache.shouldUse` — decode state is module-owned rings/caches a snapshot can't restore), SINGLE-FLIGHT admission (`modelExclusiveDecode` — module-owned `dec_state`; a 2nd concurrent request queues in `pending`, keepalives flow while it waits).

Chat: `<｜User｜>/<｜Assistant｜>`, `<think>` chat|thinking modes, DSML tools (`<｜DSML｜invoke name=… string=true|false>`); template is OURS (`src/fixtures/dsv4_chat_template.jinja`, byte-pinned vs the release's encoding_dsv4.py incl. the tools-disable-drop-thinking rule, `tests/dsv4_template_ab.py` 17/17). Oracle: `tests/dsv4_mlx_ref.py` (the mlx-lm PR is buggy — never ground truth); gates: DSV4_MINI fixture/decode-equivalence + DSV4_REAL greedy + `tests/test_dsv4.sh`.

Checkpoint is **DeepSeek-V4-Flash-0731 or later** — the preview is REJECTED at config parse (`num_nextn_predict_layers` with no `dspark_*`); 0731 keeps the trunk byte-identical and swaps the preview's single MTP module for DSpark (3 block-parallel draft stages under `mtp.*`, `dspark_block_size`/`target_layer_ids`/`markov_rank`; reference impl ships in its `inference/model.py`, draft path not wired yet). Mirror: `ddalcu/DeepSeek-V4-Flash-0731-MLX-Serve-mixed-2-3-8bit` (115.4 GB; trunk experts imatrix-calibrated 2b/g128 gate/up + 3b/g128 down on layers 0-38, affine 4b/gs64 experts on layers 39-42 (the tail4 fix), spine/embed/head 8-bit gs64, DSpark stages 4-bit gs64 — draft quality is multiplicative on throughput; built by `tests/fetch_convert_dsv4_0731.py`, which downloads/converts/drops shards per group because source+mirror do not fit on disk together). DSV4_REAL greedy tokens are re-pinned PER MIRROR from the oracle. ONLY our converter's layout is supported — externally-converted DSV4 checkpoints (renamed expert containers, pruned expert counts, dense/requantized tensors) fail load with a missing-tensor error by design.

Perf on a 128 GB M-series (after the 2026-08-01 serial + AR-kernel rounds): serial decode ~29.6 tok/s (22.9 → 26.7 → 29.6), DSpark 31-48 content-dependent (acceptance 1.8-3.4/round); prefill 8K ~267 tok/s. Decode glue rides fused kernels (see engine-mlx gotchas); audit DSpark with `MLX_SERVE_DSPARK_PROFILE=1`.

### Inkling Small (`inkling_mm_model`)

Thinking Machines Inkling Small (276B-A12B MoE; REAP builds prune `n_routed_experts`, e.g. 192/256 = 112 GB 4-bit). **NO RoPE**: position = per-layer RelativeLogits bias (`attn.wr_du` → [H, d_rel] states × `rel_logits_proj.proj [d_rel, extent]` bank, folded into the SDPA mask) + FOUR depthwise causal short-convs/layer (k/v/attn/mlp, k=4, fp32, one concatenated `conv_state [B,3,10240]` on the lfm2 ssm-entry slots — prefix-cache/hybrid checkpointing engages via `needsSsmEntries`) + log-scaling on global layers past 128K (exact no-op below, skipped). 35 sliding(512)/7 global from `local_layer_ids`; per-head q/k RMSNorm ⇒ scale 1/head_dim (`query_pre_attn_scalar = hd²`); layers 0-1 dense (`dense_mlp_idx`, widths SWAPPED vs our field names) then sigmoid+bias top-6 router whose logsigmoid-softmax also weights 2 stacked GATED shared experts (the "sink"; `mlp.gate.weight [E+2, H]` bf16, bias/global_scale f32 — but dense `mlp.global_scale` is STORED bf16, so the stream stays bf16); untied quantized `embed`/`unembed`, `embed_norm`, muP ÷16, vocab sliced to `unpadded_vocab_size`. `model.llm.` prefix. Chat = role-less MESSAGES (`<|message_*|>`/`<|content_*|>` single-token markers, EOS `<|content_model_end_sampling|>`); tool calls `NAME<|content_invoke_tool_json|>{"name":…,"args":{…}}`. MTP head dropped by the converter; spec off; serial. Oracle: the checkpoint's bundled `inkling_mlx/` (Apache-2.0) via `tests/dump_inkling_fixtures.py` (INKLING_FIXTURES units + greedy ground truth); guard `tests/test_inkling.sh`.

### Muse-Glimmer-30B (`muse_glimmer`)

meta-models Muse-Glimmer-30B (dense 30B VL; text served, vision pending). Text trunk: 52 layers, hidden 6656, GQA 32/2 hd 128, MLP 19968 silu, vocab 202048, 131K ctx. Layer = Gemma2 sandwich (input/pre-ffn norms at `rms_norm_eps` 1e-5, post-attn/post-ffn at `post_norm_eps` 1e-8, all centered 1+w folded at load via `addOne`); the FINAL `model.norm` is PLAIN-scale (ones-init, `final_norm_plain` takes a +1 handle instead of folding). Attention: weight-less SHARED qk RMS-norm (per-layer synthesized ones(hd), never +1-folded), Q ×`qk_scale_factor` 3.87 folded into `ModelConfig.attnScale()` = 3.87/√128 (a scalar commutes through RoPE and QK^T); elementwise sigmoid output gate `attn_out *= σ(self_attn.gate_proj(normed))` read off the SAME post-input-norm hidden as QKV (`LayerWeights.ag_*`). Sliding 2048 with full attention every 4th layer counted backward from the last — and exactly those layers are NoPE (`layer_rope_theta` 0 → `layerSkipsRope`, both Q and K unrotated; sliding layers rope at θ 500000). Embeddings: RMS-normed after lookup (weight-less, `normed_embeddings`, shares `ones_hidden`), NO sqrt(hidden) scale. Logits: `lm_head(h) × output_multiplier (0.19611… = 1/√26)` then the standard tanh softcap at 20. Weight prefix `model.language_model.` (flat `muse_glimmer_text` sibling → `model`); lm_head at root; vision tower/adapter/projection dropped at load (`shouldKeepWeightKey`). EOS `<|end_of_text|>` 200001 + `<|eot|>` 200008 (`ensureMuseTerminators`); `<|eom|>` 200007 is NOT eos — generation continues across channel segments. Batched decode supported (byte-identical A/B'd at 200 greedy tokens).

Chat: the shipped `chat_template.jinja` renders THROUGH our jinja.cpp byte-identical to python jinja2 (pinned by `src/fixtures/muse_chat_template.jinja` + `muse_render_reference.txt`); tool-call `arguments` must be OBJECTS (serializer already emits them); `reasoning_strength` (low|medium|high, template default high) mapped from the client's effort in `serializeExtraContext`. Output = harmony-style segments after the prompt's bare `<|start|>assistant`: ` to=self<|message|>R<|eom|>` then `<|start|>assistant to=user<|message|>C<|eot|>` or `to=<fn>` ATEM blocks (`<atem:invoke name>` + `<atem:parameter name>v</atem:parameter>`; strings RAW never trimmed, spelled JSON keeps its type, schema coercion gets the final word). Always-thinking checkpoint, tools-conditional default: a silent request resolves thinking ON with tools / OFF without (`defaultEnableThinking(has_tools)`; `/v1/messages` consults it too when `thinking` is absent), and a thinking-off no-tools render COMMITS the channel — `chat.noThinkTailSuffix` appends ` to=user<|message|>` to the bare `<|start|>assistant` generation prompt (byte-exactly the header the template renders for history content turns) so the model cannot open `to=self` and no reasoning pass runs. With tools the header stays free (a tool call is `to=<fn>`), thinking stays on, and the reasoning is DELIVERED — no surface strips generated reasoning. Tokenizer: Llama-3-style pretokenizer (`PretokStyle.llama3` — case-transition splits with ASCII-exact/caseless-non-ASCII classes, attached (?i) contractions, `\p{N}{1,3}`, `/` in the punct tail), selected from the combined Split regex signature. Mirror: `ddalcu/Muse-Glimmer-30B-MLX-Serve-8bit` (affine 8-bit gs64 on matmul-read weights incl. vision, embed/pos-table/patch-embedder bf16, 32.9 GB). Corpus family `muse_glimmer`; guards: config-parse tests, byte-pinned render test, channel/ATEM/gate unit tests, llama3 pretokenizer boundary tests.

### LFM2-VL (`lfm2_vl`)

LiquidAI LFM2.5-VL (450M/1.6B/3B). The text trunk is the `lfm2` we already serve, unchanged — `layer_types` conv/full_attention, `conv.{in_proj,conv,out_proj}`, `feed_forward.w1/w2/w3`, `operator_norm`/`ffn_norm`, `embedding_norm` as the final norm. `lfm2_vl` adds two things on top and nothing else: a stock `siglip2_vision_model` tower and LFM2-VL's own projector. LiquidAI publish MLX packs themselves (4/5/6/8-bit + bf16) and quantize ONLY the language model — `vision_tower.*` and `multi_modal_projector.*` ship dense bf16 at every width — so the tower is loaded as dense today while `Lin` still resolves `.scales` per tensor for a pack that quantizes it.

**Tower** (`src/lfm2_vision.zig`, 27 layers, hidden 1152, 16 heads hd 72, MLP 4304, LayerNorm eps 1e-6). Vanilla pre-LN ViT: `x += attn(LN1(x))`, `x += mlp(LN2(x))`, then `post_layernorm`. The patch embedder is a plain **Linear** `[1152, 768]` over flattened `[py, px, c]` patches (no conv layout), and attention is unmasked — we encode one image per call, so the reference's packed-sequence mask is all-ones. `vision_use_head: false`, so the attention-pool head is never built. `hidden_act` is `gelu_pytorch_tanh`; the projector's `projector_hidden_act` is plain erf `gelu` — two activations in one checkpoint.

**Position embeddings.** One learned `[256, 1152]` table = a 16x16 grid, resampled to each image's own patch grid by `interpolate(mode="bilinear", align_corners=False, antialias=True)`. Ours does it as two f32 matmuls against `qwen_vision.resampleWeightMatrix(.bilinear)` — Pillow's separable triangle filter, whose footprint widens on downscale, which is exactly torch's antialias. That matters whenever a grid axis is shorter than 16 (an extreme aspect inside the token budget). mlx-vlm 0.6.3 substitutes bicubic here and scores cos 0.99 / rms **1.13x** against the reference, so it cannot serve as an oracle.

**Projector.** `pixel_unshuffle` folds each `downsample_factor` (2) square block of patches into one row (channel-minor within the block, row-major over the reduced grid), then `Linear(4608 -> 2048)` -> GELU -> `Linear(2048 -> 2048)`. Output tokens = `(gh/2) * (gw/2)`.

**NaFlex preprocessing** (`smartResize` / `isImageTooLarge` / `gridLayout`, all pinned against the reference's tables). Sides snap to `patch * downsample` = 32 so the unshuffle never pads, and the area is rescaled into a 64-256 **merged-token** budget preserving aspect (so the pixel bounds carry a `downsample²` factor). It is nearly Qwen's `_smart_resize_image`, differing only in the initial snap being floored at one full factor — which changes the BRANCH for an image thinner than one patch. Python's banker's rounding is load-bearing (`roundHalfEven`; 2000x17 needs `round(62.5) == 62`).

**Tiling.** A source whose snapped area exceeds `max_image_tokens * 32² * max_pixels_tolerance` (2.0) is SPLIT: pick the `cols x rows` grid with `min_tiles(2) <= cols*rows <= max_tiles(10)` closest to the image's aspect, resize the whole image onto a `512 * cols` by `512 * rows` canvas ONCE, cut it into 512px tiles, and append a thumbnail of the whole image at the single-tile geometry. Each piece is its own `chat.ImageData` (`tile_rows`/`tile_cols`/`tile_index`, thumbnail last) and its own tower call. A 1800x1400 photo becomes 2x3 tiles + a 252-token thumbnail = 1788 tokens against 252 for the untiled view; measured on 15px print, tiled reads it exactly and the untiled geometry reads none of it. Re-resizing per tile instead of windowing the shared canvas is a different picture — `buildPixelValuesRegion` is the window.

**Prompt block.** Not a flat pad run. `lfm2ImageSegment` emits `<|image_start|>`, then per piece a marker (`<|img_row_R_col_C|>` for a tile, `<|img_thumbnail|>` for the thumbnail) followed by that piece's pads, then `<|image_end|>`. The row/col ids are one contiguous block laid out row-major over the MAX 10x10 grid, so `base + row * max_tiles + col` locates any of them. None of these ids are in config.json — `ModelConfig.populateLfm2ImageTokens` resolves them by STRING from the tokenizer at load (`tokenizer_config.json` names them: `image_token`, `image_start_token`, `image_end_token`), and a missing marker leaves its id 0, which every consumer reads as absent. Marker order must match the order the encoder concatenated the pieces, because the splice scatters embedding rows into pad slots positionally.

**Guards.** `tests/dump_lfm2_vision_fixtures.py` runs transformers' own `Siglip2VisionModel` plus the projector on OUR pack's weights (`LFM2_VISION_MODEL` + `LFM2_VISION_FIXTURE`; measured pos-embed cos 0.999999 across six grids in both resample directions, tower hidden cos 0.99998/0.99987, projected features cos 0.9997/0.9992 with rms within 0.3%). Reference-table unit tests for the resize/split/grid decisions, patch order and region windowing; `lfm2ImageSegment` layout tests; `tests/test_lfm2_vision.sh` end to end; `tests/test_vision_all.sh` carries the arch in the breadth sweep.

### DFlash block-drafter (generic; Muse-Glimmer instance)

DFlash is block-parallel speculative decoding: a small assistant transformer drafts `block_size − 1` tokens in ONE forward, conditioned on the trunk's intermediate hidden states. It is a METHOD, not a model-family feature — transformers ships a generic `DFlashCache` + `DFlashTokenCandidateGenerator`, and any family can publish a `*_assistant` sidecar. Our detection therefore keys on the CONFIG CONTRACT — a sidecar config declaring ALL of `block_size`, `mask_token_id`, `target_layer_ids` (`dflash.isDflashConfigJson`) — never on `model_type`; `--drafter <dir>` stays the one flag and the scheduler probes the sidecar kind (DFlash → `dflash.loadDflash`, else the Gemma cross-attention drafter). `MLX_SERVE_DFLASH=0` skips the sidecar; `--draft-block-size` clamps the config block DOWNWARD only (floor 2).

Assistant shape (no embed table, no lm_head — borrows the trunk's): `encoder.fc [hidden, n_targets·hidden]` + `encoder.output_norm_enc`, N standard llama-style decoder layers (GQA q/k/v/o, WEIGHTED per-head q/k RMS-norms `[head_dim]`, input+post_attention norms, silu MLP), final `norm`. Weight keys are UNPREFIXED (`layers.N.…`). Muse instance: 5 layers, 32 q / 8 kv heads, hd 128, θ 500000, all-sliding 2048, eps 1e-5, block 16, mask id 201818, targets [1, 13, 25, 37, 49]; 5.11 GB bf16 at `meta-models/Muse-Glimmer-30B-assistant` (users pull the original — no ddalcu mirror).

**Weight precision** (`dflash.DflashLinear`, one `apply` for both arms): each round reads the WHOLE assistant, so the assistant's own precision is the per-round cost. A dense bf16 checkpoint is affine-quantized AT LOAD (`MLX_SERVE_DFLASH_QUANT_BITS`, default 8, `0` = dense) and served packed through `mlx_quantized_matmul(transpose=true)` — the quantized layout IS the checkpoint's `[out, in]`, so quantizing REPLACES the dense path's pre-transpose rather than adding a step. A sidecar that already ships `.scales` is served as-is at the width its packed geometry solves to (`transformer.affineParamsFromGeometry`), and asking for dense never unpacks it. A weight whose contraction dim is divisible by neither 64 nor 32 (`dflash.quantGroupFor`) stays dense — per weight, not per model. Only DRAFT quality rides on this: the trunk verify is exact either way, so a bad width costs acceptance, never a token.

**Where the assistant comes from** (`dflash.resolveInDirDrafter`): an explicit `--drafter <dir>` always wins; with none, `<model_dir>/drafter` is probed at LOAD time for the same config contract — the sidecar-or-in-checkpoint shape `mtp.resolveMtpSource` already uses. A merged sidecar makes the drafter a load-time dependency, so a hot model switch brings its own (`--drafter` is launch-only, and the app used to have to restart for it) and no pairing table decides which sidecar goes with which trunk. `--no-drafter` is the opt-out, because an empty `drafter_dir` no longer means "off". `model_discovery.modelKindFromType` maps `*_assistant` to `.drafter` and refuses it as a standalone model, so the subdir never registers itself; the reported `drafter_path` names whatever was actually resolved. Our Muse mirrors ship it at 8-bit/gs64 (2.72 GB vs 5.11 dense) — the width the loader would have quantized to anyway, served pre-packed with no load-time requantization; the bf16 original stays at `meta-models/Muse-Glimmer-30B-assistant` for the fixture parity's dense arm.

**Block size is a HARDWARE decision** (`dflash.resolveBlockSize(config, cli, cli_explicit, wide_lane)`). The block IS the verify width: all `block_size` positions go through ONE trunk forward, and that forward's cost is a step function of its width. MLX routes transposed qmm by `get_qmv_batch_limit(K, N)` (10 on the Muse shapes) — below it the weight-reuse `qmv` runs, at/above it the 32x32-tiled `qmm_splitk` does, wasting half a tile at width 16. Our split-K verify lane (`transformer.vqmmLaneFor`) covers M 2..7 and the NAX m16 tile covers M 8..16 on G17 only, so 8..16 on older GPUs has no lane at all. `wideVerifyLaneAvailable()` false ⇒ the block is capped at `NO_WIDE_LANE_BLOCK_CAP` (5) and the boot log says `capped (no wide verify lane)`. Explicit `--draft-block-size` clamps against the CONFIG block, never the cap — the escape hatch in both directions. Measured on Muse 4-bit / applegpu_g16s, 160-token generations, serial reference in the same boot: block 16 → 0.92x, 8 → 1.16x, 7 → 1.43x, 6 → 1.79x, **5 → 1.97x**, 4 → 1.81x, 3 → 1.77x. Round profile at block 5 (`MLX_SERVE_DFLASH_TRACE=1`, which inserts eval barriers to attribute a fully lazy round — diagnostic only): verify 50.2 ms / assistant 6.2 / draft head 2.9 / append 0.9 / accept 0.2 / scheduler gap 0.01.

**Draft-only lm_head** (`MLX_SERVE_DFLASH_DRAFT_HEAD_BITS`, `dflash.DEFAULT_DRAFT_HEAD_BITS` = 0, i.e. DEFAULT OFF): the block's draft argmax otherwise reads the full trunk head (202048×6656) on top of the verify's own read of it. `bind` re-encodes it once through `mtp.requantizeRows` — the SAME chunked requantizer the MTP head uses, extended to accept a dense source — and only when that shrinks the read (a bf16 head counts as 16 bits/weight). Verification never routes through it, so the emitted distribution is untouched. It defaults OFF because at the resolved block the head is under 2 ms of a ~60 ms round and the acceptance it costs exceeds the bytes it saves: 3-bit 55.4 tok/s / 2.19 accepted per round vs trunk head 57.8 / 2.37. The lever was a win at block 16 (10 ms of a 175 ms round) — a draft-side lossy default is a ratio between a byte saving and an acceptance cost, and both sides move when the block does.

**Sampled requests** draft greedily and accept through the one-hot rule, and that is the MEASURED choice, not a v1 shortcut: on Muse 4-bit there is no temperature penalty (temp 0 → 1.98x / 55.1% per-draft; temp 0.7 → 1.98x / 57.7%, 3 reps x 4 prompts, same-boot serial reference). `MLX_SERVE_DFLASH_SAMPLED_DRAFTS=1` draws each draft from the request's own filtered distribution (`filteredProbsBlock`, one filter pass + one categorical over all m rows) and accepts through the full Leviathan ratio with the residual taken against the true q — exact, and a LOSS (1.87x / 54.5%): expected acceptance is `p(argmax q)` greedy vs `1 − TV(p,q)` sampled, and this assistant's argmax tracks the trunk while its distribution shape does not. Per-request spec cells swing ~10% run to run at temperature; 3+ reps before believing a temperature claim.

**Assistant context survives the prefix cache** (`prefix_cache.DflashSnap` / `DflashCommit` / `DflashTarget`). A restore forwards no trunk layers, so it produces no captures and the context would start EMPTY on every reused prefix — measured 92.6% → 66.5% per-draft acceptance and 80.2 → 60.9 tok/s on a full-prefix hit, i.e. the multi-turn case paid for the cache twice. The context snapshot rides `Entry` beside the KV, its bytes folded into `kv_bytes` so `--prefix-cache-mem` and eviction cover it (~20.5 KB/token here vs the trunk's ~53.2). Every failure path degrades rather than errors — no payload, SSD tier, snapshot failure, or a base past the trunk cursor all return `LookupResult.dflash_base = null` and leave the caller's cache untouched (start blind) — which is safe precisely because the state is DRAFT-side. The scheduler adopts only when `base + step == matched` EXACTLY, since `nextDflash` asserts `dctx.absLen() == anchor_pos`.

Protocol per round (`generate.nextDflash`):
1. **Context**: trunk hiddens at the OUTPUT of layers `target_layer_ids` (HF `hidden_states[i+1]`) for the tokens the trunk just verified — captured via `ForwardCtx.capture_layers` (standard forward path ONLY; `DflashModel.bind` refuses MoE/hybrid/module-owned/bert trunks by name so the seam can never be silently ignored). Concatenated on features in config order → `encoder.fc` → RMS norm → per-assistant-layer K/V (k-norm + RoPE at the tokens' ABSOLUTE positions) appended to the per-request context cache (`Generator.dflash_ctx`, a plain dense KVCache + `base_pos`). Prefill builds it chunk-by-chunk; on hot-prefix reuse the context starts late at `base_pos = matched` (sliding-window semantics, same rule as the MTP history). Invariant at every round boundary: `dflash_ctx.absLen() == trunk cache.step`.
2. **Noise block**: `[anchor(t1), mask_token × (block−1)]` through `Transformer.rawEmbedding` — the RAW table rows, no normed-embeddings RMS and no emb_scale (the un-foldable muse embed norm is exactly what makes this hook possible).
3. **Assistant forward** (`dflash.forwardBlock`): Q from the block only; K/V = cached context + fresh block K/V. Block K/V are written into the cache's SPARE CAPACITY and truncated straight back out after the forward — the reference's `crop(-block_size)` with zero copies; context K/V are computed once, ever. Masks: block↔block bidirectional; sliding layers see context within `|q−k| < window` (additive bias via `mlx_where`, built only when something is actually masked); full layers see everything.
4. **Drafts** = argmax of trunk `lm_head(assistant_hidden)[:, 1:]` — the anchor row is DROPPED (the reference applies the bare Linear; softcap/output_multiplier are monotone so draft argmax is unaffected). Greedy drafts v1; sampled requests use the shared one-hot Leviathan acceptance.
5. **Verify** = standard spec invariant: input `[t1, d1…]` width `block`, `cache.step = prompt+emitted`, t1 not in cache, correction from ORIGINAL `verify_logits[accepted]`. The verify forward doubles as the next round's context producer (capture_layers set on it).
6. **Rollback** on partial accept is an offset-only `cache.truncate` — legal because bind restricts targets to the pure-KVCache standard path, where a position's K/V depend only on earlier positions; NO re-forward runs — the next round's context is the verify captures sliced to the committed prefix (the reference's `hidden_states[:, :n_accepted]`).

Wiring: `enable_drafter` is the request-level switch for BOTH sidecar kinds (a model loads at most one); priority MTP > dflash > gemma drafter > PLD (`specInitWiring`/`specTickMode`); every existing spec gate (logprobs>0, grammar, vision, hybrid) applies unchanged; `[spec-stats] mode=dflash`. Server-side, every drafter-loaded gate must read `lm.dflash` beside `lm.drafter` — pinned by a source scan in dflash.zig after four per-surface `use_drafter` re-derivations silently decoded serial (live 2026-08-10, the per-site-wiring class).

Muse measurements. 8-bit trunk, same-boot medians, 400-token generations, block 16 (2026-08-10): echo 31.0 tok/s vs PLD 26.2 vs serial 15.6 (1.99x, avg 4.7–6.7 accepted/round vs PLD's 3.23); agent-ish 24.3 vs 18.0 vs 15.9 (1.53x); creative 17.0 vs 16.4 vs 15.7 (+8%) → default-on when the sidecar loads, no runtime acceptance gate needed (unlike the Gemma drafter). 4-bit trunk, 160-token generations, block 5 + trunk draft head (2026-08-10, the shipped defaults): echo 2.19x serial, agent 2.15x, creative 1.68x, 57.9 vs 27.0 tok/s pooled, 2.37 accepted/round — the same stack was 1.01x before the block cap, because a 4-bit serial step halved while a width-16 verify did not. Levers measured and CLOSED: cross-round pre-draft (scheduler gap is 0.01 ms — nothing to overlap), prefill capture cost (−5.2%/+2.3%/+0.3% at 1128/5525/16185 prompt tokens), adaptive block size (every content class peaks at 5 once the draft head is fixed), assistant micro-opts (halving the assistant's read outright is not faster). Full stories: `docs/gotchas/engine-mlx.md`. Parity vs the executable transformers reference (fp32 CPU, `tests/dump_dflash_fixtures.py` → `DFLASH_FIXTURES` env-gated test): encoder cos 0.999994, draft rounds cos ≥ 0.9999 with rms_ratio 1.000.

### Ling 3.0 (`bailing_hybrid`)

inclusionAI Ling 3.0 (`BailingMoeV3ForCausalLM`): a KDA + MLA hybrid MoE. Shipped tiny: 24 layers, hidden 1536, 16 heads hd 128, vocab 157184, 131K ctx, 128 experts top-8 at 512 + ONE ungated shared expert at 512, dense MLP (4608) on layer 0 (`first_k_dense_replace`). `layer_group_size` 4 ⇒ layers 3/7/11/15/19/23 are full attention (`full_attention_interval`, so `isLinearLayer` needs no new predicate); the other 18 are KDA. Weight prefix `model`, embedding `model.word_embeddings` (quantized — `getNamedWeight` takes the table's name as a RUNTIME argument so the weight/scales/biases lookups cannot drift), untied `lm_head`, norms `input_layernorm`/`post_attention_layernorm`, attention under `attention.` not `self_attn.`. Router bf16 `mlp.gate.weight` + bf16 `mlp.gate.expert_bias` (the routing chain runs f32 and promotes it; the zero-init fallback for `moe_router_enable_expert_bias: false` is f32), experts stacked `mlp.experts.{gate,up,down}_proj`, shared `mlp.shared_experts.*`. Mirror: `rapid-mlx/Ling-3.0-tiny-MLX-4bit` (affine 4-bit gs64, 4.2 GB).

**KDA (Kimi Delta Attention)** rides the GatedDeltaNet path with three differences, each its own config field. (1) The forget gate is per KEY CHANNEL, not per head: `f_proj` is [hidden → H·Dk] and the fused kernel indexes `g` by the key channel (`kda_vector_gate` → the `_vec` kernel variant; `gdnKernelSource(vectorized, capture_seq)` generates all four kernels from one recurrence so the math exists once). (2) The gate FORMULA is the bounded-sigmoid arm of fla's kernel — `g = exp(bound · σ(exp(A_log)·(a + dt_bias)))`, bounded in (exp(bound), 1) — which REPLACES `-exp(A_log)·softplus(·)` rather than clamping it; reading `kda_lower_bound` as a clamp gives a plausible, silently wrong recurrence. (3) The output gate is a plain sigmoid (`y_norm · σ(g_proj(x))`), not swish. Load-time normalizations keep the hot path identical to qwen3.5's: the three separate q/k/v projections and three depthwise convs are CONCATENATED on the output-row axis (exact — affine groups run along the input axis and depthwise channels never mix), and `A_log` [H] is EXPANDED to [H·Dk] so the gate chain is pure elementwise over flat arrays. The blocked prefill kernel declines on a vector gate (it reads a per-head gate); q/k scaling is already KDA's (`l2norm(q)·Dk^-0.5`).

**MLA** is the DeepSeek-V3 shape in its NAIVE (un-absorbed) form: `q_a_proj`(256) → `q_a_layernorm` → `q_b_proj` → [128 nope | 64 rope] per head; `kv_a_proj_with_mqa` → [512 latent | 64 shared rope key] → `kv_a_layernorm` → `kv_b_proj` → per-head [128 nope | 128 value]; the rope key is MQA and broadcast across heads so the cached K is a plain `[B,H,S,192]`. RoPE covers the rope slice ENTIRELY at θ 6e6 and rotates ADJACENT PAIRS (`rope_interleave` → mlx `traditional`; the reference de-interleaves both operands before a halves-rotation, and a consistent permutation of q and k leaves q·k unchanged, so the traditional kernel is exact, not an approximation). Scale is `qk_head_dim^-0.5` (192, not head_dim) — `query_pre_attn_scalar` and `prefillScoreHeadDim` both carry it, and `prefillMemoryNeeded` takes the score width SEPARATELY from the stored width for the same reason (billing the composed [heads, chunk, seq] score tensor against `head_dim` 128 declares it fused and bills zero). Per-head sigmoid output gate from `attention.g_proj` (`head_wise`; `element_wise` is refused by name). Output projection is `attention.dense`. The KV cache is ASYMMETRIC (K 192, V 128) — MLX's SDPA has a fused vector kernel for exactly that pair at decode and falls back to the composed path at prefill widths; the quantized-KV fused kernels assume one width for both, so MLA never opts into them. Un-absorbed costs ~10x the absorbed form's cache, which is affordable only because 6 of 24 layers attend at all.

`--kv-quant 4|8` serves this arch: the affine write path solves each of its six buffers' packed and scale widths from ITS OWN operand (K 192 → 48 u32 / 3 groups, V 128 → 32 / 2), and truncation slices the value scale/bias views against the value scales' shape. The fused quantized-KV READ kernels still assume one width for both, so MLA never opts into those — it reads `denseView`. TurboQuant cannot serve it at all (a Hadamard rotation needs a power-of-two width and the key is 192), and that is refused at LOAD by name: `KVCache.initWithConfigAndHeadDim` pow2-checks `ModelConfig.kvCacheKeyHeadDim()`, which is the cached KEY width rather than the declared `head_dim`.

Memory: `ModelConfig.kvBytesPerToken()` is the ONE KV bill — `attnCacheLayerCount()` (linear layers hold a fixed-size recurrent state, not a per-token cache) × kv_heads × (K width + V width). The old uniform `layers × 2 × kv_heads × head_dim` over-billed this arch 3.2x and pinned auto-context to under a third of what fits. Both the auto-context sizer and `prefillMemoryNeeded` read it, source-scan-pinned at the call site.

MoE routing is hy3's sigmoid+bias chain with DeepSeek's group limit inserted between the bias and the top-k (`n_group` 8 / `topk_group` 4, `noaux_tc`), which `groupLimitedRouting` already implemented for dsv4 — `computeHy3Routing` dispatches to it on `moe_n_group > 1`. That chain deliberately returns f32 weights and it is the CALLER's job to land them in the activation dtype; skipping the cast promoted the residual stream to f32 at the first MoE layer (visible only as a `[dtype-trace] residual widened` line, and it promotes every weight on read for the rest of the forward).

Chat: `chat_template.jinja` with `<role>SYSTEM|HUMAN|ASSISTANT|OBSERVATION</role>` turns, `<|role_end|>` (156895) as the sole terminator, and GLM-style tool calls (`<tool_call>NAME<arg_key>K</arg_key>\n<arg_value>V</arg_value></tool_call>`) that the existing glm-family parser already handles. `thinking_option` defaults to 'on' and the generation prompt opens `<think>` — `defaultEnableThinking` opts in so the vendor's default mode is reachable from a request that names no preference. Refused by name at config parse rather than served wrong: low-rank KDA gates (BOTH spellings — `no_kda_lora: false` and `use_kda_lora: true`), `element_wise` attention gating, a non-sigmoid `score_function`, a `kda_lower_bound` of 0 or more, a `qk_head_dim` disagreeing with nope+rope, an absent `q_lora_rank`/`kv_lora_rank`, NoPE MLA (`use_mla_nope`), a linear-attn head count of its own (`num_kv_heads_for_linear_attn`), `linear_silu: false`, and the norms the forward does not implement (`value_norm`, `up_proj_norm`, `use_nGPT`). An ABSENT `kda_lower_bound` is not a refusal — it SELECTS fla's other arm, the softplus gate, which the shared chain computes elementwise and therefore serves per-channel unchanged (`kdaUsesBoundedGate`). `kda_safe_gate` is a launch detail of the reference kernel, not a formula: both arms are evaluated in f32 here. Guards: the config-parse test in model.zig, the vectorized-gate kernel equivalence + `kdaGateChain` tests and the asymmetric-KV test in transformer.zig, `kvBytesPerToken` in server.zig, and `tests/test_bailing_hybrid.sh`.

### MiniMax-H3 numerics addenda (`minimax_h3`)

Beyond the main MiniMax-H3 section above: position coordinates are AREA-NORMALIZED per axis (`(arange(0.5,n)/n)*2-1` scaled by 32), so aspect ratio moves EVERY position and a raw-index port generates the wrong framing. Rope is PARTIAL split-half (96 of head_dim 128; the top 32 pass through). Dual sigma schedule (video shift 12.0 / audio 3.0) with the audio velocity scaled by `d(sigma_a)/d(sigma_v)` so both streams integrate on the video schedule. 24 fps fixed; frame counts on the 17k+5 ladder (the VAE folds 17 source frames into 5 latent tokens). Weights via mage_flow's shared `MfLinear`; wide-M dq-gemm is DEFAULT-ON for this path (−13%/step at 480p, output u8-identical; `MLX_SERVE_MF_DQ_GEMM` overrides). AdaLN (13B params, 39% of the DiT) is PRECOMPUTED per generation over the schedule's t-union and the weights FREED (`precomputeAdaln`, kill `MINIMAX_H3_ADALN_PRECOMPUTE=0`). Staged residency: TE run and FREED before the DiT loads; the media preflight bills `gen.h3PeakBytes` = max(TE,DiT)+VAEs, never the sum. Profile harness: `MINIMAX_H3_ABLATE=ladder` in-graph ablation arms. Fast recipe default-on (velocity cache 0.05 + attention broadcast k=2 = 2.83x at 768p; request `"fast": false` opts out, `resolveSpeed` is the chokepoint, env always wins; turbo forces it off). Training-free sparse attention behind `MINIMAX_H3_SPARSE_ATTN` (v1 mix policy needs tuning — measured dead, see engine-mlx gotchas). Turbo LoRA: larryvrh 4-step distill, verbatim HF file as `turbo_lora.safetensors`, file 0 of the shared `lora.Stack`, attached BEFORE precomputeAdaln, must hit all 259 `LORA_TARGETS` or it is a broken artifact; steps default 4; audio steps by EXACT mapped Δσa (`audioStepFactor`). Single-frame VAE ENCODER (fl2va keyframes): T==1 collapses every CausalConv3d to its last temporal tap — full-T waits for ref2va.

### Kokoro-82M TTS (`src/kokoro.zig` + `kokoro_g2p.zig`)

StyleTTS2 + iSTFTNet, NON-autoregressive, ~17x realtime, 325 MB f32: ALBERT (shared layer) → DurationEncoder (BiLSTM+AdaLayerNorm) → duration-driven frame expansion → F0/N AdainResBlk stacks → iSTFTNet generator. 54 voice packs, each a `[510,1,256]` table indexed by PHONEME COUNT; comma-separated names BLEND by mean. `kokoro_g2p.zig` = text→IPA (misaki dicts → case → suffix stemming w/ -s/-ed allomorphy → acronym spelling → letter-to-sound; NO espeak, it is GPLv3). Weights via `tests/convert_kokoro_weights.py`, published at `ddalcu/Kokoro-82M-MLX-Serve` (f32, ~345 MB, `g2p/` included), which is what the app downloads.

### Runtime LoRA (`src/lora.zig`)

Runtime unfused LoRA adapters, STACKED (`Stack` = up to `MAX_LORAS` 8 files; `deltaSum` sums per-linear, never merges) across QLinear/MixedLinear/MfLinear. `parseKey` accepts diffusers/kohya/dotted-`lora.down`/PEFT-`.default.` spellings + flat `lora_unet_` scheme; `canonicalize` maps every alias onto OUR module name per `Arch` (flux2/krea2/minimax_h3/generic), splitting a fused BFL QKV source into thirds (shared full-rank A). `validatePath` is the ONE path rule (absolute + regular file, checked before mlx sees it); `fileAlphaScale` resolves the file's declared alpha/rank out of the safetensors `__metadata__` (per-module `.alpha` tensor still wins).
