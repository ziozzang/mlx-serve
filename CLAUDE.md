# mlx-serve — project context for AI

Native Zig server running MLX-format LMs on Apple Silicon; OpenAI/Anthropic/Ollama-compatible HTTP APIs + native media generation. No Python. This file is the COMPRESSED layer; detail lives one hop away.

## Detail map (read on demand)

- `docs/reference.md` — deep detail: per-file contracts, media-gen schemas, API surfaces, LAN design, observability, embedded engines, arch numerics (dsv4/inkling/H3, Kokoro, runtime LoRA), website/tier list, licensing. Read the section BEFORE deep work on a subsystem.
- `docs/gotchas/{tool-calling,server-http,engine-mlx,models-media,app}.md` — full war stories behind every rule in `## Rules`.
- `tests/CLAUDE.md` — integration-test matrix. `app/CLAUDE.md` — Swift app layout + rules (auto-load in their dirs).
- Skills: `/release` (pre-release checklist, CalVer, CHANGELOG), `/bench` (llmprobe methodology + comparison traps).
- `containers/agent-shell-mlxserve/` — Agent Sandbox guest OCI image (`make push`/`make export`); ships dropbear (pinned by `SandboxSSHTests`).
- `containers/guest-kernel/` — guest kernel 6.6 + fuse owner-read clamp (issue #150); `build.sh` → release asset `kernel-arm64.gz`; tag pinned by `AgentSandbox.kernelTag` + `scripts/fetch-guest-rootfs.sh` (bump together).
- `website/` — GitHub Pages site + `llm-tier-list/`. Design: `docs/reference.md`. Guards: `tests/test_website_pages.sh`, `tests/website_tier_list_logic.mjs`.
- **Growth policy (ENFORCED)**: this file stays under 100k bytes and EVERY rule bullet is ≤ 3 lines. New gotcha = 1–3-line rule here + full story in `docs/gotchas/*.md`. New subsystem = one Layout row + a `docs/reference.md` section. App content → `app/CLAUDE.md`. Never paste war stories or measurements here.

## Stack

Zig 0.17 (pinned nightly via `scripts/fetch-zig.sh`; brew 0.16 no longer builds); mlx + mlx-c PINNED SUBMODULES (`lib/mlx-src` v0.32.2, `lib/mlxc-src` 56b2d39 = PR #127) self-built NAX-enabled by `scripts/build-mlx.sh` into `lib/mlx/` (FFI `src/mlx.zig`); jinja.cpp (wangzhaode, Apache-2.0, NOT llama.cpp's) as `lib/jinja_cpp/libjinja.a`; stb_image + libwebp; safetensors; BPE. Embedded engines: ds4 (`lib/ds4`, DSV4-Flash GGUF) + libllama (`lib/llama`, generic GGUF).

## Layout (`src/`)

| File | Role |
|---|---|
| `main.zig` | Entry, CLI flags + subcommands (`run/pull/list/serve/launch`) |
| `cli.zig` | Ollama-grade CLI: alias → HF repo, resumable pull into `~/.mlx-serve/models/<org>/<repo>`, `list`, `run` REPL |
| `launch.zig` | `mlx-serve launch <agent>` (claude/pi/omp/opencode/codex/hermes/aider): reads `/v1/models` for models + ADVERTISED context, writes configs into `~/.mlx-serve/<agent>/`, starts the app if server is down. Swift `CLILauncher`+`AgentConfigs` is the documented twin (omp reads `PI_CODING_AGENT_DIR`, codex Responses-only via `CODEX_HOME`, hermes `HERMES_HOME`) |
| `mlx.zig` | mlx-c FFI |
| `model.zig` | Config parse + safetensors loading |
| `tokenizer.zig` | BPE; single special-token splitter (first-byte-bucketed); per-model `digit_group` |
| `transformer.zig` | Forward pass, arch dispatch (attention/MLP/MoE/GatedDeltaNet), quant resolution, custom kernels (`msv_attn_p256`, `verifyQmm` lanes incl. NAX) |
| `generate.zig` | Generation, sampling, PLD/drafter/MTP orchestration, `StallClock`, prefill chunking, loop-stop tiers |
| `chat.zig` | Chat templates (ChatML/Gemma/Llama-3/Jinja2), thinking tags, tool-call parsing/repair/coercion |
| `vision.zig` / `qwen_vision.zig` + `mrope.zig` | Gemma SigLIP / Qwen3-VL ViT + M-RoPE |
| `muse_vision.zig` / `lfm2_vision.zig` | Muse-Glimmer ViT / LFM2-VL SigLIP2-NaFlex tower + projector + tiling |
| `server.zig` | All HTTP: `/v1/*` (chat/completions/messages/responses/embeddings/load/unload, `models/rescan`), media endpoints, `/metrics(.json)`, WS, Ollama glue, `--api-key`, console at `GET /` (`src/html/` injected as `{s}` args, renders with NO model). Embeddings: BERT + EmbeddingGemma + per-checkpoint pooling + `--embedding-max-length` |
| `lan.zig` | LAN sharing: Bonjour, `SharedSet` + `routeClass` allowlist, `<id>@<peer>` mirroring, streaming proxy. Pure transport |
| `metrics.zig` | Lock-free zero-when-off observability (`--metrics`): `vllm:`+`mlx_serve:` Prometheus + JSON |
| `ollama.zig` | `/api/*` translation, SSE→NDJSON `Sink`, tags/show/ps, `resolveName` |
| `gen.zig` | Unified media gen: modality-named engine slots, `detectModality`/`peekModelType`, per-request handlers, img2img/edit/LoRA, residency estimators |
| `krea.zig` / `flux.zig` | Image backends (Krea-2-Turbo / FLUX.2 klein 4B+9B); `MixedLinear` infers quant geometry |
| `multipart.zig` | RFC 7578 form parsing, zero-copy `Part` (only non-JSON shape: `POST /v1/images/edits`) |
| `mage_flow.zig` | MageFlow Turbo/Edit: flow DiT + DiCo VAE + Qwen3-VL TE; `MfLinear` shared with H3; DiT/TE bf16, VAE f32 (load-bearing) |
| `hunyuan3d.zig` / `hunyuan3d_paint*.zig` | 3D shape + texture paint; converted layouts BAKE OUT per-head QKV interleaves — never "fix" it |
| `acestep.zig` | ACE-Step music (Qwen3 encoder, AdaLN DiT, Euler flow-match, Oobleck VAE 48 kHz; Snake/encode f32) |
| `music3.zig` | MiniMax Music 3: Qwen3-8B global LLM (batch-2 CFG) + depth decoder → hidden states condition a flow DiT (temb as TOKEN) + Snake/DAC vocoder 44.1 kHz |
| `ltx_video.zig` / `ltx_audio.zig` | LTX video (one/two-stage/HQ, i2v, a2vid) + audio VAE/BigVGAN. `LtxVersion` (from `model_version`) keys 2.3-vs-2.5: text encoder, `ff_bias`, `keyframes_abs_pos_embedding` |
| `ltx_diffvae*.zig` | LTX-2.5 DiffVAE decoder (`"decoder":"diffusion"`): geometry/tiling, fused 3D NA Metal kernel, MLX pass. Sampler contract is MEASURED (x0, 1 step, timesteps x1000) |
| `minimax_h3*.zig` | MiniMax-H3 text-to-audio-video: joint video+audio DiT, staged residency, fast recipe, Turbo LoRA, chained windows — detail in `docs/reference.md` |
| `tts.zig` | Qwen3-TTS incl. ECAPA-TDNN voice clone |
| `kokoro.zig` / `kokoro_g2p.zig` | Kokoro-82M TTS + text→IPA G2P (no espeak — GPLv3) |
| `marching_cubes.zig` / `glb.zig` / `uvwrap.zig` / `rasterize.zig` / `texinpaint.zig` | Pure-Zig mesh/GLB/xatlas/rasterizer/inpaint (zero MLX, hermetic tests) |
| `responses.zig` | Responses API pure data: parser, envelope, `ResponseStore`, compaction |
| `ws.zig` | RFC 6455 framing (server-side) |
| `pld_index.zig` | PLD n-gram index (`findMatch`, `ngramRepeatScore`) |
| `prefix_cache.zig` / `kv_disk_cache.zig` | Hot prefix cache + SSD tier (`--prefix-cache-disk`, default OFF) |
| `drafter.zig` | Gemma 4 assistant drafter (cross-attention spec-decode) |
| `dflash.zig` | DFlash block-drafter: config-contract detection (root OR nested `dflash_config`), per-request context cache, block forward; DFlash2 adds `selectPath` + 2-tap grouped convs; trunk seam = `ForwardCtx.capture_layers` + `rawEmbedding` |
| `mtp.zig` | Qwen 3.5/3.6/3.8 native MTP head (sidecar OR in-checkpoint `mtp.*` via `resolveMtpSource`; per-weight quant re-solve; committed-history cache) |
| `diffusion.zig` | DiffusionGemma block-diffusion canvas loop |
| `deepseek_v4.zig` | DeepSeek-V4-Flash NATIVE arch (module-owned decode state on `Dsv4Model.dec_state`, NOT the KVCache) |
| `qwen4_exp.zig` | Qwen3.8-Flash-Next (`qwen4_exp`) host side: n-gram hash (splitmix multipliers, per-head primes, eos-segment shifts) + the mmapped 4-bit `ngram_table.bin` row gather. Trunk forward = `transformer.forwardQwen4With` (hyper-connections, PLE, QSA mask over `gatedFullAttnWith`) |
| `scheduler.zig` | Slots, inference thread (sole MLX caller), queues, batching, loop-stop guard, spec wiring, single-flight admission |
| `round_cost.zig` | Measured per-model/width/KV-bucket spec round-cost table (`Transformer.round_cost`), persisted under `~/.mlx-serve/round-cost/` |
| `model_discovery.zig` / `model_registry.zig` | Discovery (two-level org/name, multi-root, GGUF classification, stub meta), multi-model registry |
| `arch/ds4.zig` / `arch/llama.zig` (+ `*_ffi.zig`, `lib/llama_shim`) | Embedded-engine bridges |
| `ane.zig` + `lib/ane/` | ANE prefill offload (`--ane-prefill`, opt-in, LOSSY int8/fp16, M4-and-below): SwiGLU-MLP + fused GDN in-proj MIL programs on the private AppleNeuralEngine framework (vendored maderix/h3.c-ane, MIT, symbols `msv_ane_*`), packed as PROCEDURE BANKS, per-unit eval thread, compile cache `~/.mlx-serve/ane-cache`, channel-mode split with per-silicon default share. `/props` `"ane"` + `mlx_serve:ane_*` gauges. Detail: `docs/reference.md` + ANE rules below |
| `lora.zig` | Runtime unfused STACKED LoRA (8 max, summed never merged) across QLinear/MixedLinear/MfLinear |
| `status.zig` / `log.zig` | TUI status bar; leveled logging + file sink (`~/.mlx-serve/logs/mlx-serve-<port>.log`, 32 MB rotation) |
| `format_corpus_test.zig` / `tool_traffic_replay_test.zig` | Hermetic format corpus + real-traffic replay (`src/fixtures/tool_traffic.jsonl`) |

CLI flags: `--model --serve --host --port --prompt --max-tokens --temp --top-p --top-k --ctx-size --embedding-max-length --timeout --reasoning-budget --no-vision --pld --pld-draft-len --pld-key-len --drafter --draft-block-size --no-mtp --mtp --mtp-depth --mtp-history-window --ane-prefill --dspark --decode-attn-quant --no-decode-attn-quant --kv-quant --kv-attn-mode --prefix-cache-entries --prefix-cache-mem --prefix-cache-disk --max-concurrent --skip-mem-preflight --metrics --api-key --lan-share --lan-discover --lan-name --no-drafter --no-tool-autocorrect --ssd-streaming --no-ds4-mtp --model-dir --log-level --log-file --version --help`

Sampling defaults for omitted fields: body > launch flags > model `generation_config.json` > hardcoded (1.0/1.0/off). Missing generation_config = wild-sampling signature.

## Building

- First-time: `./scripts/fetch-zig.sh` stages the pinned Zig at `.zig-toolchain/`. Retire once 0.17.0 ships in brew.
- Zig caches configure-time subprocess output: after a toolchain/SDK change `rm -rf .zig-cache` or the link references a ghost path.
- **ALWAYS `zig build -Doptimize=ReleaseFast`, never bare `zig build`** (Debug 2–4× slower ⇒ fake regressions). `zig build test` does NOT refresh `zig-out/bin/mlx-serve` — rebuild before any live A/B.
- Swift app: `bash app/build.sh`. The two bundle binaries move together.
- mlx + mlx-c: submodules built by `scripts/build-mlx.sh` (deployment target 26.2 → NAX kernels; script + `tests/test_mlx_staged_nax.sh` ASSERT `*_nax` in the metallib). Min macOS 26.2. Bump = checkout tag → rerun → re-diff `src/mlx.zig` externs. Brew: webp ≥ 1.6.0.
- Rebuild Jinja after `lib/jinja_cpp/*.cpp` changes: `cd lib/jinja_cpp && for f in jinja_wrapper caps lexer parser runtime jinja_string value; do clang++ -std=c++17 -O2 -DNDEBUG -I . -c $f.cpp -o obj/$f.o; done && ar rcs libjinja.a obj/*.o`

## Testing — TDD is mandatory

Order: (1) failing test FIRST, for the right reason; (2) minimum code to green; (3) full suite (`zig build test` 6/6 0 fail + `cd app && swift test`/`build` + relevant `tests/*.sh`); (4) refactor. A live curl is a sanity check, NOT a test.

Feature = unit test that fails without it (+ integration script if HTTP-observable). Bug fix = regression test red→fix→green, red-on-revert. Cross-arch = cover every touched arch. Refactor = characterization test first. UI/build scripts = factor a pure helper and test that.

**Class bugs get class guards.** A live failure revealing a CLASS ships: the instance regression test; a corpus entry or universal invariant in `src/format_corpus_test.zig`; a rule here + story in `docs/gotchas/`.

Hermetic suites: `zig build test -Dtest-filter="format corpus"`, `-Dtest-filter="tool traffic"`. Full matrix: `tests/CLAUDE.md`.

## Releases & benchmarking

- Process/CalVer/CHANGELOG: `/release`. Per-release perf gate = `./tests/bench.sh` on the FINAL tree vs the previous column in `benchmarks.md` (ONE new column per release).
- Methodology: `/bench`. llmprobe measures; bench.sh drives mlx-serve, `--url` probes another engine. Only diff same-methodology cells (llmprobe from 26.8); spec cells are variance — sample across runs/boot-orders; never quote a win without naming the engine; same-session ratios only; an A/B arm is proven by ENGAGEMENT lines in its own log. Record = `benchmarks.md` + `~/claude-tmp/bench-<tag>/`.

## Conventions

- Minimal DRY Zig; tests at the bottom of each source file; shell integration tests in `tests/`.
- Inference thread is the SOLE mlx caller (even frees) — media gen posts to `gen_queue`, never a gpu mutex. A long gen blocks chat decode (accepted).
- Concurrent requests batch-decode on pure-attention archs; `--max-concurrent` sizes the submit queue, not a decode gate. Slots entering a batch mid-generation drain lazy pipeline state first.
- A batched decode group is capped by PADDING WASTE, not slot count (`batchedKvKeepCount`, `MAX_PAD_WASTE` 1.5 — must stay < 2.0): `padAndStackBatchedKV` pads to the group's longest, an unbilled per-tick transient (~6.5 GB on a qwen3_5 trunk). Group sorts by kv_len, keeps the largest prefix under the bar; longest slots fall to serial.
- A batched-decode guard that only runs at N=1 pins a shape that never ships: `MLX_SERVE_FORCE_BATCHED=1` at one slot still has `batch == 1` inside the forward (decode-attn-quant + fused QK-norm gates key on it). `tests/test_batched_equivalence.sh` runs a real two-stream arm; both kernels log `[batched] ... engaged (slots=N)`.
- A cold prefill YIELDS to decode ticks at chunk boundaries (`scheduler.interleaveDecodeTick`); greedy byte-identical, idle cost zero, `MLX_SERVE_PREFILL_INTERLEAVE=0` restores. Guard: `tests/test_prefill_interleave.sh`.
- KV reuse via prompt-prefix matching; invalidated after tool calls + pad-only gens; hot cache spills to SSD; RAM invalidation propagates to disk.
- Chat templates live in model dirs; Jinja renders with fallback formatting.

## Supported architectures

Dispatch on `config.json` `model_type`. GGUF bypasses MLX → embedded engine by header (`gguf_meta.preferredEngine`: antirez DSV4-Flash → ds4, else llama.cpp).

| model_type | Notes |
|---|---|
| `gemma4`, `gemma4_text` | `language_model.model` prefix; SigLIP vision; clipped linears, PLE |
| `diffusion_gemma` | Gemma 4 26B-A4B trunk, BLOCK-DIFFUSION (diffusion.zig): ≤48-step canvas denoise; PLD/drafter/MTP/batching/prefix-cache never apply; instruct-only |
| `gemma3`, `gemma3_text` | + flat text-only sibling; EmbeddingGemma encoder when `use_bidirectional_attention` |
| `qwen3` | QK norm |
| `qwen3_5`, `qwen3_5_moe(_text)` | GatedDeltaNet + optional MoE, shared expert; Qwen3-VL vision. Qwen3.8 packs serve on this arch |
| `qwen3_next` | DeltaNet |
| `nemotron_h` | Hybrid transformer + Mamba2 (`backbone` prefix) |
| `lfm2`, `lfm2_moe`, `lfm2_vl` | Hybrid gated conv + attention; `lfm2_moe` swaps FF for sparse MoE past `num_dense_layers` (sigmoid routing, selection-only `expert_bias`, no shared expert); dense MLP spelled `w1/w3/w2` OR `gate/up/down_proj` (probed); `lfm2_vl` = siglip2 tower + projector, NaFlex 64-256 merged-token budget, tiling past `max_image_tokens x 2.0` into 512px tiles + thumbnail, labelled `<|img_row_R_col_C|>`/`<|img_thumbnail|>` |
| `hy_v3` | Hunyuan 3 MoE (expert container probed) |
| `laguna` | poolside Laguna S 2.1 (117.6B-A8.5B): nvfp4 experts, softplus attn out gate, YaRN + sliding, sigmoid routing, UNGATED shared expert. Serial |
| `inkling_mm_model` | Thinking Machines Inkling Small (276B-A12B, REAP-prunable). NO RoPE (RelativeLogits bias + causal short-convs); role-less markers; serial, spec off |
| `muse_glimmer` | Muse-Glimmer-30B (dense VL, GQA 32/2 hd128, 52 layers): sandwich norms with separate post-norm eps 1e-8, weight-less qk-norm folded into `attnScale()`, sigmoid attn out-gate, NoPE every 4th layer, RMS-normed embeddings, logits ×output_multiplier softcap 20. Harmony channels + ATEM tools; always-thinking — silent default ON with tools / OFF without (`defaultEnableThinking`), thinking-off commits ` to=user<|message|>`. Llama-3 pretokenizer. Vision via `muse_vision.zig` (window attn 3/4 layers, resampled 32x32 pos table, 2D RoPE, CHANNEL-major pixel-shuffle, `smart_resize`). DFlash via `-assistant` sidecar. Mirror `ddalcu/Muse-Glimmer-30B-MLX-Serve-8bit` |
| `llama`, `mistral` | Standard |
| `deepseek_v4` | DeepSeek-V4-Flash NATIVE (284B-A13B, 1M ctx; safetensors only). Serial, spec hard-off, prefix cache OFF, single-flight; DSML tools on OUR byte-pinned template; checkpoint 0731+ only; only our converter's layout; mirror `ddalcu/…mixed-2-3-8bit`. DSpark spec |
| `qwen4_exp` | Qwen3.8-Flash-Next (125B-A6B + 51B n-gram + 4B MTP): qwen3_5 GDN+MoE trunk inside 4 hyper-connection residual streams (`hcRead`/`hcWrite`, grouped `(1+w)` norms FOLDED by the converter), n-gram PLE at layer 1 (host gather from `ngram_table.bin`, never resident), QSA sparse attention past 2048 tokens (`qsaMask`: 4-token blocks, top-512 by relu-sum indexer score, per-query tail, lower-index ties), final `hyper_connection_mixer` replaces `model.norm`. `Transformer.qwen4` is READ-ONLY module state (hash + mmap), so text slots BATCH-decode like qwen3_5 (`forwardMoeBatchedDecode`: GDN + PLE window merged, QSA mask per slot false-padded to kv_max); the MTP SLOT is exclusive (`scheduler.slotExclusiveDecode`); prefix cache ON (aux state rides `SSMCacheEntrySnapshot`); MTP = the checkpoint's own hyper-connected QSA+MoE layer over the PRE-mixer stream (`Qwen4Mtp`, `MtpHeadRef.qwen4`, default-off on MoE ⇒ `--mtp`/`enable_mtp`), rollback via `ssmRollbackFromCapture` (GDN capture + PLE window/tokens + positional QSA keys). Vision = the Qwen3-VL tower byte-for-byte (`qwen_vision.zig`, `model.visual.` prefix, dense bf16 `model-vision.safetensors`) + interleaved M-RoPE through attention AND the QSA indexer; image/video turns decode serially (MTP declined). Oracle: `tests/dump_qwen4_exp_fixtures.py` (`--vision`). Converter: `tests/convert_qwen38_flash_next.py` (streams from the Hub; ~107 GB pack, ~75 GB resident; `--add-vision` appends the tower) |
| `bailing_hybrid` | Ling 3.0 (BailingMoeV3): KDA + MLA hybrid MoE, `layer_group_size` 4 → `full_attention_interval`; KDA = GDN with PER-CHANNEL gate (`_vec` kernel), BOUNDED-SIGMOID gate (`kda_lower_bound` REPLACES softplus), sigmoid out-gate; MLA = naive DeepSeek-V3 (512 latent + 64 rope key, ASYMMETRIC K192/V128 cache); `noaux_tc` routing; `--kv-quant 4|8` ok, turbo refused (192 not pow2). Thinking ON; GLM tool tags. Mirror `rapid-mlx/Ling-3.0-tiny-MLX-4bit` |
| `*.gguf` | ds4/llama.cpp; GGUF presence WINS over stray config.json. ds4 DSpark: `--dspark` arms when a `-DSpark-` GGUF sits beside the model (gate keys on `mtpDraftTokens()>1` NOT `hasMtp()`); ~0 net on 0731 |
| `minimax_h3` | MiniMax-H3 text-to-audio-video: joint denoise, 17k+5 frame ladder, 24 fps, two partitions (fl2va/ref2va — `tasks` is the ONLY discriminator), Turbo LoRA, chained windows, fast recipe default-on |
| media types | `flux2*`/`krea*`/`mage_flow*`/`qwen3_tts`/`acestep`/`minimax_music3`/`AudioVideo` (LTX 2.3 + 2.5 by `model_version`)/`hunyuan3d*` → gen.zig slots (`mage_flow` has NO root config.json — classified from `model_index.json` by `gen.peekModelType` + `model_discovery.peekMageFlowIndex`, kept in sync) |

TODO: `phi/phi3`, `command-r`. Models with `vision_config` but no vision weights disable vision. Embedded-engine detail: `docs/reference.md`.

## Unified media generation

One server, one registry — image/audio/video/3D coexist with chat. Engine slots are MODALITY-named unions on `LoadedModel`; new backend = one union arm + impl file. Gen runs on the inference thread via `gen_queue`; app flow load→generate→unload; headless starts idle. `model_discovery.isMediaModelType` and `gen.modalityFromType` are documented duplication — keep in sync. Downloads → `~/.mlx-serve/models` (single source of truth, Swift + Zig).

- `POST /v1/images/edits` = OpenAI multipart translated by `gen.openaiEditFormToJson` into the `mode:"edit"` JSON body; unhonored fields = NAMED 400.
- LoRAs are STACKED, ONE grammar across image/LTX/H3: `lora_paths`+`lora_scales` (cap 8, `gen.parseLoraFields`), summed at forward — never merged. Resident backends reconcile via `setLoras`; H3 pre-validates (`lora.validatePath`), Turbo = file 0.
- Endpoint/field/backend detail: `docs/reference.md`. Guards: `tests/test_unified_gen.sh` + per-modality scripts; parity via env-gated cos oracles (`tests/dump_*_fixtures.py`).

## HTTP APIs

- **OpenAI chat/completions + Responses**: usage ALWAYS carries `prompt_tokens_details.cached_tokens` (ONE `formatChatUsage`); thinking opt-ins = `reasoning_effort` OR `enable_thinking` (`reasoning_budget_tokens` outranks); `n>1` 400s. `/v1/responses`: envelope echoes request, every SSE event has `sequence_number`, `background:true` → 400, stateful via `ResponseStore`, WS via Upgrade, NO `[DONE]` on WS.
- **Continuing a partial reply** (`chat.continuationRequested`/`continuationPrefill`): trailing assistant message becomes a PREFILL (prompt ends mid-turn, whitespace trimmed). `/v1/chat/completions` takes `continue_final_message` EXPLICITLY; `/v1/messages` INFERS; `/v1/responses` neither. Ignored when not continuable; tool-call replies never are. Content channel committed first; in the tokenize-cache key; ds4 can't (`continuationRejectReason`).
- Context-overflow 400s name BOTH counts (`contextOverflowMessage`); the app renders it as a card.
- **Anthropic `/v1/messages`** (Claude Code): typed blocks, `input_schema`→`parameters`, stop-reason map incl. `stop_sequence` echo, full SSE block lifecycle. Launcher env: `ANTHROPIC_BASE_URL` + dummy keys + `ANTHROPIC_DEFAULT_*_MODEL=mlx-serve`.
- **Ollama `/api/*`**: pure translation, no-model endpoints answered pre-scheduler; `resolveName` handles `name:tag`. One path must never register under TWO ids (`registry.peekByPath` — double-residency OOM).
- `/v1/models` rows carry `context_length`+`max_model_len` at TOP level twinning `meta.context_length` (#188): discovery clients never read `meta.*`. Both emitters. Guard: `tests/test_models_capabilities.sh` [4b].
- **LAN sharing**: proxy is a TRANSPORT; keyless gate = `routeClass` × `SharedSet`; `<id>@<peer>` mirroring; loops impossible by construction (self-token + tunnel marker, one hop). Design: `docs/reference.md`.
- **Observability** (`--metrics`): zero cost off; TTFT at prefill completion; live tok/s via ONE atomic per tick. `--api-key`: loopback exempt; `/health`+OPTIONS open; `constTimeEql`. No admin surface.

## Tool calling (server pipeline)

With `tools`, tokens buffer for detection (all tag families + raw JSON); thinking buffered separately. Parse chain: strict → tolerant repairs → truncation salvage; then the ONE chokepoint `server.parseToolCallsForRequest` = parse → inferred-name filter → parallel clamp → buried-param hoist → schema coercion (hoist+coercion gated by `--no-tool-autocorrect`; emitted `arguments` ALWAYS valid JSON). Serialization (`chat.serializeMessagesJson`): role "tool" native, args as JSON STRINGS, every string via `appendJsonString`. Streaming: full args in ONE SSE delta; thinking → `reasoning_content`. Fallback (`fallbackFormatChat`): ChatML, Llama ipython, Gemma "Tool result:". Client side: `app/CLAUDE.md`.

## Prompt-based skills / downloads / debugging

- Product skills: `~/.mlx-serve/skills/*.md` (frontmatter trigger substring → body into system prompt; `SkillManager` rescans on mtime).
- `DownloadManager`: streams to `.partial`, Range resume, 3 retries, cancel preserves partial, size-matching files skipped.
- Server log `~/.mlx-serve/logs/mlx-serve-<port>.log` is THE post-mortem file (`--log-level debug`). Grep: `jinja error:`, `[cache] reusing/invalidated`, `<- N+M tokens`, `tool_msgs=`, `[spec-stats] mode=`, `spec-gate:`, `[loop-stop]`, `[lan] proxy`, `[disk-cache] restored`, `[hot-cache]`. Capture: `MLX_SERVE_RAW_DUMP_FILE=<abs>` → `tests/harvest_tool_traffic.py`. Reproduce tool bugs `stream:false` first; `pkill -f mlx-serve` between KV-poison tests.

## Rules (distilled gotchas — stories in docs/gotchas/; every bullet ≤ 3 lines)

### Tool calling & formats (→ docs/gotchas/tool-calling.md)

- **Control bytes**: ONE raw byte <0x20 in history kills the strict render → SILENT `fallbackFormatChat` (model loses its stop token). Everything through `appendJsonString`; wrong-family tags out ⇒ suspect silent fallback first.
- **A `chat_template` value can be a POINTER** (`{% include 'chat_template.jinja' %}`, transformers ≥5, issue #169): `chat.isIncludeStub` reads an include-only value as "no inline template" so the sidecar loads. Grep the log for `jinja` first.
- **A JSON-dialect call cut INSIDE the object still names its tool** (`chat.truncatedJsonCallName`, depth-1 `"name"` only): balanced-snap declines, XML/pythonic arms had NAME + `{}` salvage, the Hermes JSON arm had none — a 4 KB `edit` that hit EOS mid-string vanished at the flush as an empty `stop`. Shipping the raw markup as content is NOT the fix.
- **Model-mangled arg JSON**: strict parse fails → `looseRepairToolCallJson`; never drop the whole call. **Truncated opener**: recover NAME + `{}` — NEVER ship partial values.
- **Delimiter-drop tolerance** (hy3): a tag parser never bails on ONE missing delimiter; plural-wrapper recovery keys strictly on the SUFFIXED `<tool_calls:` form.
- **A `<tool_call>` body carrying `<function=` is the XML dialect and is read FIRST** (qwen 3.5+ template mandates it): the JSON branch once snapped a package.json out of a `content` parameter and shipped its `"name"` as the tool. A parameter VALUE never decides the call.
- **A `</think>` inside a tool ARGUMENT is payload** (`chat.thinkCloseIsToolCallPayload`): decline a close whose nearest preceding tool opener is still OPEN AND whose block closes afterwards — both halves load-bearing.
- **Near-repeat loop tier** (`generate.isNearRepeatTailLoop`): 1024-token window, THREE ratios must ALL be low (distinct ≤0.12, 4-grams ≤0.35, novelty ≤0.10); the third (PROGRESS) tells a loop from procedural code. Bar errs toward acquittal.
- **Loop-stop cuts are truncations**: `finish_reason "length"` (`scheduler.loopStopReason`), `[loop-stop]` logged, no fragment values; `finish_details:{"type":"repetition_loop"}` on chat+completions; non-streaming trimmed to the span start (`loopTrimmedIds`, `MLX_SERVE_LOOP_TRIM=0`). Guard: `tests/test_loop_stop_signal.sh`.
- **Types come from the SCHEMA, never the value's spelling** (`coerceToolArgsToSchema`; undecidable → untouched). ONE chokepoint `server.parseToolCallsForRequest` — never call `chat.parseToolCalls` from a handler.
- **Buried required params**: `hoistMisplacedRequiredParams` lifts only on all-schema-read unanimity. Pristine args = the parse layer is innocent.
- **Heuristic raw-JSON inference must name a DECLARED tool** (`filterInferredBySchema`); explicit tag calls never filtered; new heuristics set `.inferred`; not autocorrect-gated.
- **Hard invariants (replay-pinned)**: emitted args ALWAYS valid JSON; every converter escapes + dedups; coercion never worsens conformance; broken output stays honest. Harness: `src/tool_traffic_replay_test.zig`.
- **A Hermes param value keeps its own whitespace** (`chat.stripHermesValueFraming`): the template frames it with EXACTLY one newline per side and that is all the parser strips — trimming ate an `old_string`'s indentation and mis-edited at the wrong nesting. Padded SCALARS still type from their spelling (the trim survives only as the `isJsonLiteral` probe).
- **Gemma dropped `<|"|>`**: rich bare values run to the CONFIRMED closing delimiter or top-level final `}`, never the first `,`/`}` inside markup.
- **Think-tag leaks**: strip pos-0 unclosed openers; `trimTrailingThinkClosers`; universal no-tag-leak corpus invariant covers new entries.
- **Unparsed tool markup never rides out as reasoning OR content** (`chat.trimLeakedToolMarkup`): one cut at the first wrapper opener, applied ONCE; `/v1/messages` cuts at emission; streaming end-flush concatenates BEFORE cutting.
- **Streaming + tools + thinking**: buffer until pattern resolution; reasoning streams INCREMENTALLY on the tools path (`.hold_thinking` + `chat.unstreamedReasoning`, never a resend; a BUDGET keeps buffering). Pinned by corpus streaming replay + `tests/test_messages_stream_thinking_tools.sh`.
- **The streaming think gate scans with a CURSOR** (`chat.ThinkScan`, O(n)); close tag cannot latch once a `tool_call` substring is seen — falls back to the full scan.
- **Pythonic tool calls (lfm2/LFM2.5)**: call-expression grammar; TYPES live in the SPELLING; marker-gated ⇒ additive; truncation ships NAME + `{}`.
- **A template can open `<think>` unconditionally** (LFM2.5): whether a prompt ends inside a think block is a property of the RENDERED BYTES (`server.promptOpensThink`), never ANDed with `enable_thinking`; thinking-off + prompt-opened ⇒ a stream arm DROPS the block.
- **`in_think_block` seeds from `prompt_opened_think` ALONE at every stream site, never the request flag** (LFM2-VL shipped its whole answer as reasoning with empty content). Class guard = source scan; bar = stream-vs-non-stream byte invariant (`tests/test_lfm2_vision.sh` [6/7]).
- **An integration assertion that a MODEL must think/answer/call is a checkpoint expectation**: assert the INVARIANT, branch on the model's choice.
- **A contract COMMENT is read as a spec** — pin it with a test or it gets filed as a bug (#94).
- **Assistant-history reasoning round-trips** (`Message.reasoning_content`, OMITTED when absent): reasoning-persisting templates (laguna, inkling) otherwise render nothink signatures from turn 2.
- **A template can raise_exception on OUR extra-context values** (Inkling, Qwen3.8): `serializeExtraContext` sniffs the family; tool-call `arguments` stay OBJECTS; history tool_calls carry `"id"`. Qwen3.8 = THIRD effort vocabulary (`xhigh|medium|low`); thinking-off refusal splits INSIDE the family, so it is sniffed on the refusal string and only a refusing template gets `noThinkTailSuffix`.
- **A transcribed chat template is worth what it is PINNED against; whitespace is a token-level contract** (`tests/dsv4_template_ab.py`, byte equality over the shapes the server emits).
- **Inkling channel markers are single special tokens, but tool turns need more**: `streamShouldBufferForTools` buffers on the invoke marker and HOLDS bare-identifier segments after boundary markers.
- **A muse channel HEADER is ordinary text between single-token markers**: every stream surface HOLDS an unresolved header (incl. sub-`to=` prefixes) and drops header tokens (`museHeaderSkipNext`); `to=self`→reasoning, `to=user`/bare→content, else→ATEM tool (`splitMuseChannels`/`museStreamVerdict`). ATEM strings RAW, spelled JSON keeps type, truncation ships NAME + completed params.
- **An error echo teaches the model the error** (Inkling name salvage): NAME = trailing identifier run; body = BALANCED JSON; a parsed NAME never contains `<|` (corpus invariant); bare-JSON inference never runs on invoke-marker text; `applyFamilySamplingDefaults` fills top_p 0.95 when no generation_config.
- **Thinking-off is enforced in the PROMPT; generated reasoning is ALWAYS delivered** (`chat.noThinkTailSuffix`: muse commits ` to=user<|message|>` without tools, LFM2.5 gets `</think>` appended). Every delivery site splits via `splitThinkBlock(text, true, …)` — no strip-and-discard path.
- **Muse renders round-tripped reasoning as `to=self` HISTORY, so PRIOR-turn reasoning is dropped from the prompt** (`chat.dropPriorTurnReasoning`): only assistant messages AFTER the last user message keep `reasoning_content`. laguna/inkling untouched — per-family.

### Server, HTTP, lifecycle (→ docs/gotchas/server-http.md)

- **`messages.deinit(allocator)` frees the Message array and NOTHING it points at** (live 2026-08-25): `handleChatCompletions` + the Anthropic `/v1/messages` handler `toOwnedSlice`d each message's decoded `images[].pixels` (+ videos/audio) into a `chat_mod.Message` and never freed them — leaked on the SUCCESS path and on every early return after the parse loop (measured 40 requests x 3 MB = +121 MB RSS, retained). A `defer` per exit path is how the next exit path gets added without one, so request media is owned by ONE `server.RequestMedia` beside the `messages` list; `Message` BORROWS the slice. Ownership by PROVENANCE — a media list is only obtainable from `openImages`/`openVideos`/`openAudio`, owned from its first append; slots are INDICES, not pointers. `responses.ParsedInput.owned_images` was already this shape. Guards: source scan `a request handler never owns its own media list` + a `std.testing.allocator` test on `RequestMedia`.
- **A `seed` binds EVERY sampler with a fresh key PER DRAW** (`generate.seedKey` + `SamplingParams.draw`; `Generator.sampleLazy` is the one lazy sampler): the lazy path used MLX's global RNG (seed worked only with `logprobs`, replaying ONE key every step).
- **Logprobs are the MODEL's distribution, ids travel WITH values, entry belongs to the RETURNED token**: pre-temperature; argpartition+gather (`mlx_topk` is NOT descending); one-token delay (`Generator.pending_logprob`). Bar: temp-0 rank 1 == chosen token.
- **Streaming logprobs on BOTH surfaces**: `logprobs` is a SIBLING of `delta`/`text` (`ChunkExtras`, null never absent); ONE collector `StreamLogprobs`; drain against a HIGH-WATER MARK, ship EXACTLY once; token+entry publish in ONE critical section. Guards: `tests/test_logprobs.sh`. Gaps: `logit_bias` unparsed, `/v1/responses` no logprobs.
- **`logprobs.content` describes `message.content`** — slice by `contentTokenRange`; streaming is structural (reasoning emitters never drain; content chunks `skipToContent`; empty-content arm = `dropPending` at all four sites).
- **A logprobs token string is a BPE FRAGMENT, not necessarily UTF-8**: `jsonEscapeLossy` at the four token-string sites, `bytes` keeps exact bytes; never widen to `jsonEscape`.
- **`/v1/completions` logprobs is an INTEGER and a different shape** (four parallel arrays); measure over the CHAT surface.
- **Liveness is a property of the SOCKET**: `beatStreamKeepalive` at the bottom of every streaming loop; emit on 5 s byte-silence. `StreamHeartbeat` (client) mirrors `StallClock` (server). GAP: Ollama sink drops SSE comments.
- **A missing tensor is a load ERROR, never `unreachable`** (#217): every non-optional weight getter (`getLayerWeight`/`getWeightFmt`/`getBert*`/`getLayerScaleOrEmpty`/`getLayerBias`) returns `error.MissingWeight` → named 503 via `loadErrorFromName`; one unloadable pack on disk used to end the process and every queued request.
- **`modelDiskBytes` bills the shards the INDEX names** (#274): `model.safetensors.index.json` `weight_map` is the set; a stray `.safetensors` beside it is dead weight and no eviction trigger. No index = every shard (single-file + media packs).
- **`POST /v1/images/edits` forwards `lora_paths`/`lora_scales`/`lora_path`/`lora_scale`** (#268): the multipart rebuild used to drop them silently, contradicting the "unhonored = NAMED 400" rule. Array forms pass as raw JSON; `parseLoraFields` owns the 400.
- **A load failure crosses the inference thread by NAME** (`req.error_name` → `ModelRegistry.loadErrorFromName`): preflight refusals → `error.InsufficientMemory` → named 503 + entry reset to `.unloaded`; everything else `Model load failed: <name>` (#144).
- **Image placeholder tokens are identical across images, so a vision prefix is keyed on the PIXELS** (`vision_key` = `server.mediaKey` hash beside `has_tools` on every RAM entry; vision entries never spill to the SSD tier): vision slots commit AND restore; a restored prefix hands its placeholder count to the splice (`InitOptions.vision_rows_before` → `vision_splice_offset`); `shouldCheckpointSsmPrefill` no longer excludes vision (a hybrid image turn was a guaranteed `hybrid miss`). Live: 483/514 reused on qwen4. Guard: `tests/test_vision_prefix_cache.sh`.
- **Thread lifecycle**: detach every per-connection `std.Thread` immediately; on teardown drain conn threads (`active_conn_threads` + `cancelAllInFlight`) BEFORE `scheduler.deinit`.
- **An adopted spec cache has ONE owner at a time** (#266): `Generator.initWithOptions` frees the restored MTP/DFlash caches on its own error paths (disconnect ⇒ `error.Cancelled` mid-prefill), so `runPrefill` clears its locals BEFORE the call — `MtpCacheRef`/`DflashCtx` hold the KVCache BY VALUE and a second errdefer deinit is a SIGSEGV in `freeKVEntry`. Scan-pinned.
- **Serial ≠ exclusive**: module-owned decode state needs admission single-flight — `scheduler.admitPendingTick` keyed on `modelExclusiveDecode` (NEVER `!modelBatchable`); held slots stay in `pending` so keepalives flow.
- **`--model-dir` is REPEATABLE**: `discoverModelsMany` merges roots FIRST-WINS; app `ModelRoots` is the ONE answer — READS go through `readRoots` (every served folder), writes/cancel/DELETE stay on `ownedRoots`. Guards: `tests/test_multi_model_dir.sh`, `ModelRootsTests`, `OwnedRootDiscoveryTests`.
- **A KV bill is per CACHING LAYER at the arch's OWN K and V widths** (`ModelConfig.kvBytesPerToken` fed by `attnCacheLayerCount`): linear layers hold fixed state; MLA K is wider than V. Auto-context sizer + `prefillMemoryNeeded` read the ONE helper, scan-pinned.
- **The STORED width and the SCORED width are two parameters** (`prefillMemoryNeeded(… hdim, score_hdim …)`): score term reads `prefillScoreHeadDim()`, dequant transient reads stored `head_dim`. One value for both billed MLA's 192-wide scores at ZERO → uncatchable Metal OOM.
- **An MLP envelope is not a prefill working set — the arch's OWN streams are the missing term** (`prefillStreamBytesPerToken`): linear-attention hybrids hold a chunk-wide q/k/v stream in EVERY linear layer; MoE replicates the hidden stream `top_k` times. `max(3 x mlp, mlp + fwd x stream)` — the max is a FLOOR.
- **The chunk-independent part is a RUNTIME floor** (`PREFILL_RUNTIME_FLOOR_BYTES`, both consumers + dsv4 sibling): dense bf16 pays it too. Costs the SIZER (27B @ chunk 512 on 16 GB ≈ 18k, because that is its peak); `--prefill-chunk` buys it back.
- **The prefill CHUNK is a machine decision** (`resolvePrefillChunk` + `ModelConfig.pinned_prefill_chunk`): ladder 8192→512 takes the widest rung whose reserve ≤ a QUARTER of what's left after weights + hot cache; `--prefill-chunk` wins. The pin rides `InitOptions.pinned_prefill_chunk` (Transformer holds a config COPY). Vision prefill chunks like text (#197, `vision_splice_offset`; `MLX_SERVE_VISION_CHUNKED=0`).
- **A `layer_block_types` hybrid was billed KV for every layer** (`attnCacheLayerCount`): lfm2/nemotron_h set `layer_types`/`hybrid_override_pattern`, not `full_attention_interval`. Only `.attention` blocks reach `ctx.cache`; past the 128-entry table keep the `.attention` default.
- **A gate that runs BEFORE the estimator that knows better IS the estimator** (#126): eviction gate + preflight + COMMIT read ONE estimator (`gen.estimatePeakResidentBytes` → `gateEstimateBytes`); refusals name `--max-resident-mem`.
- **A refusal quotes the number it COMPARED** (`scheduler.loadRequirementBytes` is the ONE number, formatted FIRST, scan-pinned across BOTH text + media sites); `loadErrorFromName` keeps BOTH InsufficientMemory/OutOfMemory as a memory 503. App: all four gen panes fall back to the server log tail.
- **A staged bill is per-STAGE**: transient allowance belongs to the allocating stage; a stage's weights are what it KEEPS (`h3DitResidentBytes`); `gateEstimateBytes` adds no 10% to media peaks.
- **A residency bill is a PLAN** (`stagedPeakBytes`): sum-of-directory only for hold-everything backends; LTX = sum − spare variant + out-of-dir TE (`ltxPeakBytes`). New backend declares its plan or gets the sum.
- **A video response is ONE JSON body, so the transport cap bills DELIVERED frames** (#283, `gen.videoRgbTransportReason`, 768 MB raw RGB = the app's `maxFramePayloadBytes`): 5 chained H3 windows rendered 29 s then died `WriteFailed` because `chain_windows` was never in the bill. Both video admissions refuse by NAME before a denoise step; the app bills windows into `frameOptions` and its stepper stops at the server's 6.
- **A long job's abort must not depend on the RESPONSE SHAPE** (`gen_sse.StreamCtx.stream`): probe is `Conn.peerClosed`; `stream:false` no-ops the event cb; cancel arms report `error.Cancelled`.
- **Non-text model on a text surface must 400 BEFORE prefill** (`textGenRejectReason`, arch_hint peek + post-load). New surface → `isTextGenRoute`; new modality → `modalityFromType`.
- **An arg loop with no else branch is a silent flag eater** (`cli.classifyUnparsedArg`). Every `--flag` any script or `ServerOptions.toCLIArgs` passes must be in main.zig's match list. Related settings travel as ONE value (`server.PldDefaults`); guard `tests/test_headless_spec_flags.sh`.
- **A client-supplied PATH is proven on OUR side of the mlx boundary** (`lora.loadFile` stat → 400; an MLX error KILLS the process).
- **Hand-written error text is not JSON**: escape at the SINK (`gen_sse.jsonEscapeMessage`), truncate on a UTF-8 boundary.
- **Endpoint EXISTENCE must not depend on model state, and the 404 is answered BEFORE the model is resolved** (a typo'd `/v1/load` once cost 2m42s + 121 GB): gate is unconditional, above every load, below the LAN proxy, ONE copy; `ROUTE_PATHS` scan-pinned. Guard: `tests/test_route_404_no_load.sh`.
- **A launch flag that shapes a LOAD must be retained on the Scheduler**: `ensureLoaded`'s cold-load `LoadRequest` is a SECOND construction site (prefix-cache, MTP, llama, drafter, ssd all bit). `--drafter <path>` is the exception: applied only when the entry IS the launch model (`coldLoadDrafterDir`). Guards: source scan + `tests/test_cold_load_launch_flags.sh`.
- **A cache derived from a MODEL is per-model** (JSON grammar token→bytes table was a process singleton; other models masked with a foreign vocab): lives on `LoadedModel` (`grammarTokenBytes`). Guard: `tests/test_json_mode_multi_model.sh`.
- **Every grammar state must have a legal byte** — an all-false mask is `-inf` everywhere and argmax picks id 0. `stepObject` rejects `,` when `additionalProperties:false` and all seen; `nextConstrained` logs an all-false mask.
- **A page that must render on an EMPTY server can't render FROM a model**: `GET /` renders above resolution, fetches `/v1/models` client-side; `index.html` is a std.fmt FORMAT string (CSS/JS injected as `{s}`). Guards: `tests/test_index_page.sh`, `tests/html_console_test.mjs`.
- **Console**: mic ONLY in `listening`; replies via `speakableChunks` (markdown stripped); one composer, Recents in localStorage (`storableTurns` strips images); markdown from ESCAPED input; no sampling knobs. Media is ASKED for: ONE gen per turn, tool `model` enum == `editableIds`, system prompt built from `/v1/models`.
- **A stream and a non-stream answer must be the SAME BYTES; leading whitespace is the one thing a stream may withhold** (`chat.streamContentLead`): suppress only an ENTIRELY-whitespace lead chunk (a partial trim breaks logprobs), and retire its pending entry (`lps.dropPending()`). All four content emitters, both surfaces.
- **A client cannot time our own stream**: use the final chunk's server `timings`, summed across rounds; `stream_options.include_usage` gates it. **The `include_usage` chunk ships `"choices": []`** (`sendSSEUsageChunk`) — the ending appears on exactly ONE chunk. Guard: `tests/test_loop_stop_signal.sh` [2][4].
- **Media on a tower-less model is refused by NAME** (`server.mediaRejectReason`, all three surfaces, before prefill): `--no-vision` parsed the image, dropped it and answered "Sky" for a house with a 200. Guard: `tests/test_qwen4_exp.sh` [11] (tower line absent, capabilities drop `vision`, active_bytes −2.4 GB, image → 400).
- **The QSA prefill mask is a memory term no envelope models** (`server.qsaMaskBytes`: 4 B per (query, key) for ONE live layer, 4096 x 25k = 410 MB): billed by the chunk sizer at kv = chunk and by the admission guard at the prompt length; zero without `indexer_budget`.
- **A field DISPATCH reads must be readable from every body SHAPE**: `parseModelFromRequest(body, content_type)` feeds dispatch AND `lanShareDenial` (multipart `model`).
- **A content array's text parts JOIN in order — never last/first-wins** (`server.joinedTextParts`, all three parse sites; issue #195 opencode plan mode).
- **A header PARAMETER lookup keys on the parameter at a boundary** (`name=` also matches inside `filename=`). **A body log fine for JSON is not fine for BINARY** (`bodyIsText`/`bodyPreview`).
- **Auto-context bills KV at the CONFIGURED width and activations ONCE** (`kvBytesPerTokenAtBits` + `prefillTransientReserve`, shared with the admission guard, scan-pinned): the prefill transient is flat in prompt length, keyed on `--prefill-chunk`. `prefillMemoryNeeded` UNDER-bills quantized prefill ~22% (known).
- **Auto-context is PINNED at load** (`pinAutoContext`); 85% margin on the MEMORY ceiling only; ask `getEffectiveContextLength`, never `server_config.max_context_size`.
- **GPU memory ceiling must see EXTERNAL pressure** (`currentGpuMemoryCeiling`); Metal OOM is UNCATCHABLE (libllama frames are a red herring); embedded-engine requests are EXEMPT from MLX guards (`mlxMemoryGuardApplies`).
- **The prefill guard bills what the ARCH reads**: `prefillAttnKeys(seq)`, `prefillFfnWidth` from the DECLARED config (MoE = top_k × moe_intermediate + shared), chunk term arch-owned (`server.dsv4PrefillMemoryNeeded`); all scan-pinned.
- **Metal at the working-set edge returns ZEROS before it aborts**: all-zero logits from healthy inputs = MEMORY symptom, not math.
- **A weight outside every warmup forward is still LAZY at serve time**: force-eval at init (`appendWeightArrays`), budget-gate on LOGICAL bytes (`dsparkFitsBudget`); DSpark is opt-in `--dspark`.
- **`--timeout` is a STALL timeout** (seconds without a new token); `toolCallFinishReason` preserves "length" through tool parse on all four surfaces; `outputBudgetGuidance` only when tight (<12288).
- **Ownership by PROVENANCE, never content**: `{slice, owned}` returns, never free-unless-equals-literal.
- **LAN robustness**: per-INTERFACE dns_sd callbacks, loopback-first fetches (Local Network privacy blackholes LAN SYNs), eviction only via `attemptKnown` after `PEER_DROP_FAILS`, dead refs revived by the browser loop, raw-body id compares canonicalize `\/`.
- **@peer proxying is bounded by the TUNNEL MARKER**: any direct client gets ONE hop; `X-MLX-LAN` requests never proxy again (`isTunneledRequest` at gate AND dispatch). List filters key on the discovery fetch's marker; `lan_peer` entries never re-export; self-fetch → `error.SelfFetch`.
- **A llama session trim is FALLIBLE on recurrent/hybrid GGUFs** (#286 + #287; `mlx_llama_session_trim` returns 1 = cleared): `llama_memory_seq_rm` refuses a partial tail past the recurrent snapshot window and mutates NOTHING, so a trimmed mirror over an untrimmed KV served the PREVIOUS request's tool calls (qwen35, qwen3next, nemotron_h). Cold-prefill on refusal; PLD was never involved. Guard: `llama: re-sync after a long generated tail`.
- **A READY model never advertises LESS capability than its stub** (`readyHasChat` counts embedded engines; app `lanAdvertises` tolerates empty caps).
- **Default bind is 0.0.0.0 and serve mode WARNS** (`server.shouldWarnOpenBind`); flips to 127.0.0.1 in a future release. The app always passes `--host` explicitly.

### Engine: KV, spec-decode, kernels, MLX (→ docs/gotchas/engine-mlx.md)

- **The prefill-chunk cap reads the SCORE width** (`ModelConfig.prefillScoreHeadDim`) — the `<=128` fused-SDPA early-out otherwise exempts MLA; hd-256 policy branches key on 256 EXACTLY.
- **On NAX the stock sdpa is the hd-256 kernel** (`naxSdpaPreferred`, default = NAX available, `MLX_SERVE_NAX_SDPA=0|1`): `msv_attn_p256`'s causal arm declines, stock fallthroughs pass `force_fused` ONLY where mlx has a fused kernel (`sdpaForceFusedFor`: hd 256, > 8 rows, q <= k — <= 8 rows is the vector kernel and force_fused THROWS on its gqa wall). Band arm stays ours; Gemma-4 global layers are hd 512 (declined). M5 A/B still owed.
- **MLX sdpa has a WIDTH WALL at hd 256** (vector kernel serves `q_len*gqa <= 32`): dense causal q 6..9 ride `splitCausalSdpa` (split at row 5, byte-identical; `MLX_SERVE_SDPA_SPLIT=0`; +4..9%). The challenge tree's long-KV warmup does NOT transfer (AOT metallib). `--mtp-depth` is a CAP — force verify width via PLD draft-len in A/Bs.
- **Fused decode QK-norm+RoPE** (`fusedQkNormRope`, laguna, default ON, `MLX_SERVE_QK_NORM_ROPE_FUSED=0`): bit-identical; cos/sin from probe rows (`ropeAngleRow`); live paired A/B is the bar.
- **Decode-only dense-attention requant** (`--decode-attn-quant`, default ON, LOSSY): side copies at decode AND spec-verify (same weights); prefill dense; tail layers nvfp4-g16 (`attnDqFor`). A/B per newly-adopted dense arch.
- **MoE decode down-path tail is ONE dispatch** (`gatherQmvDownReduce`, default ON, bit-identical, `MLX_SERVE_MOE_DOWN_REDUCE_FUSED=0`). **lm_head prune is OPT-IN** (`MLX_SERVE_LMHEAD_PRUNE=1`, `ForwardCtx.argmax_only`). Interleaved A/B trick: `presence_penalty: 1e-9` as a per-request lever.
- **Every qwen3.5/3.6/3.8 checkpoint is hd 256** — read the CHECKPOINT before porting an hd-128 kernel.
- **A reference probe with SYNTHETIC dtypes proves the reference's SEMANTICS, not the checkpoint**; dtype-gate any `mlx_array_data_float32` load-time read.
- **KV invalidation** after tool calls + pad-only gens. Sliding-window layers keep the full BUFFER; the VIEW slices to the reachable tail.
- **A sliding layer's view is sized by the whole BLOCK** (`transformer.slidingViewFor`, `window + q_len - 1`, `MLX_SERVE_SLIDING_BLOCK_TRIM=0`): every consumer derives offset RELATIVELY so a trimmed view is correct; the fused hd-256 band kernel (absolute positions) declines the trim keyed on `FUSED256_MIN_Q_LEN`. Guard: `tests/test_sliding_window_trim.sh` + dflash equivalence.
- **Hot-cache restore ALWAYS clamps**: unconditional `truncate(final_len)` — restored offset = MATCHED length.
- **Slice-born weights into gather_qmm/quantized_matmul are `mlx_contiguous`-materialized at load** (`splitPackedGateUp`) — lazy views compute wrong at scale.
- **SSM/hybrid**: init checks `ssm_state.ctx == null` NOT `!initialized`; param-free RMS norm passes `ones()`; Nemotron dt clip = only `time_step_limit`; PLD snapshots need per-FIELD null guards.
- **Spec verify invariant** (all drafters): `cache.step = prompt_len + emitted`, t1 NOT in cache on entry; verify input `[t1, draft…]`; partial-accept correction from ORIGINAL `verify_logits[accepted]`.
- **The token budget is a PRE-COMMIT invariant in EVERY block decoder**: cap accepted drafts before any mutation; the scheduler publishes every block through ONE incremental `+= 1` loop.
- **The batched-decode gate reads DISPATCH, not ARMED flags** (`scheduler.slotTicksRegular` asks `specTickMode` + `gen.spec_disabled_runtime`): a PLD-armed slot that runtime-disabled itself lost batching forever. A solo slot never reaches the batched path, so PLD's re-enable still runs.
- **Spec dispatch discipline**: all four surfaces × stream/non-stream wire `use_*`; MTP default = `server.defaultEnableMtp` ONLY (MoE OFF, `--mtp` flips); engagement COUNTS in tests; priority MTP > dflash > drafter > PLD; logprobs>0 + grammar disable spec; tools disable NOTHING.
- **DFlash is a METHOD, keyed on the CONFIG CONTRACT** (`block_size` + `mask_token_id` + `target_layer_ids`; `--drafter` is the ONE flag): drafts = `block_size−1` from ONE assistant forward, anchor row DROPPED; context = trunk `hidden_states[i+1]` via `capture_layers`; noise embeds = RAW rows; block K/V never cached; rollback = offset-only truncate. Gates consult `lm.dflash` beside `lm.drafter` (scan-pinned).
- **DFlash2 = v1 + selector + convs, keyed on NESTED `dflash_config`, z-lab naming**: encoder `fc`/`hidden_norm` (or `encoder.*`, probed); selector codebooks SUFFIX-LESS bf16 gather tables, NEVER quantized; bare `model_type` "qwen3" so discovery classifies by contract (`peekConfig`). Base_kernel axes `[prepare|finish, tap, channel]`; pos 0 predecessor tap ZERO. `MLX_SERVE_DFLASH_SELECTOR=0` restores v1; pays only at block 8.
- **DSpark = DFlash + Markov chain, config split across two objects** (`dflash.Contract`, `MarkovHead`): `block_size` at ROOT, rest under `dflash_config`; flat `rope_theta`; `rope_is_neox_style:false` = MLX `traditional=true`. Markov head adds `w2(w1[prev])` per position (`MLX_SERVE_DFLASH_MARKOV=0` → 30%→12%); `w1` DENSE; only `vanilla` ported. Row convention = `anchor_row_drafts`.
- **Hybrid forward is capture-capable; a partial accept rolls the CONV state back** (`supportsLayerCapture` includes hybrids; `ssmRollbackFromCapture` takes the verify width). `nextDflash` decides `hybrid_path` separately from `moe_path`: a hybrid's `cache.step` is genuine.
- **DFlash gate's bar is round cost / serial step cost, calibrated on DENSE trunks** (`dflashGateMinimum`, `DFLASH_MOE_GATE_MIN_ACCEPTED_PER_ROUND` 1.8): a sparse target takes an absolute floor after width scaling.
- **The hybrid veto on assistant sidecars belongs to the GEMMA drafter** (`server.archBlocksAssistantSidecar`, ONE predicate across four surfaces) — DSpark on LFM2 otherwise decodes serial with green boot lines.
- **The ngram spec-gate scores the PROMPT; a DFlash drafter is EXEMPT** (its runtime yield gate sees realized acceptance; llmprobe can't send `enable_drafter`). Gemma drafter keeps the gate. Acceptance is a THINKING-MODE property (muse DFlash2 12.5% off vs 47.8% on).
- **A spec sidecar the checkpoint SHIPS is a load-time dependency** (`dflash.resolveInDirDrafter`, `<model_dir>/drafter`): explicit `--drafter` wins, else probed at LOAD; opt-out `--no-drafter`; discovery refuses `*_assistant` standalone. Ship at the width the server quantizes to anyway (8-bit).
- **The spec BLOCK is the verify WIDTH, a MACHINE property** (`dflash.resolveBlockSize` + `wideVerifyLaneAvailable`, `blockCapForMachine` on sysctl brand: M3 Ultra 8, else 5); `--draft-block-size` clamps against CONFIG; yield gate scales calibration by `(block−1)/15`. `DEFAULT_DRAFT_HEAD_BITS` = 0.
- **A prefix-cache restore forwards NO trunk layers, so trunk-derived draft state rides the cache** (`prefix_cache.DflashSnap`, `Entry.mtp`, `restoreSpecSnap`): adopt only on `base + step == matched`; commit dflash only when `absLen() == prompt + generated`; MTP trims to `mtpCommittedLen` and declines a short history. Survives the SSD tier (manifest v4 `spec.safetensors`; spec salvage failure drops only the spec).
- **A reduction helper that has only seen ONE row cannot reveal an axis bug**: sampling/filter helpers that could take a block use the `_axis` op (`applyTopK` NaN'd on `[m, V]`).
- **A spec sidecar's own weights are a per-round READ — precision is a LEVER**: dense bf16 assistants affine-quantized at load (`MLX_SERVE_DFLASH_QUANT_BITS` 8), packed REPLACING dense; indivisible widths stay dense per WEIGHT. Draft head re-encode (`MLX_SERVE_DFLASH_DRAFT_HEAD_BITS`) default OFF. The greedy-equivalence test is the guard.
- **Hand-typed per-silicon spec tables and boot-probe cost ladders were BOTH wrong**: a ladder can't see ACCEPTANCE, a forward is not a ROUND (10x), attention does NOT amortize across verify rows (kv term measured −2.7%). Per-silicon rows stay as COLD-START caps; the measured round-cost table decides.
- **Round cost is MEASURED per model/width/KV bucket from live SINGLE-CHUNK rounds** (`src/round_cost.zig`, `MLX_SERVE_MTP_COST_TABLE=0` = prior only): EMAs of ms AND tokens, solo rounds, transitions dropped, cell counts at 3 samples, stale blends, m_lo 5% hysteresis. EV plan (`MtpCostSource`): linear between measured widths, prior below the anchor, max(slope, prior) past the widest, `clearlyWorse` (≥20%) floors the horizon.
- **Width trials** (`mtpWidthTrialTarget`, 3-round block per period 16+): m_lo, then m_lo+1, periodic m_lo+1 — NEVER m_lo−1. The silicon depth row is a COLD-START cap (`mtp_depth_free`), exceeded only up to the widest TRUSTED width. Build trial plans from a scalar (`.m_hi = plan.m_lo + 1` aliased). `[spec-stats]` carries `width_trials= table=`; both arms OBSERVE, only on READS.
- **Table PERSISTS per (chip, model, quant, OS build)** under `~/.mlx-serve/round-cost/<key>.txt` (`MLX_SERVE_ROUND_COST_PERSIST=0`), restored cells trusted at 0.5 first sample. DFlash/DSpark: `round_cost.WidthChooser` picks block PER ROUND (serial STICKY, never trialled), OPT-IN `MLX_SERVE_DFLASH_CHOOSER=1`.
- **An f32 SCALAR array promotes every bf16 operand it touches** (`mlx_array_new_float` in `scaleTriple`, the PLE gate, the `2·sigmoid` inject closure): one stray scalar widened the whole qwen4 residual stream to f32 — 22.0 → 18.4 ms/forward, prefill 560 → 750 tok/s once fixed. Scalars go through `scalarOf(v, dtype)`; `[dtype-trace] residual widened` is the tell.
- **A decode custom kernel is LATENCY-bound, not op-bound** (qwen4 fused hc read, `hcReadFused` N/D/U, `MLX_SERVE_HC_FUSED=0`): one lane walking 40 dependent loads is ~16–36 us per kernel regardless of work; split-K across simdgroups + compile-time-unrolled loads made the 3-kernel read beat the 11-op chain (18.4 → 16.8 ms). A hot-weight microbench cannot see it — the in-situ `MLX_SERVE_DECODE_FWD_UBENCH` is the meter. The following `hcWrite` is DEFERRED into the next read's N kernel (`HcPending`, flushed before PLE/capture/profiling).
- **`mx.quantize` packs DENSELY: element i at bit offset `i*bits` of the u32 stream, straddling words at 3/5/6 bits** (#305, `NgramTable.dequantRow`): a `32 / bits` per-word reader is right only where bits divides 32, and a 6-bit n-gram pack served noise with fluent text (cos 0.066). Every hand-rolled unpack is tested at EVERY width mx.quantize ships, never just 4.
- **A random read into a cold 32 GB mmap is an SSD fault, serial, and faults on ONE mapping serialize on the VM map lock** (`NgramTable.gather` → `PrefetchPool`, `QWEN4_PLE_PREFETCH=0`): 48 faults/token were 5 ms of every qwen4 decode step; `madvise(WILLNEED)` did nothing on Darwin, 16 fault threads 0.7 ms, 48 fault threads SLOWER; 48 parked `pread` workers 0.45 ms. Decode widths only — prefill rows are page-cache hits and 1024 wake rounds per chunk cost more.
- **The qwen4 residual stream is bf16 like the checkpoint; the f32 fixture is the MATH oracle** (`Transformer.qwen4_stream_f32`, set by the `qwen4 fixture` test, env `QWEN4_STREAM_F32=1`): a tiny random model's logits are chaotic between any two bf16 renderings (0.905 vs f32, 0.89 vs bf16-torch), its streams are not — `qwen4 fixture bf16` pins them at 0.999 against `QWEN4_REF_DTYPE=bf16`.
- **GDN decode is three fused dispatches per layer** (`gdnPreworkFused` S 1..9 incl. old-state conv rows + in-kernel gate/beta, `gdnNormGateFused` swish/sigmoid epilogue, `MLX_SERVE_GDN_DECODE_FUSED=0`): hermetic bit-identity at S 1..9; in-situ 179/180 layer checks exact, ONE k element 1 bf16 ulp (rms reduction tie) — a greedy byte flip from that is legit. qwen4 15.45 → 14.84 ms/forward.
- **A routing-independent chain the GPU already OVERLAPS is not a dispatch to fuse**: shared expert as an 11th gather slot −3% (put ~8 kernels on the critical path), gated shared tail in the down+reduce epilogue null, in_proj fold null, multi-row gather kernels for verify widths S=4 +38% (per-slot simdgroup kernel scales linearly). Measure the marginal in-situ (`MLX_SERVE_DECODE_FWD_UBENCH_S=<rows>` / `_KV=<prefill>`), never a hot-weight microbench.
- **A diagnostic env read with `getenv != null` is ARMED by `=0`** (`run.sh` exported `QWEN4_PROFILE_FWD=0`; every MTP arm ran with the per-block SYNC profiler, verify read 70 ms): diagnostics go through `diagEnvOn` (absent or `0` = off). Real qwen4 MTP round at 8.5k = verify build 5 + eval 25 + draft 0.6 ms.
- **Spec acceptance is a PROMPT-TYPE property; measure it per index before touching a round's cost** (`MLX_SERVE_MTP_FORCE_DEPTH=n` + `acc_idx=` on `[mtp-trace]`): qwen4 code 0.91/0.80/0.68 (= Table 4), prose 0.65/0.37/0.25, a repetitive 8.4k prompt 0.62/0.41/0.22 — on BOTH the 4-bit and 8-bit head packs. Bookkeeping acquitted by the cross-round head test (cos 1.00000). After the verify-row split (08-27): auto code +41%, prose −4%, 8.5k −4%; fixed depth 2 beats auto (8.5k a wash) — the CONTROLLER is the next lever. MTP stays opt-in.
- **The QSA indexer ropes with the SAME M-RoPE table as attention** (`qsaMask` takes `ctx`; `beginMropeChunk` is ONE preamble for forwardMoeWith + forwardQwen4With): queries from the chunk's `mrope_cos/sin_cur`, pooled block keys at 3-D block-START positions (`mropeCosSinAt(stride = ratio)` = the reference's `full_cos[group_starts]`), both `abs + delta` past the table. A scalar rope on image blocks picks different blocks — the layer-3 mask in `qwen4 fixture vision` is EXACT on decisive rows (32 mismatches red). MTP declined on image turns (`specInitWiring` image_request): the head's QSA has no mrope arm.
- **MLX's `use_fallback` has NO fused arm for an hd-256 ARRAY mask** (q > 8 → unfused; q ≤ 8 needs `q·gqa ≤ 32`, i.e. S ≤ 2 at qwen4's gqa 12): every QSA-masked prefill chunk AND verify width 3..15 ran the materialized path (44% of a 4096-row chunk). Prefill = `fusedSdpa256Masked` (mask arm of `msv_attn_p256` + per-tile any-visible skip table, `MLX_SERVE_QSA_FUSED=0`); verify = `splitMaskedSdpa256` (vector-kernel row groups, `MLX_SERVE_SDPA_SPLIT=0`). Measured qwen4 4-bit M4 Max prefill: 8k ~1%, 25k +2.7% (40.9 vs 42.0 s) — the 64-row tile union skips few blocks; the win is fusion, not sparsity. Meters: `QWEN4_STANDIN=attn_qsa,attn_sdpa`, `[qsa-fused] engaged`, `[sdpa-split] masked arm engaged`.
- **A per-arch exclusion as a hand-rolled conjunct is a list of ONE**: both sides read `Transformer.ownsModuleDecodeState()` + `scheduler.specInitWiring`; class scan pins every `?*<x>_mod.<Y>` field. Exclusion is per-arch CAPABILITY (`moduleStateSpecRollback`), pinned via error RATIO never bit equality.
- **"Module-owned" is a property of the STATE, not the pointer field** (`module_owned_state_fields` dsv4 vs `module_shared_readonly_fields` qwen4): a read-only module (hash + mmap) admits and batches freely; only the slot driving a module-owned MTP head is exclusive (`slotExclusiveDecode`). Spec wiring keys on `moduleSpecWiring` (both lists) so a shared arch never inherits PLD by default.
- **A decode kernel keyed on `batch*seq == 1` declines every verify row AND every batched slot** (qwen4 hc read/write, GDN prework/norm-gate): rows are independent, the grid carries them (`HC_FUSED_MAX_ROWS`/`GDN_FUSED_MAX_ROWS` 16), bit-identical per row to the N=1 kernel. S=2 forward 24.4→22.4 ms; prose 2/4-stream aggregate 1.14x→1.28x / 1.69x→1.81x. A per-row `gatherQmv` loop for the MoE at S>1 was flat at S=2 and −11% at S=4 — the sort path stays.
- **A qwen4 verify row is BYTES, not dispatches** (S=1 16.0 ms, S=2 22.4, S=4 30.6; `MLX_SERVE_VERIFY_QMM=0` halves the op count and moves nothing): a second row's own experts are read. A depth-2 round = 2.05 serial forwards, break-even >1.05 accepted/round, prose sits at 1.0 — the controller already plans m=2 (`m_avg=2.00`), auto ≈ fixed-2, and MTP stays opt-in. The persisted round-cost table is keyed on (chip, model, quant, OS build), NOT the engine build: a cell measured before a verify-path change keeps its number until a trial lands on it.
- **`--no-mtp` must gate an IN-CHECKPOINT head too** (qwen4's layer loads with the trunk; `entry.mtp` bound it regardless and the `--no-mtp` baseline of `test_mtp_equivalence.sh` ran MTP rounds): `entry.mtp` reads `params.mtp_enabled`, `slotExclusiveDecode` keys on the ENTRY's head. The script accepts `[qwen4] MTP head loaded` + the `mtp.fc_hidden.weight` marker; `MTP_FORCE_ENABLE=1` on MoE.
- **The qwen4 MTP head rides the slot's M-RoPE table on image turns** (`qwen4MtpForward(…, mrope_ctx)`: `beginMropeChunk` at its ABSOLUTE `pos_base + seq_offset`, scalar `offset + delta` past the table — the trunk's two arms; its history spans the image rows, so the delta alone was never enough). The `image_request` decline is gone; bar = `test_qwen4_exp.sh` [7b].
- **A GDN trunk's `KVCache.step` is 0 forever** (it advances on layer 0, a linear layer): the batched tick roped every qwen3_5 decode token at position 0 (diverged from serial at token 14). Batched rope offsets read the slot's `moe_seq_offset`, which the batched tick now ADVANCES (the forward only moves its scratch). Bar: `tests/test_batched_equivalence.sh` forced-N=1 byte-identical; the N=2 arm acquits near-ties (≤0.15 nats).
- **A module-owned forward that ignores `ctx.capture_hidden` hands spec an EMPTY hidden**: publish last-row + all-rows via `skip_lm_head`. **A guard that shapes INIT options does not bind DISPATCH**: tick dispatch through `specTickMode`; `nextPld` self-declines on `!pld_enabled`.
- **A host read inside a layer loop is a GPU BARRIER**: defer non-consumed reads into ONE batched eval (`chunkCrossesBoundary` is pure arithmetic). **A rollback that re-forwards is a SECOND forward**: capture only what verify overwrites (`DsparkAnchors`), truncate the rest by offset.
- **A lever's DEFAULT belongs to the engine that MEASURED it** (DSpark confidence gate OFF here); allocator-cache clears tuned for prefill are wrong for spec widths; profile laps honest only where the path already syncs.
- **DSpark serves SAMPLED requests via MTP one-hot acceptance** (default ON, `MLX_SERVE_DSV4_DSPARK_STOCH=0`); penalties/grammar/logprobs serial; greedy byte-pinned.
- **MTP head**: delta-encoded norms AUTO-FOLD at load; every weight re-solves quant PER WEIGHT; greedy drafts via DRAFT-RERANK (2-bit coarse → `draftTop32` → trunk head re-scores; `MLX_SERVE_MTP_DRAFT_RERANK=0`); verification always trunk head; ONE bounded sync per stochastic round.
- **A sidecar carries weights and NO config, so its quant MODE is solved from GEOMETRY** (`transformer.quantParamsFromGeometry`): affine is the only mode shipping per-group biases, so `loadLinear` takes `.biases` optionally and `qLinearFwd` passes `mode.cstr()`, never a literal `"affine"`. Demanding the triple killed every nvfp4/mxfp4/mxfp8 head at load (`missing tensor: mtp.fc.biases`) and silently disabled MTP.
- **A dense bf16 MTP head trunk is requantized at load** (`mtp.loadTrunkLinear`, `MLX_SERVE_MTP_HEAD_QUANT_BITS` 4/g64; fc stays bf16 for m5Nax): +10% decode. `test_mtp_equivalence.sh` acquits divergences at serial top-2 gap ≤ 0.15 nats and boots `--prefix-cache-entries 0`.
- **The head's `fc` can ship QUANTIZED** (`MtpModel.fc` is a `QLinear`; `fcMatchesHidden`/`affineParamsFromGeometry` solve dims from packed geometry; axis 0 quantized, axis 1 dense). m5Nax profile still demands bf16 fc → falls to `.generic`.
- **A norm-repair heuristic keyed only on an ANCHOR GAP convicts correct packs** (`mtpNormNeedsRepair` also reads the norm's OWN negative fraction; whole-head 5% bar is NOT per-tensor). Alis's correct post_attn got a second +1 → acceptance 86%→33%.
- **No KV snapshots across verify**: rollback = scalar anchors + per-position SSM capture + offset-only `truncate`. **A multi-token forward is not a prefill**: `prefillEvalCadenceApplies` (seq ≥ 32) exempts verify/canvas/batched forwards.
- **INT4 long-greedy divergence is legit**; byte-stable greedy ⇒ no spec + `--kv-quant off/8`. **A prefix-cache HIT is not bit-identical on a HYBRID** (≤0.047 nats) ⇒ `--prefix-cache-entries 0` for byte-stable.
- **Quantized-KV packed reads are kernel-or-DENSE, per WIDTH** (`kvAttnFusedEligible` t_q==1 → `qkvAttnDecodeKernel`; `kvAttnVerifyEligible` t_q 2..8 → `qkvAttnVerifyKernel`; else dense — the composed packed-qmm chain never serves, scan-pinned). Verify kernel OFF on G17 only (`MLX_SERVE_KV_ATTN_VERIFY=1|0`); floor 2048. Engagement + `Tq` asserted per arm in `tests/test_kv_quant_fused_equivalence.sh`.
- **A parity loop asserts FINITENESS before it diffs**; additive masks via `mlx_where`. **Threadgroup memory is an OCCUPANCY decision**: tg bytes ≤ ~10 KiB (`qkvDecBlockFor`).
- **kv-quant contract**: attention always reads `KVCache.denseView` on EVERY path (batched included); schemes extend via enum + two switch arms; `HotEntry` records its scheme; quant snapshots carry 6 handles.
- **Hybrid-SSM prefix cache retains ~3.4× what it reports**: lever is ENTRY COUNT (`ramCappedPrefixCacheEntries`). **SSM-checkpoint stride never sub-divides the chunk** (`effectiveSsmCheckpointStride`); dense hd-256 chunk cap 8192, MoE 4096.
- **The always-on SSM snapshot sits `SSM_SNAPSHOT_BACKOFF` (30) tokens BEFORE prompt end** (a snapshot AT the end is unreachable next turn — the llmprobe cache-hit mechanism).
- **hd-256 prefill kernel (`msv_attn_p256`)**: band always fused; plain causal default-on via kv-chunk budget; q_len < 16 ALWAYS declined; guards bill through `prefillHeadDimFused`.
- **A load-time constant table in the WRONG DTYPE silently widens every read** (Laguna mscale f32 = 3.03x): `constTableAs` hands tables back in the activation dtype; bf16 IS the reference on bf16-calibrated checkpoints; bisect with `diagProjBench`. Scan-guarded.
- **Every forward path carries a `[dtype-trace]`**; 1-D f16 tables narrowed at load (`narrowsLoadedF16`, `MLX_SERVE_F16_NARROW_1D=0`); multi-D dense f16 stays. Big models need ~45 s settle between boots.
- **A 3x forward fix invalidates every ratio calibrated against the slow forward**: re-measure KV dtype, gather policies, spec warmups (`SPEC_YIELD_WARMUP` 8). **A one-run baseline makes every diff a coin flip**: per-cell MEDIANS; `MLX_SERVE_DECODE_PROFILE` is NOT a sizing tool.
- **A weight-layout fusion is not output-preserving** (changes which KERNEL runs); fused QKV measured null → opt-in. **A decode fusion pays only if it removes launches the GPU was NOT overlapping** (`fusedAddRmsNorm` negative). **A fusion must shorten the DEPENDENCY CHAIN** (`MLX_SERVE_DISPATCH_PROBE=N` ~1.5–1.7 µs/dispatch).
- **Reproducing an MLX op means reproducing its REDUCTION TREE and ACCUMULATOR**; `mlx_compile` on the same math is NOT output-preserving. **JIT vs metallib transcendentals disagree; a 16-bit domain is swept ENTIRELY** (`swigluSigTable`); f32 DECLINED.
- **Fusing gate+up flipped `gatherQmv` into the decode default** (`gatherQmvGateUp`): eligibility = the FUSED KERNEL'S OWN conditions (`useGatherQmvDecode`), never a model_type list.
- **A `metal_kernel` config rebuilt per call is CPU tax — cache keyed by FULL SHAPE** (`ShapeKey`; element-count key collided and killed the server). **A bandwidth bench smaller than a real step measures CACHE.**
- **A lever that pays in another harness may pay for a constraint we don't have** (intra-step async-eval: negative, OFF). **"Duplicate and read the marginal" is unsound for anything that MUTATES state.**
- **Prefill perf kernels A/B PER ARCH before default-on** (`gdnBlockedEligible`, `prefillDqGemm`); fused-causal hd-256 archs cap auto chunk at `min(base, 4096)`.
- **MTP spec levers all kill-switched, each bit-identical to off**: greedy drafts; early dispatch + pre-draft; oQ raw-HF head norms get the `+1` repair; EV gates fed by REALIZED rates.
- **Verify-qmm plain-SIMD tiles are BITS-templated; SHAPE-gating is the win** (`mixedPlainShapeEnabled`, default 6-bit gs64 `N >= 5120`, `MLX_SERVE_VERIFY_QMM_MIXED_PLAIN=0|1`): 1.10–1.22x on 27B 6-bit; every-shape-on only 1.03x (1024-wide K/V lose). 8-bit/5-bit default OFF. Hoist the WORDS, not the pointer. `BITS` needs no cache-key change.
- **A verify lane is never byte-identical to stock** — bars are fp32-dequant parity per width + bit-identity of 4-bit ACROSS a refactor; reverse CELL order per pass (first cell reads ~2% fast).
- **Parity bars never reference stock's WORST element; cosine can't stand in for per-element** (`VerifyQmmParity`): ABSOLUTE gross ceiling 0.5 + RMS ratio vs stock 3.0x + cosine. `MLX_SERVE_VQMM_PARITY_DEBUG=1`.
- **Per-silicon parity SLACK and MTP depth CAP are machine measurements keyed on `ane.chipBrandString`** (`verifyQmmParitySlack`, `mtp.adaptiveDepthCapForMachine`; M1 Pro rows 0.04 / 4; never interpolated). A row CARRIES ITS LABEL in the log (`[mtp] adaptive depth cap 4 (m1-pro row, default 6)`).
- **verifyQmm lanes** (`vqmmLaneFor`): split-K M 2–7 / wide tile N≥100K / NAX m16 M 8–16 (q8: M 7–16; G17 + macOS≥26.2) / crossrow OPT-IN (`MLX_SERVE_VERIFY_QMM_CROSSROW=1`, measured −4%). NAX mixed-width q5/q6/q8 g64 N≥5120 (`MLX_SERVE_VERIFY_QMM_NAX_MIXED=0`); q8 needs its own unpack arm — never let a templated `else` inherit q6. Codegen NAMED SCALARS; M=8 is the plain-SIMD cliff.
- **EV cost tables are refit whenever the verify forward changes** (`MTP_EV_DEFAULT_COSTS` doc comment: method + numbers; refit #4 flat_max 4, hi 0.26, k≥7 cliff 0.62). `MLX_SERVE_MTP_EV_COSTS` ZEROES the third region. GDN rollback capture <1% of round time.
- **An MTP sidecar never names the target round-cost surface**: `MtpCostProfile` from the full runtime fingerprint (arch + storage + sidecar + draft head + live NAX); unmeasured = generic/cap-6.
- **qwen4 has its own measured G17 surface** (`g17_nax_qwen4_q4_gs64`, M5 Max 2026-08-27): a verify row is bytes at EVERY width — marginals .28-.34/pos, 3-5x the dense sidecars, no NAX flattening (routed experts never ride the vqmm lane) — so its cap KEEPS the default 6: the NAX cap 8 is for surfaces that flatten. `MLX_SERVE_MTP_QWEN4_PROFILE=0` revokes the profile ONLY (compute untouched) — the cost-surface A/B arm.
- **A cost-profile A/B with the persisted round-cost table live measures the TABLE, not the profile** (`MtpCostSource` hands planning to a matured bucket): `MLX_SERVE_ROUND_COST_PERSIST=0` on BOTH arms. The within-boot serial cell is the drift meter — cross-boot MTP cells compare only as MTP/serial ratios, counterbalanced A-B/B-A (same-evening boots drifted 5-12% on profile-independent serials).
- **The grouped-expert NAX tile is a measured LOSS at verify widths** (~−9% on the forward, two kernel variants; M5 plan item 2): runs are 1-2 rows so m16 amortization never engages, and SORTED gather_qmm already sits near the expert-byte floor (~1.7 ms/row). Nothing ships — kernel + parity test live at `holtsway/mlx-serve@feat/qwen4-moe-verify-nax`; the bytes are irreducible without cross-row expert overlap.
- **Reproducing an upstream NO-OP faithfully means NOT doing it** (Kokoro SineGen): measure the reference's own seed-to-seed self-similarity first.
- **Kokoro traps**: safetensors READ on CPU stream; depthwise `ConvTranspose1d` transposes `{0,2,1}`; complex via `mlx_array_new_complex`; `AdaIN1d` is pure instance norm; vocab includes uppercase ASCII as diphthongs — every emitted symbol must be IN the vocab (`Vocab.encode` DROPS unknowns).
- **Kernel testing**: GPU parity = no-worse-than fp32 ground truth, never kernel-vs-kernel; same-boot A/Bs are the bar; eligibility predicates adopt every matching shape — A/B each; never fit spec constants without checking realized `m_avg`.
- **A sparse-attention PATTERN that isolates a spatial neighbourhood cannot be tuned into correctness** (H3, DEAD): CLIPS decide; H3 DiT is at roofline — wall clock moves only by FEWER forwards.
- **MoE PREFILL without the global sort re-reads each expert bank per TOKEN**: new MoE prefill uses `_gather_sort` (decode C==1 unsorted). **dsv4 decode glue is FUSED** (`dsv4_emit_win`, `dsv4_dec_chain` +8.6%; exact `frexp`/`ldexp`).
- **A cache that hands out BORROWED handles evicts LRU, never a fixed slot** (`vqmmScalarEvictIndex`; SIGBUS). Whole conformance surfaces 0/N = DEAD PROCESS, not a bad model.
- **A `metal_kernel` helper owes its own `streamIsGpu` guard; a per-token-varying TEMPLATE value is a fresh JIT per value** — ramping values ride INPUTS.
- **When an A/B's arms fork CONTENT, per-prompt spec cells are variance**: sample across prompts, 3+ REPS at temperature; a prompt NONCE varies by REP, never by arm. Sampled drafts with the full Leviathan ratio measured a LOSS — `p(argmax q)` beats `1 − TV`.
- **dsv4 comp_in int8 requant is EXPLICIT-opt-in** (`decodeAttnQuantExplicit`) — a shared lossy flag's default is not transferable between archs. **MoE `gather_qmm` costs O(EXPERT-BANK)** — superseded; stock gather default everywhere since the mscale fix.
- **A kernel's dtype and its threadgroup BLOCK SIZE are ONE decision** (`gdnBlockTFor`; dtypes off the ARRAY; Metal has no implicit float→bfloat).
- **Every KV buffer is sized from the OPERAND it stores; a scheme with a shape constraint refuses at LOAD**: K/V widths differ on MLA; TurboQuant needs pow2 → `initWithConfigAndHeadDim` checks `kvCacheKeyHeadDim()`.
- **A fallible re-init BEHIND a `deinit` leaves a freed object on the error path** — build first, then swap (`KVCache.reinit`). Scan-pinned: no `.cache = try` in transformer/scheduler/main.
- **An MLA cache is billed per ATTENTION head** (`kvBytesPerToken`): the latent decompresses to all heads; only an asymmetric config tells the spellings apart.
- **A gate's LOWER BOUND may REPLACE the formula, not clamp it** (KDA: `g = exp(bound·σ(…))`); two arms means the arm is SELECTED (`kdaUsesBoundedGate`): absent bound = softplus arm, never bound 0. Read the reference KERNEL's arms.
- **Per-head vs per-channel gating is an INDEXING contract**: one recurrence generated for both (`gdnKernelSource(vectorized, capture_seq)`); other-shape kernels DECLINE; test = per-channel gate held UNIFORM must match the scalar kernel EXACTLY.
- **A struct inside a generic fn that captures NO comptime param is memoized to ONE type** — capture the params (`const is_vectorized = vectorized;`).
- **A custom kernel's signature comes from each input's ACTUAL dtype; <8-element arrays land in `constant`**: index raw buffer names; every new kernel ships a one-shot "engaged" log + parity on the LIVE dtype.
- **A helper that materializes a VIEW owns the view** (`sliceContig`, one per file). RSS is blind to Metal — `/props` `active_bytes` + vmmap. **A weights MAP outliving the model pins every buffer**: scope inside `Model.load`. **mlx-c iterator refs**: `iterator_next` hands a +1.
- **Allocator-cache growth**: `mlx_clear_cache()` once per CHUNK and per emitted block; the clear must be un-skippable (#110): `server.applyMlxCacheLimit` ONCE at top of `main()`; cadence INTERVAL-based (`step -| last_clear >= 256`, never `%`); `Generator.advanceStep` is the one step mover (scan-pinned); `finishSlot` clears the tail.
- **KV growth is PROPORTIONAL** (`nextCapacity` +25%, floored one chunk, capped 8192). **`active` flat while phys climbs = the POOL**: `/props` `memory.cache_bytes`, `mlx_serve:mlx_cache_bytes`, tray `(+N GB cache)`.
- **Special-token splitting only in `Tokenizer.encode`** (first-byte buckets) — per-special rescan is O(specials×text), shipped TWICE. Any per-position loop over a vocab-derived collection needs an index.
- **mlx `Copy`/`contiguous` are VIEW ops**: a slice OUTLIVING its parent goes through `materializedOwnedCopy` + eval. `mlx_save_safetensors` appends ".safetensors". **A raw data-pointer read must PROVE row-major contiguity** (post-eval strides; FLAT RESHAPE).
- **mlx-c upgrades**: diff `lib/mlxc-src/mlx/c/*.h` vs `src/mlx.zig`. ds4 bumps: re-sync `ds4_ffi.zig` EngineOptions + mirror CORE_OBJS in build.zig. **NAX presence asserted, never assumed.** **`mlx_array_new_data` COPIES shape-worth of bytes** — a smaller buffer is latent UB.
- **A model whose per-request state lives OUTSIDE the KVCache is excluded from prefix-cache reuse AND owed a `Transformer.deinit` arm** (dsv4 leaked 108 GB). **`openDirAbsolute` on an empty/relative path is ReleaseFast UB that miscompiles the CALLER** — guard every site.
- **ANE compile STAGING must stay in $TMPDIR** (aned can't read home paths); cache entries in `~/.mlx-serve/ane-cache`. **A single deep-K ANE conv is a 2.6x cliff**: K-chunk past ~4.6k inputs in-graph; ONE compiled tile width (`denseMLPMaybeAne`), tails run GPU; `msv_ane_model_eval` returns 1 on SUCCESS.
- **fp16 plane row pitch must be ≡ 0 mod 32 — off-grid tiles compile fine then fail EVERY eval** ("Program Inference error"; the v2 share-0.35 collapse). `aneShareRows` floors to 32; both C emitters refuse by name.
- **The ANE tile is sized by `effectivePrefillChunk`, never the pin alone** (scan-pinned) — a bare-pin tile is built-but-never-dispatched. **ANE prefill is M4-AND-BELOW** (`anePrefillAllowed`; M5 Max measured a LOSS; `MLX_SERVE_ANE_FORCE=1`).
- **Channel mode is the default split** (`MLX_SERVE_ANE_MODE`, shares channel 0.45 / row 0.40): output-channel slices at ffn'=k, GPU rest via axis-0 packed VIEWS (bit-exact), boundaries on `CHANNEL_ALIGN` 128, down partials ADD at the seam. A failed channel eval recomputes the WHOLE layer.
- **ANE I/O planes shared per SHAPE CLASS within a unit** (evals serial; 3 surfaces replace 224; never memset at bind). **MoE gets GDN-ONLY coverage** (shared expert ~5% ceiling; +3.9% at 16k).
- **An ANE build under 1 GiB internal free disk is REFUSED** (`BUILD_DISK_FLOOR_BYTES`, probe `/private/tmp`). **Compile cache has LRU pruning** (`MLX_SERVE_ANE_CACHE_CAP_GB` 40) — a full disk ships SILENT PARTIAL COVERAGE (`ready: N/M` under-count is the tell).
- **A compile session's budget is the INTERNAL free disk at boot (~260 MB/program); in-session retries are futile** — a cold build CONVERGES across boots (converge-then-measure).
- **A killed ANE server leaks staging (8–20 GB/boot); the framework OWNS `$TMPDIR/<identifier>`**: `msv-ane.pid` markers, first create reaps dead owners; deleting a LIVE program's staging is unsafe (only weights blob + MIL text deletable post-load). Guard: `tests/test_ane_prefill.sh`.
- **fp16 planes are 1-ulp different from f32; byte-identity is not the ANE bar** — per-program cos/rms parity + perceived-content greedy equivalence.
- **GDN input projections ride ONE fused ANE conv** (`msv_ane_gdn_create`: qkv + z stacked; `gdnProjMaybeAne` seam; combined-proj archs stay GPU; `MLX_SERVE_ANE_GDN=0`); EACH seam logs its own engagement line.
- **int4/sub-byte ANE weights are a NO-GO on macOS 26.x aned** (bisected) — ANE copy stays int8, billed per model (`engineBillBytes` + `gateAllows`, headroom 12 GB). Probe convs need ROWS≥32.
- **A pinned second ANE is DEFAULT on M3 Ultra (+7.1%), opt-in elsewhere; a SILENTLY ignored affinity hint is undetectable in-process** (`MLX_SERVE_ANE_DUAL`): `kANEFAneInstanceHint` AND `kANEFProcedureVariantHint` together at compile+load+eval; instance 0 keeps `@{}`; verify with `macpow --dump | grep ANE0_` (both counters move). Per-unit planes; input packed once then memcpy'd.
- **The ANE program is a procedure BANK (runtime caps ~121 handles)**: symbol indices READ from `procedureInfoForProcedureIndex:` on the `_ANEModel` behind `-model` (NOT `inputSymbolIndicesForProcedureIndex:` — answers 0; identity indices fail every procedure >0 as a swallowed 23% "slowdown"; `eval_failures` is the tell). Cap `MLX_SERVE_ANE_BANK_MAX_BYTES` 2 GiB, halves down a ladder; never a coverage decision.
- **The ANE split's optimum is per SILICON** (`ane.defaultShare`; M4 channel 0.45 → 311/304, 0.50 regresses; M3 Ultra 0.45 ≈ nothing, 0.35 +8.5%/+13.7%). A share change needs its own A/B, never interpolation. fp16 down-conv wears the (1/16..x16) pow2 wrap.

### Model loading, configs, converters, media parity (→ docs/gotchas/models-media.md)

- **A qwen4_exp checkpoint is NOT a qwen3_5 pack** (model_type `qwen4_exp`): three new blocks (hyper-connections, n-gram PLE, QSA) around the qwen3_5 trunk. The HF fixture's `hidden_states[i]` is the INPUT of layer i (stream_0 = tiled embeddings) — compare layer-i output with stream_{i+1}.
- **A random tiny MoE oracle ties everywhere, and the tie RATE is scale-invariant** (8 experts x 8 layers x T rows; relu leaves EXACT-ZERO block scores): our kernels' ~1% bf16-class noise flips one top-k expert or QSA block per prompt whatever the router scale or seed. `dump_qwen4_exp_fixtures.py` runs every expert (`num_experts_per_tok = num_experts`, mapping + weighting still measured) and dumps the reference's OWN margins (`qsa_gap`, `logit_margin`); `Qwen4Ties` acquits rows under 3% by those numbers, never by our output, and the chunked arm's bar is our own full forward.
- **A k < E tiny fixture ties in most trunk rows across every MoE layer, so its top-k SELECTION coverage is the MTP head's ONE MoE layer** (`dump_qwen4_exp_fixtures.py build --topk 2`; dump reads k back from the checkpoint; `route_gap`/`mtp_route_gap` = softmax margin at the top-k boundary, `QWEN4_TIE_REL_ROUTE` 5% — a row flipped at 3.5%; trunk decisive floors drop only when a routing table is present). The k = E fixture stays the CI one.
- **qwen4_exp splices vision rows BEFORE the hyper-connection tile** (`forwardQwen4With`: `applyVisionEmbeddingsWith` on the `hidden`-wide embeddings, THEN `mlx_tile` — the reference masked_scatters, then repeats); a splice after the tile meets 4x-wide rows. Tower + preprocessor + M-RoPE table are Qwen3-VL's (`config.qwen_vision`, `parseQwenVisionFields` shared with qwen3_5); the pack gets the tower via `--add-vision` (one HF shard, bf16). Guard: `qwen4 fixture vision`.
- **QSA's always-visible tail is PER QUERY** (tokens at/after `ratio·floor((p+1)/ratio)`), never `kv % ratio`; scores in f32 like the reference; `torch.topk` keeps the LOWER block index on the exact-zero ties relu leaves — the mask carries an index-descending 1e-7 bias. The n-gram hash's eos is the TEXT config's (`ngram_eos`), not the generation stop set.
- **On qwen4_exp the spec "hidden" IS the pre-mixer stream** (`capture_hidden(_all)` = `[B,L,hc*hidden]`, never the mixed 2560): the MTP head fuses `fc_hidden(rms_{4H}(stream)) + fc_embedding(rms_H(embed))` (vLLM #53896 / SGLang #36497; HF ignores `mtp.*`). Head row r = (stream at r, token r+1) at query position r+1 — QSA takes a `pos_base` because its cache starts at position 1.
- **Per-request state outside conv/ssm rides `SSMCacheEntry.aux_state` + `ple_prev`** (PLE dilated-conv window, QSA raw-key history); every reset/free site nulls it (`resetCache` left a dangling handle → SIGBUS in warmup). `SSMCacheEntrySnapshot` carries `aux_state`/`qsa_pooled`/`qsa_ratio`/`ple_prev` (disk keys `l{d}.aux/pooled/ple`, meta `qsa_ratio`), so the prefix cache is ON; the qwen4 MTP head is NOT KV-only (`MtpCacheRef.kv()` → null: aux keys + pooled blocks + row count) so the spec-snap machinery neither commits nor restores it — a KV-only restore left stale rows under a fresh aux state; the restore-parity bar on this arch is the CHUNKING class (cold chunk-1024 vs chunk-4096 differ by 0.34 nats top-5; restored-vs-cold 0.14–0.30), not the hybrid 0.047.
- **ACE-Step's timbre slot is the silence latent OR the reference clip's VAE mean, never both** (`MusicRequest.ref_audio`, #259): `refer_audio_acoustic_hidden_states_packed` is the same slot text2music fills with silence, so reference audio is a conditioning SWAP, not a branch — `acestep.referenceWindow` makes every clip 30 s (= `timbre_fix_frame` 750; tile short, start-of-each-third long, deterministic where the reference randomizes) and `refTimbreLatents` encodes the MEAN. Guards assert the `[acestep] reference audio:` line, never output.
- **An ACE-Step TASK is the DiT's context stream plus the instruction line — same forward** (`MusicRequest.task`): `complete` (vocal2bgm) = the source clip's raw VAE latent as `[src|ones]` + "Complete the input track with A | B:"; `cover` = the latent round-tripped through pooler → FSQ (soft clamp `c·tanh(z/c)`, c = L/(L−1), THEN the preserve-symmetry hard-clamp floor grid — neither alone; read a quantizer's flags off an INSTANCE) → detokenizer + "Generate audio semantic tokens…"; the source's length IS the track length. FSQ weights ship as a SEPARATE, DENSE-bf16 `fsq.safetensors` (an 8-bit pooler flips near-tie codes: cover e2e vs reference 0.64 → 0.985 dense; lazy `ensureFsq`, stat-probed so the app can drop it into a resident pack); the app fetches it into pre-cover packs (TEMPORARY migration). Guards: oracles 8-11 (indices EXACT, cover e2e ≥ 0.95), `[acestep] cover:`/`complete:` lines.
- **A standalone-frame latent belongs in latent slot 0 ONLY** (#260): the causal VAE encodes one image as a 1-pixel-frame latent; dropped into slot `F_lat-1` it decodes as 8 frames of noise. First anchor REPLACES frame 0 (`VideoConditionByLatentIndex`); last anchor is APPENDED as `HW` clean tokens at pixel position `num_frames-1`, one frame wide (`VideoConditionByKeyframeIndex`), trimmed before decode (`KeyframeCond.trim`). `keyframeMask` + `keyframePositions` are the ONE place; `last_frame_image` stays NOT graceful (named 400s).
- **A capability the reference API exposes as a FLAG may reach the checkpoint only as TEXT** (`instrumental`, both music backends → `[Instrumental]` section tag). Flag + non-empty lyrics = NAMED 400 (`gen.instrumentalConflicts`; whitespace-only = absent). Guards assert `instrumental=true` IN THE LOG.
- **An adapter's alpha lives wherever its exporter put it** (`lora.fileAlphaScale`: nested-PEFT > flat > `ss_*` > none; per-module tensor WINS; non-finite DECLINED). A LoRA test comparing only BYTES can't tell adapter from noise — `tests/lora_noise.py` + `tests/test_real_loras.sh`.
- **A condition stream is assembled in SEGMENT order by ONE resolver** (H3 `resolveRefs`; `refBlockFor` DERIVED). **Two partitions with IDENTICAL files need a DECLARED discriminator** (H3 `tasks` → `h3ConfigDeclaresRef2va`; server 400s, app gates the FIELDS).
- **A tiled decoder's tiles are not slices of an untiled pass when positions normalize over the EXTENT**: `fitsSingleTile` → `error.TilingUnsupported`; temporal chunking is semantic (17-frame clips).
- **A COSINE parity test cannot see a SCALE error**: anything CONCATENATED asserts `rms_ratio` too. A faithful port can make the checkpoint WORSE (H3 `<Picture i>` splice default-off fl2va). **An oracle that cannot execute the reference must say so.** **A permutation-invariant checksum cannot see a permutation** (`pos_weighted`).
- **A distilled FEW-STEP diffusion model can REQUIRE bf16** (MageFlow Turbo): RoPE rotates f32 then casts; timestep + freq table bf16-ROUNDED (`roundBf16`); `scalarLike`. Debug the LOOP per-step in the native dtype.
- **A reference-image editor must not change its input's geometry**: `resolveEditTargetSize` = `fitAspect` → `fitWithinCap`; `encodeEdit` asserts pad count == merged vision rows.
- **A gather-read table may be quantized only where the READER has a quantized-gather path**: media backends read with dense `mlx_take_axis` → `NEVER_QUANTIZE` lists; LM `embed_tokens` via `gatherQuantizedRows` is a SIZE decision (measured within ±2se; our packs quantize it, `tests/restandardize_qwen38_pack.py`).
- **Publish MTP head norms FOLDED** (`--fold-mtp-norms`): same bytes at serve time as delta + load-fold; drops reliance on detection heuristics. A pack whose BACKBONE norms move the anchor (Alis) is what breaks oMLX, not the convention.
- **A chain that returns f32 BY DESIGN makes the CALLER own the activation dtype** (`groupLimitedRouting`): forgetting promotes the residual at the first MoE layer — ONE `[dtype-trace] residual widened` line is the symptom.
- **Calibrated quantization**: channel weights PER-INPUT-CHANNEL and PER-EXPERT (imatrix value IS the weight); bit width beats group granularity ≤3 bits; round (s,b) to the STORED dtype before q; self-test against a weight-BLIND arm.
- **A sampler never draws a RESERVED special; the legit set is DERIVED** (`tokenizer.reservedOutputIds` → `installSuppressMask`, `MLX_SERVE_SUPPRESS_RESERVED=0`); logprobs stay RAW.
- **A hand-rolled pretokenizer is calibrated to ONE tokenizer.json — digit GROUPING is per-model** (`Tokenizer.digit_group`); mis-grouping presents as a QUALITY bug; cross-check `/tokenize` vs HF at bring-up. **A COMBINED Split regex hides the digit rule** (Llama-3 pattern → `pretok_style = .llama3`); pin against raw regex findall.
- **The degenerate-tail loop guard needs a LONG-period tier**: periods 9..64 at 10 reps (`isDegenerateTailLoopRange`).
- **An imatrix is only valid for the WEIGHTS it was collected on**; byte-identical shard reuse is enforced by COPYING. **Uniform ≤2-bit experts to the LAST layer cause TURN-LEVEL agent loops** — low-bit gates need an AGENT-LOOP cell; 4-bit tail experts fix it.
- **A configless repo SHAPE must be taught to every "is this a model?" answer**: ONE predicate (`model_discovery.peekMageFlowIndex`; klein keyed on a WEIGHT NAME); `gen.peekModelType` DELEGATES.
- **A directory filter that skips `.sym_link` measures an HF-cache model at ZERO**: every size sum stats THROUGH symlinks (`statFile` follows, post-stat `.file` check). Sites: `scheduler.modelDiskBytes`, discovery, `gen.sumSafetensorsIn`, cli.
- **An incomplete media pack is invisible until its marker lands** (`model_discovery.requiredMediaMarker`, `gen.requiredMarkerFor` delegates): discovery skips, `probeModelDir` refuses `error.IncompleteMediaPack`, preload refuses by name; app Turbo fetch downloads INTO the pack dir. Guards: `tests/test_model_rescan.sh`, `TurboLoraFetchTargetTests`.
- **A family's geometry comes from the CHECKPOINT once a second size exists** (FLUX.2 klein 4B/9B): derive from SHAPES; TE heads from `q_proj` ROWS; fused gate+up = rows÷2.
- **An engine that hardcodes its pack's quant WIDTH can only load that pack** (ltx `bits=4` literal killed the 8-bit pack): every site solves via `affineParamsFromGeometry` (`quantGeom`). Scan-pinned: no literal width at any `mlx_quantized_matmul`/`mlx_dequantize` site.
- **4-bit affine on a video DiT is a QUALITY setting** (LTX-2.5: ~9.9% noise/linear vs 0.6% at 8-bit, 3.5% time cost). Mirror `ddalcu/LTX-2.5-MLX-Serve-8bit`; `scripts/quantize_ltx25.py --bits 8`.
- **A module's constructor arguments are not its config** (LTX DiffVAE: class defaults run the weights into STATIC; measured 1-step x0 at t×1000). `ltx_diffvae_forward.Sampler` pins all three behind kill switches; faithful-port parity CANNOT see this.
- **The DiffVAE's tile budget is a per-REQUEST memory decision** (`tileTokensForMemory` from free RAM at decode; stages 1-3 full volume, stage 4 + diffusion tile with trapezoid ramps summing to 1). Not a load-gate term.
- **A canvas the model was never asked to fill cannot be sharpened by steps/guidance/quant**: `VideoModelPreset.recommendedResolution(totalGB:)` sizes per Mac, capped by `autoCanvasCapPixels` (TIME budget); two-stage denoises at HALF (`twoStageCanvasNote`); frame ladder per canvas; `maxFramePayloadBytes` is a TRANSPORT limit.
- **A media pack's RELEASE is a config field, and the text encoder is what it decides** (`LtxVersion` from `model_version`, absent ⇒ 2.3): 2.3 borrows `mlx-community/gemma-3-12b-it-4bit`, 2.5 ships `gemma4-12b-ltx-v1/` in-pack (pinned to `MediaBundle.ltx25TextEncoderDir`). Mismatch generates a plausible video for the wrong prompt — guard asserts RESOLUTION in the load line.
- **The 2.5 text encoder runs the REAL gemma4 transformer** (`ltx_video.gemmaCapture4` via `capture_layers` + `skip_lm_head`); left-padded prompts ride `ForwardCtx.prefill_mask_add` (already causal, FINITE -1e9, declines both fused prefill kernels; null on serving paths).
- **`keyframes_abs_pos_embedding` marks GENERATED keyframe slots, never given-content tokens** (reference `extend_keyframes_mask(marked=False)` for image guidance; the 2.5 checkpoint's row is ~0 anyway). We build no generated slots, so the parameter is parsed and never added.
- **A bytes-read table prices a lever only for a path already AT its read floor** (music3 depth KV: estimated 1–3 ms, measured −11.6). Per-stage probe laps (`MUSIC3_COST_PROBE`) before bandwidth arithmetic.
- **Music3 is NOT an ACE-Step variant**: timestep is a prepended TOKEN (no AdaLN), SwiGLU REVERSED (`first * silu(second)`), Snake alpha-only no exp, DiT RoPE dims-32 theta-10000 hardcoded, AR frame 0 never emitted but feeds back. Full list: the music3 story.
- **When an arch's reference IGNORES a config field, that field is not the truth** (laguna YaRN mscale COMPUTED). **A config field HF allows in two SHAPES must be read as both** (`chat_template` string OR list); `.string` on unchecked `std.json.Value` panics. **Read `text_config` FIRST, then root, PER FIELD.**
- **`x-mlx-pixels` is a GEMMA format; the client gate is an allowlist of ONE** (`wantsServerPreprocess` = `!hasPrefix("gemma")`); server `parseImageUrlContent` refuses when `vp.mode != .gemma`; `VisionEncoder.forward` returns named errors, never dereferences sentinels.
- **A placeholder id the splice mask does not name is a modality that never reaches the prompt** (`spliceVisionRows` keyed on image | audio ids; Qwen VIDEO pads were inserted, encoded (690 rows) and never scattered — the model narrated a clip it never saw, on qwen3_5 too, whose e2e bar of >= 2 generic subjects passed on hallucination). Mask + `countSpliceRows` take `video_token_id`; guard = the video-rows splice test; an e2e vision bar must name something only the pixels could supply.
- **A chunked vision splice resumes its ROW INDEX** (#197, `vision_splice_offset`); repro needs prompt > chunk+512 (`TAIL_MERGE_MAX`); bar = PERCEIVED-content invariant, never bytes. Guard: `tests/test_vision_chunked_prefill.sh`.
- **A forward ARM that never splices vision is invisible from the outside** (LFM2-VL hybrid arm answered "a black rectangle"). Class guard: source scan `every generative forward arm splices vision embeddings`, encoder-only arms exempt BY NAME.
- **A resampled position table is `bilinear` WITH antialias** (SigLIP2-NaFlex; mlx-vlm's bicubic is a 1.13x rms scale error, not an oracle): `qwen_vision.resampleWeightMatrix(.bilinear)`.
- **Patch feature order is per-ARCH** (LFM2-VL channel INNERMOST; Qwen/Muse outermost). **Two GELUs in one checkpoint** (LFM2-VL encoder tanh, projector erf). **For NaFlex, TILING is the resolution** (canvas resampled ONCE, read as windows; markers `<|img_row_R_col_C|>` order must match encoder concat). Guards: `tests/test_lfm2_vision.sh`.
- **A checkpoint's processor pixel bound is not the ENGINE's** (`qwen_vision.effectivePixelBounds`, `ENGINE_MAX_PIXELS` 1536²): our tower MATERIALIZES the score matrix; a 5100x3300 photo OOM'd. Logs once.
- **A tower's PREFIX and `patch_embed.proj` AXIS ORDER are converter choices — probe** (`resolveVisionPrefix` + `patchProjLayout`; channels-last vs channels-first reads as a striped-pattern tower). Probe colours in THREE.
- **Two towers on one surface means the PREPROCESSOR is part of the arch** (muse: aspect-closest grid under MERGED cap, Lanczos, row-major, CHANNEL-major shuffle). `VisionPreproc.mode` picks all of it. Guards: `tests/dump_muse_vision_fixture.py`, `tests/test_muse_vision.sh`.
- **A config default only ONE family wants is a per-arch trap — pin every value PER LAYER TYPE** (muse sliding layers read `rope_local_base_freq` default 10000 vs its 500000 → first-turn loops). Guard: `tests/test_muse_repetition.sh`.
- **A module TREE is the converter's choice** (`lagunaRouterBase`: flat `mlp.gate.weight` vs nested `.proj.`; probe, flat preferred — the `hy3ExpertContainer` pattern). **WEIGHT PREFIX is a property of the checkpoint** (`model.resolveWeightPrefix` probes).
- **A layer-init path that DEMANDS `.scales` can't load a DENSE checkpoint**: scales absence PER-TENSOR (`getLayerScaleOpt`); every dense contracted weight owes `maybeTransposeForBf16` — never depthwise conv or SSM state. Source scan rejects the mandatory getter.
- **`*_text` siblings**: accept the tag, collapse to base type, prefix by `text_config` presence, force `tie_word_embeddings` for Gemma, add to BOTH visibility allowlists. **Gemma terminators merge ADDITIVELY** (`ensureGemmaTerminators`).
- **Quant modes resolve PER WEIGHT** (`computeQuantParams`; scales dtype decides fp8 vs affine; `.biases` mandatory under affine). **A per-tensor override can hide INSIDE the fp family** (`fpParamsFromGeometry` solves from packed geometry). **Affine bits outside {2,3,4,5,6,8} rejected at PARSE** (`UnsupportedQuantBits`).
- **A GGUF folder is a SHELF of models**: one `LocalModel` per quant (`id#<file>`), sidecar classifiers in sync in Swift AND Zig.
- **DiffusionGemma parity = converged-canvas self-consistency** (`MLX_SERVE_DIFFUSION_TRACE=1`); sliding prefill takes `"causal"` when `total_kv ≤ sw`. **Optional per-token modulation gates on the mask** (`nv_pt==0` → exact scalar path).
- **Parity fixtures for deep ViTs/DiTs: dump fp32 on CPU** (MPS fp16 decorrelates); transformers ≥5 zeroes custom rotary buffers — dump scripts assert non-zero; an oracle that IMPROVES when you remove a transform had it silently disabled.
- **TTS**: `TimeDelayNetBlock` = conv + IMPLICIT ReLU; embedding lookups reshape to the TABLE's width (`getShape`); voice clone = ECAPA embedding as one codec-prefix position, `ref_text` ignored.

### Licensing & third-party code

Ported kernels + vendored code are enumerated in `NOTICE` (the ONE place); `LICENSE`/`LICENSE-APACHE-2.0`/`NOTICE` ride every packaging path, pinned by `tests/test_release_workflow_gates.sh`. To enumerate ports, grep comments for `mlxfast|oMLX|MTPLX|mlx-lm|port`. Full story: `docs/reference.md` "Licensing".

### App (Swift)

All app-side rules: `app/CLAUDE.md`; stories: `docs/gotchas/app.md`. Crossovers: agent budgets derive from advertised `context_length`; the app ALWAYS emits memory-critical launch flags (`ServerOptions` defaults mirror Zig defaults — change both together); the two bundle binaries move together.
