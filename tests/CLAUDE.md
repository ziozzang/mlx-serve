# tests/ — test matrix & guard reference (auto-loaded when working here)

Root CLAUDE.md carries the TDD policy; this file is the full matrix of integration/guard scripts and what each pins.

## Testing

**TDD is mandatory for every code change — features, bug fixes, refactors, build-script tweaks, all of it.** No exceptions, no "I'll add the test after the smoke test passes." A live curl against a running server is a useful sanity check but it is NOT a test — it doesn't survive in CI, it doesn't pin the behavior against future regressions, and it doesn't prove the fix is on the right code path.

The order is fixed:

1. **Write the failing test first.** It must fail for the right reason — i.e. the symptom you're fixing or the capability you're adding. If you can't articulate a failing test, you don't yet understand the change you're making.
2. **Make it pass.** Minimum code to flip the test green; no opportunistic refactoring in the same step.
3. **Run the full suite** (`zig build test` + `cd app && swift build` + the relevant integration script) — a green new test doesn't excuse a red regression elsewhere.
4. **Then iterate** — refactor / clean up / extend, with the test as a tripwire.

Concrete rules for each change class:

- **Feature**: at least one unit test that fails without the feature and passes with it. Add an integration test (`tests/test_*.sh`) when the feature is observable over HTTP.
- **Bug fix**: a regression test that reproduces the bug from the user's perspective. The diff order must be `test (red) → fix → test (green)` — verify locally that reverting the fix turns the test red again.
- **Cross-arch work**: cover every architecture the change touches, not just the one you're holding (`gemma4`, `qwen3_5_moe`, `lfm2`, `nemotron_h`, `deepseek_v4` via ds4, etc.). One arch's pass is not the suite's pass.
- **Refactor / no-behavior-change**: at least one existing test must exercise the refactored path before the change. If coverage isn't there, add a characterization test first, then refactor under it.
- **Untestable surface (UI, build scripts, etc.)**: factor a pure helper out and test that. "I can't test SwiftUI" usually means "I didn't extract the testable piece yet."
- **Class bugs get class guards — no whack-a-mole.** When a live failure reveals a CLASS of bug (an escaping gap, a format-family leak, a dispatch hole — anything where "fix one model" could break or miss another), the fix ships with THREE things: (1) the instance regression test; (2) a corpus entry or universal invariant in `src/format_corpus_test.zig` that exercises the whole class across every family/entry, so future additions are covered automatically; (3) a CLAUDE.md gotcha naming the class, its symptom signature, and the rule that prevents reintroduction. Precedents: the no-tag-leak + valid-JSON-args invariants, the history round-trip serialization invariant, the spec-decode dispatch engagement counts.

Existing tools:

- Unit tests OK; integration tests with real models are the real tests.
- After big features: build `mlx-serve` + the app bundle, run `.app` with `TestServer.swift` enabled, run the agentic harness.
- Always run `zig build test` and `swift test` (or `swift build`) before submitting.

| Command | Purpose |
|---|---|
| `zig build test` | Zig unit tests |
| `zig build test -Dtest-filter="format corpus"` | Hermetic format-correctness corpus (`src/format_corpus_test.zig`): real captured model outputs across families through splitThinkBlock/stripThinkBlock/parseToolCalls, plus universal no-tag-leak + valid-JSON-args invariants. No weights; harvest workflow in the file header. |
| `FORMAT_MODELS=<csv> ./tests/test_format_matrix.sh` | Live format matrix: 7 format-family representatives (Qwen think-tags, Gemma 4 channel-tags + custom tool args, Qwen MoE raw-JSON, Gemma 3 fallback/fenced-JSON, GGUF/llama.cpp, DSV4-Flash/ds4) × 8 checks: thinking split, stream+tools content routing, tool-arg byte-fidelity (chat + /v1/messages tool_use), omitted max_tokens. One model at a time on port 11297 with `--log-level debug`; FAILs echo raw-output dumps for corpus harvesting. `FORMAT_MODELS=gemma4-e4b` ≈ 3 min smoke; missing paths skip cleanly. |
| `VALIDATOR_MODELS=<csv> ./tests/test_validator_matrix.sh` | API-compliance + agentic matrix: per architecture (gemma4, gemma3, qwen3_5 dense, qwen3_5_moe, qwen3_moe, llama-engine GGUF, ds4 GGUF) boots a server, runs llmprobe (`~/projects/agents/llmprobe`, 112 checks across /v1/responses + /v1/chat/completions + /v1/messages), then the pi 2-turn html agentic case (multi-turn tool calls) via pi_integration_run.sh. `SKIP_PI=1`/`SKIP_PROBE=1` run one layer; pkills ALL serving mlx-serve instances (81GB ds4 coexistence). Logs in tests/validator-results/. |
| `./tests/llmprobe_smoke_test.sh` | Birds-eye conformance + speed across every MLX family on the box, under each spec-decode config. Boots ONE headless server over `--model-dir`, groups `/v1/models` into families (`familyKey` = base arch + MoE-ness), picks a representative each (prefers one shipping an MTP head; skips giants via `SMOKE_MAX_GB`, default 60), then boots a server per CELL and runs `npx llmprobe --quick --bench`: **none** (`--no-pld --no-mtp` — PLD is default-ON, so a real baseline needs BOTH), **pld**, **mtp** (`--mtp`, only when a sidecar exists), **drafter** (Gemma 4 dense + a matching `gemma4_assistant` on disk), plus a KV-quant crash check (`--kv-quant 4` / `turbo4`) on the ONE fastest model at its default spec config. Zig MLX only — GGUF/ds4/encoders/media are filtered out. **The engagement column is the point**: each cell's server log is scraped for `[spec-stats] mode=`, an `mtp` cell that never engaged FAILS (the head is not prompt-gated, so zero engagements = dispatch hole), and a cell that DISABLED speculation yet shows engagements also FAILS (`expectNoSpec` — its launch flags never landed, so every number in the row is for the wrong config; this caught a live IFS tab-collapse bug in the harness itself). Boots with `--prefix-cache-entries 0`: llmprobe re-sends the same benchmark prompt, so a warm hot-cache reports the CACHE's prefill rate, not the engine's (measured 145,000 tok/s / 16 ms TTFT on an E2B — the same numerator/denominator class as the metrics panel). Env: `SMOKE_MODELS` (csv of ids), `SMOKE_CELLS` (csv of cell ids), `SMOKE_DEPTH` (quick\|default\|full — quick skips llmprobe's capability evals, so that column reads `—`), `SMOKE_PLAN_ONLY=1`. Output: `tests/llmprobe-smoke-results/`. Pure decision layer (family grouping, cell planning, engagement verdicts, roll-up) is `tests/llmprobe_smoke_lib.mjs`, unit-tested by `node --test tests/llmprobe_smoke_lib.test.mjs`. |
| `cd app && swift test` | Swift unit tests |
| `./tests/integration_test.sh [model_dir] [port]` | 36 end-to-end API tests |
| `./tests/test_ollama_api.sh [port]` | Ollama API (`/api/*`) end-to-end: version/tags/show/ps, chat (NDJSON stream + non-stream + tool-call args-as-objects + done_reason), generate (templated + raw), embed (new + legacy), tagged-name resolution, pull fast-path, `mlx-serve list`. Opt-in: `OLLAMA_PULL_NET=1` (real HF download into a scratch HOME), `OLLAMA_RUN_REPL=1` (pty REPL round-trip). Hermetic counterparts: translation/sink tests in `src/ollama.zig` + alias/tree/REPL-codec tests in `src/cli.zig` |
| `./tests/test_lan_share.sh [model] [portA]` | LAN sharing end-to-end: two servers on ONE machine — Bonjour discovery (`<id>@<peer>` entries + `lan_peer` badge on B), proxied chat through B (non-stream + SSE `[DONE]`, `[lan] proxy` engagement line), load/unload 200 no-ops on remote ids, the non-loopback 403 gate matrix on A via the Mac's LAN IP (metrics/status page/load-model/`@peer` ids denied; /health + filtered /v1/models open; loopback untouched), and self-detection (A never mirrors its own advertisement). SKIPs without the model or a non-loopback IP. Hermetic counterparts: lan.zig tests (incl. the tunnel pump against a fake peer socket) + `lanShareDenial` (server.zig). |
| `HOT_SWITCH_MODELS=<csv> ./tests/test_hot_switch_state.sh [root] [port]` | Every model answers with ITS OWN tokenizer, template and grammar table. Several models interleaved on ONE server (A, B, C, then A again — a singleton built by A leaks into B, and A's SECOND visit catches a table rebuilt for B), each round asserting: `/tokenize` returns a stable token count for that model across switches (a foreign tokenizer changes it — the digit-grouping class), a plain turn answers with no markup leak, and `response_format: json_object` yields PARSEABLE JSON. Plus a per-model `[grammar]` build count. The 2026-08-11 process-wide `global_token_bytes` bug is this class and showed ONLY as bad output (`## Attributes` / `郑重(郑重)`) — nothing crashed and nothing logged an error. |
| `LONGCTX_TEST_MODEL=<dir> ./tests/test_tools_long_context.sh [port]` | A tool call still PARSES at 16k/32k/64k, where the sliding-window block trim and spec verify interact. Both are guarded for speed and for byte-identity on prose; neither guarded a tool call at the lengths where the trim actually engages (below ~8k there is nothing to trim, so short tool tests pass for the wrong reason). Per rung: the call fires with the declared name, arguments are valid JSON, a planted identifier survives into them, no markup leaks — and the log is checked for the `[sliding] block trim engaged` line, since a rung that fell back to short-context handling proves nothing. `LONGCTX_RUNGS` overrides. |
| `CONC_TEST_MODEL=<dir> CONC_N=4 ./tests/test_tools_concurrent.sh [port]` | Concurrent agent loops each get THEIR OWN tool calls. Batched decode and tool buffering are each tested alone and never together; a cross-slot leak looks like one client receiving another's arguments, which no single-stream test can see. N streams, each planting a DISTINCT code, assert: every stream reaches `[DONE]`, every call carries its own code and NO other stream's, arguments valid JSON, no markup in content, server functional after. |
| `CANCEL_TEST_MODEL=<dir> ./tests/test_cancel_mid_tool_buffer.sh [port]` | Disconnect WHILE the stream is buffering for tool detection. That window — server holding an unflushed buffer AND a live slot, emitting nothing until the pattern resolves — is not covered by `test_disconnect_cancel.sh`, which cancels a prose stream where every token has already gone out. Asserts one `[cancel]` per round, no runaway `active_bytes` growth across rounds (the buffer is freed), and that the server still answers with tools and without. |
| `./tests/test_tool_response.sh [port]` | Tool calling round-trip |
| `./tests/test_kv_cache_poison.sh [port]` | KV cache poisoning regression |
| `./tests/test_anthropic_api.sh [port]` | Anthropic Messages API |
| `LLAMA_GGUF_MODEL=<file.gguf> ./tests/test_llama_gguf.sh [port]` | Embedded llama.cpp GGUF engine end-to-end (auto-routing, chat, streaming, Anthropic, prefix reuse) + the issue-#59 headless case: GGUF dir discovered via `--model-dir` (no config.json), cold-loads on first request, unloads, reloads |
| `PLD_TEST_MODEL=<dir> ./tests/test_pld_equivalence.sh` | PLD byte-equivalence (default gemma-4-e4b-it-8bit) |
| `./tests/test_streaming_pld.sh [port]` | Streaming PLD byte-identical to non-streaming |
| `SLIDING_GEMMA4_MOE_MODEL=<dir> SLIDING_LAGUNA_MODEL=<dir> SLIDING_INKLING_MODEL=<dir> ./tests/test_sliding_window_trim.sh [port]` | Sliding-window BLOCK trim, per arch (gemma4-MoE / laguna / inkling; defaults point at the external Drive SSD copies, each arch SKIPs when absent). Greedy temp-0 output with `MLX_SERVE_SLIDING_BLOCK_TRIM=1` must be BYTE-IDENTICAL to `=0` — a diff means that arch's mask or bias reads ABSOLUTE positions and is not view-relative, so report it rather than shipping. Each arm also asserts the one-shot `[sliding] block trim engaged` line in its OWN log (present in the ON arm, absent in the OFF arm): a prefill chunk only trims once it lands at a non-zero offset, so at 8k a serial arch prefills in ONE chunk and both arms agree for the wrong reason — hence the per-arch prompt length (320 filler sections ≈ 20k tokens for laguna/inkling, past 2x the default 8192 chunk). gemma4 runs `--pld` because its win is the 4-8-wide spec VERIFY block, below the fused hd-256 kernel's 16-row floor. |
| `./tests/test_pld_tools.sh [port]` | PLD with tools present: engages (`[spec-stats] mode=pld`), tool calls + echo text byte-identical to no-PLD baseline (starts its own server) |
| `./tests/test_drafter_tools.sh [port]` | Drafter with tools present: same contract as test_pld_tools.sh for the Gemma 4 assistant drafter, across chat non-stream, /v1/messages stream + non-stream, and /v1/responses non-stream, with a per-request `[spec-stats]` engagement count — equality checks alone can't catch a dispatch hole because the regular-decode fallback is output-identical (starts its own server) |
| `MTP_TEST_MODEL=<dir> ./tests/test_mtp_equivalence.sh [port]` | Qwen native-MTP head (default `~/.mlx-serve/models/ddalcu/Qwen3.8-27B-MLX-Serve-4bit`): auto-load, per-request opt-out, first-100-char temp-0 equivalence vs `--no-mtp` (TIE-AWARE: on a byte mismatch it replays serially with logprobs and acquits only when the first divergent token is a near-tie, top-2 gap ≤ 0.15 nats — verify widths move ties, a plumbing bug diverges at a confident position; servers boot `--prefix-cache-entries 0` per the hybrid byte-stability rule), and a per-request `[spec-stats] mode=mtp` ENGAGEMENT count on chat (stream + non-stream) + /v1/messages — output equality alone can't see a silent fallback to regular decode. Calibrated surfaces additionally set `MTP_EXPECT_AUTO_PROFILE=<enum-tag>` + `MTP_EXPECT_AUTO_DEPTH=<n>` together: the boot log must name that exact full-fingerprint arm, and the max-confidence echo must realize that depth in `[spec-stats]`, so a conservative generic/cap-6 fallback cannot pass merely because MTP still engaged. Includes an ACCEPTANCE-FLOOR check (best avg_per_round >= 0.5 — depth-independent, unlike per_draft_pct which legitimately dilutes to ~25% on chained creative drafts at the depth-3 default): a structurally broken head — e.g. a sidecar built without folding +1 into Qwen's delta-encoded norms — engages, accepts ~0/round, gate-falls-back to regular decode, and passes both equivalence and engagement. MoE targets (default-off MTP) run with `MTP_FORCE_ENABLE=1`, which injects `enable_mtp:true` into every request. Discovery mirrors production: every `mtp.sidecar_rel_paths` filename plus sharded-index and monolithic-checkpoint `mtp_marker_keys` are accepted. Standalone sidecars rebuild from the original Qwen repo via `tests/build_mtp_sidecar.py`. |
| `./tests/test_completions_spec.sh [port]` | /v1/completions spec-decode: PLD + drafter engage on the FIM endpoint, first-N-token equivalence, stream/non-stream leading-space parity (starts its own server) |
| `./tests/qwen4_ab.sh mtp\|batched <tag> [server flags]` | Same-boot qwen4_exp A/B harness (prompt fixtures in `tests/fixtures/qwen4_ab/`: 400-token code + prose, one 8.4k prompt; nonce varies by REP never by arm): `mtp` = MTP vs serial interleaved x3 per prompt + an MTP-beside-plain concurrent arm; `batched` = serial x3 then 2-/4-stream prose + 2-stream 8.5k aggregates. `MLX_SERVE_BIN` picks the binary (a control binary must sit in `zig-out/bin/` for its rpath), `QWEN4_AB_PORT`, engine env passes through (`MLX_SERVE_MTP_FORCE_DEPTH`, `MLX_SERVE_MTP_TRACE`, kill switches). Numbers in `docs/gotchas/engine-mlx.md`; the M5 box re-measures with the same script. |
| `MTP_FORCE_ENABLE=1 MTP_TEST_MODEL=<qwen4 pack> ./tests/test_mtp_equivalence.sh` | Runs on qwen4_exp (11/11 on the 4-bit pack): the checkpoint marker `language_model.mtp.fc_hidden.weight`, the head's own `[qwen4] MTP head loaded` line, `MTP_FORCE_ENABLE=1` because MoE keeps MTP opt-in per request. Its `--no-mtp` baseline caught the in-checkpoint head ignoring the flag. |
| `./tests/test_batched_transition.sh [port]` | Concurrent-stream consistency: legacy→batched decode transition must not duplicate/drop tokens (starts its own server). On a GDN trunk (`BATCH_TEST_MODEL=<qwen3_5/qwen4 pack>`) this is also the batched-then-serial == all-serial bar: the batched tick advances each slot's `moe_seq_offset` |
| `./tests/test_multiclient_stress.sh [port]` | The "several pi's + several chats + voice + image gen" mix on ONE server: concurrent tool-calling agent streams (chat A) + plain streams (chat B, a DIFFERENT model → per-model batch groups) + Kokoro TTS + FLUX image gen, all at once; asserts no lost request, [DONE] termination, real bytes, server alive + functional after, no crash signature, and log ENGAGEMENT per leg (a silently-skipped leg can't pass on transport). Knobs: `STRESS_THINK`/`STRESS_MTP` (agent leg only), `N_ABORT` (mid-stream client vanish), `N_CHURN` (load/unload cycles), `SOAK_WAVES=N` (repeat against ONE server, /props memory sampled per wave; ≥3 waves asserts final active ≤ 1.5× wave-2 — the leak class), `STRESS_CHAT_A2` (alternate agent model on even waves → registry evict/reload churn under the default 3-model resident cap). Workers RETRY 503s: with >3 models fighting the resident cap and every victim refcounted mid-stream, the load path 503s honestly ("retry after current requests complete") — a lost request is a failure, an honest backpressure 503 is not. Video excluded (minutes-long). |
| `./tests/test_drafter_equivalence.sh [port]` | Gemma 4 drafter byte-equivalence |
| `UD_MOE_MODEL=<dir> ./tests/test_ud_moe.sh` | Unsloth UD MoE load + generate (default Qwen3.6-27B-UD-MLX-4bit) |
| `NVFP4_TEST_MODEL=<dir> ./tests/test_nvfp4.sh` | NVFP4 checkpoint (issue #24): load, banner reports nvfp4 mode, temp-0 coherence canary. Default Qwen3-0.6B-nvfp4; pass a gemma-4 QAT nvfp4 dir to exercise mixed affine-override resolution. Hermetic counterparts: `computeQuantParams` + nvfp4 qmatmul/gather tests in transformer.zig. |
| `./tests/test_diffusion_gemma.sh [port]` | DiffusionGemma block diffusion end-to-end: load, greedy chat coherence + EOS semantics, diffusion-loop engagement + early-stop convergence (steps < max), block-wise SSE streaming, /v1/messages, tool calls, thinking, multi-canvas long generation. Hermetic counterparts: sampler math tests in diffusion.zig. Live parity: `DIFFUSION_TEST_MODEL=<dir> zig build test` runs the converged-canvas self-consistency test (reference 63/64 no-sc, 64/64 with sc) — this is the test that catches the lazy-slice expert-zeros class (red on revert) where toy-scale synthetic gathers stay green. |
| `./tests/test_long_agent_memory.sh [port]` | 10-turn Claude-Code-style agent: plants 3 facts in turn 1, recalls them across mode transitions (tools on/off, thinking on/off). Catches "model acts like first-time-seen" regressions. |
| `EMBED_TEST_MODEL=<dir> ./tests/test_embeddings.sh [port]` | /v1/embeddings (encoder-only BERT): OpenAI shape, batched-vs-single cosine parity across mixed lengths (pins the padding mask + masked mean-pool), >EMBED_MAX_BATCH round-trip, OpenAI `dimensions` (truncate + L2-renorm via `server.truncateEmbeddingDims`; over-native/non-positive → 400), 400 on generation, hot-load + stub `embeddings` capability beside a chat default, and `/v1/load-model` register-by-absolute-path (incl. the JSON `\/`-escaped form Swift clients send) for encoders outside --model-dir; plus (issues #116/#117) the bge CLS name-fallback boot log, `meta.embedding_max_length` on the ready model, and the over-limit structured 400 (index + counts, auto ceiling = the model's 512 window). Default mlx-community/bge-small-en-v1.5-8bit. Hermetic counterparts: `buildPaddedBatch`/`buildKeyPadMask`/`maskedMeanPoolNormalize`/`gatherTokenPool`/`embedSubBatchEnd` tests in generate.zig + the pooling-resolution tests in model.zig. Reference parity for pooling modes: `tests/dump_qwen3_embedding_fixtures.py` (last-token Qwen3-Embedding, also reusable for CLS bge — dump via `uv run --with sentence-transformers`, compare against a running server). Live app-side flow: `DOCS_RAG_AUTODL_PORT=<port> swift test --filter testAutoProviderProvisionsEncoderEndToEnd` (downloads from HF). |
| `./tests/test_stream_keepalive.sh [port]` | Streaming-silence class guard: for EVERY streaming surface (chat / messages / responses) a long buffered tool call must never leave the socket silent longer than the keepalive interval — max inter-chunk gap < 15s, at least one `: keepalive`/`event: ping` observed, and the tool call still parses with valid-JSON args (injected keepalive bytes must not corrupt the SSE stream). SKIPs a surface whose generation was too short to fill the buffer. Pre-fix all three FAIL at `max_gap ≈ 17.8s, keepalives=0`. Starts its own server; `KEEPALIVE_TEST_MODEL` overrides the default gemma-4-e4b-it-4bit. Hermetic counterpart: `StreamHeartbeat` tests in server.zig. |
| `./tests/test_disconnect_cancel.sh [model_dir] [port]` | Client-disconnect handling during long prefills: SSE keepalives flow every 5s while a request waits on first tokens (Anthropic `ping` events / OpenAI `: keepalive` comments), and a vanished client cancels its slot within one probe interval — the chunk loop aborts the ghost prefill (`error.Cancelled`) instead of grinding minutes of abandoned work that piles up behind Claude Code retries. Starts its own server. |
| `./tests/test_cancel_commit.sh [model_dir] [port]` | Cancelled requests PRESERVE their committed KV instead of dying with the slot. Decode-phase: a stream killed mid-generation (the slot bypasses finishSlot via `complete()` → cleanup queue) commits prompt+emitted tokens from the cleanup drain — proven by an identical re-issue reporting `cached_tokens ≈ prompt` plus a `[hot-cache] reused` line. Prefill-phase (hybrid AND plain-attn): a mid-prefill kill commits the FORWARDED PREFIX — the boot pins `--prefill-chunk/--ssm-checkpoint-stride 512` so the cancel window always spans stride boundaries; hybrids ride the Cancelled-checkpoint salvage (`CancelledCheckpointSink` — initWithOptions hands its captured checkpoints + the authoritative `forwarded` count to the slot; `cache.step` reads 0 on every aborted prefill since it only advances at init completion) and the re-issue restores at the last checkpoint ≤ matched. The killed stream's captured body self-diagnoses which phase the kill landed in (content OR reasoning_content deltas ⇒ decode). Warmup request first — headless boots load lazily. Hermetic counterparts: `cancelledPrefillCommitLen` unit test + three source-scan class guards (cleanup-drain commit before deinit; commitSlotIfApplicable routing BEFORE the pad-only guard — `was_pad_only` starts true and a cancelled prefill never emits a token to clear it; sink handoff + `salvage.forwarded` + Slot.deinit salvage free) in scheduler.zig. |
| `./tests/test_messages_stream_thinking_tools.sh [model_dir] [port]` | /v1/messages STREAMING with thinking + tools together (Claude Code's exact shape) on a template-opened-think model: no think-tag leak in text deltas, SSE content-block lifecycle validity (every delta/stop references an open block), thinking_delta + tool_use emission. Starts its own server. Hermetic counterpart: `chat.streamThinkGate` unit tests + the corpus streaming-gate replay. |
| `./tests/test_kv_quant_equivalence.sh [model_dir] [port]` | Off-vs-4-bit-vs-8-bit KV cache equivalence (per-arch first-N-token thresholds, env-overridable). Batched × kv-quant crash guard lives in `test_batched_equivalence.sh` (its last section: `MLX_SERVE_FORCE_BATCHED=1` + `--kv-quant 8` — the batched kernel once fed raw packed KV to SDPA and killed the server on the first concurrent decode). |
| `./tests/test_kv_quant_per_request.sh [model_dir] [port]` | Per-request `kv_quant` body field plumbing — confirms parse + scheduler routing for each scheme. |
| `./tests/test_kv_quant_fused_equivalence.sh [model_dir] [port]` | Packed quantized-KV reads: dense vs fused first-N-token equivalence at `--kv-quant 4` PLUS engagement both ways — the fused arm must log `[kv-attn] fused engaged` AND `[kv-attn] decode kernel engaged`, the dense arm neither (the flag shipped as a silent no-op for months; output equality alone is structurally blind to that). Boots with `MLX_SERVE_KV_ATTN_MIN_TK=1` so the short prompt still exercises the fused paths (the default floor is a perf gate). The spec-on arm (PLD, echo prompt, default model) pins that the DECODE kernel serves only t_q == 1 (`Tq=1` on its one-shot line; verify widths used to fall to the composed packed-qmm chain — the 2.1x decode loss at 8k+, 2026-08-15) plus greedy equivalence vs the dense spec arm — deliberately NO verify-kernel assertion there, gemma's shapes sit outside the measured adoption set (full-attn dk 512, sliding "array") and zero engagements is that arch's correct behavior. The third arm boots a qwen-shaped model (`KV_VERIFY_SPEC_MODEL`, default Qwen3.5-0.8B — hd 256, the adoption set) and asserts `[kv-attn] verify kernel engaged` + PLD engagement + equivalence: the dense fallback is output-equivalent, so a verify dispatch hole is invisible to everything but the log. RATE recovery is deliberately not asserted anywhere here (one-run spec cells are variance — it lives in the same-boot bench A/B). Hermetic counterparts: grouped-Q/strided/multi-block parity tests in kv_quant.zig + transformer.zig, `qkvVerParityCase` (TQ 2..8, causal tail mutation-checked), the kernel-or-DENSE source scan, `kvAttnFusedEligible`/`kvAttnVerifyEligible` (red-verified) + `resolveKvAttnFusedPure` unit tests. |
| `./tests/test_ane_prefill.sh [model_dir] [port]` | ANE prefill offload (`--ane-prefill`) across six boots of the SAME binary: build + per-seam engagement (`[ane] prefill offload engaged` and, when the ready line reports gdn layers, `[ane] gdn offload engaged` — a built-but-never-dispatched program is the dispatch-hole class), the `MLX_SERVE_ANE_GDN=0` MLP-only lever, the `MLX_SERVE_ANE_MODE=row` lever, the strictly-opt-in off arm (ZERO `[ane]` lines), and the orphan-staging reap. **Every arm asserts ZERO `eval failed` lines** — a failed eval recomputes that chunk on the GPU, so the request still answers correctly and only the RATE moves: the first procedure-bank build lost 200 of 210 evals to wrong symbol indices (identity instead of `procedureInfoForProcedureIndex:`) and read as "banks are 23% slower" (2026-08-20). Bank arms: the ready line must report strictly FEWER banks than covered programs (per-layer handles are what the ~121-handle runtime limit refuses) and `units=1`; a tiny `MLX_SERVE_ANE_BANK_MAX_BYTES` must produce MORE banks at IDENTICAL coverage (packaging is never a coverage decision); and `MLX_SERVE_ANE_DUAL=1` on single-ANE silicon must refuse by name, still build `units=1`, and still engage. Rates are deliberately NOT compared (one-run rates are variance). Defaults to Qwen3.5-0.8B-MLX-4bit; `ANE_PREFILL_MODEL` overrides; SKIPs when the private framework is absent. Hermetic counterparts: `channelSliceWidthUnits`/`unitCount`/`bankGroupLen`/`engineBillBytes`/`aneShareRows`/`gateAllows` in ane.zig, `anePropsJson` (one- and two-unit) in server.zig, and the `effectivePrefillChunk` build-site scan in scheduler.zig. |
| `./tests/test_prefix_cache_mem.sh [model_dir] [port]` | `--prefix-cache-mem` budget — long-prompt traffic must stay within the byte cap and produce eviction log lines. The #330 arm drives ONE growing conversation past the budget and asserts a `trimmed oversized entry to N/…` line plus a later `reused M/…` with M >= N: from the outside a trim that silently stops engaging is indistinguishable from a legit decline, so only the log pins it (the unit tests own the trim's correctness). A hybrid whose smallest checkpoint outweighs the budget declines legitimately — WARN, gated on the `; ssm` clause in eviction lines; on plain attention a decline IS the cliff and FAILs (red-verified against the pre-fix binary). |
| `./tests/test_headless_spec_flags.sh [port]` | Headless (`--serve --model-dir`, no `--model`) must honor the CLI's spec-decode flags — the mode the app ALWAYS launches. `runHeadlessServe` hand-rolled all three PLD fields as `false`/`5`/`3` literals; #95 threaded `--pld` and left `--pld-draft-len`/`--pld-key-len` still dropped. Asserts the boot banner echoes non-default lengths, `--no-pld` still wins, and a bare boot reports 5/3. Section [4/4] pins a second headless-only class: **endpoint existence must not depend on model state** — resolution runs ahead of dispatch, so an unknown path used to answer 503 "No default model configured" instead of 404, and every endpoint-probing client (llmprobe) then read the server as a catch-all and scored all surfaces absent. Checks unknown → 404 while served endpoints still report 503. FULLY HERMETIC — an empty `--model-dir` discovers zero models, so no checkpoint is needed and it runs in seconds. Hermetic counterpart: `server.PldDefaults` tests. |
| `LOGPROBS_TEST_MODEL=<dir> ./tests/test_logprobs.sh [port]` | The logprobs wire contract on every surface that serves it. Sections [1]-[3] pin the three defects fixed 2026-08-05 (temperature-scaled logits, float-equality id recovery, a one-token offset) through their ONE observable bar: at temp 0 the emitted token IS the argmax, so rank 1 of `top_logprobs` must equal it — plus the same token's logprob must not MOVE between temp 0 and temp 2 (it is the model's distribution, not the sampler's), the distribution must not be saturated to 0.0, and `/v1/completions` must answer the legacy shape (integer `logprobs`, four parallel arrays, `top_logprobs` a text-keyed map). Sections [4] and [5] are the class those three did not cover: **STREAMING accepted the field, disabled speculation to honor it, computed every entry, and dropped them** — on chat the SSE chunk template had no `logprobs` slot, and on `/v1/completions` the streaming handler hardcoded `logprobs_n = 0` beside a parser that reads it. Each asserts streaming carries them AND agrees with non-streaming token-for-token and VALUE-for-value on the same greedy request, which is what catches a partial drain, a duplicated one, or an off-by-one high-water mark; [5] additionally pins that legacy `text_offset` ASCENDS ACROSS CHUNKS and matches non-streaming, since it indexes the whole completion and a per-chunk reset to 0 is the natural bug. Structurally invisible to everything else: output-equality tests see byte-identical deltas either way, and llmprobe probes logprobs non-streaming only — it scored this server 100% on `Logprob consistency` while streaming returned nothing. Section [6] pins the NEXT layer of the same class: the array was built from the raw `token_ids`, so it described the reasoning block we strip — on every model that thinks, `logprobs.content[0]` was the first token of the THOUGHT (measured 37-186 entries against 8 characters of content across Qwen3.6-27B ×3, Qwen3.6-35B-A3B, gemma-4-31b, gemma-4-e4b, Qwen3-4B, LFM2.5). It asserts the entries RECONSTRUCT `message.content` on a thinking request, and — the red-on-revert half — that their count is BELOW `completion_tokens` whenever reasoning was stripped, since pre-fix the two were equal by construction; streaming is held to its OWN deltas, because the gate trims differently and the invariant is that entries describe what was sent. Section [7] pins that every response body is valid UTF-8: a token is a BPE fragment and can hold half a multi-byte character, and raw bytes in a JSON string make the whole body unparseable rather than merely wrong (live: a `b"\xf0\x9f"` candidate on Jundot/Qwen3.6-27B-oQ4e-mtp). It checks EVERY response the script makes, not one crafted request, because which one draws a split candidate into its top-5 is luck. Any chat model works (default LFM2.5-2.6B-8bit); SKIPs without one. Hermetic counterparts: `computeLogprobs` rank/tie test + the `last_logprob` assignment scan (generate.zig), `formatCompletionsLogprobs` shape test, `contentTokenRange`, the UTF-8 lossy-escape test, and the streaming-emitter scan `every streaming chat emitter carries logprobs` (server.zig) — now TWO-SIDED: a content emitter must drain, a reasoning emitter must NOT, so neither "a new emitter forgets" nor "a reasoning chunk ships the thought's entries" can return. |
| `LOOP_TEST_MODEL=<dir> ./tests/test_loop_stop_signal.sh [port]` | The degenerate-tail guard's WIRE contract, on every surface that can emit it. A loop cut keeps `finish_reason: "length"` (moving it re-opens the 2026-07-14 php.html class) and reports its CAUSE beside it as `finish_details:{"type":"repetition_loop"}` — asserted on chat + completions, stream and non-stream, because a missing field is invisible to any output-equality test. Section [1/6] also pins the TRIM: a non-streaming body must not carry the degenerate tail back, since an agent re-sends the cut turn as history and the model resumes its own loop (live 2026-08-05: five loop-stops in a row at 1254/1071/1079/102/58 tokens, each firing sooner). The bar is absolute — the guard only fires after 16 identical cycles, so an untrimmed body necessarily carries at least 16, and one kept copy is deliberate. Section [6/6] boots with `MLX_SERVE_LOOP_TRIM=0` so the trim's own red-on-revert runs every time (32 repetitions off vs 2 on). Needs any chat model — the `ping pong` prompt loops on all of them and the period-1..8 tier convicts within ~130 tokens, so the whole script is fast; SKIPs without one. Hermetic counterparts: `degenerateTail` tier + trim-start tests (generate.zig), `loopStopDecision` (scheduler.zig), `finishDetailsField` / `loopTrimmedIds` / the emitter source scan (server.zig). |
| `./tests/test_route_404_no_load.sh [model_dir] [port]` | A path the server does not serve costs NOTHING. Model resolution ran ahead of dispatch and the existence check lived in `ensureLoaded`'s `NoDefaultModel` arm, so it only fired when there was no model to load: a POST to a bogus path carrying a resolvable `model` cold-loaded that checkpoint and THEN 404'd (found by typing `/v1/load` for `/v1/load-model` — 2m42s and 121 GB resident for a wrong URL, and a one-liner to pin the box). Boots headless over a `--model-dir` with NO `--model` so every entry starts `unloaded`, POSTs three plausible typos/probes naming a real id, and asserts 404 on each AND that the entry is still `unloaded` — the state check is the assertion, the 404s pass either way. Also re-checks that a real route and `/props`-with-no-default still answer 200, since the gate sits above both. Nothing loads when the fix holds; the small model loads when it doesn't, so it is fast either way. Hermetic counterpart: the `answered BEFORE the model is resolved` source-order scan (server.zig). |
| `./tests/test_cold_load_launch_flags.sh [model] [port]` | Launch flags survive a COLD load (hot switch, `/v1/load`, first request naming an unloaded model) — `ensureLoaded` builds a SECOND `LoadRequest` and three rounds of this bug have shipped through it (prefix-cache, MTP+llama, drafter+ssd). Boots the primary, cold-loads a symlink clone carrying an in-dir `drafter/config.json` that declares the DFlash contract and nothing else: the arm that probes fails its load LOUDLY, the arm with `--no-drafter` is silent, and that difference is the assertion — no real sidecar, no GPU, seconds per arm. Baseline arm runs first so a setup that stopped exercising the probe FAILS instead of passing vacuously. Both arms check the `/v1/load-model` HTTP status and the `--no-drafter` arm additionally asserts the clone reached `ready` — "no DFlash line" is also what a cold load that never happened looks like, and a typo'd path 404s while STILL cold-loading the model named in the body, so the silent arm has to prove it did the work. Any small chat model works; SKIPs without one. Hermetic counterparts: the `re-applies EVERY retained launch setting` source scan + `coldLoadDrafterDir` / `coldLoadVision` in scheduler.zig. |
| `./tests/test_json_mode_multi_model.sh [modelA] [modelB] [port]` | The JSON grammar mask is built PER MODEL. One server, two resident models, `json_object` + `json_schema` against A then B then A — under the process-wide `global_token_bytes` singleton (live 2026-08-11) B masked its logits with A's vocabulary, ids meant different bytes there, and the constraint fell open: `## Attributes`, `郑重(郑重)`, `<<<<<<< Vcc`. Which model breaks depends on request ORDER, so a single-model script cannot see it. Also asserts one `[grammar] building token-byte table for <id>` line PER id — a shared table logs exactly one, which is the class assertion output parsing alone would miss on a model that happens to answer JSON anyway. Defaults to the two smallest local checkpoints (Qwen3.5-0.8B + LFM2.5-2.6B, ~2 GB total); SKIPs without them. Hermetic counterpart: the `grammar token-byte table is per MODEL` test in model_registry.zig. |
| `./tests/test_json_schema_thinking.sh [model_dir] [port]` | JSON-schema output + thinking enabled lands the JSON in CONTENT on every mask-building surface (#331). A token-0 grammar mask can't emit `</think>`, so on templates that open a think block (qwen3.5/3.8) the schema-valid object shipped inside `reasoning_content` with `content` empty — `/v1/messages` had the thinking-off gate (pinned by `test_output_config.sh`), chat-completions and responses did not. Arms: chat non-stream + stream (`delta.content`), responses `output_text`, plus a `[grammar] enforcing` engagement count so prompt-only enforcement can't luck the arms green. Defaults to Qwen3.5-0.8B; SKIPs without it. Hermetic counterpart: the `every JSON grammar mask site pairs with the schema thinking-off gate` source scan in server.zig. |
| `./tests/test_continuation.sh [model_dir] [port]` | Continuing a partial assistant reply EXTENDS it instead of restarting it. A conversation ending in an assistant message used to render that message as history and then let `add_generation_prompt` open a SECOND assistant turn, and the model answers a doubled turn by starting over. Pins both surfaces asking their own documented way — an explicit `continue_final_message` on `/v1/chat/completions` (vLLM's spelling), an implicit trailing assistant message on `/v1/messages` (Anthropic's own behaviour) — and that the flag's ABSENCE leaves chat-completions unchanged, since agent frameworks legitimately POST assistant-last history there and expect a fresh turn. The "did it restart?" check is `grep -vi` on the prefix, and the without-flag arm is measured on `prompt_tokens` rather than on the reply: "yellow." is the natural next word whether the model is finishing the sentence or answering afresh, so a working feature failed the first cut of that test — what the flag changes is where the prompt ENDS, which costs a close tag plus a turn header and is true of every checkpoint. Greedy throughout (a sampled reply makes a restart check a coin flip). Any small chat model; SKIPs without one. Hermetic counterparts: the five `renderChatTemplate: continuation…` tests (chat.zig — open turn, content channel, whitespace trim, non-assistant decline, muse channel commit), `keyFor: a continuation does not share a key` (tokenize_cache.zig), and `continuationRejectReason` + the `every continuation surface consults` scan (server.zig), which is what keeps an embedded-engine model from reaching a backstop that answers by closing the socket. |
| `./tests/test_index_page.sh [port]` | The built-in console at `GET /` (sidebar: **New chat / Monitor / API** + Recents; chat is the landing view and opens in its empty state). Pins that it renders with **no model**: the arm sat below model resolution and rendered one `*LoadedModel`, so a headless boot (`mlx-serve serve` / `--serve --model-dir` — the default way the server starts and the only way the app launches it) answered 503 "No default model configured" at the ROOT, on the first page a person opens, and on the page that IS the model picker. Also asserts the tab/control markup, the `#mlx-metrics` mount present-with/absent-without `--metrics` (deliberate duplicate of one `test_metrics.sh` assertion — that script needs a checkpoint, this one doesn't), and that **every** endpoint the server serves is documented, `/api/*` included (the hand-written reference had omitted the whole Ollama surface: 22 of 31). FULLY HERMETIC — empty `--model-dir`, seconds, no weights. Red-on-revert 1/20. Hermetic counterparts: `the index page documents every endpoint the server serves` (server.zig, pins the reference against `ROUTE_PATHS`) + `node --test tests/html_console_test.mjs`. |
| `./tests/test_prefill_interleave.sh [port]` | Prefill-side interleaving: a cold prefill must not stall concurrent decode streams for its whole duration. Two arms of the SAME binary (`MLX_SERVE_PREFILL_INTERLEAVE=0` vs default-on) each run a stream + an ~8k-token cold prefill at `--prefill-chunk 1024`; asserts the on arm logs `[interleave] engaged` and the off arm doesn't (the silent-no-op class), the on arm's max inter-token gap is under HALF the off arm's (measured: 428ms → 114ms), and the stream's greedy output hash matches across arms (interleaving reorders ticks, never math). Needs a small chat model (`INTERLEAVE_TEST_MODEL`); SKIPs without one. Hermetic counterparts: the chunk-loop yield scan (generate.zig) + the hook-wiring/prefill_ns-billing scan (scheduler.zig). |
| `./tests/test_vision_chunked_prefill.sh [port]` | Vision prefill chunks like text (issue #197): the whole-prompt single forward made the memory guard bill full width, so long conversations + one screenshot 400'd unfixably. Two arms of the SAME binary (`MLX_SERVE_VISION_CHUNKED=0` vs default-on) at `MLX_SERVE_PREFILL_CHUNK=32` with a prompt padded past `TAIL_MERGE_MAX` so boundaries land INSIDE the image's placeholder span. Asserts the on arm logs `[vision] chunked prefill` and the off arm doesn't (silent-no-op class), and that BOTH arms perceive the SAME color set on a four-quadrant image — the class bug (splice rows restarting per chunk) collapses the bottom quadrants onto the top colors. Deliberately does NOT assert byte-equality across arms: chunk width changes GEMM shapes and 4-bit near-ties flip across widths. Checkpoint-agnostic color bar (gemma-4-qat calls dark yellow "brown" in both arms). Defaults to LFM2.5-VL-1.6B-4bit; SKIPs without a vision model; validated on the gemma-4 and Muse-Glimmer arms too via `VISION_CHUNK_TEST_MODEL`. Hermetic counterparts: `spliceVisionRows` chunk-equivalence + audio-order + video-rows tests (transformer.zig — the video one is the guard for the 2026-08-26 hole where Qwen `<|video_pad|>` rows were encoded, inserted and never scattered), `countSpliceRows` + wiring scan (generate.zig), guard-billing scan (server.zig). |
| `./tests/test_connection_thread_reaping.sh [port]` | Per-request pthread stacks are reclaimed. Boots headless, records macOS `vmmap` Stack-region count, sends 300 fresh HTTP connections, then asserts the count did not grow with the request count and the server remains healthy. Reproduces a long-running E2B workload's 4–5 GB → 10 GB process-footprint climb where `/props cache_bytes` was only 2.9 MB: pre-fix each discarded joinable `std.Thread` handle retained one stack mapping plus page-table cost. Hermetic counterpart: `each connection thread handle is detached after spawn` in server.zig. |
| `./tests/test_sleep_inhibit.sh [model_dir] [port]` | Checks no assertion while idle, assertion during generation and startup load, release after work, and `--no-prevent-sleep`. Needs macOS and a small chat model; otherwise skips. |
| `./tests/test_model_rescan.sh [port]` | `POST /v1/models/rescan`: discovery walks the `--model-dir` roots ONCE at boot, so a model the app's Model Browser downloads mid-session was invisible to `/v1/models` (and to the media panes' "On This Mac" rows, which read it) until a restart. Boots headless on an empty root, drops a fake `minimax_h3` dir in after boot, and asserts rescan reports `{"added":1}`, the stub appears in `/v1/models` advertising its media capability, and a second rescan adds nothing (add-only, idempotent — existing entries are never re-pointed or removed). FULLY HERMETIC — stubs never load weights, so a config.json + stub safetensors is enough. Hermetic counterpart: `ModelRegistry: rescan absorbs newly downloaded dirs` (model_registry.zig). |
| `./tests/test_load_model_default.sh [port]` | `/v1/load-model` `"default": true` re-points the server's default model — the app's picker hot-switches a running server instead of restarting it, and a hot-load alone leaves the default untouched, so requests that omit `model` (the "mlx-serve" alias: the Claude Code launcher env) and `/v1/models`' default-first sort (what the app's pill/tray read back) stayed on the OLD model. Three arms: a side-load WITHOUT the flag leaves the default alone (media-gen loads must never steal the chat default); `default:true` promotes; an omitted-`model` chat request routes to the new default (told apart by the echoed `model_type`). NEEDS REAL MODELS — only a load that SUCCEEDS may be promoted, so stub dirs can't reach the branch; skips when `BOOT_MODEL`/`SWITCH_MODEL` (LFM2.5 + Qwen3.6-27B by default) are absent. App counterparts: `AppStateUseModelTests` (`modelSwitchAction` — hot-switch by ABSOLUTE PATH, restart only mid-boot), `APIClientLoadModelTests` (`loadModelBody` — the flag rides switches only). |
| `./tests/test_launch_cmd.sh [model_dir] [port]` | `mlx-serve launch <agent>` (issue #188): unknown agent errors naming the choices; dead server + `--no-start` instructs `mlx-serve serve` (never a silent hang); against a live server, `--print` scripts for omp/codex/claude export the right dedicated-dir env vars (`PI_CODING_AGENT_DIR` for omp — the OMP_ rename never reached the env read) and the written configs carry the server's ADVERTISED context, never a hardcoded one (omp `contextWindow`, codex `model_context_window` + `wire_api = "responses"`, claude's derived `CLAUDE_CODE_MAX_OUTPUT_TOKENS`); extras after `--` ride the invocation line shell-quoted. Needs any small chat model (default Qwen3.5-0.8B); SKIPs without one. Hermetic counterparts: builder/budget/arg-parse tests in `launch.zig`; the Swift twin is pinned by `CLISetupInstructionsTests`. |
| `node --test tests/html_console_test.mjs` | Pure decision layer of the console (`src/html/app.js`), no DOM. Plumbing: capability filtering (Speech must exclude music backends — they advertise BOTH `audio` and `music`), SSE frame cutting across arbitrary chunk boundaries (`\r\n` frames, comments, `[DONE]`, one byte at a time), request-body/`FormData` construction, `?api_key=` passthrough. **Agent layer** — every one of these is a live failure driven out in a real browser: ONE media generation per user turn (`ctx.mediaUsed`; a 2B answered one request with four GPU generations) refused in a sentence the model can act on; `edit_image`'s `model` enum and its resolution are the SAME list (`editableIds`, mirroring the server's name-based `mage_flow.dirIsEdit` gate — the enum used to offer txt2img checkpoints and the model picked one); candidate ranking by usability (resident → sized → unsized → `error`, since a null `bytes_on_disk` is an incomplete download — the broken Qwen3-TTS bf16 dir sorted first and burned a load attempt every time); `systemPrompt` carries the base URL, real request fields and the model inventory (every gap the prompt leaves, the model fills with invention). **Shell layer**: chat history for Recents (`chatTitle`, `storableTurns` — image payloads never reach localStorage or one 1024² PNG evicts every other conversation, while `tool_calls`/`tool` results survive so a reloaded chat can be continued — `historyUpsert` capped by count AND size); markdown rendering built from ESCAPED input (a `<script>` survives as text even inside a fence; only `http(s)` produces an `href`); and `addTimings`/`formatTurnStats`, which read the SERVER's timing block because a client cannot time our own stream — with `tools` present the server buffers and flushes at the end, so wall-clock decode is ~0 and the console reported 937 tok/s on a 2B. Plus a static wiring cross-check both ways: every id `app.js` reaches for exists in `index.html`, and every rendered control is read by `app.js` — the class no HTTP assertion can see, since a typo'd id leaves a silently dead button on a page that still renders and still serves. **Voice layer**: `speakableChunks` strips markdown instead of reciting it (a fence becomes "(code block)", a bare URL becomes "a link", `**bold**` loses its stars) and splits on sentence ends so playback starts on sentence 1 — including the UNTERMINATED fence a cut stream leaves; the runt-merge bar is 8 chars, because a generous one flattens "Yes indeed!" into the previous sentence; chunks are capped at 300 chars since Kokoro's 510-token context 400s rather than truncating. `voiceNext`/`micShouldRun` pin the state machine whose audible failure is a half-state — the mic live during playback makes the page transcribe the assistant and answer ITSELF — so `speaking:transcript` is inert, `thinking:error` returns to listening (never a dead end), and `disable` wins from every state. `speechBody` never sends `voice` and `ref_audio` together (each is a named 400 on the other backend). Same pattern as `tests/metrics_panel_test.mjs`. |
| `./tests/test_metrics.sh [model_dir] [port]` | Observability (`--metrics`): 503-when-off on `/metrics` + `/metrics.json`; Prometheus text shape (`vllm:`/`mlx_serve:` names, TTFT histogram +Inf bucket, gauges); counters/histograms increment after one chat; live-gauge invariants (`generation_tokens_live > 0` after a request, `== generation_tokens_total` at rest); `/metrics.json` JSON shape; and `GET /` panel present-with/absent-without `--metrics`. No admin/auth surface (retired). Hermetic counterparts: `metrics.zig` unit tests (incl. `liveGenerationTokens`), `scheduler.zig` `sumInflightGeneratedTokens`, `status.zig` `getAppMemFootprintMb`, `server.zig` `apiKeyAuthorized`/`ipIsLoopback`. |
| `./tests/test_prefix_cache_disk.sh [model_dir] [port]` | SSD prefix-cache persistence: restart round-trip (persist → kill → reboot → `[disk-cache] restored` + byte-identical output + ≥2x TTFT win), chunked on-disk layout, multi-turn append, `--prefix-cache-disk off`, and a **hybrid section** (Qwen3.5-0.8B GatedDeltaNet — persist SSM checkpoints → restart → `restored … (ssm@pos)` + byte-identical). Isolated HOME — never touches the real kv-cache. Hermetic counterparts: DiskTier round-trip/extend/GC/salvage/flush-cap + hybrid SSM round-trip/retention/salvage/flush-cap + v4 spec-snapshot round-trip (dflash/MTP sidecar, v3-restores-clean) tests in `kv_disk_cache.zig`, two-tier restore/invalidation + hybrid two-tier restore/RAM-beats-disk + spec-snapshots-survive-restart tests in `prefix_cache.zig`. The live dflash disk arm is `tests/test_dflash.sh` [12]. |
| `./tests/test_turboquant_equivalence.sh [model_dir] [port]` | TurboQuant smoke (non-NaN, ≥5 tokens, first-N diff vs dense). |
| `HY3D_MODEL=<dir> ./tests/test_3d_gen.sh [port]` | Hunyuan3D shape gen (`.mesh` modality): headless boot → load-by-path → `"3d"` capability → `/v1/3d/generations` (small res + few steps) → GLB magic/JSON-chunk/vertex validity → 400 on missing image → SSE streaming → chat coexistence → unload. SKIPs without a converted model (`tests/convert_hunyuan3d_weights.py`). Hermetic counterparts: manifold/Euler/normal tests in `marching_cubes.zig`, parse-back tests in `glb.zig`, config/sigmas/Fourier/MoE-gate tests in `hunyuan3d.zig`; parity via the five `HY3D_*` oracles (`tests/dump_hunyuan3d_fixtures.py`, fp16 build). |
| `./tests/test_3d_paint.sh [port]` | Hunyuan3D TEXTURED gen: headless boot → `"texture": true` on `/v1/3d/generations` → GLB has TEXCOORD_0 + PBR material (baseColor + metallicRoughness) + embedded PNGs, UV accessor count == POSITION count → SSE streams the 8 `paint-*` stage labels. SKIPs without BOTH converted models (shape + `tests/convert_hunyuan3d_paint_weights.py`). Hermetic counterparts across uvwrap/rasterize/bake/texinpaint/glb + the `HY3DP_*` oracles. |
| `TTS_MODEL=<dir> ./tests/test_tts_clone_cache.sh [port]` | Speaker-embedding cache (voice-clone path, `docs/qwentts-cache.md` Option A): same clip+text twice → byte-identical WAVs + a `speaker embedding: cache hit` engagement line (output equality alone can't see a silent fallback); different clip → different WAV (no key-collision wrong-voice hit); `MLX_SERVE_TTS_SPK_CACHE=0` boot → byte-identical to the cache-on output with NO hit line (the kill switch restores the uncached path exactly). Synthetic deterministic clips — needs only a Qwen3-TTS `-Base` checkpoint (defaults to `~/.mlx-serve/models/mlx-community/Qwen3-TTS-12Hz-*-Base-*`), SKIPs without one. Section [5/5] pins the `warm_only` pre-warm (miss→hit, first synthesis after a warm is a cache hit, byte-identical WAV, named 400 without ref_audio). Hermetic counterparts: `spk cache` unit tests in `src/tts.zig` (LRU mechanics + the warm-path gated test) + the TTS_TEST_MODEL-gated bit-identity/skipped-re-embed test. |
| `TTS_MODEL=<dir> ./tests/test_tts_fastpath_equivalence.sh [port]` | Qwen3-TTS fast-path guard, TWO contracts over three boots. Bit-exact set (GPU-chained lazy predictor — 1 sync/frame instead of 16 — + fused SwiGLU/QK-norm+RoPE/residual+RMSNorm): the banded-levers-killed boot must be BYTE-IDENTICAL to the everything-killed boot (plain AND cloned voice). Banded set (KV-cached predictor steps `MLX_SERVE_TTS_CP_CACHE`, compiled chain `MLX_SERVE_TTS_COMPILE` — reduction-order deviations, −49%/frame): the full-default boot must stay within duration/energy bands of composed (energy banded on the CLONE pair only — an unconditioned `-Base` plain voice legitimately varies rendition to rendition). Every lever must ENGAGE on its boot and show NO engagement under its kill switch — both directions of the expectNoSpec class; the engagement lines are also what caught a zsh `env $VAR` harness bug that silently ran a "composed" A/B arm as the fast path. SKIPs without a `-Base` checkpoint. Hermetic counterparts: `gpu predict` + `compiled chain` + `cached predictor` tests in `src/tts.zig` (lazy==eager sampling; TTS_TEST_MODEL-gated equivalence/band tests at temp 0 and 0.9, greedy agreement 1.0000 measured). |
| `KOKORO_MODEL=<dir> ./tests/test_kokoro_tts.sh [port]` | Kokoro TTS end-to-end (`.audio` third backend): discovery → load-by-path → WAV validity (RIFF, non-silent, bounded) → distinct voices + comma-separated BLEND that is not an alias of its first voice → speed scales duration → the named-400 surface (`ref_audio` on a non-cloning backend, unknown voice, one bad voice in a blend, speed out of range, empty input, music endpoint, chat endpoint) → SSE → **text normalization reaching the audio** (Kokoro's vocab has NO digits, so an unexpanded "42" is silently DROPPED by the encoder rather than erroring — the test compares durations with and without the number). NOTE the harness builds each JSON body into a variable first: `check "l" "$(status "{\"a\":1}")" "400"` mis-parses in bash (arg vector came out 5 words instead of 3, so every assertion compared garbage while the server was correct). SKIPs without a converted checkpoint. Hermetic counterparts: ~30 unit tests in `src/kokoro.zig` + `src/kokoro_g2p.zig`; parity via `KOKORO_FIXTURES` (`tests/dump_kokoro_fixtures.py`): durations EXACT, f0/n/asr cos 1.000000, audio cos 0.9968 against a measured reference self-similarity floor of 0.9941–0.9960. |
| `ACESTEP_MODEL=<dir> ./tests/test_music_gen.sh [port]` | ACE-Step music gen (`.audio` second backend): headless boot → load-by-path → `"audio"`+`"music"` capabilities → `/v1/audio/music-generations` (10 s) → WAV validity (48 kHz stereo PCM16, right duration, non-silent) → `ref_audio` engagement → the source-audio TASKS: `complete` (the first arm's WAV as `src_audio` + `track_classes`, asserts the `[acestep] complete: 250 source frames … classes=BASS | DRUMS` line and that the output is the SOURCE's length, not `duration_seconds`) and `cover` (SKIPs when the pack has no `fsq.safetensors`; asserts `-> 50 codes`, the `cover_strength<1` condition-set switch and the `cover_noise_strength` start-point lines, source length kept) → 400s (missing prompt, bad duration, cover without `src_audio`, `src_audio` on text2music, unknown task, unknown track class, TTS-endpoint-on-music-model) → SSE progress+complete → chat coexistence → unload. SKIPs without a converted model (`tests/convert_acestep_weights.py`; `--fsq-only` adds the cover file to an existing pack). Hermetic counterparts: schedule/DCW/prompt-format/task-instruction/FSQ-grid/nearestStep tests in `acestep.zig`; parity via the `ACESTEP_*` oracles (`tests/dump_acestep_fixtures.py`; 8/9/10 = tokenizer indices (near-tie acquittal on the reference's `z`), detokenizer cos + rms, cover DiT velocity; 11 = `--cover-clip <wav>`: the WHOLE cover pipeline on a real clip vs the reference's 8-step cover, bar 0.95 — the one that caught the 8-bit FSQ file (0.64 → 0.985 dense)). |
| `MUSIC3_MODEL=<dir> ./tests/test_music3_gen.sh [port]` | MiniMax Music 3 gen (`.audio` third backend): headless boot → load-by-path → `"audio"`+`"music"` capabilities → the named-400 family FIRST (missing prompt/lyrics, duration/steps out of range, and each ACE-Step-only field — bpm/keyscale/timesignature/vocal_language — refused BY NAME with a message that names the field, plus TTS-endpoint-on-music-model) → `/v1/audio/music-generations` (8 s, 8 steps) → WAV validity (44.1 kHz stereo PCM16, duration ≤ requested — an UPPER bound, the LLM may stop early — non-silent) + `[music3] AR stage` engagement in the log → SSE `frames`/`diffuse` progress + base64 complete → chat coexistence → unload. SKIPs without the pack (default `ddalcu/MiniMax-Music3-MLX-Serve-8bit`; `scripts/convert_music3_weights.py`). Hermetic counterparts: prompt-assembly byte tables (pinned against the reference Python regexes), chunk/latent/flow-time/uncond-ids math in `music3.zig`; parity via the `MUSIC3_*` oracles (`tests/dump_music3_fixtures.py` — run on the 128 GB Mac, fp32 CPU). |
| `SOAK_DURATION_HOURS=N ./tests/test_soak_24h.sh [model_dir]` | Plan 01 A8 — 4-way concurrent soak (chat/agent/Anthropic/tool). RSS drift bounded; default 24h, set `SOAK_DURATION_HOURS=1` for CI smoke. |
| `./tests/bench.sh [--only <substr>] [--url <host:port> -m <id>] [--full]` | THE bench. Boots mlx-serve (boot → probe → kill → settle, one model at a time) while **llmprobe** takes every number — `--bench-only`, `--full` for median-of-3 per rung to 64k. Each cell is mlx-serve at its FASTEST: `--mtp` is forced wherever the checkpoint ships a head (default-OFF on MoE targets is exactly where it pays), everything else is already default-on. The engaged mode is read off the server's own `[spec-stats] mode=` lines and printed beside the number, so a mode that silently stops engaging shows up as a bare cell. Another engine = start it yourself and pass `--url host:port -m <id>`: same script, same probe, and nothing about their binaries/ports/versions lives here (the LM Studio daemon that must never be killed is why). Output is paste-ready markdown rows for `benchmarks.md`; reports + server logs land in `~/claude-tmp/bench-<tag>/`. No CSVs, no charts. |
| `./tests/bench_laguna_xs.sh [--cells serial\|unconstrained] [--runs N] [--label L] [--no-csv]` | Laguna XS 2.1 NVFP4 decode/prefill bench, mirroring the mlx.fast challenge's frozen timed window (512-token prompt, 128 decode steps, temp 0) so numbers are directly comparable to the Layr-Labs/mlxfast-challenge tree. Reports decode in **ms/token** (their unit) from the SERVER's own `timings`, never client-side stream timing. Two cells, never merged: `serial` (PLD/drafter/MTP all off — their non-speculative rule, the only cell that may be compared to them) and `unconstrained` (mlx-serve's best config). Per-request nonce defeats the prefix cache and the script FAILS a cell whose `cached_n` exceeds the chat-template header. Writes `~/claude-tmp/bench-laguna/laguna-xs-<date>.csv`; `--no-csv` for kill-switch A/B iteration. Sub-token probes for when a number here looks wrong: `MLX_SERVE_DECODE_FWD_UBENCH=N` (N decode forwards with no sampling/detok/HTTP around them, split into CPU graph-build vs GPU eval, ops/forward, plus a terminal-only lm_head ablation), the `diagProjBench` rung ladder it fires (bare q/k/v/o matmul -> whole layer -> the SHIPPED attention function, in both issue orders — the rung where the reconstruction DISAGREES with the real function is what localizes a bug the op count cannot see), and `MLX_SERVE_LAYER_CAP=N` for a slope-vs-fixed-cost sweep. This is the ladder that found the f32-mscale promotion (3.03x on Laguna XS); story in `docs/gotchas/engine-mlx.md`. |
| `tests/fwd_ubench.sh <model-dir> [iters] [flags...]` | Diagnostic, NOT a test. Boots one model, fires `MLX_SERVE_DECODE_FWD_UBENCH` at load and prints the ms/forward + CPU-build/GPU-eval split + ops/forward + terminal lm_head ablation, then shuts down. The unit of every decode-perf A/B in this repo: run it once per boot per arm and ALTERNATE the arms per boot (a block-of-N schedule reads drift as an effect). Companion env probes: `MLX_SERVE_LAYER_CAP=N` (slope vs fixed cost) and `MLX_SERVE_DISPATCH_PROBE=N` (prices one GPU dispatch by injecting N exact-1.0 multiplies per MoE layer — pure and consumed, so MLX can't elide it and nothing is mutated). |
| `tests/greedy_ab.sh <model-dir> <ENV_VAR> <off-value> [max_tokens] [prompt]` | The acceptance bar for any change that claims to preserve output: boots the same model twice, once per arm of a kill switch, and diffs the temp-0 chat continuation. Reports `completion_tokens` and `finish_reason` so a short/early-stopped generation can't pass as agreement — a 53-byte completion proves nothing. Use ≥ 200 tokens; the numerics classes this catches (INT4 near-tie argmax, a 1-in-65536 activation rounding) surface between tokens 50 and 100, not at token 5. |
| `tests/prefill_ab.sh <model-dir> <ENV_VAR> <on> <off> [reps]` | Prefill-time A/B for a kill switch, reading the SERVER's `timings.prompt_ms` rather than client wall clock. Boots on/off/on/off so a drifting machine cannot land on one arm. Each request carries a nonce so the prefix cache cannot serve the body, and a reading whose `cached_tokens` exceeds the chat-template header (~40) is FLAGGED — a cached prefill is not a prefill. Used to retire mlxfast item 1: the fused-QKV prefill hypothesis measured null, with the two blocks contradicting each other. |
| `python3 tests/qwen38_imatrix_collect.py --out <file>` | Collects the per-input-channel activation statistics the iQ-MLX Qwen3.8 pack is calibrated on, on the bf16 SOURCE weights (an imatrix is only valid for the weights it was collected on). Wraps every `nn.Linear` in the mlx-lm tree and accumulates `sum(x**2)`; keys the output by SOURCE weight name. Corpus is local and seeded — real captured tool traffic (`src/fixtures/tool_traffic.jsonl`) with the model's own chat template and tool schemas, this repo's sources and docs, SWE-bench Lite problem statements and patches, and generated arithmetic — split by `split_docs`, which withholds every tenth document so `qwen38_iq_battery.py` has a genuine holdout. Asserts COVERAGE against the checkpoint's own weight list at load, before any corpus work: a wrapped-module walk that misses a weight would silently leave it uncalibrated. ~54 GB resident, nothing else large may run. |
| `python3 tests/qwen38_iq_allocate.py measure\|allocate` | `measure` scores every quantizable weight at each `(bits, group_size)` in {2,3,4}x{64,128} by imatrix-weighted reconstruction error (`weighted_affine_quant` from `dsv4_imatrix.py`), on a random sample of output rows — the metric is a mean over rows and concentrates, and the CHOSEN width is applied to the full tensor by the converter, never here. `allocate` spends a byte budget greedily, best error-reduction-per-byte first, from a floor, with attention/GDN-gates/MTP pinned at 4-bit gs-64. Split so re-running at a new budget is seconds: the phase loop is "battery fails -> raise the budget -> allocate again". `--objective rel` (default) divides each tensor's error by its own signal energy before weighting by size, because activation scale spans 6e4 across depth on this checkpoint and an absolute objective starves the early layers; `abs` is the llama.cpp-style alternative. Measured: at a fixed 8.6 GB the two land within 0.4 points of each other, so the budget binds, not the objective. |
| `python3 tests/convert_qwen38_flash_next.py --dst <pack> --stage <dir>` (or `--src <local hf dir>`) | Qwen3.8-Flash-Next (`qwen4_exp`) → the pack mlx-serve loads, STREAMED from the Hub one HF shard at a time (download → quantize → delete; resume-safe via `.convert_state.json`), so the 360 GB bf16 never lands. Experts 4-bit gs64, everything else 8-bit, embed 4-bit, the 51B n-gram table 4-bit gs32 into ONE `ngram_table.bin` (safetensors-format, `.bin` so the directory loader never mlx-loads it — the engine gathers rows from the mmap). Folds `+1` into every `Qwen4ExpTextRMSNorm`, transposes depthwise convs, splits `experts.gate_up_proj`, skips the vision tower in the stream. `--add-vision --dst <pack> --stage <dir>` (or `--src`) then fetches ONLY the HF shard holding `model.visual.*`, writes them bf16 pass-through into `model-vision.safetensors`, merges the index and restores `vision_config` — idempotent, no re-stream. |
| `QWEN4_MODEL=<pack> ./tests/test_qwen4_exp.sh [port]` | Qwen3.8-Flash-Next live end-to-end on the converted pack (booted `--max-concurrent 4 --prefix-cache-entries 0`; 38 checks): [7b] the image turn under `enable_mtp` (head reads the slot's M-RoPE table: one more `[spec-stats] mode=mtp` + tie-aware == the serial image answer; the tie helper compares lead-STRIPPED text — the first logprob token is `" House"` while `content` is lead-trimmed, which read as a 2-nat divergence at token 0), [11] a SECOND boot `--no-vision` (tower load line absent, `capabilities` drop `vision`, `active_bytes` lower than the post-image sample by >= 400 MB, text answers, the image turn is a 400 naming the tower — it was a 200 with a hallucinated answer). Then: [8] two concurrent 400-token plain requests → `[batched] gdn batched decode engaged (slots=2)` + each answer == its own serial answer (30 tokens, tie-aware ≤0.15 nats), [9] the same with two ~3k prompts (batched decode under the per-slot QSA mask, both needles), [10] an `enable_mtp` request beside a plain one (MTP slot exclusive, plain interleaves; exactly one more `[spec-stats] mode=mtp`, both == serial). `BATCHED_TEST_MODEL=<pack> ./tests/test_batched_equivalence.sh` runs on this arch too (forced N=1 byte-identical + two-stream arm; measured 4/4 on the 4-bit pack). Hermetic twin: `qwen4 batched decode: one forwardMoeBatchedDecode tick == two serial ticks` (tiny fixture, cos 1.00000, per-slot PLE history / QSA key rows / pooled blocks equal). Then: architecture + n-gram-table boot line, greedy short answer, tool call + args, streaming cleanliness, a prompt past the 2048-token QSA budget asserted through `[qsa] sparse attention engaged` + `[qsa-fused] engaged` (the mask arm of `msv_attn_p256`) + a needle, [5b] the same prompt under MTP (booted `MLX_SERVE_MTP_FORCE_DEPTH=3`) asserting the needle + `[sdpa-split] masked arm engaged` (verify rows 3..15 under an array mask were MLX's unfused fallback), and the MTP head (`enable_mtp:true` on this MoE target): `[spec-stats] mode=mtp` engagement, greedy MTP == serial, acceptance floor 0.5/round; [7] an image turn on `house.jpeg` asserting 200 + a relevant word + the `Vision encoder: Qwen3-VL ViT` load line + the `M-RoPE: 1 images` engagement line (SKIPs when the pack has no `model-vision.safetensors`). The `qwen4-exp` row of `test_vision_all.sh` runs the same turn in the arch-dispatch sweep. SKIPs without the pack. Hermetic counterparts in transformer.zig: `gdn packed prework: bit-identical to the composed chain at S 1..9 …`, `gdn norm-gate fused: bit-identical to rms_norm + silu(z) * y …`, `gdn gate: compiled closure vs graph chain vs prework kernel (bit-identity probe)` (pins the assumption the fused GDN decode rests on), `ssm checkpoint carries the qwen4_exp aux state … through capture and restore` (prefix cache on qwen4), plus the extended `DiskTier: hybrid entry round-trips` in kv_disk_cache.zig (`l{d}.aux/pooled/ple` + `qsa_ratio`). Decode meter: `MLX_SERVE_DECODE_FWD_UBENCH=N` (+ `_S=<rows>` verify width, `_KV=<tokens>` prefill first). |
| `./tests/test_vision_prefix_cache.sh [model] [port]` | Image conversations reuse the prefix cache (vision slots were excluded from commit AND lookup, so every image turn re-prefilled the conversation). A Harness-style active turn still processes media when user-role context messages follow the human's image, places its soft tokens at that message's marker, and builds Qwen M-RoPE from the same message; a text continuation after an assistant boundary does not reprocess historical media, while explicit `continue_final_message` still sees current media before its trailing assistant prefix. Same image twice → `cached_tokens > 0` + `[hot-cache] reused`, warm == cold bytes, the answer still names PIXEL-ONLY content (the street signs' "Grey Fox" / "Waterfall"); a DIFFERENT image under the same placeholder tokens → `cached_tokens 0` (`vision_key`); a new image-bearing turn on the first image restores its image prefix and reads the stop sign. Defaults to Qwen3.5-0.8B; SKIPs without a vision model. No byte-equality across warm/cold (hybrid restore class: one token moved on qwen4). Measured on qwen4_exp 2026-08-26: 483/514 reused, foreign image 0, follow-up 483/508. Historical image reconstruction on a later text-only turn is a separate multi-message feature. Hermetic counterparts: the `vision_key` arms of `HotPrefixCache: findBestMatch` (prefix_cache.zig) + `vision prefill checkpoints SSM state only when it chunks like text` (generate.zig — the first live run was a `hybrid miss` because vision prompts never checkpointed). |
| `tests/dump_qwen4_exp_fixtures.py build\|dump` + `QWEN4_TEST_MODEL=<pack> QWEN4_FIXTURE=<file> zig build test -Dtest-filter="qwen4 fixture"` | The qwen4_exp engine oracle on a TINY random model (torch venv with transformers main): `build` writes a random checkpoint in the real naming, the converter packs it, `dump` runs the HF reference on OUR dequantized weights (input ids, full-prefill logits, prefill+stepwise-decode logits past the QSA budget, per-layer streams, layer-0/1/3 sub-module captures, the reference n-gram row ids and QSA mask). The Zig test prints a per-layer bisect ladder and asserts cos > 0.995 + argmax agreement on full prefill AND on chunked prefill + 6 decode steps, the MTP head against a torch rendering of the vLLM/SGLang math (synthetic `mtp.*` in the tiny checkpoint), verify→rollback(accept 1)→continue == fresh forward (GDN + PLE + QSA state), and the MTP head's CROSS-ROUND bookkeeping (prefill history → two rounds of chain draft → capturing verify → accept 1 then 0 → trunk rollback → Phase 5a stash → consume-time `qwen4MtpTruncate` → the third round's merged head forward == a fresh head over every committed row, cos 1.00000; head KV + QSA aux keys + pooled blocks past the tiny budget of 8). `build --vision` / `dump --vision` add a tiny tower (depth 2, hidden 64) and dump ONE image prompt (`pixel_values` + grid, the tower's `pooler_output`, 3-D `position_ids` + `rope_delta`, full/prefill/decode logits, streams, the layer-3 QSA mask); the converter packs it with `--add-vision --src`; `QWEN4_FIXTURE_VISION=<file>` runs `qwen4 fixture vision`: tower rows vs the reference, `getRopeIndex` == the reference table, full prefill cos > 0.995 + argmax, the layer-3 QSA mask EXACT (pooled block keys at 3-D block-start angles; scalar-roping them red-probes at 32 mismatches), a two-chunk prefill split inside the image span (bar = our own full forward) + 6 decode steps past the table. Both dumps run every expert (`num_experts_per_tok = num_experts`) and emit the reference's own per-row `qsa_gap` / `logit_margin` / `route_gap` (+ `mtp_route_gap`); `Qwen4Ties` acquits rows under 3% (5% for a routing margin). `build --topk 2` makes a k < E pack (`~/claude-tmp/qwen4-tiny-k2/`) whose trunk ties in 18/20 rows across 8 MoE layers — its SELECTION coverage is the MTP head's one MoE layer (12/19 decisive, cos 1.0000); run it with the same env vars, not in CI — a random tiny MoE ties everywhere and our kernels flip one pick per prompt at any router scale or seed (story in models-media.md). Hermetic twin: `mrope fillCosSin strided rows == every stride-th row` in mrope.zig. SKIPs without the env vars. Acceptance meter: `MLX_SERVE_MTP_FORCE_DEPTH=n` (every round drafts n, controllers never demote) + `acc_idx=a0/a1/…` on the `[mtp-trace]` line (hermetic: `MtpTrace per-index acceptance` in generate.zig); harness `~/claude-tmp/qwen4-mtp-prof/acc.sh <tag> <pack> <depth>`. |
| `python3 tests/convert_qwen38_iq.py --src <bf16> --dst <out> --alloc <json>` | The calibrated converter. Per-weight widths from the allocation table, `weighted_affine_quant` where an imatrix entry exists and plain `mx.quantize` where none does (embedding table, MTP head), logged per weight. Carries the three load-bearing raw-checkpoint fixes (the `+1` fold into the delta-encoded norms, the depthwise conv1d transpose, and — moot here — the channels-last Conv3d patch embed) and DROPS the vision tower, including `vision_config`, since `has_vision` is set from its presence. `--verify` re-solves every written weight's `(bits, group_size)` from packed geometry the way `transformer.affineParamsFromGeometry` does and asserts it matches the allocation: mlx-serve reads GEOMETRY for an affine weight at every hinted call site, never the config block, so that is the property that has to hold. Named `convert_*.py` so `test_model_card_frontmatter.sh` discovers its embedded `README`. |
| `python3 tests/qwen38_iq_battery.py reference\|tokens\|agent\|perf` | The quality gate, run on the SHIPPED 4-bit and 8-bit packs first so the bar is those two points rather than an invented threshold. `tokens` = top-1 agreement + mean KL against the bf16 source over held-out windows. `agent` = the longest run of consecutive IDENTICAL tool calls over a fixed multi-round loop; results must depend on the ARGUMENTS or the metric measures nothing — with one canned answer per tool name the shipped 8-bit pack scored WORSE than the 4-bit one, because re-reading is rational when every path returns the same bytes. Report rounds-to-answer beside it: a collapsed build fails by making no tool calls at all, which scores a perfect 1 on repeats. `perf` = decode with and without MTP plus `[spec-stats]`, which is where sub-4-bit weights show their cost (they fall outside `verifyQmmLane`, a 4/5/6-bit specialisation). **mlx-lm is not a valid oracle for our packs unmodified**: it folds the `+1` into Qwen3.8's delta-encoded norms whenever it sees an MTP head, so it double-folds on anything our converter produced — the production 4-bit pack scored 0.2% top-1 against its own source before `_install_sanitize_fix`. |
| `./tests/test_website_pages.sh` | Static checks for the `website/` GitHub Pages site (moved out of docs/ 2026-07): per-slug index.html existence + SEO contract (title/description/canonical/OG/JSON-LD/shared css+js), every relative href/src resolves (screenshots report PENDING), sitemap+robots coverage, homepage internal links. Hermetic — no browser, no server. |
| `./tests/test_resource_xattrs.sh` | Checked-in `app/Sources/MLXServe/Resources/*` carry none of the extended attributes that break code signing (`com.apple.FinderInfo`, resource forks, quarantine) — an asset dragged out of Finder brings them, and codesign then refuses the ENTIRE bundle with "resource fork, Finder information, or similar detritus not allowed", three minutes into a build and naming the binary rather than the file. Deliberately ignores `com.apple.macl`/`com.apple.provenance`: macOS adds those to any file a sandboxed app touches, every resource has always had them, and they sign fine. Hermetic — one `xattr` call per file. |
| `./tests/test_release_workflow_gates.sh` | Static guard for `.github/workflows/release.yml` event gating: PR trigger present, fork-PR job guard, Create Release + Homebrew push gated OFF for `pull_request`, tag creation dispatch-only, notarize steps NOT excluded on PRs, artifact upload covers PRs. Pins the class "a PR build accidentally releases" / "PR DMGs silently stop being notarized". Also pins `app/build.sh`'s CalVer timezone + license packaging, and the whole of `FAST_DEV` — the same class one layer down, "an iteration lever becomes a shipping mode": FAST_DEV defaults to 0, the debug Swift configuration sits directly under its gate while the else arm still compiles `-c release`, `rm -rf "$APP"` stays on the release path, the zig optimize mode defaults to ReleaseFast in exactly one place with the single `-Doptimize=Debug` directly under the `ZIG_DEBUG` gate — which is FAST_DEV-only, refused by name, and exits before any build work (mutation-checked three ways: FAST_DEV implying Debug, the refusal deleted, the refusal warning without exiting) — framework fixups + signing are skipped only together with the staging that invalidates them, and the FAST_DEV `exit 0` lands after the app is signed and before notarytool/productbuild/create-dmg. Red-on-revert verified against the draft that reindented the script and replaced notarization + the App Store .pkg with `# ... (rest of the block is identical)`. Hermetic — PyYAML parse, no runners. |
| `./tests/test_mlx_staged_nax.sh` | Static guard for the self-built mlx + mlx-c stage (`scripts/build-mlx.sh` from the pinned submodules): staged dylibs + metallib + `.version` stamp exist, the metallib actually contains `*_nax` kernels (MLX's 26.2 CMake gate fails SILENTLY — the brew bottle ships NAX-off), `libmlx.dylib` minos ≥ 26.2, `libmlxc.dylib` links the staged mlx (no `/opt/homebrew` refs), and a built `zig-out/bin/mlx-serve` links no brew mlx/mlx-c. Hermetic — no GPU, no server. |
| `./tests/test_model_card_frontmatter.sh` | Static guard for the HF model cards our converters generate (`README` constant pulled by `ast`, never imported — these scripts import mlx): frontmatter parses, declares `base_model`, declares `base_model_relation: quantized`, declares an explicit `library_name`. Pins the class "HF DEFAULTS a missing `base_model_relation` to `finetune`" — a quantized mirror that omits it is published as a finetune of its base model, silently (both Mage-Flow mirrors shipped that way, live 2026-07-25). Hermetic — no weights, no network. |
| `DSV4_TEST_MODEL=<mirror dir> ./tests/test_dsv4.sh [DSV4_TEST_PORT=11351]` | DeepSeek-V4-Flash NATIVE live end-to-end on the ~115 GB tail4 mirror (env-gated; `DSV4_TEST_SKIP_PREFLIGHT=1` is how it runs on the 128 GB box): step [0] template A/B vs the release's `encoding_dsv4.py` (17/17 byte-exact, when the encoder is staged), greedy raw-completion prefix (oracle ground truth), chat on both thinking arms, streaming delta cleanliness, DSML tool call + round-trip, and step [7] **single-flight admission** — a second concurrent request must queue, never clobber the module-owned `dec_state` (greedy solo baseline vs the same request with a marker chat fired mid-generation: byte-equal long output, marker answered after queueing, no cross-request leak; pre-fix RED literally leaked the marker word into the other stream — the live 2026-08-02 app-chat-into-pi incident). Booted `--pld --mtp` to prove the dsv4 spec hard-off guards hold. Hermetic counterparts: DSV4_MINI fixture/decode-equivalence gates, `admitPendingTick` contract + wiring-scan tests (scheduler.zig), corpus family "dsv4-dsml". SKIPs without the env var. |
| `MUSE_TEST_MODEL=<mirror dir> ./tests/test_muse_glimmer.sh [MUSE_TEST_PORT=11351]` | Muse-Glimmer-30B (`muse_glimmer`) live end-to-end on the 33 GB 8-bit mirror (env-gated): tokenizer digit grouping ("84" one token — the per-digit "8 4" echo class, live 2026-08-10), channel split (to=self reasoning / to=user content, no marker or ` to=` header leaks) with thinking explicitly ON, the committed ` to=user<|message|>` channel on a SILENT no-tools request (content, NO reasoning_content — no reasoning pass runs at all), ATEM tool call with typed args + round-trip, and streaming delta cleanliness. Asserts INVARIANTS, branches on the model's choice (always-thinking checkpoint). Optional `MUSE_TEST_DRAFTER=<assistant dir>` boots with the DFlash sidecar so the whole suite runs with speculation engaged. Hermetic counterparts: byte-pinned render test, corpus family "muse_glimmer", channel/ATEM/llama3-pretokenizer unit tests. SKIPs without the env var. |
| `./tests/test_qwen_video_input.sh [model] [port]` | Qwen video input (4 distinct frames as one `video_url`): ≥ 2 of the frames' subjects named AND something only the PIXELS supply — frame 3's sign text "STOP" / "Grey Fox" / "Waterfall" (the generic-subject bar passed for weeks on a build that encoded the frames and never spliced them). On a checkpoint that cannot read the signs this is a checkpoint expectation, not a wire failure (qwen4_exp reads them; the 0.8B default is unverified). |
| `./tests/test_vision_all.sh [port]` | Vision ARCH-DISPATCH sweep — BREADTH, the guard for shared-path edits (image decode branch, `VisionPreproc`, encode dispatch). One image turn per vision code path (gemma SigLIP / gemma4_unified encoder-free / Qwen3-VL / Muse-Glimmer / LFM2-VL), asserting BOTH a relevant answer AND the encoder's own boot line, so a change that silently routes one arch into another's encoder cannot pass on the answer alone. Patch-grid archs additionally get the Gemma-only `x-mlx-pixels` buffer at the size the app emits and must SURVIVE it with a logged refusal (the 2026-08-11 crash: a gridless buffer reached the SigLIP `forward` and dereferenced sentinel weights — verified red-on-revert, and it reproduced on Qwen too, so the class was never muse-specific). Runs EVERY present candidate per arch across all four known model roots (the packs differ in ways the loader must absorb: mlx-community's muse ships the tower DENSE bf16 under bare `vision_tower.` prefixes, ours ships it quantized under `model.`); per-arch SKIP keeps it green on a partial library. gemma-3 is deliberately absent — mlx-community's pack ships SigLIP under HF names (`vision_tower.vision_model.encoder.*`) rather than the Gemma-4 names the loader reads, so it serves text-only; never wired, not a regression. DEPTH stays in the per-arch scripts. |
| `./tests/test_muse_vision.sh [model_dir] [port]` | Muse-Glimmer image input e2e: `/v1/models` vision capability, OpenAI + Anthropic image turns on `tests/fixtures/house.jpeg`, the exact grid/pad-token accounting the splice depends on (730x487 -> 756x504 -> grid 36x54 -> 486 pads), two images in one turn, and `--no-vision` dropping the tower. Defaults to the 4-bit mirror; SKIPs when absent. Parity counterpart: the `MUSE_VISION_MODEL`/`MUSE_VISION_FIXTURE` oracle in src/muse_vision.zig. |
| `./tests/test_lfm2_vision.sh [model_dir] [port]` | LFM2-VL image input e2e. The two things output alone cannot see come from the server's own log: the SPLIT DECISION and the token accounting the splice depends on — a tiled image that silently fell back to one low-res view still answers plausibly, it just cannot read anything small. Sections: the tower's own boot line + the image marker ids resolved from the TOKENIZER (they are not in config.json); a single-tile image landing on the reference's exact geometry (730x487 -> 26x38, 247 tokens); a large image SPLITTING (1800x1400 -> 2x3 tiles + a 252-token thumbnail = 1788, `7 image(s)` encoded separately, all 1788 pads reaching the prompt); and the OCR arm run BOTH ways — 15px print at full size vs the same picture downscaled to the untiled geometry, because this checkpoint reads large text fine at thumbnail resolution and an absolute bar on the tiled arm alone would pass a build where tiling stopped happening (measured 3/3 exact vs 0/3). Plus `/v1/messages`. Defaults to the 3B 4-bit pack on the external drive; SKIPs without it or without PIL. Parity counterpart: the `LFM2_VISION_MODEL`/`LFM2_VISION_FIXTURE` oracle in src/lfm2_vision.zig (`tests/dump_lfm2_vision_fixtures.py`). |
| `MUSE_TEST_MODEL=<mirror dir> ./tests/test_muse_repetition.sh [MUSE_TEST_PORT=11357]` | Muse repetition-loop regression gate: replays the REAL captured pi first-turn request (`tests/fixtures/muse_pi_doom_request.json` — pi 0.84.1 system prompt + 4 tools + the doom-clone task, frozen 2026-08-11) N times and FAILS on any `finish_details.type == "repetition_loop"`, cross-checked against `[loop-stop]` in its own server log; looping bodies dump to `~/claude-tmp/muse-loop/`. Was the TRIGGER (2-3/4 reps at the incident config, 3/3 at greedy) for the rope_local_base_freq root cause — sliding layers rotated at the Gemma-flavored 10000 default instead of muse's one 500000 theta — and is now the green gate. Knobs rewrite ONLY sampling/effort/tool fields on the fixture: `MUSE_REP_REPS` (4), `MUSE_REP_TEMP` (0.7 = the incident's app-launch-flag sampling; 0 = greedy), `MUSE_REP_TOP_P`/`MUSE_REP_TOP_K`, `MUSE_REP_EFFORT` (minimal\|high\|off), `MUSE_REP_NO_TOOLS=1`, `MUSE_REP_NO_DRAFTER=1` (default: the mirror's own `drafter/` auto-probes → dflash engaged), `MUSE_REP_NO_DAQ=1` (bf16 arms). Hermetic counterpart: the `rope_local_base_freq == 500000` assertion in model.zig's muse config test. SKIPs without the env var. |
| `DFLASH_TEST_MODEL=<target dir> DFLASH_TEST_DRAFTER=<assistant dir> ./tests/test_dflash.sh [DFLASH_TEST_PORT=11353]` | DFlash block-drafter live end-to-end (env-gated; Muse-Glimmer-30B 8-bit + its `meta-models/…-assistant` sidecar today): sidecar-kind probe at boot (`DFlash drafter ready`), engagement COUNTS (`[spec-stats] mode=dflash attempts>0` — the regular-decode fallback is output-identical, so equality alone can't catch a dispatch hole), greedy dflash-on == dflash-off over an exact 30-token budget, adaptive-gate invariant (`runtime_disabled` is legal only when `avg_per_round < gate_min`; either engaged/disabled branch may be correct for the prompt), exact non-stream + stream clipping at `max_tokens=17`, per-request opt-out, a tool round, machine-width resolution, and aligned assistant-context prefix restore. Hermetic counterparts: dflash.zig config/mask/forward/cache tests + fixture parity (`DFLASH_FIXTURES`, tests/dump_dflash_fixtures.py), nextDflash greedy equivalence with the economics gate explicitly disabled, all-five pre-commit budget/source guards (generate.zig), shared scheduler publishing/gate/cache policy tests, and specInitWiring/specTickMode arms. SKIPs without the env vars. |
| `DSPARK_TEST_MODEL=<target dir> ./tests/test_dspark_lfm2.sh [DSPARK_TEST_PORT=11357]` | DSpark (LiquidAI LFM2.5) on a target whose `drafter/` subdir holds the sidecar — 2.6B (dense hybrid) and 8B-A1B (`lfm2_moe`) both. Pins the three silent zeros this port shipped through: the sidecar is CLASSIFIED as DSpark (`dspark: markov head rank=` — the split root/`dflash_config` contract), the hybrid trunk no longer VETOES the sidecar (`[spec-wiring] ... dflash=true` — four surfaces re-derive that gate and all four decoded serial with a green boot line), and rounds ENGAGE with accepts > 0 (the anchor-row convention answers 0% while everything looks healthy). Arm [3] is greedy byte-equality against a `--no-drafter` boot on `/v1/completions` — the RAW surface deliberately, since a chat model's reasoning block amplifies one near-tie argmax flip into a different plan and the arm is about the spec path, not the model's taste; the echo prompt keeps acceptance at 96-100% so partial-accept rounds (the conv-state rollback) still happen. Arm [4] proves the Markov chain is LOAD-BEARING by halving: `MLX_SERVE_DFLASH_MARKOV=0` on a NOVEL prompt (an echo prompt drafts fine from the base logits alone, so the comparison has to run where the chain is the only thing carrying it) — measured 30% vs 12%. SKIPs without `$MODEL/drafter/config.json`. Hermetic counterparts: the split-contract + `anchor_row_drafts` + unported-markov-head-type parse tests (dflash.zig) and the sparse-target floor in `dflashGateMinimum` (scheduler.zig). |
| `LING_TEST_MODEL=<mirror dir> ./tests/test_bailing_hybrid.sh [LING_TEST_PORT=11357]` | Ling 3.0 (`bailing_hybrid`) live end-to-end on the 4.2 GB 4-bit mirror (env-gated). Both mixers are new, so the checks are chosen to fail loudly if either is wrong: architecture advertised, short answer, thinking split by DEFAULT (the template's own `thinking_option` is on), GLM-style tool call + round-trip, a needle recovered ~5K tokens back (the check a wrong per-channel gate stride, a mis-strided KDA decay or a wrong MLA rope mode fails while every short prompt still reads fine), prefix reuse returning the SAME answer with cached_tokens > 0 (the hybrid restores recurrent state, not just a KV slice), and streaming deltas free of think/tool/role markup. Hermetic counterparts: the config-parse test + the by-name refusal table + `kdaUsesBoundedGate` in model.zig, the vectorized-gate kernel equivalence, `kdaGateChain`, the dense AND affine asymmetric-KV-cache tests and the TurboQuant pow2 load-time refusal in transformer.zig, `kvBytesPerToken` + the score-vs-stored width bill in server.zig. Live `--kv-quant 8` on this arch is the one path with no hermetic twin for the whole stack (the unit test covers the cache; the arch's forward under quantized KV is checked by hand). SKIPs without the env var. |
| `FLUX_MODEL=<dir> ./tests/test_image_gen.sh [port]` | Image gen end to end on an edit-capable backend (FLUX.2-klein): txt2img PNG + SSE stream, img2img structure retention at strength 0.2, instruction edit (pure-noise start) retention, multi-reference edit + engagement log, conditioning rebalance (count validation + output differs), zero-B LoRA byte-transparency + non-matching-LoRA 400, and the whole 400 surface (bad strength, ref_images outside edit mode, non-array, over-cap). Also covers the OpenAI-compatible `POST /v1/images/edits` multipart surface: PNG in the vendor response shape, repeated `image[]` multi-image, and the named 400s for mask / n>1 / response_format=url / output_format=jpeg / stream / missing image / JSON-posted-to-multipart. The sizeless-edit case SKIPs here by design (FLUX resolves edit geometry from the request, not the reference — the log line gates it). |
| `MEDIA_MEM_MODEL=<dir> ./tests/test_media_gen_memory.sh [port]` | Media-gen memory accounting: a generation must hand back every byte it took. Reads `mlx_active` from `/props` — **RSS is blind to Metal buffers** (it sat flat at 8.48 GB while 10 GB leaked, so `ps` reports a healthy process). Two assertions: generations at VARYING sizes (512², 768², 640x896) don't ratchet the resident footprint, and three load→generate→unload cycles return to the pre-load baseline with no creep. **The sizes must vary** — a fixed-size replay cannot separate a leak from ordinary size-keyed caching. Pins the class "a helper materializes a view and never frees the intermediate, pinning its parent buffer" (`mage_flow.sliceSeq`: ~47 MB per DiT block = 2.2 GB per megapixel per generation, compounding and surviving unload; red-on-revert at +3.18 GB across four generations). SKIPs without a checkpoint; defaults to Mage-Flow-Turbo-MLX-Serve-8bit. Hermetic counterpart: `materializing helpers hand back every array they take` in `mage_flow.zig`. |
| `KV_MEM_MODEL=<dir> ./tests/test_kv_cache_growth_memory.sh [port]` | Long-context decode must not ratchet the process footprint (#110). Boots the reporter's own model (`ddalcu/Qwen3.6-27B-4bit-MTP-MLX-Serve`) with `--metrics --prefix-cache-entries 0` and runs three GROWING turns, sampling `/props` `active_bytes` + `cache_bytes` and `/metrics` `mlx_serve:memory_mb` around each. Three assertions, written against the gap the reporter screenshotted (81.4 GB process, 19.6 GB panel): `cache_bytes` ≤ 10 GB at every sample, `memory_mb - active_bytes` ≤ 12 GB, and ≤ 2 GB of footprint ratchet from turn 1 to turn 3 (turn 1 is the baseline — it pays the one-time first-prefill KV and pool warm-up that a ratchet check must not bill). **Asserts `[spec-stats] mode=mtp` in the server log**: MTP is default-on for this dense-trunk checkpoint and was the ONE decode path with no `mlx_clear_cache()` at all, so without the engagement check the script silently measures `Generator.next` — which always cleared — and passes green against the exact defect it exists to catch. Same engagement rule that caught the two hardcoded `use_drafter=false` call sites. ~7 min; `KV_MEM_FULL=1` runs the reporter's shape verbatim (120 K ctx, ~78 K prompt). Red-on-revert verified against a rebuilt pre-fix binary: `memory_mb - active_bytes` 14.61 GB after ONE turn vs 3.87 GB fixed. SKIPs without the 15 GB checkpoint. Hermetic counterparts: `KVCache growth strands bounded bytes` + `KV growth does not ratchet the MLX buffer pool` (transformer.zig — the second reproduces the mechanism in 3 s with no weights, 1544 MB → 274 MB), `clear cadence survives variable spec strides` + the `advanceStep` source-scan class guard (generate.zig), `mlxCacheLimitBytes` (server.zig). |
| `./tests/test_multi_model_dir.sh [port]` | `--model-dir` is REPEATABLE and every folder it names is served. HERMETIC — the "models" are empty dirs holding a config.json, so this pins DISCOVERY and the arg loop, which is where the bug was; nothing is ever loaded. The flag took ONE directory, so a library spread over more than one folder (the app's download destination, the folder it used to be, an LM Studio tree) had the rest listed by the app's own picker and ABSENT from `/v1/models` — switching to one of those cost a full server restart instead of a hot swap, because the id was not in the registry. Four arms: three folders all discovered; a duplicate id resolving to the FIRST folder (the two copies declare different `model_type`s, so `meta.architecture` names which one won — a count of one would pass either way) plus its skip logged; a single folder byte-unchanged; an unreachable folder SKIPPED with a warning, not fatal (the second folder can live on an external drive); and a 9th `--model-dir` REFUSED by name rather than silently dropped (the silent-flag-eater class). Hermetic counterparts: `discoverModelsMany` (model_discovery.zig), `ModelRootsTests` (swift — root ordering, de-dup, the server-cap bound, and a source scan that no site builds `--model-dir` or the models root itself). |
| `./tests/test_multi_lora.sh [port]` | Multi-LoRA STACKING over the API, across every backend that takes adapters (FLUX.2, Krea-2, LTX-2.3, MiniMax-H3) + the Turbo adapter. The claim is not "an adapter attaches" (test_image_gen.sh pins that for one file) but that several attach and their deltas SUM — proven EXACTLY, no eyeballing and no reference image: **two identical adapters at 1.0 must be byte-for-byte the output of one at 2.0**, because `d+d == 2*d` in IEEE. Last-one-wins, a dropped second file, and a double-counted first all break that equality; the companion inequalities (1.0 differs from baseline, 2.0 differs from 1.0) stop it passing vacuously on a zero delta. Also: zero-B byte-transparency, order-independence, mixed scales folding into the same sum (0.5+1.5 == 1+1), the legacy singular field byte-matching a one-file array, and the named 400s (foreign architecture, 9 > the cap of 8, relative path). Adapters are SYNTHETIC — a rank-2 f32 pair sized from each checkpoint's real module — so no style LoRA needs to exist. **NOISE passes every one of those comparisons**, which is how a real adapter rendering pure static went green here 56/56 (its alpha lived in the file metadata, so it ran 8x too strong), so baseline / one-adapter / stacked-pair / H3-turbo renders now also go through `tests/lora_noise.py` — mean absolute difference between horizontally adjacent pixels, bar 20, measured 4-8 on real renders against 43-50 on static. numpy/PIL missing SKIPs loudly rather than passing. The scale-2.0 render needs no check of its own: it is byte-identical to the stacked pair by [5]. Real published adapters live in `test_real_loras.sh`. Every generation runs at each backend's floor (512x512/2 steps; 9 frames for LTX's 8N+1, 5 for H3's 17k+5) because only the bytes are compared (the noise metric works fine on a 2-step render). H3 runs the `core` level: it re-stages text encoder + DiT for EVERY generation, so its matrix is priced per request. H3-only arms: turbo generates, turbo attaches all 259 modules, turbo changes the render at matched steps, and a style adapter STACKS on top of turbo (`lora 2/2` in the log). Missing models SKIP rather than fail. |
| `./tests/test_real_loras.sh [port]` | REAL community LoRAs off the Hub, asserting the renders are USABLE rather than merely different — the companion `test_multi_lora.sh` needed, since its exact byte maths is satisfied by static. Four backends x (baseline -> adapter A -> A+B stacked), each render checked against the `lora_noise.py` bar, each step required to differ from the one before, plus the RESOLVED SCALE asserted against what the file declares and the attach line's adapter count. Rows and what each uniquely covers: **klein-4B** Norod78 vintage-book-cover + old-gods, i.e. BFL `diffusion_model.double_blocks.N.img_attn.qkv` keys = the fused-QKV fan-out and third-split on a real file (100 attachments from 80 modules), no alpha declared anywhere -> 1.0; **klein-9B** linoyts dreambooth + Delight, THE alpha fix — both declare alpha 4 / r 32 inside a JSON document held in one `lora_adapter_metadata` string, so both ran 8x hot before it and the first rendered static (this row is the red-on-revert case: metric 43.0 reverted vs 4.4 fixed); **Krea-2** gokaygokay Realism (flat `lora_alpha`/`lora_rank`, PEFT `base_model.model.` prefix) + krea darkbrush (NO metadata at all, krea aliases like `img_in`); **LTX-2.3** joyfox Transition + Cseti CrossView-Warp (video path, `diffusion_model.` prefix, no metadata). Lightricks' own IC-LoRAs are gated and 403 even with a token, hence the substitute. H3's real adapter is the Turbo file in its own pack and stays in `test_multi_lora.sh` (259/259 + a noise check) — H3 re-stages per request, so it is not repeated for zero new information. Adapters download ONCE into `~/claude-tmp/lora-real/` (~1.9 GB, resumable, skipped when present) and every render is KEPT in `~/claude-tmp/lora-real/out/` — the metric is a tripwire for static, opening the files is still the decisive check. A missing model or a failed download SKIPs. |
| `./tests/test_media_eviction_gate.sh [port]` | The registry's eviction gate against a staged-residency media model (#126). HERMETIC — the "model" is four SPARSE files carrying the real 4-bit H3 pack's byte sizes plus a `config.json` naming the backend, so nothing is ever read and the load fails at engine build; WHICH failure it is, is the test. The gate reserves post-load bytes BEFORE the media preflight runs and used to bill every entry the sum of its safetensors, so H3's 37.55 GiB sum against a 48 GB Mac's 30.0 GiB auto cap refused every load, permanently, on an idle server with nothing to evict and nothing to wait for. Two arms: a 30 GB cap (exactly what `auto` resolves to there) must NOT answer 503 and must log no refusal; an 8 GB cap must still refuse, name `--max-resident-mem` in the BODY, and log an estimate in the 24-29 GB band — the staged peak plus the gate's 10%, never the 41.3 GiB sum. Verified red-on-revert. Hermetic counterparts: `gateEstimateBytes` / `genLoadResidentBytes` + the one-estimator source scan (scheduler.zig), `not_enough_memory_message` (server.zig). Also carries the H3 turbo/chain/LoRA REQUEST-SURFACE arms (they fire before any weight is read, so the sparse pack exercises them): turbo without `turbo_lora.safetensors` and turbo below 4 steps are named 400s, turbo with the file present clears the probe (dies later on the sparse weights — proof the 400s were the probe); `chain_windows` out of 1-6 and chaining on a ref2va-config pack are named 400s; a malformed `lora_paths` proves the SHARED parser is reached on H3, and a relative path is a named 400 from the handler's `lora.validatePath` pre-flight (H3 loads its DiT per request, so `loadFile`'s own check comes minutes too late). |
| `H3_MODEL=<dir> ./tests/test_minimax_h3.sh [port]` | MiniMax-H3 (Hailuo 3.0) audio-video end-to-end on the converted pack: `video` capability, the staged-residency media preflight engagement line (`media peak` — max(TE,DiT)+VAEs, never the sum), the named-400 surface (LoRA — the ONE field the backend cannot honor in any form; non-/32 canvas; chat against a video model), a small non-stream generation returning rgb8 + pcm_s16le with the RIGHT byte lengths and audio/video duration sync, SSE progress→complete with 40 frames snapping UP to 56 on the 17k+5 ladder, fl2va adherence at 256px under `"fast": false` (64px collapses the keyframe to FOUR cond tokens — adherence there is chance), and the fast recipe BOTH ways by engagement count: a `fast:false` request must log zero reuse lines, a default request must log both `step-cache reused` and `attn-broadcast reused`. The preflight check asserts the staged NUMBER sits in the 30-50 GB band, never the log line's presence (the line printed happily while the stub's modality-static model_type billed the 64.5 GB sum). Weight staging (TE freed before DiT) dominates the wall clock, not the steps. SKIPs without the pack (default `ddalcu/MiniMax-H3-FL2VA-MLX-Serve-8bit`). Hermetic counterparts: the layout/schedule fixture tests in `minimax_h3.zig` (vs ComfyUI's own output, INCLUDING the six ref2va block cases — note every fixture reference is a different aspect from the target on purpose, or "the block's own w extremes" and "the target's" are the same numbers and the case cannot fail), the vision-presentation fixture tests in `minimax_h3_vision.zig` (resize policy, adaLN token tags, mRoPE position ids, interleaved-mRoPE axis map — from `tests/dump_minimax_h3_vision_fixtures.py`), tile/temporal-plan tests in `minimax_h3_vae.zig`, `h3PeakBytes`/`estimatePeakResidentBytesIn` in `gen.zig`. Live parity: `MINIMAX_H3_DIR=... zig build test -Dtest-filter="minimax h3 live"`; VAE-encoder parity (image, tiled, and two VIDEO cases whose frames MOVE so a collapsed/reversed/mis-strided temporal axis cannot pass) via `MINIMAX_H3_VAE_ENC_FIXTURE=... MINIMAX_H3_MODEL=...`; audio-encoder parity via `MINIMAX_H3_AUDIO_ENC_FIXTURE=...` (**asserts MAX-ABS error, not cosine** — a qkv bias-order swap scores 0.999998 and a dropped GeGLU LayerNorm scores a clean 1.000000, so a cosine bar there is a test that cannot fail); Qwen3-VL **vision tower** parity via `MINIMAX_H3_VIT_FIXTURE=...` (`tests/dump_minimax_h3_vision_tower_fixture.py`, ComfyUI's tower on OUR dequantized weights, both f32 and bf16) which asserts **`rms_ratio` beside the cosine** — the reference's vision rows carry 41x the norm of a token embedding, so a 40x scale error would score a perfect cosine while unbalancing the sequence it is spliced into. fl2va's `<Picture i>` splice is DEFAULT-OFF (it measured worse — see `docs/gotchas/models-media.md`); the bisect arms are `MINIMAX_H3_VISION_BLOCKS` / `_MROPE` / `_DEEPSTACK`. **Which half of the script runs is decided by the pack's own `config.json` `tasks` list**, never by its directory name: the two partitions ship identical files and geometry, so asserting fl2va keyframe adherence on a REF2VA DiT would be a checkpoint expectation in a server test's clothes. The **ref2va** section pins the eight named 400s (over-cap on each of the three lists, a 4-frame clip, an empty clip, an undecodable image, an undecodable WAV, a bad `ref_image_size`) plus references against a non-REF2VA pack; then one mixed image + soundtracked clip + standalone audio generation over SSE, whose ENGAGEMENT is counted in the server log — the resolver's order and ordinals (`image#1 / video#1 / audio#2`, the soundtrack taking `<Audio 1>` ahead of the standalone one, which an ordering bug cannot fake) and the exact cond-row budgets (12 visual = 1+2 latent frames x 4 rows; audio a multiple of 4 = two equal stereo blocks). Output alone is structurally blind to a silently ignored reference set: the generation succeeds either way. Hermetic counterparts for the new surface: `refBlockFor` derivation + `audioLatentToRows`/`audioRowsToLatent` round trip (minimax_h3.zig), `iterJsonObjectArray` + `h3ConfigDeclaresRef2va` + the 32 kHz stereo planar conversion (gen.zig), and `testRef2vaFieldsAreGatedOnTheModelsOwnCapability` + `refVideoFrameCount` (swift). |
| `MAGEFLOW_EDIT_MODEL=<dir> ./tests/test_mageflow_edit.sh [port]` | Mage-Flow-Edit, whose repertoire `test_image_gen.sh` structurally can't cover (no img2img, no LoRA, no rebalance): EDIT-capability detection from the boot banner (both repos ship byte-identical configs, so the dir name is the only discriminator), txt2img on the edit checkpoint, **sizeless edit keeps the SOURCE's resolution** (`max_size = source size`) with a non-square fixture so it can't pass trivially, explicit size = pixel budget at the source's aspect, multi-reference composition + `edit ref 2` engagement, honest 400s for the two capabilities the UI hides (variation, LoRA), the OpenAI multipart surface, and a final **form-named model** section that boots a SECOND server headless over the checkpoint's discovery root and names the model in the multipart `model` field (everything before it boots `--model $MODEL`, so the default is already the model under test and structurally cannot see an ignored id; red-on-revert 503 "No default model configured"). SKIPs without the ~17 GB checkpoint. Hermetic counterparts: `fitAspect` (incl. the match-source round-trip) + `openaiEditFormToJson` in `gen.zig`, the parser in `multipart.zig`, and the nine `MAGEFLOW_*` cos oracles fed by `tests/dump_mageflow_*_fixture.py`. |
